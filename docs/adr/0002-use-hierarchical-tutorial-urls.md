---
status: accepted
---

# Use hierarchical tutorial URLs

Framework tutorials use `/tutorials/{framework}/{topic}/` as their canonical
URL. This structure keeps related topics under one durable framework namespace,
supports future tutorial expansion, and makes navigation relationships clear;
legacy flat URLs receive permanent redirects as each framework path migrates.
