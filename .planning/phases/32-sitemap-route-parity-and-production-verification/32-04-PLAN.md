---
phase: 32-sitemap-route-parity-and-production-verification
plan: "04"
type: execute
wave: 4
depends_on: ["32-03"]
files_modified:
  - .planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md
autonomous: false
requirements: [PARITY-02, DELIVERY-02]
requirements_addressed: [PARITY-02, DELIVERY-02]
decisions: [D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "The pre-release record preserves the known stale deployed index and a fresh live read before deployment."
    - "One accepted 40-character upstream/main SHA owns successful Vercel and Cloudflare production workflow URLs."
    - "Production acceptance runs the retained command against https://sealos.io and records four 2,000 counts, 2,000 direct HTTP 200 routes, and zero findings."
    - "DELIVERY-02 remains pending while either workflow evidence or the complete remote gate is absent."
  artifacts:
    - path: .planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md
      provides: UTC-bound stale baseline, exact SHA, workflow URLs, command, counts, statuses, and zero-mismatch production evidence.
  key_links:
    - from: upstream/main
      to: .github/workflows/deploy.yml
      via: Full head_sha equality on a completed successful Vercel production run.
      pattern: 40-character SHA
    - from: upstream/main
      to: .github/workflows/deploy-cloudflare.yml
      via: Full head_sha equality on a completed successful Cloudflare production run.
      pattern: 40-character SHA
    - from: .planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md
      to: https://sealos.io
      via: Exact retained remote verifier command and complete zero-finding report.
      pattern: npm run verify:ai-faq-routes -- https://sealos.io
---

<objective>
Bind Phase 32 acceptance to fresh production truth and one exact commit across
both production deployment systems.

Purpose: Close DELIVERY-02 only with reproducible same-SHA Vercel, Cloudflare,
and complete Sealos route evidence.
Output: A retained execution summary containing the stale baseline, exact
40-character SHA, both successful workflow URLs, full remote command, all
counts/statuses, and zero mismatch totals.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
@$HOME/.codex/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-CONTEXT.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-RESEARCH.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-VALIDATION.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-03-SUMMARY.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-VERIFICATION.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-UAT.md
@scripts/verify-ai-faq-routes.mjs
@package.json
@.github/workflows/deploy.yml
@.github/workflows/deploy-cloudflare.yml
</context>

<interfaces>
- Repository: `labring/sealos.io`.
- Accepted commit source: fresh `upstream/main`, exactly 40 lowercase hexadecimal characters.
- Vercel workflow: `.github/workflows/deploy.yml` / `Deploy Sealos Website`.
- Cloudflare workflow: `.github/workflows/deploy-cloudflare.yml` / `Deploy Home Page (Cloudflare Production)`.
- Production target: `https://sealos.io`.
- Current planning fact: Vercel workflow state is `disabled_manually`.
- Current planning fact: production index has 28 slug and 5 description drifts while sitemap matches all 2,000 canonical source slugs.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the stale production baseline and release readiness state</name>
  <files>.planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md</files>
  <read_first>
    - `32-RESEARCH.md` live production snapshot and exact-SHA release contract.
    - `32-CONTEXT.md` D-09 through D-16.
    - Phase 31 verification/UAT external-evidence gap.
    - Current workflow definitions and GitHub workflow states.
    - Plan 32-03 summary for the final remote command behavior.
  </read_first>
  <action>
Run a fresh read-only production command and capture UTC, target, process
status, four counts, HTTP histogram, index-field totals, set totals, and
identity totals. Preserve the plan-time baseline from `32-RESEARCH.md`: 2,000
source, 2,000 deployed index, 2,000 sitemap; 28 source/index slug differences;
five description drifts; non-canonical index bytes; representative stale route
404. Label it with its plan-time UTC so it remains historical evidence if live
state has advanced.

Query both GitHub workflow records and the latest main runs. Record workflow
state, full `head_sha`, status, conclusion, UTC, and `html_url`. The current
Vercel `disabled_manually` state is an explicit blocking result. Keep
PARITY-02 and DELIVERY-02 pending and stop before acceptance while any workflow
state/run condition is incomplete.
  </action>
  <acceptance_criteria>
    - The summary preserves both the timestamped plan-time stale snapshot and a fresh live snapshot.
    - Fresh output comes from the retained remote command with exact target and UTC.
    - Both workflow records include state and latest main run metadata.
    - Disabled workflow, missing run, SHA mismatch, incomplete status, or non-success conclusion is visibly BLOCKED.
    - Requirement status remains pending at this gate.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set +e; npm run verify:ai-faq-routes -- https://sealos.io; route_status=$?; set -e; test "$route_status" -eq 0 -o "$route_status" -eq 1' &amp;&amp; gh api repos/labring/sealos.io/actions/workflows/deploy.yml --jq '{state,path}' &amp;&amp; gh api repos/labring/sealos.io/actions/workflows/deploy-cloudflare.yml --jq '{state,path}'</automated>
  </verify>
  <done>The release record preserves the stale regression baseline and exposes every current workflow prerequisite.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Complete the approved main release through existing production workflows</name>
  <files>.planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md</files>
  <read_first>
    - Task 1 BLOCKED/PASS workflow readiness record.
    - Existing reviewed PR-to-main policy for this repository.
    - `.github/workflows/deploy.yml` and `.github/workflows/deploy-cloudflare.yml` push and workflow-dispatch triggers.
    - GitHub documentation linked from `32-RESEARCH.md` for workflow dispatch.
  </read_first>
  <action>
The repository maintainer completes the approved PR-to-`main` merge through the
existing review flow and restores the existing Vercel production workflow to
active state. Both production deployments must execute from the resulting
main commit. When a push-triggered Vercel run was unavailable because the
workflow became active after the merge, dispatch `deploy.yml` against `main`.
Dispatch the Cloudflare production workflow against `main` only when its push
run is absent. Record the action and resulting run identifiers.

This checkpoint authorizes the existing production release path only. It does
not authorize workflow source edits, alternate deployments, force pushes,
history rewrites, or acceptance from preview runs.
  </action>
  <acceptance_criteria>
    - The reviewed Phase 32 implementation is present on `upstream/main`.
    - Vercel and Cloudflare production workflows are active.
    - Each production workflow has one run for the exact resulting main SHA.
    - The checkpoint record names the merge commit and both run IDs.
  </acceptance_criteria>
  <verify>
    <automated>git fetch upstream main &amp;&amp; test "$(git rev-parse upstream/main | wc -c | tr -d ' ')" = "41" &amp;&amp; gh api repos/labring/sealos.io/actions/workflows/deploy.yml --jq -e '.state == "active"' &amp;&amp; gh api repos/labring/sealos.io/actions/workflows/deploy-cloudflare.yml --jq -e '.state == "active"'</automated>
  </verify>
  <done>The accepted main commit has active Vercel and Cloudflare production runs ready for exact-SHA validation.</done>
</task>

<task type="auto">
  <name>Task 3: Prove same-SHA deployments and complete production parity</name>
  <files>.planning/phases/32-sitemap-route-parity-and-production-verification/32-04-SUMMARY.md</files>
  <read_first>
    - Task 2 accepted main SHA and production run IDs.
    - `32-VALIDATION.md` Gate D.
    - `scripts/verify-ai-faq-routes.mjs` final output contract.
    - GitHub workflow-runs REST documentation linked from research.
  </read_first>
  <action>
Fetch `upstream/main` and store `TARGET_SHA=$(git rev-parse upstream/main)`.
Require the exact 40-character value in both workflow run records. For each
production workflow, query by `branch=main` and `head_sha=$TARGET_SHA`, select a
completed successful run, and retain its `html_url`, run ID, timestamps,
status, and conclusion. Preview runs and short SHAs provide zero acceptance
credit.

After both same-SHA runs succeed, execute exactly
`npm run verify:ai-faq-routes -- https://sealos.io`. Rerun the complete command
after a transient network/timeout result. Record the final UTC, target,
command, SHA, both workflow URLs, source/index/sitemap/route total and unique
counts, HTTP status histogram, network/timeout/body totals, duplicate and
membership totals, invalid-route totals, index-field totals, and title/H1/
meta-description/canonical totals.

Mark Plan 32-04 complete only when every inventory count is 2,000, HTTP 200 is
2,000, both invalid routes have accepted 404/410 statuses, and every finding
total is zero. Leave requirement completion to the subsequent independent
`$gsd-verify-work 32` workflow.
  </action>
  <acceptance_criteria>
    - `TARGET_SHA` is one exact 40-character `upstream/main` commit.
    - Both retained production run records have `head_sha == TARGET_SHA`, `status == completed`, and `conclusion == success`.
    - Both workflow `html_url` values are recorded.
    - The production command reports source/index/sitemap/route totals and unique counts of 2,000.
    - The production command reports 2,000 direct HTTP 200 detail routes and zero other canonical-route statuses.
    - Duplicate, membership, network, timeout, body, invalid-route, index-field, title, H1, meta-description, and canonical finding totals are zero.
    - Exact command, target, UTC, SHA, workflow URLs, counts, histogram, and mismatch totals are retained in the summary.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; git fetch upstream main; target_sha=$(git rev-parse upstream/main); test "${#target_sha}" -eq 40; for workflow_file in deploy.yml deploy-cloudflare.yml; do run_sha=$(gh api "repos/labring/sealos.io/actions/workflows/${workflow_file}/runs?branch=main&amp;head_sha=${target_sha}&amp;status=success&amp;per_page=1" --jq ".workflow_runs[0].head_sha // \"\""); run_url=$(gh api "repos/labring/sealos.io/actions/workflows/${workflow_file}/runs?branch=main&amp;head_sha=${target_sha}&amp;status=success&amp;per_page=1" --jq ".workflow_runs[0].html_url // \"\""); test "$run_sha" = "$target_sha"; case "$run_url" in https://github.com/labring/sealos.io/actions/runs/*) ;; *) exit 1 ;; esac; done; npm run verify:ai-faq-routes -- https://sealos.io'</automated>
  </verify>
  <done>Production evidence proves all 2,000 English records and routes at one exact Vercel/Cloudflare main commit.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: release provenance and acceptance integrity.

| Threat | Severity | Planned control |
|---|---|---|
| Historic stale evidence is overwritten by a later snapshot | medium | Preserve timestamped plan-time and fresh pre-release records separately. |
| Successful run belongs to another commit | high | Full 40-character equality against fresh `upstream/main`. |
| One hosting system serves another build | high | Require separate Vercel and Cloudflare successful production URLs for the same SHA. |
| Disabled workflow is treated as deployment evidence | high | Active workflow state is a blocking prerequisite. |
| Preview run is substituted for production | high | Accept exact production workflow paths and main branch only. |
| Partial/transient crawl is accepted | high | Complete remote command rerun and zero-finding terminal report. |
</threat_model>

## Four-Source Coverage Audit

| Source | ID | Contract | Plan/task | Status |
|---|---|---|---|---|
| GOAL | Phase 32 | exact canonical slug in index, sitemap, and deployed route | 32-01 through 32-04 | COVERED |
| REQ | PARITY-02 | four exact 2,000 English slug sets | 32-01 Tasks 1-2; 32-02 Task 1; 32-04 Task 3 | COVERED |
| REQ | DELIVERY-02 | every deployed index URL, sitemap, identities, zero stale/404 | 32-03 Tasks 1-2; 32-04 Tasks 1-3 | COVERED |
| CONTEXT | D-01 | validated 2,000 source authority | 32-01 Task 1; 32-02 Task 1 | COVERED |
| CONTEXT | D-02 | exact bidirectional sets and deterministic diagnostics | 32-01 Tasks 1-2 | COVERED |
| CONTEXT | D-03 | 2,000 unique canonical sitemap URLs | 32-02 Task 1; 32-04 Task 3 | COVERED |
| CONTEXT | D-04 | enumerate readable local `index.html` routes both directions | 32-02 Task 1 | COVERED |
| CONTEXT | D-05 | retain command, `out` default, optional URL | 32-01 Task 2; 32-02 Task 2; 32-03 Task 2 | COVERED |
| CONTEXT | D-06 | all 2,000 title/H1/description/canonical checks | 32-01 Task 1; 32-02 Task 1; 32-03 Task 1 | COVERED |
| CONTEXT | D-07 | direct local static and deployed HTTP boundaries | 32-02 Task 1; 32-03 Task 1 | COVERED |
| CONTEXT | D-08 | retain ambiguous and unknown numbered rejection | 32-02 Task 1; 32-03 Task 2 | COVERED |
| CONTEXT | D-09 | deployed index and sitemap are live truth | 32-03 Task 1; 32-04 Task 1 | COVERED |
| CONTEXT | D-10 | 2,000 requests, deterministic pool of 8 | 32-03 Task 1 | COVERED |
| CONTEXT | D-11 | direct 200 and four identities from one body | 32-03 Tasks 1-2; 32-04 Task 3 | COVERED |
| CONTEXT | D-12 | timeouts, one pass, grouped totals, full rerun | 32-03 Tasks 1-2; 32-04 Task 3 | COVERED |
| CONTEXT | D-13 | reviewed main and existing production workflows | 32-04 Task 2 | COVERED |
| CONTEXT | D-14 | exact upstream/main SHA and two successful run URLs | 32-04 Tasks 2-3 | COVERED |
| CONTEXT | D-15 | retain command, target, UTC, SHA, URLs, counts, totals | 32-04 Tasks 1-3 | COVERED |
| CONTEXT | D-16 | complete only at 2,000/2,000/2,000/2,000 and zero findings | 32-04 Task 3 | COVERED |
| RESEARCH | current stale index regression baseline | 32-03 Task 2; 32-04 Task 1 | COVERED |
| RESEARCH | bounded local reads and remote requests | 32-02 Task 1; 32-03 Task 1 | COVERED |
| RESEARCH | Vercel disabled workflow blocker | 32-04 Tasks 1-2 | COVERED |
| PATTERN | shared source loader and formatter conventions | 32-01 Tasks 1-2 | COVERED |
| PATTERN | post-normalization build stage | 32-02 Task 2 | COVERED |
| VALIDATION | Wave 0 focused route test | 32-01 Tasks 1-2 | COVERED |
| VALIDATION | local, network, and exact-SHA gates | 32-02 through 32-04 | COVERED |

## Scope Audit

| Surface | Disposition |
|---|---|
| Route verifier and focused tests | implement |
| Package and timed build placement | implement |
| Route/page/sitemap rendering code | read-only acceptance |
| Generated index/static output | read-only acceptance |
| Vercel/Cloudflare workflow source | read-only acceptance |
| Existing workflow activation and approved release | blocking release action |
| Locale/hreflang/taxonomy/content/Search Console | deferred |

<verification>
Plan 32-04 performs release evidence collection. Independent phase verification
begins afterward and must re-read the retained summary, workflow APIs, and live
production output before marking requirements complete.
</verification>

<success_criteria>
- The plan-time stale production state remains documented.
- Both hosting systems prove the same exact accepted main SHA.
- The live remote gate reports all 2,000 routes and zero findings.
- Requirement status stays pending until independent verification.
</success_criteria>

<output>
Create `32-04-SUMMARY.md` with timestamped stale and final snapshots, exact
40-character SHA, Vercel and Cloudflare run URLs, exact remote command, four
counts, status histogram, every mismatch total, checkpoint action, and any
deviations.
</output>
