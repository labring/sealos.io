---
phase: 32-sitemap-route-parity-and-production-verification
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/verify-ai-faq-routes.mjs
  - scripts/verify-ai-faq-routes.test.mjs
autonomous: true
requirements: [PARITY-02, DELIVERY-02]
requirements_addressed: [PARITY-02, DELIVERY-02]
decisions: [D-01, D-02, D-03, D-05, D-06, D-11, D-12]
must_haves:
  truths:
    - "All route verification findings are collected before process status is selected, with fixed English categories and deterministic numeric ordering."
    - "Source, index, sitemap, and route inventories expose total, unique, duplicate, and explicit bidirectional membership facts."
    - "Title, H1, meta description, and canonical extraction supports generated nested markup, escaped values, and attribute-order variation."
    - "The existing verify:ai-faq-routes command, default out target, and optional HTTP(S) target remain the public interface."
  artifacts:
    - path: scripts/verify-ai-faq-routes.mjs
      provides: Testable four-set, identity, report, and CLI domain boundary.
    - path: scripts/verify-ai-faq-routes.test.mjs
      provides: Focused deterministic inventory, HTML, formatter, and CLI regression tests.
  key_links:
    - from: scripts/verify-ai-faq-routes.mjs
      to: scripts/ai-faq-index.mjs
      via: Canonical numeric source records and exact projection semantics.
      pattern: loadCanonicalFAQSource
    - from: scripts/verify-ai-faq-routes.test.mjs
      to: scripts/verify-ai-faq-routes.mjs
      via: Direct imports of exported pure helpers and injected runner.
      pattern: runVerifyFAQRoutes
---

<objective>
Replace assertion-first route sampling with a testable deterministic report
core that can represent the complete Phase 32 contract.

Purpose: Establish exact inventory, identity, diagnostics, and CLI semantics
before filesystem and network adapters add 2,000-route work.
Output: Import-safe verifier helpers, an injected runner boundary, and focused
TDD coverage for every report category.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-CONTEXT.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-RESEARCH.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-PATTERNS.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-VALIDATION.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-03-SUMMARY.md
@scripts/verify-ai-faq-routes.mjs
@scripts/ai-faq-index.mjs
@scripts/ai-faq-fixture.mjs
@scripts/ai-faq-index.test.mjs
@lib/utils/metadata.ts
@app/[lang]/(home)/ai-quick-reference/[slug]/page.tsx
</context>

<interfaces>
- Source authority: `loadCanonicalFAQSource()` returns 2,000 records ordered by numeric ID.
- Public CLI: `npm run verify:ai-faq-routes` targets `out`; one positional HTTP(S) URL selects remote mode.
- Expected title: `${record.data.title} | Sealos`.
- Expected H1: `record.data.title`.
- Expected meta description: `record.data.description`.
- Expected canonical: `https://sealos.io/ai-quick-reference/${record.slug}/`.
- Detail limit: 20 findings per category while totals cover every finding.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Define exact inventory and identity comparison semantics</name>
  <files>scripts/verify-ai-faq-routes.mjs, scripts/verify-ai-faq-routes.test.mjs</files>
  <read_first>
    - `scripts/verify-ai-faq-routes.mjs` for the current source fixture, target argument, HTML helpers, sitemap extraction, collision samples, and invalid-route probes.
    - `scripts/ai-faq-index.mjs` for numeric source identity, deterministic comparison, category collection, safe diagnostic values, and 20-detail formatting.
    - `scripts/ai-faq-fixture.mjs` for the retained page shape and normalized collision groups.
    - `lib/utils/metadata.ts` and the detail page for exact title, description, canonical, and H1 producers.
    - `32-CONTEXT.md` D-01, D-02, D-03, D-06, and D-11.
  </read_first>
  <behavior>
    - RED: four equal 2,000-style fixture sets produce zero duplicate or membership findings and retain numeric record order.
    - RED: source/index, source/sitemap, and source/route missing/extra mutations each produce an explicit directional category plus a four-set presence matrix.
    - RED: duplicates in every set retain complete totals and stable numeric detail order.
    - RED: title, nested H1, meta description, and canonical extraction handles HTML escaping and arbitrary attribute order and reports missing, duplicate, and mismatched fields independently.
    - GREEN: exported pure functions return structured reports without writing streams or selecting process status.
  </behavior>
  <action>
RED: create `scripts/verify-ai-faq-routes.test.mjs` with small records that use
numeric slugs in non-numeric input order. Add table-driven mutations for every
duplicate and directional set category. Add HTML fixtures with nested H1
markup, `&amp;`, quotes, reordered `meta`/`link` attributes, missing fields, and
duplicate identity tags. Assert report objects and byte-identical ordering
across reversed inputs.

GREEN: refactor `scripts/verify-ai-faq-routes.mjs` into import-safe exported
helpers. Represent each inventory as ordered entries plus slug groups. Compare
source against index, sitemap, and route in both directions, and attach
`presentIn` facts for source/index/sitemap/route to each membership detail.
Keep fixed category order. Implement order-independent opening-tag attribute
parsing and nested text extraction for the four exact identities. Reuse
canonical source order and JSON-safe values.

REFACTOR: keep target I/O outside pure comparison helpers. Retain the current
`escapeHtml()` behavior where generated HTML comparison requires escaped
values. Give every finding enough slug, numeric ID, field, expected, and
actual context for repair.
  </action>
  <acceptance_criteria>
    - Equal inventories expose totals and unique counts of 2,000 with zero findings.
    - Every derived-set missing/extra direction and every duplicate set has an independently tested category.
    - Membership details identify presence across all four sets and sort by numeric ID, slug, and field.
    - All four identity fields support exact logical comparison against generated HTML.
    - Parser failures become structured findings and never terminate collection through `assert`.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-ai-faq-routes.test.mjs</automated>
  </verify>
  <done>The verifier owns deterministic four-set and four-field comparison semantics with complete fixture coverage.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Define bounded diagnostics and the retained CLI contract</name>
  <files>scripts/verify-ai-faq-routes.mjs, scripts/verify-ai-faq-routes.test.mjs</files>
  <read_first>
    - Task 1 exports and test fixtures.
    - `scripts/verify-ai-faq-index.mjs` for injected streams, returned status, and guarded ESM entrypoint.
    - `scripts/ai-faq-index.mjs` `formatFAQIndexReport()` for fixed totals and 20-detail output.
    - `package.json` for the exact retained `verify:ai-faq-routes` script.
    - `32-CONTEXT.md` D-05 and D-12.
  </read_first>
  <behavior>
    - RED: a valid injected report writes a concise English pass summary to stdout and returns 0.
    - RED: any category finding writes one fixed English failure report to stderr and returns 1 after every category total is rendered.
    - RED: 21 findings render a total of 21 and exactly 20 details; control characters remain JSON escaped.
    - RED: reversed completion/input order yields byte-identical formatted output.
    - RED: no positional target selects `out`; one HTTP(S) target selects remote mode; unsupported target shapes produce one structured target finding.
  </behavior>
  <action>
RED: add stream-capture tests for success, failure, category order, 21 findings,
newline/control characters, target parsing, and the real package command string.
Require summary fields for target, mode, source/index/sitemap/route total and
unique counts, status totals, identity totals, configured limits, and final
status.

GREEN: export `formatFAQRouteReport()` and `runVerifyFAQRoutes()` with injected
stdout, stderr, filesystem/fetch seams, and time source. The runner selects
status only after formatting the complete report. Add a guarded ESM main block
that defaults to `out`, accepts one optional HTTP(S) base URL, and assigns
`process.exitCode`. Preserve the exact package command and positional URL
usage. Keep output English, deterministic, and bounded to 20 details per
category.

REFACTOR: centralize category metadata and safe value serialization. Keep
formatting independent from filesystem/network execution so later waves can
add adapters without changing report order.
  </action>
  <acceptance_criteria>
    - The module can be imported by `node:test` without executing a real target.
    - The formatter prints every category total in fixed order and at most 20 details per category.
    - Success and failure select stdout/stderr and process status 0/1 through the injected runner.
    - `npm run verify:ai-faq-routes` retains its exact name and `out` default.
    - One HTTP(S) positional base URL remains the supported remote invocation.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-ai-faq-routes.test.mjs &amp;&amp; node --check scripts/verify-ai-faq-routes.mjs</automated>
  </verify>
  <done>The route verifier has a stable importable runner, bounded English report, and backward-compatible CLI.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: diagnostic integrity and target parsing.

| Threat | Severity | Planned control |
|---|---|---|
| Count-valid stale data hides directional set drift | high | Explicit pairwise categories and four-set presence details. |
| Assertion stops at the first failure | medium | Collect every category, format once, then choose status. |
| Completion order changes retained evidence | medium | Numeric ID and code-unit sorting before formatting. |
| HTML/control values corrupt terminal output | low | JSON-serialize all diagnostic values. |
| Unsupported target shape reaches an unintended adapter | low | Parse local path or absolute HTTP(S) base into an explicit mode. |
</threat_model>

## Artifacts This Plan Produces

- `scripts/verify-ai-faq-routes.mjs` - deterministic comparison, identity,
  formatting, runner, and guarded CLI seams.
- `scripts/verify-ai-faq-routes.test.mjs` - Wave 0 focused test infrastructure
  and domain/CLI contract coverage.

<verification>
Run the focused route test after each RED/GREEN/refactor cycle. Finish with the
focused test and syntax check. Local filesystem and live network commands begin
in later plans.
</verification>

<success_criteria>
- Four-set and four-field semantics are precise and testable.
- Diagnostics are complete, deterministic, bounded, and English.
- The public command contract is unchanged.
</success_criteria>

<output>
Create `32-01-SUMMARY.md` with RED/GREEN commits, category coverage, report
ordering, exact commands, and any deviations.
</output>
