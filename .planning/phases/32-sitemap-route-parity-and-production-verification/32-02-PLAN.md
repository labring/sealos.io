---
phase: 32-sitemap-route-parity-and-production-verification
plan: "02"
type: tdd
wave: 2
depends_on: ["32-01"]
files_modified:
  - scripts/verify-ai-faq-routes.mjs
  - scripts/verify-ai-faq-routes.test.mjs
  - package.json
  - scripts/measure-build-pipeline.js
  - scripts/measure-build-pipeline.test.mjs
autonomous: true
requirements: [PARITY-02]
requirements_addressed: [PARITY-02]
decisions: [D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08]
must_haves:
  truths:
    - "A local run proves exact source/index/sitemap/readable-route parity for all 2,000 English slugs in both directions."
    - "Every local page-index route has readable static HTML with exact title, H1, meta description, and canonical identity."
    - "Local page reads remain bounded at 32 and ambiguous normalized plus unknown numbered routes retain rejection coverage."
    - "Standard and timed build paths run the retained route command after static output and root-locale normalization exist."
  artifacts:
    - path: scripts/verify-ai-faq-routes.mjs
      provides: Complete bounded local target adapter and all-page identity gate.
    - path: scripts/verify-ai-faq-routes.test.mjs
      provides: Filesystem, full-inventory, unreadable-route, and invalid-route regression coverage.
    - path: package.json
      provides: Post-build route parity through the existing command.
    - path: scripts/measure-build-pipeline.js
      provides: Equivalent post-normalization timed route stage.
    - path: scripts/measure-build-pipeline.test.mjs
      provides: Exact route-stage order and fail-fast proof.
  key_links:
    - from: package.json
      to: scripts/verify-ai-faq-routes.mjs
      via: build and build:analyze invoke the existing package command after normalization.
      pattern: verify:ai-faq-routes
    - from: scripts/measure-build-pipeline.js
      to: scripts/verify-ai-faq-routes.mjs
      via: AI FAQ route parity stage after root locale normalization.
      pattern: AI FAQ route parity
---

<objective>
Turn the deterministic core into the complete local static-output gate and
place it after every repository-owned static build.

Purpose: Prove PARITY-02 against all 2,000 generated English pages before any
deployment begins.
Output: Bounded local filesystem adapter, full identity inspection, retained
invalid-route checks, and standard/timed build integration.
</objective>

<execution_context>
@$HOME/.codex/gsd-core/workflows/execute-plan.md
@$HOME/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-CONTEXT.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-RESEARCH.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-PATTERNS.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-VALIDATION.md
@.planning/phases/32-sitemap-route-parity-and-production-verification/32-01-SUMMARY.md
@scripts/verify-ai-faq-routes.mjs
@scripts/verify-ai-faq-routes.test.mjs
@scripts/ai-faq-index.mjs
@scripts/ai-faq-fixture.mjs
@scripts/measure-build-pipeline.js
@scripts/measure-build-pipeline.test.mjs
@package.json
@.github/workflows/deploy-cloudflare.yml
@.github/workflows/deploy.yml
@Dockerfile
</context>

<interfaces>
- Local index path: `<target>/ai-faqs.en.json`.
- Local sitemap path: `<target>/ai-quick-reference/sitemap.xml`.
- Local route path: `<target>/ai-quick-reference/<slug>/index.html`.
- Local route inventory: immediate route directories with readable `index.html`.
- Local read limit: batches of at most 32.
- Existing source/index gate stays before Next; route gate belongs after normalization.
</interfaces>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Inspect every local inventory entry and static page</name>
  <files>scripts/verify-ai-faq-routes.mjs, scripts/verify-ai-faq-routes.test.mjs</files>
  <read_first>
    - Plan 32-01 summary and current verifier exports.
    - `scripts/ai-faq-index.mjs` source loader, `decodeJSONBuffer()`, and read-batch pattern.
    - `scripts/ai-faq-fixture.mjs` collision grouping used by retained invalid-route checks.
    - Current `out/ai-faqs.en.json`, `out/ai-quick-reference` layout, sitemap, and representative `index.html`.
    - `app/[lang]/(home)/ai-quick-reference/sitemap.ts` and `app/[lang]/(home)/ai-quick-reference/[slug]/page.tsx` for the generated source surfaces.
    - `32-CONTEXT.md` D-01 through D-08.
  </read_first>
  <behavior>
    - RED: a complete fixture enumerates source, local index, local sitemap, and readable local route slugs in both directions and inspects every page-index HTML file.
    - RED: extra route directories, missing route files, unreadable files, duplicate sitemap/index slugs, malformed target data, and identity mismatches produce structured findings while remaining pages are inspected.
    - RED: an injected filesystem observes at most 32 simultaneous page reads and numeric output order after reversed directory enumeration.
    - RED: ambiguous normalized and unknown numbered routes resolve to a local missing status while all 2,000 canonical files remain accepted.
    - GREEN: a complete local fixture reports equal inventories, all page identities, and zero findings; Task 2 owns the fresh real build/output proof.
  </behavior>
  <action>
RED: extend the focused test with temporary local target roots. Generate compact
index/sitemap/route fixtures, then mutate every local ingestion, duplicate,
membership, identity, and invalid-route category. Instrument `readFile` to
measure active operations and delay completion in reverse order. Assert every
page-index candidate is attempted and report output remains numeric.

GREEN: implement the local adapter. Load canonical source through the Phase 31
loader; read/decode `<target>/ai-faqs.en.json`; parse the target sitemap into
canonical English slugs; enumerate immediate target route directories and
include only readable `index.html` in route membership while reporting read
failures. Read all page-index HTML files in deterministic batches capped at 32
and compare all four identity fields. Preserve the collision-derived ambiguous
normalized slug and unknown numbered slug checks as structured 404/410-style
local results.

REFACTOR: read each static HTML file once. Continue across file failures and
field mismatches. Suppress only derivative identity comparisons when a body is
unavailable, while preserving primary read/status findings and exact route-set
membership.
  </action>
  <acceptance_criteria>
    - A complete local fixture inspects every page-index route and every identity field.
    - Exact 2,000-record production constants remain enforced through the canonical source default and Task 2 real build.
    - Local reads never exceed 32 active operations.
    - Missing, unreadable, extra, duplicate, malformed, and identity cases retain complete deterministic totals.
    - Ambiguous normalized and unknown numbered local routes remain unresolved.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-ai-faq-routes.test.mjs &amp;&amp; npm run verify:ai-faq-index</automated>
  </verify>
  <done>The local command proves exact four-set and four-field parity across all 2,000 generated English pages.</done>
</task>

<task type="auto">
  <name>Task 2: Run local route parity after standard and timed builds</name>
  <files>package.json, scripts/measure-build-pipeline.js, scripts/measure-build-pipeline.test.mjs, scripts/verify-ai-faq-routes.test.mjs</files>
  <read_first>
    - `package.json` build, build:analyze, verify:ai-faq-index, verify:ai-faq-routes, and normalization commands.
    - `scripts/measure-build-pipeline.js` build/analyze stage arrays and fail-fast loop.
    - `scripts/measure-build-pipeline.test.mjs` exact stage assertions and index-parity failure case.
    - `.github/workflows/deploy.yml`, `.github/workflows/deploy-cloudflare.yml`, and `Dockerfile` for inherited package build surfaces.
    - Task 1 local command contract.
  </read_first>
  <action>
Append `npm run verify:ai-faq-routes` to `build` and `build:analyze` after
`scripts/normalize-root-locale.js`. Preserve the exact existing
`verify:ai-faq-routes` script value and every preceding command.

Add an `AI FAQ route parity` stage to timed build and analyze modes after
`root locale normalization` and before `post generated diff guard`. Update
exact stage-list tests. Add a non-zero route-stage test that observes all prior
stages, stops before the post generated-diff guard, and prints only completed
timings. Add package-string assertions to the focused route test.

Treat Vercel, Cloudflare, and Docker definitions as read-only acceptance
surfaces. Confirm they continue to reach the package build path or record the
Vercel resolved build command in later exact-SHA evidence.
  </action>
  <acceptance_criteria>
    - `verify:ai-faq-routes` remains exactly `node scripts/verify-ai-faq-routes.mjs`.
    - Standard and analyzer package builds invoke route parity after normalization.
    - Timed build and analyzer stages place route parity after normalization and before the final generated-diff guard.
    - A route-stage failure prevents later stages and returns its non-zero status.
    - Node.js 20 `npm run build` completes with a four-set 2,000 success summary.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/verify-ai-faq-routes.test.mjs &amp;&amp; node --test scripts/measure-build-pipeline.test.mjs &amp;&amp; npm run verify:ai-faq-index &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <done>Every repository-owned local static build reaches the complete route gate after output generation.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: local build integrity and resource bounds.

| Threat | Severity | Planned control |
|---|---|---|
| Generated route tree diverges from source/index/sitemap | high | Immediate-directory enumeration and exact bidirectional set comparison. |
| Missing or unreadable HTML is counted as a valid route | high | Route membership requires readable `index.html`; read failure remains explicit. |
| Large corpus exhausts file descriptors | medium | Sequential batches capped at 32 and active-read regression test. |
| Direct timed build bypasses package route gate | medium | Equivalent post-normalization stage with fail-fast test. |
| Invalid normalized route behavior regresses | medium | Retained collision and unknown numbered route checks. |
</threat_model>

## Artifacts This Plan Produces

- Complete local adapter and local invalid-route coverage in the verifier.
- Local filesystem/full-output cases in the focused test.
- Standard and timed post-build route gate wiring.

<verification>
Run focused tests after each adapter change, then the full non-build Phase 32
suite. Finish on Node.js 20 with `npm run build`; preserve the generated output
for the explicit local command and record all four 2,000 counts in the summary.
</verification>

<success_criteria>
- PARITY-02 has complete local automated proof.
- Local resource usage is bounded and deterministic.
- Every standard/timed static build executes the same retained route command.
</success_criteria>

<output>
Create `32-02-SUMMARY.md` with Node version, build command, stage order, all
four local counts, identity totals, invalid-route totals, elapsed time, and
deviations.
</output>
