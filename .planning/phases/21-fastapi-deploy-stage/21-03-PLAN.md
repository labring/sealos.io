---
phase: 21-fastapi-deploy-stage
plan: 03
type: execute
wave: 3
depends_on: ["21-02"]
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
autonomous: true
requirements: [FAST-01]
must_haves:
  truths:
    - "A reader can follow the README from Python 3.12 setup through locked install, nine named behavior functions as 12 pytest cases, Uvicorn on 0.0.0.0:8000, and public HTTP verification."
    - "The accepted Stage 1 tree passes its full suite, dependency-export check, and actual port-8000 health/docs smoke before publication."
    - "The public repository exposes main and an annotated stage-1-deploy tag whose peeled remote SHA equals the accepted local HEAD."
    - "An active tag ruleset protects refs/tags/stage-* from updates and deletion."
    - "A fresh public clone at stage-1-deploy installs from the lock and passes nine named behavior functions as 12 collected pytest cases."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md"
      provides: "Clone, setup, test, run, endpoint verification, and Stage 1 lifecycle guidance"
      contains: "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
    - path: "https://github.com/yangchuansheng/sealos-fastapi-tutorial"
      provides: "Public Tasks API source repository with main and immutable Stage 1 source"
    - path: "refs/tags/stage-1-deploy"
      provides: "Annotated accepted Phase 21 source identity"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      via: "documented Uvicorn module target and public curl examples"
      pattern: "uv run uvicorn app.main:app --host 0.0.0.0 --port 8000"
    - from: "accepted local HEAD"
      to: "refs/tags/stage-1-deploy^{}"
      via: "annotated tag and peeled remote SHA equality"
      pattern: "git ls-remote --tags origin"
    - from: "refs/tags/stage-* ruleset"
      to: "stage-1-deploy"
      via: "active GitHub tag update/deletion rules"
      pattern: "refs/tags/stage-*"
    - from: "public stage-1-deploy clone"
      to: "uv.lock and tests/test_api.py"
      via: "uv sync --locked followed by nine named public HTTP functions and 12 collected pytest cases"
      pattern: "uv sync --locked"
---

<objective>
Document, accept, publish, protect, and independently reproduce the complete
FastAPI deploy stage per D-02, D-03, D-04, and D-05.

Purpose: Turn the green local application into a durable reader-facing source
stage whose documentation, Git identity, and public clone are independently
verifiable.
Output: README, accepted port-8000 runtime, public GitHub repository, annotated
`stage-1-deploy`, active tag ruleset, and clean-clone evidence.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md
@.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md
@.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md
@.planning/phases/21-fastapi-deploy-stage/21-VALIDATION.md
@.planning/phases/21-fastapi-deploy-stage/21-02-SUMMARY.md
@.planning/quick/260715-e9k-define-fastapi-and-django-tutorial-expan/260715-e9k-CONTEXT.md
@AGENTS.md

<interfaces>
- Local run command: `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`.
- Local acceptance: nine named behavior functions as 12 pytest cases, a diff-free locked runtime export, health JSON,
  and Swagger HTML on the actual port 8000.
- Public repository: `yangchuansheng/sealos-fastapi-tutorial`, default branch
  `main`, visibility public.
- Source identity: annotated `stage-1-deploy` at the accepted local HEAD.
- Immutability control: active tag ruleset for `refs/tags/stage-*` containing
  update and deletion rules.
- Public acceptance: fresh clone of the tag, `uv sync --locked`, and all 12 cases from nine named functions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document and accept the local Stage 1 reader workflow</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py`
    - `.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md` D-03 through D-05
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` observable contract, risks, and validation architecture
    - `.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md` README reader workflow
  </read_first>
  <action>
Create `README.md` in English with: project purpose and Stage 1 scope; Python
3.12 plus `uv` prerequisites; clone of the immutable D-02
`stage-1-deploy` source; `uv sync --locked`; `uv run pytest -q`; the D-03
runtime command `uv run uvicorn app.main:app --host 0.0.0.0 --port 8000`;
health and docs curl checks; and executable JSON examples for create, list,
item read, complete PUT, and DELETE. State the D-05 lifecycle clearly: records
live in one application process, restart clears them, and the reader uses one
Uvicorn process for this source stage. Point database persistence and production
runtime work to their named later stages without introducing their files or
commands. Commit the README before acceptance.

Run all nine named public behavior functions as 12 collected pytest cases per
D-04. Regenerate the D-03 runtime-only
`requirements.txt` from `uv.lock` and require zero diff. Start the accepted
tree on the actual `0.0.0.0:8000`, wait for readiness with a bounded retry loop,
then require exact health JSON and successful Swagger HTML. Stop the server and
retain the command results in the plan summary. Confirm the tracked inventory
contains exactly the nine Phase 21 files, preserving D-05.
  </action>
  <acceptance_criteria>
    - README gives a clean Python 3.12/uv path for clone, locked install, tests, Uvicorn on `0.0.0.0:8000`, and curl verification.
    - README includes usable examples for every D-01 task operation and states the process-local reset lifecycle.
    - Nine named public behavior functions pass as 12 collected pytest cases and D-04 history alternates each of nine exact RED subjects before its matching GREEN subject.
    - Locked runtime export regeneration produces zero tracked diff.
    - Actual port 8000 returns `{"status":"ok"}` from `/health` and Swagger UI HTML from `/docs`.
    - Port 8000 is free before launch, the spawned Uvicorn PID remains alive through readiness, and an early child exit fails with its captured log.
    - Git tracks only `.gitignore`, `.python-version`, `README.md`, `app/__init__.py`, `app/main.py`, `pyproject.toml`, `requirements.txt`, `tests/test_api.py`, and `uv.lock`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv sync --locked &amp;&amp; uv run pytest -q &amp;&amp; test "$(rg -c '^def test_[A-Za-z0-9_]+\(' tests/test_api.py)" -eq 9 &amp;&amp; test "$(uv run pytest tests/test_api.py --collect-only -q | grep -Ec '::test_')" -eq 12 &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-0[12]\):') &amp;&amp; expected=$(printf '%s\n' 'test(21-01): specify public health' 'feat(21-01): add public health endpoint' 'test(21-01): specify generated Swagger UI' 'feat(21-01): enable generated Swagger UI' 'test(21-02): specify task creation' 'feat(21-02): add task creation' 'test(21-02): specify task listing' 'feat(21-02): add task listing' 'test(21-02): specify task retrieval' 'feat(21-02): add task retrieval' 'test(21-02): specify task update' 'feat(21-02): add task update' 'test(21-02): specify task deletion' 'feat(21-02): add task deletion' 'test(21-02): specify invalid task rejection' 'feat(21-02): enforce task title bounds' 'test(21-02): specify missing task responses' 'feat(21-02): stabilize missing task responses') &amp;&amp; test "$actual" = "$expected" &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- uv.lock requirements.txt README.md &amp;&amp; test "$(git ls-files | sort)" = "$(printf '%s\n' .gitignore .python-version README.md app/__init__.py app/main.py pyproject.toml requirements.txt tests/test_api.py uv.lock | sort)" &amp;&amp; bash -lc 'set -euo pipefail; if lsof -nP -iTCP:8000 -sTCP:LISTEN &gt;/dev/null 2&gt;&amp;1; then echo "port 8000 already has a listener" &gt;&amp;2; exit 1; fi; log=$(mktemp); health=$(mktemp); uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 &gt;"$log" 2&gt;&amp;1 &amp; pid=$!; cleanup(){ kill "$pid" 2&gt;/dev/null || true; wait "$pid" 2&gt;/dev/null || true; rm -f "$log" "$health"; }; trap cleanup EXIT; ready=0; for _ in {1..40}; do if ! kill -0 "$pid" 2&gt;/dev/null; then cat "$log" &gt;&amp;2; exit 1; fi; if curl --fail --silent http://127.0.0.1:8000/health &gt;"$health"; then ready=1; break; fi; sleep 0.25; done; if test "$ready" -ne 1; then cat "$log" &gt;&amp;2; exit 1; fi; kill -0 "$pid"; test "$(cat "$health")" = "{\"status\":\"ok\"}"; curl --fail --silent http://127.0.0.1:8000/docs | grep -q "Swagger UI"'</automated>
  </verify>
  <done>The committed README and accepted local tree reproduce nine named behaviors as 12 collected cases, the exact RED/GREEN sequence, the locked export, and the owned port-8000 reader workflow within the D-05 Stage 1 fence.</done>
</task>

<task type="auto">
  <name>Task 2: Publish, protect, and clean-clone the immutable Stage 1 source</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/.git/config, refs/tags/stage-1-deploy, https://github.com/yangchuansheng/sealos-fastapi-tutorial, GitHub tag ruleset</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt`
    - `.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md` D-02 through D-05
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` Publication and Immutable Stage Sequence
    - `.planning/phases/21-fastapi-deploy-stage/21-VALIDATION.md` public clone and tag integrity gates
  </read_first>
  <action>
Record `ACCEPTED_HEAD` from the fully green local `main`. Confirm authenticated
GitHub identity `yangchuansheng`. Per D-02, query the repository first. When it
is absent, run `gh repo create yangchuansheng/sealos-fastapi-tutorial --public
--source=. --remote=origin --push`. When it exists, require exact owner, public
visibility, default branch `main`, and remote `main == ACCEPTED_HEAD`; abort on
every mismatch. This gives recovery an exact-state no-op and closes ambiguous
existing-repository paths.

Build the ruleset payload with `jq -nc` as
`{"name":"Protect tutorial stage tags","target":"tag","enforcement":"active",
"bypass_actors":[],"conditions":{"ref_name":{"exclude":[],"include":
["refs/tags/stage-*"]}},"rules":[{"type":"update"},{"type":"deletion"}]}`.
Query rulesets by exact name and require a count of zero or one. For zero, pipe
the payload to `gh api --method POST
repos/yangchuansheng/sealos-fastapi-tutorial/rulesets --input -`. For one, pipe
the same complete payload to `gh api --method PUT
repos/yangchuansheng/sealos-fastapi-tutorial/rulesets/{ruleset_id} --input -`.
Abort when duplicate named rulesets exist. Read back the resulting ruleset and
require the complete payload state.

Resolve the local direct/peeled values and remote direct/peeled values for
`stage-1-deploy` before mutation, then handle exactly four states. When both
refs are absent, create `git tag -a stage-1-deploy -m 'FastAPI deploy stage'
ACCEPTED_HEAD` and push it. When the remote ref exists and the local ref is
absent, require a remote peeled SHA, fetch the exact object with `git fetch
origin refs/tags/stage-1-deploy:refs/tags/stage-1-deploy`, then require local
object type `tag`, local direct SHA equal to remote direct SHA, and local peeled
commit equal to remote peeled commit and `ACCEPTED_HEAD`. When both refs exist,
require local object type `tag`, equal direct SHAs, and both peeled SHAs equal to
`ACCEPTED_HEAD`. When the local ref exists and the remote ref is absent, require
local object type `tag` plus peeled commit `ACCEPTED_HEAD`, then push once and
verify the new remote direct and peeled SHAs. Abort on every lightweight tag,
missing peeled value, type mismatch, or SHA mismatch. Per D-04, the resulting
remote publishes the complete ordered red-green history.

Clone the public tag into a fresh temporary directory, verify its HEAD equals
the accepted SHA, run `uv sync --locked`, collect nine named behavior functions
as 12 pytest cases, pass all 12 cases, regenerate the runtime-only export with
zero diff, and verify the exact 18-subject RED/GREEN sequence. Remove the clone,
then read back repository and ruleset state once more. Keep D-05 later-stage
source, deployment, tutorial, and screenshot artifacts outside this tag.
  </action>
  <acceptance_criteria>
    - The GitHub repository is public, owned by `yangchuansheng`, and defaults to `main`.
    - Remote `main` resolves to the accepted local HEAD.
    - `stage-1-deploy` is annotated and its peeled remote SHA equals the accepted local HEAD.
    - Exactly one active `refs/tags/stage-*` ruleset contains update and deletion rules with an empty bypass list; create and recovery paths use the explicit full POST/PUT payload.
    - Existing repository, ruleset, and tag state is accepted only when it exactly matches the accepted local state; every conflict aborts.
    - A fresh public clone at the tag synchronizes from `uv.lock` and passes nine named behavior functions as 12 collected pytest cases.
    - The clean clone reproduces `requirements.txt` with zero diff and contains the exact D-05 Stage 1 inventory.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; test "$(gh repo view yangchuansheng/sealos-fastapi-tutorial --json nameWithOwner --jq .nameWithOwner)" = yangchuansheng/sealos-fastapi-tutorial &amp;&amp; test "$(gh repo view yangchuansheng/sealos-fastapi-tutorial --json isPrivate --jq .isPrivate)" = false &amp;&amp; test "$(gh repo view yangchuansheng/sealos-fastapi-tutorial --json defaultBranchRef --jq .defaultBranchRef.name)" = main &amp;&amp; accepted=$(git rev-parse HEAD) &amp;&amp; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$accepted" &amp;&amp; test "$(git cat-file -t stage-1-deploy)" = tag &amp;&amp; test "$(git rev-parse 'stage-1-deploy^{}')" = "$accepted" &amp;&amp; test "$(git ls-remote --tags origin refs/tags/stage-1-deploy | cut -f1)" = "$(git rev-parse stage-1-deploy)" &amp;&amp; test "$(git ls-remote --tags origin 'refs/tags/stage-1-deploy^{}' | cut -f1)" = "$accepted" &amp;&amp; ruleset_ids=$(gh api repos/yangchuansheng/sealos-fastapi-tutorial/rulesets --jq '.[] | select(.name == "Protect tutorial stage tags") | .id') &amp;&amp; test "$(printf '%s\n' "$ruleset_ids" | sed '/^$/d' | wc -l | tr -d ' ')" -eq 1 &amp;&amp; ruleset_id=$(printf '%s\n' "$ruleset_ids") &amp;&amp; gh api "repos/yangchuansheng/sealos-fastapi-tutorial/rulesets/$ruleset_id" | jq -e '.name == "Protect tutorial stage tags" and .target == "tag" and .enforcement == "active" and .conditions.ref_name.exclude == [] and .conditions.ref_name.include == ["refs/tags/stage-*"] and ([.rules[].type] | sort) == ["deletion","update"] and (.bypass_actors | length == 0)' &gt;/dev/null &amp;&amp; bash -lc 'set -euo pipefail; accepted=$(git -C /Users/longnv/bin/repo/sealos-fastapi-tutorial rev-parse HEAD); tmp=$(mktemp -d); trap '\''rm -rf "$tmp"'\'' EXIT; git clone --branch stage-1-deploy https://github.com/yangchuansheng/sealos-fastapi-tutorial.git "$tmp/repo"; cd "$tmp/repo"; test "$(git rev-parse HEAD)" = "$accepted"; uv sync --locked; uv run pytest -q; test "$(rg -c "^def test_[A-Za-z0-9_]+\\(" tests/test_api.py)" -eq 9; test "$(uv run pytest tests/test_api.py --collect-only -q | grep -Ec "::test_")" -eq 12; actual=$(git log --reverse --format=%s | grep -E "^(test|feat)\\(21-0[12]\\):"); expected=$(printf "%s\n" "test(21-01): specify public health" "feat(21-01): add public health endpoint" "test(21-01): specify generated Swagger UI" "feat(21-01): enable generated Swagger UI" "test(21-02): specify task creation" "feat(21-02): add task creation" "test(21-02): specify task listing" "feat(21-02): add task listing" "test(21-02): specify task retrieval" "feat(21-02): add task retrieval" "test(21-02): specify task update" "feat(21-02): add task update" "test(21-02): specify task deletion" "feat(21-02): add task deletion" "test(21-02): specify invalid task rejection" "feat(21-02): enforce task title bounds" "test(21-02): specify missing task responses" "feat(21-02): stabilize missing task responses"); test "$actual" = "$expected"; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt; git diff --exit-code -- uv.lock requirements.txt; test "$(git ls-files | sort)" = "$(printf "%s\n" .gitignore .python-version README.md app/__init__.py app/main.py pyproject.toml requirements.txt tests/test_api.py uv.lock | sort)"'</automated>
  </verify>
  <done>The public repository, protected annotated tag, peeled SHA, and fresh-clone test suite prove the accepted immutable Stage 1 source per D-02 through D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local accepted Git tree to public GitHub | Reader-executable source and history cross into a public repository. |
| GitHub tag and ruleset to future maintainers | Mutable platform state controls the identity of the tutorial source stage. |
| Public clone to local execution | Remote source and package locks become executable reader input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-03 | Tampering | `stage-1-deploy` | high | mitigate | Use an annotated tag, compare the peeled remote SHA with accepted HEAD, and enforce active update/deletion rules on `refs/tags/stage-*`. |
| T-21-03A | Information Disclosure | Public repository contents | high | mitigate | Gate publication on the exact nine-file inventory and a clean Git status containing source, lock, tests, and README only. |
| T-21-03B | Spoofing | Public repository identity | medium | mitigate | Verify authenticated owner, public visibility, canonical URL, default branch, and remote main SHA through GitHub CLI/API. |
| T-21-SC | Tampering | Clean-clone dependency install | high | mitigate | Run `uv sync --locked`, exact export regeneration, and all 12 cases from nine named functions at the public immutable tag. |
</threat_model>

<verification>
Run the local acceptance gate before any publication mutation. After publication,
verify repository metadata, annotated/peeled tag identity, ruleset fields, and
the fresh public clone's lock, export, inventory, and nine-test result.
</verification>

<success_criteria>
- README reproduces setup, testing, port-8000 runtime, every endpoint, and process-local lifecycle.
- Local full suite, export, and actual port smoke pass before publication.
- Public `main` and annotated `stage-1-deploy` resolve to the accepted HEAD.
- Active update/deletion protection covers every `refs/tags/stage-*` tag.
- A fresh public clone at the tag passes locked installation and all 12 cases from nine named functions.
- The published tag contains exactly the D-05 Stage 1 source inventory.
</success_criteria>

## Artifacts this phase produces

- Files: `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`, `requirements.txt`, `app/__init__.py`, `app/main.py`, `tests/test_api.py`, and `README.md` under `/Users/longnv/bin/repo/sealos-fastapi-tutorial`.
- Public Python symbols: `app.main.Task`, `app.main.TaskCreate`, `app.main.TaskUpdate`, `app.main.create_app`, and `app.main.app`.
- Public HTTP operations: `GET /health`, `GET /docs`, `POST /tasks`, `GET /tasks`, `GET /tasks/{task_id}`, `PUT /tasks/{task_id}`, and `DELETE /tasks/{task_id}`.
- Public GitHub state: `yangchuansheng/sealos-fastapi-tutorial`, default branch `main`, annotated `stage-1-deploy`, and active `refs/tags/stage-*` update/deletion ruleset.

## Source Coverage Audit

| Source | ID | Feature or constraint | Plans | Status |
|--------|----|-----------------------|-------|--------|
| GOAL | - | Clone, run, and verify the public Tasks API deploy stage | 21-01, 21-02, 21-03 | COVERED |
| REQ | FAST-01 | Public health, docs, in-memory CRUD, and port 8000 | 21-01, 21-02, 21-03 | COVERED |
| RESEARCH | - | Exact lock/export and package legitimacy gate | 21-01 | COVERED |
| RESEARCH | - | Nine vertical public HTTP RED/GREEN slices | 21-01, 21-02 | COVERED |
| RESEARCH | - | README, local smoke, public clone, annotated tag, SHA and ruleset integrity | 21-03 | COVERED |
| CONTEXT | D-01 | Public HTTP contract | 21-01, 21-02, 21-03 | COVERED |
| CONTEXT | D-02 | Public repository and immutable stage identity | 21-03 | COVERED |
| CONTEXT | D-03 | Python 3.12, uv lock/export, and reader commands | 21-01, 21-03 | COVERED |
| CONTEXT | D-04 | Behavior-first public HTTP history with real collaborators | 21-01, 21-02, 21-03 | COVERED |
| CONTEXT | D-05 | Process-local persistence and Stage 1 source fence | 21-01, 21-02, 21-03 | COVERED |

<output>
Create `.planning/phases/21-fastapi-deploy-stage/21-03-SUMMARY.md` with local acceptance, GitHub publication, tag/ruleset integrity, and fresh-clone evidence.
</output>
