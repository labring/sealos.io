---
phase: 21-fastapi-deploy-stage
plan: 02
type: tdd
wave: 2
depends_on: ["21-01"]
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
autonomous: true
requirements: [FAST-01]
must_haves:
  truths:
    - "A reader can create, list, fetch, update, and delete tasks through JSON HTTP."
    - "Task IDs are deterministic integers and fresh application instances start with empty process-local state."
    - "Invalid titles receive 422 and every unknown item operation receives the stable 404 body."
    - "Seven CRUD/error behaviors each retain RED before GREEN and all nine named behavior functions pass as 12 collected pytest cases."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      provides: "Typed in-memory task CRUD and stable validation/error responses"
      exports: ["Task", "TaskCreate", "TaskUpdate", "create_app", "app"]
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py"
      provides: "Nine named public HTTP behavior functions yielding 12 collected pytest cases with fresh TestClient instances"
      contains: "test_missing_task_returns_404"
  key_links:
    - from: "TestClient(create_app())"
      to: "factory-local task dictionary and ID counter"
      via: "one fresh public application per test"
      pattern: "TestClient\\(create_app\\(\\)\\)"
    - from: "POST /tasks response"
      to: "list, item, update, and delete assertions"
      via: "later HTTP requests use the returned integer ID"
      pattern: "created.json\\(\\)"
    - from: "Pydantic title constraints"
      to: "POST and PUT request validation"
      via: "shared 1..200 character contract"
      pattern: "Field\\(min_length=1, max_length=200\\)"
---

<objective>
Complete all seven task CRUD, validation, and missing-item tracer slices through
the confirmed public HTTP seam per D-01, D-04, and D-05.

Purpose: Make Tasks API fully usable while preserving readable one-test/one-
implementation history and real framework collaborators.
Output: Typed process-local CRUD, nine total public tests, and fourteen ordered
CRUD RED/GREEN commits.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md
@.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md
@.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md
@.planning/phases/21-fastapi-deploy-stage/21-VALIDATION.md
@.planning/phases/21-fastapi-deploy-stage/21-01-SUMMARY.md
@AGENTS.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md

<interfaces>
- `TaskCreate`: `title: str`, `completed: bool = False`.
- `TaskUpdate`: `title: str`, `completed: bool`.
- `Task`: `id: int`, `title: str`, `completed: bool`.
- Valid titles contain 1 through 200 characters.
- POST returns 201; GET collection/item and PUT return 200; DELETE returns an
  empty 204; invalid bodies return FastAPI 422; unknown IDs return 404 with
  `{"detail": "Task not found"}`.
- Collection order is ascending integer ID.
</interfaces>
</context>

<feature>
  <name>Public in-memory Tasks API lifecycle</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <behavior>
    - Create -> 201 with ID 1 and `completed=false`.
    - Create then list -> exact record in ascending-ID collection.
    - Create then item GET -> exact record by returned ID.
    - Create, PUT, then GET -> complete replacement retained.
    - Create, DELETE, then GET -> empty 204 followed by 404.
    - Empty and 201-character titles -> 422.
    - GET, PUT, and DELETE for unknown ID -> stable 404 body.
  </behavior>
  <implementation>Implement one route or constraint only after its focused public test produces the expected RED result.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive create, list, and item retrieval as three vertical slices</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` TDD slices 3 through 5
    - `.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md` model, factory, and lifecycle assignments
  </read_first>
  <action>
Execute three complete RED -> GREEN -> REFACTOR-review cycles in order per
D-04. First, add `test_create_task`, observe POST 404, commit
`test(21-02): specify task creation`, then define `TaskCreate`, `Task`, the
factory-local dictionary/counter, and POST 201; commit
`feat(21-02): add task creation`. Keep title unconstrained until its dedicated
validation RED cycle. Second, add `test_list_tasks`, create through HTTP,
observe GET collection 405, commit exactly
`test(21-02): specify task listing`, add ascending-ID GET collection, then
commit exactly `feat(21-02): add task listing`. Third, add `test_get_task`,
derive the path from the POST response, observe item 404, commit exactly
`test(21-02): specify task retrieval`, add item GET with a generic framework
404 for unknown IDs, then commit exactly
`feat(21-02): add task retrieval`. After each GREEN, run the
named test and accumulated suite; perform the review gate and commit refactoring
only when code changes. Export `Task`, `TaskCreate`, `create_app`, and `app`.
  </action>
  <acceptance_criteria>
    - Create RED observes 404; GREEN returns exact ID 1/title/default completed JSON with status 201.
    - List RED observes 405; GREEN returns the exact earlier POST response in a one-element array.
    - Item RED observes 404 for an existing returned ID; GREEN returns the exact earlier POST response.
    - Each test builds prerequisites and verifies results exclusively through HTTP.
    - Three RED commits precede their three matching GREEN commits.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_api.py -q -k 'create or list or get' &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-02\): (specify task (creation|listing|retrieval)|add task (creation|listing|retrieval))$') &amp;&amp; expected=$(printf '%s\n' 'test(21-02): specify task creation' 'feat(21-02): add task creation' 'test(21-02): specify task listing' 'feat(21-02): add task listing' 'test(21-02): specify task retrieval' 'feat(21-02): add task retrieval') &amp;&amp; test "$actual" = "$expected"</automated>
  </verify>
  <done>Create, collection read, and item read pass through fresh public HTTP clients with three ordered tracer histories.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Drive update and delete as two later-read slices</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` TDD slices 6 and 7
    - `/Users/longnv/.agents/skills/tdd/tests.md` later-read behavior pattern
  </read_first>
  <action>
Execute two complete cycles per D-01 and D-04. Add `test_update_task`: create
through POST, PUT a complete replacement, then GET the same ID; observe PUT
405; commit exactly `test(21-02): specify task update`. Define `TaskUpdate`, add
typed item PUT with generic unknown-ID 404, export the model, run GREEN, and
commit exactly `feat(21-02): add task update`. Add `test_delete_task`: create,
DELETE, assert an empty 204, then GET and assert 404; observe DELETE 405; commit
exactly `test(21-02): specify task deletion`. Add item DELETE with explicit empty
204 and a generic framework 404 for an unknown ID, preserving the later stable-
body RED mismatch; run GREEN and commit exactly
`feat(21-02): add task deletion`. Complete a
REFACTOR review after each GREEN and run the accumulated suite. Per D-05, every
mutation remains inside the factory-local dictionary.
  </action>
  <acceptance_criteria>
    - Update RED observes 405; GREEN returns the complete replacement and later GET returns the same representation.
    - Delete RED observes 405; GREEN returns an empty 204 and later GET returns 404.
    - `TaskUpdate` accepts complete replacement fields and preserves the integer ID.
    - Two RED commits precede their two matching GREEN commits.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_api.py -q -k 'update or delete' &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-02\): (specify task (update|deletion)|add task (update|deletion))$') &amp;&amp; expected=$(printf '%s\n' 'test(21-02): specify task update' 'feat(21-02): add task update' 'test(21-02): specify task deletion' 'feat(21-02): add task deletion') &amp;&amp; test "$actual" = "$expected"</automated>
  </verify>
  <done>Update and delete are verified through later public reads with ordered RED/GREEN evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Drive bounded validation and stable missing-item responses</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` TDD slices 8 and 9 plus Security Domain
    - `/Users/longnv/.agents/skills/tdd/mocking.md`
  </read_first>
  <action>
Execute the final two cycles. Add parameterized `test_reject_invalid_task` for
empty and 201-character titles; observe POST acceptance; commit exactly
`test(21-02): specify invalid task rejection`. Apply
Pydantic `Field(min_length=1, max_length=200)` to create and update titles;
assert framework-generated 422; commit exactly
`feat(21-02): enforce task title bounds`. Add parameterized
`test_missing_task_returns_404` covering GET, valid PUT, and DELETE for an
unknown integer; assert `{"detail":"Task not found"}`; observe the existing
generic body; commit exactly `test(21-02): specify missing task responses`.
Centralize factory-local lookup so all three handlers raise the stable 404 body;
commit exactly `feat(21-02): stabilize missing task responses`. Complete the
final REFACTOR review, run all nine named behavior functions as 12 collected
pytest cases, verify fresh-client isolation, and audit that tests contain
zero internal collaborator mocks or side-channel state reads per D-04.
  </action>
  <acceptance_criteria>
    - Validation RED accepts at least one invalid title; GREEN returns 422 for both bounds.
    - Missing-item RED receives a generic body; GREEN returns the stable body for GET, PUT, and DELETE.
    - All nine named public behavior functions pass as 12 collected pytest cases from fresh application instances.
    - The test suite imports the public factory and uses real framework collaborators.
    - Two RED commits precede their two matching GREEN commits.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_api.py -q -x &amp;&amp; test "$(rg -c '^def test_[A-Za-z0-9_]+\(' tests/test_api.py)" -eq 9 &amp;&amp; test "$(uv run pytest tests/test_api.py --collect-only -q | grep -Ec '::test_')" -eq 12 &amp;&amp; ! rg -n '(unittest\.mock|monkeypatch|\bpatch\()' tests &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-02\): (specify (invalid task rejection|missing task responses)|enforce task title bounds|stabilize missing task responses)$') &amp;&amp; expected=$(printf '%s\n' 'test(21-02): specify invalid task rejection' 'feat(21-02): enforce task title bounds' 'test(21-02): specify missing task responses' 'feat(21-02): stabilize missing task responses') &amp;&amp; test "$actual" = "$expected"</automated>
  </verify>
  <done>The complete nine-behavior contract is green with bounded input, stable errors, real collaborators, and seven ordered CRUD/error tracer histories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON bodies to Pydantic | Reader-controlled task data enters process-local state. |
| Integer path IDs to task lookup | Reader-controlled identifiers select, mutate, or delete records. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-02 | Tampering | POST and PUT bodies | medium | mitigate | Pydantic enforces types and 1..200-character titles; tests assert 422 through public HTTP. |
| T-21-02A | Information Disclosure | Unknown item handlers | medium | mitigate | Centralize a fixed 404 response and verify GET, PUT, and DELETE bodies through HTTP. |
| T-21-02B | Denial of Service | Process-local task input | medium | mitigate | Bound each title to 200 characters and document Stage 1 process-local lifecycle in Plan 03. |
| T-21-SC | Tampering | Locked FastAPI/Pydantic stack | high | mitigate | Execute all slices with the approved exact lock and a full suite after each task. |
</threat_model>

<verification>
Run all nine named behavior functions as 12 collected pytest cases, assert the
exact collected count, inspect the fourteen Plan 02
RED/GREEN commit subjects in behavior order, and verify the test suite remains
at the `TestClient(create_app())` seam.
</verification>

<success_criteria>
- Nine named public HTTP behavior functions pass as 12 collected pytest cases.
- Create/list/get/update/delete satisfy D-01 status and representation contracts.
- Invalid input and unknown IDs satisfy stable framework-native responses.
- All seven CRUD/error RED commits precede their matching GREEN commits.
- D-05 process-local persistence remains the complete storage model.
</success_criteria>

## Artifacts this phase produces

- Files: `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`, `requirements.txt`, `app/__init__.py`, `app/main.py`, `tests/test_api.py`, and `README.md` under `/Users/longnv/bin/repo/sealos-fastapi-tutorial`.
- Public Python symbols: `app.main.Task`, `app.main.TaskCreate`, `app.main.TaskUpdate`, `app.main.create_app`, and `app.main.app`.
- Public HTTP operations: `GET /health`, `GET /docs`, `POST /tasks`, `GET /tasks`, `GET /tasks/{task_id}`, `PUT /tasks/{task_id}`, and `DELETE /tasks/{task_id}`.
- Public GitHub state: `yangchuansheng/sealos-fastapi-tutorial`, annotated `stage-1-deploy`, and an active `refs/tags/stage-*` update/deletion ruleset.

<output>
Create `.planning/phases/21-fastapi-deploy-stage/21-02-SUMMARY.md` with per-slice RED, GREEN, review, test-output, and commit evidence.
</output>
