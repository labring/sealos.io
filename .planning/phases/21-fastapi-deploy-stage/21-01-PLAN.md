---
phase: 21-fastapi-deploy-stage
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/.gitignore
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/.python-version
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/__init__.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
autonomous: true
requirements: [FAST-01]
must_haves:
  truths:
    - "A Python 3.12 reader can reproduce the exact dependency graph with uv and the runtime-only requirements export."
    - "GET /health returns the exact public health payload through a fresh TestClient."
    - "GET /docs serves FastAPI-generated Swagger UI through a fresh TestClient."
    - "Health and docs each retain a failing specification commit before their passing implementation commit."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml"
      provides: "Exact Python, runtime, development, and pytest contract"
      contains: 'requires-python = ">=3.12,<3.13"'
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock"
      provides: "Locked Python 3.12 dependency graph"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt"
      provides: "Exact runtime-only compatibility export"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      provides: "Application factory, health route, and generated docs"
      exports: ["create_app", "app"]
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py"
      provides: "Public health and Swagger UI behavior tests"
  key_links:
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "exact direct pins resolved by uv"
      pattern: "fastapi==0\\.139\\.0"
    - from: "uv.lock"
      to: "requirements.txt"
      via: "uv export --locked --no-dev --no-emit-project --no-hashes"
      pattern: "uv export.*--locked.*--no-dev"
    - from: "tests/test_api.py"
      to: "app/main.py"
      via: "function-scoped TestClient(create_app())"
      pattern: "TestClient\\(create_app\\(\\)\\)"
---

<objective>
Create the approved local repository, lock its Python 3.12 environment, and
deliver health plus generated documentation through explicit RED -> GREEN ->
REFACTOR-review gates per D-01 through D-05.

Purpose: Establish the reproducible public seam that every CRUD slice extends.
Output: Local Git repository, exact lock/export, application factory, two public
HTTP behaviors, and ordered TDD history.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md
@.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md
@.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md
@.planning/phases/21-fastapi-deploy-stage/21-VALIDATION.md
@.planning/quick/260715-e9k-define-fastapi-and-django-tutorial-expan/260715-e9k-CONTEXT.md
@AGENTS.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md

<interfaces>
- `create_app() -> FastAPI` creates a fresh process-local application instance.
- `app = create_app()` is the Uvicorn import target.
- The initial scaffold disables docs/OpenAPI and exposes zero business routes,
  yielding authentic 404 RED results for health and docs.
- Health contract: 200 with `{"status": "ok"}`.
- Docs contract: 200 HTML containing the Swagger UI marker.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize the approved repository and reproducible test seam</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/.gitignore, /Users/longnv/bin/repo/sealos-fastapi-tutorial/.python-version, /Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock, /Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/__init__.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `.planning/phases/21-fastapi-deploy-stage/21-CONTEXT.md` D-02 through D-05
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` Standard Stack, Package Legitimacy Audit, and Stage-1 Layout
    - `.planning/phases/21-fastapi-deploy-stage/21-PATTERNS.md` application-factory and dependency assignments
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial` directory state
  </read_first>
  <action>
Proceed when the D-02 target is absent or empty and initialize Git on `main`.
Per D-03, create `.python-version` with `3.12`; declare Python
`>=3.12,<3.13`; pin FastAPI 0.139.0, Pydantic 2.13.4, and Uvicorn 0.51.0 as
runtime dependencies; pin HTTPX 0.28.1 and pytest 9.1.1 in the development
group; configure pytest for `tests/`. Honor the recorded 2026-07-15 approval
for all five SUS audit rows. Before lock generation, execute a shell loop over
`fastapi|0.139.0|https://github.com/fastapi/fastapi`,
`pydantic|2.13.4|https://github.com/pydantic/pydantic`,
`uvicorn|0.51.0|https://github.com/Kludex/uvicorn`,
`httpx|0.28.1|https://github.com/encode/httpx`, and
`pytest|9.1.1|https://github.com/pytest-dev/pytest`; fetch each exact
`https://pypi.org/pypi/{package}/{version}/json` document with `curl -fsSL` and
use `jq -e` to require `.info.version` plus an official repository URL. Generate
`uv.lock`, inspect `uv tree --locked`, synchronize with `uv sync --locked`, and
export exact runtime requirements using `--locked --no-dev --no-emit-project
--no-hashes`. Create the Python `.gitignore`.

Create `app/__init__.py` and `app/main.py` with `create_app() -> FastAPI`,
module-level `app`, and `__all__ = ["app", "create_app"]`. Disable docs, redoc,
and OpenAPI in this initial seam and leave all D-01 routes unregistered so D-04
owns every behavior. Per D-05, keep the tracked source limited to process-local
Stage 1 concerns. Verify the red baseline and commit the foundation.
  </action>
  <acceptance_criteria>
    - The approved directory is a Git repository on `main`.
    - Exact PyPI metadata and official source checks pass for all five approved packages before lock generation.
    - `uv lock --check`, `uv tree --locked`, and `uv sync --locked` pass under Python 3.12.
    - Regenerating `requirements.txt` yields zero diff and the runtime export omits HTTPX and pytest entries.
    - Fresh TestClient probes return 404 for `/health`, `/docs`, and `/tasks`.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; for spec in 'fastapi|0.139.0|https://github.com/fastapi/fastapi' 'pydantic|2.13.4|https://github.com/pydantic/pydantic' 'uvicorn|0.51.0|https://github.com/Kludex/uvicorn' 'httpx|0.28.1|https://github.com/encode/httpx' 'pytest|9.1.1|https://github.com/pytest-dev/pytest'; do package=${spec%%|*}; remainder=${spec#*|}; version=${remainder%%|*}; repository=${remainder#*|}; curl -fsSL "https://pypi.org/pypi/$package/$version/json" | jq -e --arg version "$version" --arg repository "$repository" '.info.version == $version and (($repository | ascii_downcase) as $expected | any(.info.project_urls[]?; type == "string" and (ascii_downcase | startswith($expected))))' &gt;/dev/null || exit 1; done &amp;&amp; test "$(git branch --show-current)" = main &amp;&amp; test "$(cat .python-version)" = 3.12 &amp;&amp; uv lock --check &amp;&amp; tree=$(uv tree --locked --depth 1) &amp;&amp; for pin in 'fastapi v0.139.0' 'pydantic v2.13.4' 'uvicorn v0.51.0' 'httpx v0.28.1' 'pytest v9.1.1'; do grep -Fq "$pin" &lt;&lt;&lt;"$tree" || exit 1; done &amp;&amp; uv sync --locked &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- uv.lock requirements.txt &amp;&amp; ! grep -Eq '^(httpx|pytest)==' requirements.txt &amp;&amp; uv run python -c 'from fastapi.testclient import TestClient; from app.main import create_app; c=TestClient(create_app()); assert [c.get(p).status_code for p in ("/health","/docs","/tasks")] == [404,404,404]'</automated>
  </verify>
  <done>The local repository has an approved exact lock/export and an importable behavior-free FastAPI seam per D-02 through D-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify and expose public health</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml`
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` TDD slice 1
    - `/Users/longnv/.agents/skills/tdd/tests.md`
  </read_first>
  <action>
RED per D-04: create a function-scoped `TestClient(create_app())` fixture and
`test_health_is_public`; assert 200 plus the literal health JSON; run the named
test; retain the expected 404 failure; commit as `test(21-01): specify public
health`. GREEN per D-01: add only `GET /health` inside `create_app()`, return the
literal payload, rerun the named test, and commit as `feat(21-01): add public
health endpoint`. Complete the REFACTOR review after GREEN, keep collaborators
real, rerun the named test, and create a refactor commit only when reviewed code
changes.
  </action>
  <acceptance_criteria>
    - RED fails on observed 404 versus expected 200.
    - GREEN returns exactly 200 and `{"status": "ok"}`.
    - The RED commit precedes the GREEN commit.
    - The test observes behavior solely through public HTTP.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_api.py::test_health_is_public -q -x &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-01\): .*health') &amp;&amp; expected=$(printf '%s\n' 'test(21-01): specify public health' 'feat(21-01): add public health endpoint') &amp;&amp; test "$actual" = "$expected"</automated>
  </verify>
  <done>Public health is green with retained RED/GREEN history at the confirmed HTTP seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Specify and enable generated Swagger UI</name>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py`
    - `.planning/phases/21-fastapi-deploy-stage/21-RESEARCH.md` TDD slice 2
    - `/Users/longnv/.agents/skills/tdd/SKILL.md`
  </read_first>
  <action>
RED per D-04: add `test_swagger_ui_is_public`; assert status 200, HTML content
type, and the `Swagger UI` marker; run the named test; retain the expected 404;
commit as `test(21-01): specify generated Swagger UI`. GREEN per D-01: construct
FastAPI with title `Tasks API`, version `0.1.0`, and framework-generated docs at
`/docs`; preserve health; rerun the named test; commit as
`feat(21-01): enable generated Swagger UI`. Complete the REFACTOR review, run
both tests, and preserve D-05's process-local stage fence.
  </action>
  <acceptance_criteria>
    - RED fails with `/docs` returning 404.
    - GREEN returns 200 HTML containing the Swagger UI marker.
    - FastAPI owns the generated OpenAPI and Swagger surfaces.
    - Both accumulated tests pass and RED precedes GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_api.py -q -k 'health or swagger' &amp;&amp; actual=$(git log --reverse --format=%s | grep -E '^(test|feat)\(21-01\):') &amp;&amp; expected=$(printf '%s\n' 'test(21-01): specify public health' 'feat(21-01): add public health endpoint' 'test(21-01): specify generated Swagger UI' 'feat(21-01): enable generated Swagger UI') &amp;&amp; test "$actual" = "$expected"</automated>
  </verify>
  <done>Health and framework-generated docs are green through public HTTP with ordered TDD evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI to local lock | Registry metadata and artifacts enter reader-executable source. |
| Public HTTP to FastAPI | Reader-controlled paths reach health and generated documentation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-21-01 | Tampering | Python dependency graph | high | mitigate | Verify exact approved PyPI metadata and official sources, inspect `uv tree --locked`, commit `uv.lock`, and require a diff-free runtime export. |
| T-21-02 | Information Disclosure | `/health` and `/docs` | low | accept | Health exposes one fixed status field; generated docs expose only the intentional Stage 1 schema. |
| T-21-SC | Tampering | Package installation | high | mitigate | Apply the recorded human approval for all SUS rows, exact pins, official metadata checks, and `uv sync --locked`. |
</threat_model>

<verification>
Run the full two-test suite, lock/export gate, and git-log checks for both RED ->
GREEN sequences. Confirm the application imports through `app.main:app`.
</verification>

<success_criteria>
- The local repository and exact Python 3.12 environment reproduce cleanly.
- `/health` and `/docs` satisfy their public contracts.
- Both slices retain RED commits before GREEN commits.
- D-05 later-stage concerns remain outside the tracked file inventory.
</success_criteria>

## Artifacts this phase produces

- Files: `.gitignore`, `.python-version`, `pyproject.toml`, `uv.lock`, `requirements.txt`, `app/__init__.py`, `app/main.py`, `tests/test_api.py`, and `README.md` under `/Users/longnv/bin/repo/sealos-fastapi-tutorial`.
- Public Python symbols: `app.main.Task`, `app.main.TaskCreate`, `app.main.TaskUpdate`, `app.main.create_app`, and `app.main.app`.
- Public HTTP operations: `GET /health`, `GET /docs`, `POST /tasks`, `GET /tasks`, `GET /tasks/{task_id}`, `PUT /tasks/{task_id}`, and `DELETE /tasks/{task_id}`.
- Public GitHub state: `yangchuansheng/sealos-fastapi-tutorial`, annotated `stage-1-deploy`, and an active `refs/tags/stage-*` update/deletion ruleset.

<output>
Create `.planning/phases/21-fastapi-deploy-stage/21-01-SUMMARY.md` with RED, GREEN, review, package-audit, and commit evidence.
</output>
