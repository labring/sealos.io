---
phase: 25-django-postgresql-stage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
  - /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock
  - /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt
autonomous: false
requirements:
  - DJAN-02
must_haves:
  truths:
    - "D-02: Package mutation starts only after human approval confirms the official psycopg and psycopg-binary 3.3.4 identities."
    - "D-07: Python 3.12 resolves psycopg[binary] 3.3.4 through uv.lock and requirements.txt reproduces the runtime-only graph exactly."
    - "The dependency commit changes only pyproject.toml, uv.lock, and requirements.txt and has the exact subject chore(25-01): lock approved psycopg dependency."
    - "Django 5.2.16, pytest 9.1.1, pytest-django 4.12.0, the 24-file Stage 1 inventory, and the protected Stage 1 tag remain unchanged."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml"
      provides: "Exact Django and Psycopg runtime pins plus retained development pins"
      contains: "psycopg[binary]==3.3.4"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      provides: "Locked Python 3.12 dependency graph containing psycopg and psycopg-binary 3.3.4"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt"
      provides: "Exact runtime-only compatibility export"
  key_links:
    - from: "Human package identity checkpoint"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml"
      via: "blocking approval before uv resolution"
      pattern: "psycopg\\[binary\\]==3.3.4"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt"
      via: "locked runtime-only uv export"
      pattern: "psycopg-binary==3.3.4"
---

<objective>
Approve and lock the exact Psycopg Stage 2 dependency boundary for D-02 and
D-07 before executable PostgreSQL work begins.

Purpose: Resolve the mandatory supply-chain checkpoint and create one clean,
reproducible parent commit for every database behavior.
Output: Approved official identities, exact `psycopg[binary]==3.3.4` lock and
runtime export, preserved Stage 1 identity, and the required dependency commit.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-django-postgresql-stage/25-CONTEXT.md
@.planning/phases/25-django-postgresql-stage/25-RESEARCH.md
@.planning/phases/25-django-postgresql-stage/25-PATTERNS.md
@.planning/phases/25-django-postgresql-stage/25-VALIDATION.md
@.planning/phases/24-django-deploy-stage/24-VERIFICATION.md
@/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
@/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock

<interfaces>
- Source repository: `/Users/longnv/bin/repo/sealos-django-tutorial`, branch `main`.
- Retained direct pins: Django 5.2.16, pytest 9.1.1, and pytest-django 4.12.0.
- New runtime pin: `psycopg[binary]==3.3.4`.
- Official upstream tag `3.3.4` peels to `83f110367cdd249cc0a352e2246ecea9e878e5a0`.
- Stage 1 direct tag object: `0d9254d37914976898039ff3c55f94399aa1d7c0`.
- Stage 1 peeled commit: `ca115bf21b599c14e667b336bd78e3c587c24208`.
- Canonical export: `uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm exact Psycopg package identities</name>
  <files>.planning/phases/25-django-postgresql-stage/25-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/25-django-postgresql-stage/25-RESEARCH.md` Package Legitimacy Audit and Open Questions (RESOLVED)
    - `.planning/phases/25-django-postgresql-stage/25-CONTEXT.md` D-02 and D-07
    - `https://pypi.org/project/psycopg/3.3.4/`
    - `https://pypi.org/project/psycopg-binary/3.3.4/`
    - `https://www.psycopg.org/psycopg3/docs/basic/install.html`
    - `https://github.com/psycopg/psycopg/tree/3.3.4`
  </read_first>
  <action>
Retrieve the official PyPI JSON records for `psycopg` and `psycopg-binary`
3.3.4, the official installation page prescribing the binary extra, and the
annotated upstream tag. Present package name, exact release, Python
requirement, official code repository, release date, tag type, and peeled
commit. Record the GSD SUS reason that download telemetry is unavailable and
the fact that no SLOP verdict exists. Per D-02, hold every edit and uv package
operation until the human approval signal confirms all four official identity
facts.
  </action>
  <acceptance_criteria>
    - PyPI resolves `psycopg` 3.3.4 to the official Psycopg repository.
    - PyPI resolves `psycopg-binary` 3.3.4 with Python `>=3.10`.
    - Official installation documentation prescribes `psycopg[binary]`.
    - Upstream tag `3.3.4` peels to `83f110367cdd249cc0a352e2246ecea9e878e5a0`.
    - Human approval confirms the exact identities before package mutation.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; p=$(mktemp); b=$(mktemp); trap '\''rm -f "$p" "$b"'\'' EXIT; curl -fsSL https://pypi.org/pypi/psycopg/3.3.4/json &gt;"$p"; curl -fsSL https://pypi.org/pypi/psycopg-binary/3.3.4/json &gt;"$b"; jq -e '\''.info.version == "3.3.4" and ([.info.project_urls[]] | any(test("github.com/psycopg/psycopg"))) and ([.urls[].yanked] | all(. == false))'\'' "$p" &gt;/dev/null; jq -e '\''.info.version == "3.3.4" and .info.requires_python == ">=3.10" and ([.urls[].yanked] | all(. == false))'\'' "$b" &gt;/dev/null; curl -fsSL https://www.psycopg.org/psycopg3/docs/basic/install.html | rg -F 'psycopg[binary]' &gt;/dev/null; git ls-remote --exit-code --tags https://github.com/psycopg/psycopg.git refs/tags/3.3.4 refs/tags/3.3.4^{} | rg 83f110367cdd249cc0a352e2246ecea9e878e5a0 &gt;/dev/null'</automated>
  </verify>
  <done>The human approves both official distributions, their shared version and source, the binary-extra prescription, and the upstream peeled commit.</done>
  <what-built>An official-source identity record for the two SUS distributions and their shared 3.3.4 release.</what-built>
  <how-to-verify>
    1. Confirm both PyPI records show version 3.3.4 and the official Psycopg project.
    2. Confirm the official installation page prescribes the binary extra.
    3. Confirm the annotated upstream tag peels to the recorded commit.
    4. Confirm the SUS classification is limited to missing download telemetry.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the identities, or report the exact mismatched field.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Lock the approved Psycopg runtime graph</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml, /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock, /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt`
    - `.planning/phases/25-django-postgresql-stage/25-VALIDATION.md` Exact Phase Assertions
  </read_first>
  <action>
After Task 1 approval, require the source at clean accepted Stage 1 main and
reverify the direct and peeled Stage 1 identities. Add only
`psycopg[binary]==3.3.4` to runtime dependencies; retain every existing direct
pin and Python range. Regenerate `uv.lock`, run locked sync, inspect the lock
for `psycopg==3.3.4` and `psycopg-binary==3.3.4`, and regenerate the
runtime-only `requirements.txt`. Confirm development-only pytest packages stay
outside the runtime export.

Stage exactly the three dependency files and commit exactly
`chore(25-01): lock approved psycopg dependency`. Verify this commit is a
direct child of Stage 1 accepted main and changes only those three paths. Run
lock, sync, Django version, export, Stage 1 tag, migration SHA-256, initial
24-file parent inventory, and clean-status assertions after committing.
  </action>
  <acceptance_criteria>
    - The approved exact pin and both resolved Psycopg distributions are present at 3.3.4.
    - `requirements.txt` regenerates from the lock with zero diff and excludes development dependencies.
    - The exact commit subject and three-file scope pass.
    - Stage 1 direct/peeled tag identities and `tasks.0001_initial` SHA-256 remain unchanged.
    - The source tree is clean before Plan 25-02 begins.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv lock --check &amp;&amp; uv sync --locked &amp;&amp; uv run python -m django --version | grep -Fx 5.2.16 &amp;&amp; uv tree --locked | rg 'psycopg' &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- uv.lock requirements.txt &amp;&amp; rg -F 'psycopg==3.3.4' requirements.txt &gt;/dev/null &amp;&amp; rg -F 'psycopg-binary==3.3.4' requirements.txt &gt;/dev/null &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'chore(25-01): lock approved psycopg dependency' &amp;&amp; test "$(git rev-parse HEAD^)" = ca115bf21b599c14e667b336bd78e3c587c24208 &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort)" = "$(printf '%s\n' pyproject.toml requirements.txt uv.lock)" &amp;&amp; test "$(git rev-parse stage-1-deploy)" = 0d9254d37914976898039ff3c55f94399aa1d7c0 &amp;&amp; test "$(git rev-parse 'stage-1-deploy^{}')" = ca115bf21b599c14e667b336bd78e3c587c24208 &amp;&amp; test "$(sha256sum tasks/migrations/0001_initial.py | cut -d' ' -f1)" = 745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3 &amp;&amp; test -z "$(git status --porcelain --untracked-files=all)"</automated>
  </verify>
  <done>The exact approved Psycopg graph is locked and exported from one clean, three-file Stage 2 dependency commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI, official docs, and upstream Git to local lock | External package identity becomes executable reader source. |
| Approved lock to runtime export | Generated resolution determines packages installed outside development. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-SC | Tampering | `psycopg` and `psycopg-binary` package install | high | mitigate | Block all mutation on human approval of exact PyPI records, official binary-extra documentation, upstream tag, and peeled commit; inspect the resulting lock/export. |
| T-25-01 | Tampering | Existing Stage 1 dependency baseline | medium | mitigate | Permit one exact new runtime pin, retain every approved direct version, enforce a three-file commit scope, and reverify Stage 1 identities. |
</threat_model>

<verification>
Run the package identity checkpoint, lock/export reproduction, exact commit
scope, retained direct versions, immutable migration hash, Stage 1 tag
identities, and clean source gate.
</verification>

<success_criteria>
- D-02 receives the exact approved psycopg 3 runtime dependency.
- D-07 lock and runtime export reproduce under Python 3.12.
- Stage 1 source, tag identity, schema migration, and public behavior remain unchanged.
- DJAN-02 has a trusted dependency parent for real PostgreSQL implementation.
</success_criteria>

<output>
Create `.planning/phases/25-django-postgresql-stage/25-01-SUMMARY.md` with
human approval, official package facts, lock/export identities, exact commit
scope, Stage 1 invariants, and clean-status evidence.
</output>
