---
phase: 25-django-postgresql-stage
plan: "03"
type: tdd
wave: 3
depends_on:
  - 25-02
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migrations.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
  - /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml
  - /Users/longnv/bin/repo/sealos-django-tutorial/deploy/source-migration-job.yaml
autonomous: true
requirements:
  - TDD-03
  - DJAN-02
must_haves:
  truths:
    - "D-03: The unchanged tasks.0001_initial migration applies on a fresh PostgreSQL database, a second migrate succeeds at current head, and model drift is zero."
    - "D-06: The production Job runs python manage.py migrate --noinput from a future immutable application image and reads DATABASE_URL from Secret key url."
    - "D-06: One allowlisted source Job reaches Complete, is deleted, is recreated, and reaches Complete again before public health is accepted."
    - "D-08: Every render, ConfigMap input, Job log, and cleanup path is bounded, credential-free, and owned by the exact run identity."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migrations.py"
      provides: "Immutable fresh/current-head Django migration contract"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py"
      provides: "Mode, render, allowlist, redaction, and cleanup contracts"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py"
      provides: "Exact production and source Job manifest contracts"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml"
      provides: "Future application-image one-shot migration contract"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/source-migration-job.yaml"
      provides: "Allowlisted pre-image execution adapter"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py"
      via: "fresh and repeat manage.py migrate commands"
      pattern: "manage.py migrate --noinput"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml"
      to: "Kubernetes Secret key url"
      via: "DATABASE_URL secretKeyRef"
      pattern: "secretKeyRef"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/source-migration-job.yaml"
      via: "exact token renderer, tracked ConfigMap allowlist, strict validation, and two Complete waits"
      pattern: "condition=complete"
---

<objective>
Make D-03 migration ownership and D-06 one-shot Job ordering executable against
fresh real PostgreSQL.

Purpose: Prove both operator commands at the database boundary before the
runtime restart and publication gates depend on them.
Output: Fresh/repeat migration mode, immutable migration tests, production and
source Job manifests, two source Job completions, and two direct RED/GREEN
pairs.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-django-postgresql-stage/25-CONTEXT.md
@.planning/phases/25-django-postgresql-stage/25-RESEARCH.md
@.planning/phases/25-django-postgresql-stage/25-PATTERNS.md
@.planning/phases/25-django-postgresql-stage/25-VALIDATION.md
@.planning/phases/25-django-postgresql-stage/25-02-SUMMARY.md
@.planning/phases/22-fastapi-postgresql-stage/22-03-SUMMARY.md
@/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
@/Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_postgres_harness.py

<interfaces>
- Reader migration command: `python manage.py migrate --noinput`.
- Readback: `python manage.py showmigrations tasks`; drift: `python manage.py makemigrations --check --dry-run`.
- Immutable migration SHA-256: `745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3`.
- Harness adds `--migrations-only`, `--jobs-only [--state-file PATH]`, and retains all Plan 25-02 modes.
- Production placeholders: `__JOB_NAME__`, `__RUN_ID__`, `__IMAGE_REFERENCE__`, and `__SECRET_NAME__` only.
- Source placeholders additionally include `__CONFIGMAP_NAME__`.
- Source ConfigMap allowlist: `manage.py`, `taskboard/__init__.py`, `taskboard/settings.py`, `tasks/__init__.py`, `tasks/apps.py`, `tasks/admin.py`, `tasks/models.py`, `tasks/migrations/__init__.py`, `tasks/migrations/0001_initial.py`, and `requirements.txt`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement the repeatable Django migration gate</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migrations.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py, /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh</files>
  <behavior>
    - The committed migration hash remains exact and the physical Task table is absent at the tasks zero state.
    - `migrate --noinput` creates the Stage 1 Task columns on PostgreSQL; a second invocation succeeds at current head.
    - `showmigrations tasks` reports `[X] 0001_initial` and `makemigrations --check --dry-run` reports zero drift.
    - The harness exposes a bounded `--migrations-only` mode and never creates schema from startup or test helpers.
  </behavior>
  <action>
RED per D-03: add `tests/test_migrations.py` using Django
`MigrationExecutor`, `call_command`, and introspection against the explicit
pytest PostgreSQL database; always restore the migration head in `finally`.
Add harness contract cases for the migrations-only mode, exact fresh/repeat/
show/drift command grammar, immutable source hash, and exact-label cleanup.
Run the focused tests after a real PostgreSQL preflight. RED must fail because
the operator mode and command sequence are absent, with the migration API
cases reaching PostgreSQL. Commit exactly `test(25-03): specify repeatable
Django migration gate`, changing only both test files.

GREEN: extend the harness with one-shot and attached migrations-only flows.
Against a new base database, run the exact reader command twice, require
`[X] 0001_initial`, run the drift check, verify the Task table columns and
immutable source hash, and emit only stable redacted status. Preserve the
existing session/pytest modes and exact cleanup. Run focused tests and a fresh
external migrations-only replay, then commit exactly `feat(25-03): add
repeatable Django migration gate`, changing only the harness as RED's direct
child.
  </action>
  <acceptance_criteria>
    - RED fails for the absent public operator mode after database connectivity succeeds.
    - GREEN proves fresh and repeat migration, applied state, zero drift, exact columns, and immutable hash.
    - RED changes only two test files; GREEN changes only the harness and directly follows RED.
    - A fresh one-shot replay leaves zero labeled objects and stopped owned processes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_migrations.py tests/test_postgres_harness.py -q -x &amp;&amp; ./scripts/test-postgres.sh --migrations-only &amp;&amp; RED=$(git log --format='%H' --grep='^test(25-03): specify repeatable Django migration gate$' -1) &amp;&amp; GREEN=$(git log --format='%H' --grep='^feat(25-03): add repeatable Django migration gate$' -1) &amp;&amp; test "$(git rev-parse "$GREEN^")" = "$RED" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$RED" | sort)" = "$(printf '%s\n' tests/test_migrations.py tests/test_postgres_harness.py)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$GREEN")" = scripts/test-postgres.sh</automated>
  </verify>
  <done>Django migrations exclusively and repeatably establish the PostgreSQL schema through one tested operator mode.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify and implement repeatable migration Job contracts</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py, /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml, /Users/longnv/bin/repo/sealos-django-tutorial/deploy/source-migration-job.yaml, /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh</files>
  <behavior>
    - Production manifest has `batch/v1`, one bounded Job, exact Django migration command, Secret key `url`, disabled service-account token, resources, deadline, and non-root security.
    - Renderer accepts only the declared tokens, writes a 0600 temporary manifest, rejects unresolved/unexpected tokens, validates server-side strictly, and removes the render.
    - Source Job projects only the ten tracked migration inputs, installs the exact runtime export, migrates, and reports applied Task state.
    - The same source Job identity reaches `Complete=True`, is deleted, is recreated, and reaches `Complete=True` again before ready health.
  </behavior>
  <action>
RED per D-06: add an exact-text static production/source manifest contract in
`tests/test_migration_job.py` and extend harness tests for token allowlists,
tracked ConfigMap inputs, safe temporary rendering, disabled evidence helpers,
jobs-only mode, and two ordered completion records. Static tests invoke no
kubectl, source Job, subprocess phase gate, or recursive harness call. RED must
fail for missing tracked manifests. Commit exactly `test(25-03): specify
repeatable migration Job`, changing only the two test files.

GREEN: add `deploy/migration-job.yaml` with the exact future application-image
contract, `restartPolicy: Never`, `backoffLimit: 1`,
`activeDeadlineSeconds: 300`, read-only root, dropped capabilities, memory
`/tmp`, and Secret key `url`. Add `deploy/source-migration-job.yaml` using
Python 3.12 and the exact ten-file ConfigMap projection. Extend the harness to
construct the ConfigMap from tracked files, render only declared tokens,
strictly validate the production and source manifests, run the source Job to
Complete, record redacted applied state, delete/wait, recreate the same Job,
and require a second Complete before exact 200 health. Commit exactly
`feat(25-03): add repeatable migration Job contracts`, changing only both
manifests and the harness as RED's direct child. Run static pytest, then a fresh
live jobs-only gate and exact cleanup.
  </action>
  <acceptance_criteria>
    - RED fails only for absent Job artifacts after source/test loading succeeds.
    - Production validation is strict and carries the exact future-image, Secret, security, resource, retry, and deadline contract.
    - One source Job reaches Complete twice through delete/recreate before ready health.
    - ConfigMap, render, logs, Jobs, database objects, and processes are credential-free and clean to zero.
    - RED/GREEN adjacency and exact test-only versus three-file implementation scopes pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_migration_job.py tests/test_postgres_harness.py -q -x &amp;&amp; python -c "from pathlib import Path; s=Path('tests/test_migration_job.py').read_text(); assert all(x not in s for x in ('kubectl', 'subprocess', 'jobs-only', 'test-postgres.sh'))" &amp;&amp; ./scripts/test-postgres.sh --jobs-only &amp;&amp; RED=$(git log --format='%H' --grep='^test(25-03): specify repeatable migration Job$' -1) &amp;&amp; GREEN=$(git log --format='%H' --grep='^feat(25-03): add repeatable migration Job contracts$' -1) &amp;&amp; test "$(git rev-parse "$GREEN^")" = "$RED" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$RED" | sort)" = "$(printf '%s\n' tests/test_migration_job.py tests/test_postgres_harness.py)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$GREEN" | sort)" = "$(printf '%s\n' deploy/migration-job.yaml deploy/source-migration-job.yaml scripts/test-postgres.sh)"</automated>
  </verify>
  <done>The production contract validates and the allowlisted source adapter completes the same Django migration twice before readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed Django migration to PostgreSQL | A privileged command owns schema creation and migration history. |
| Tracked source and requirements to ConfigMap Job | Selected repository content executes in an external container. |
| Kubernetes Job to Secret | The migration process receives the database URL from cluster state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-05 | Tampering | Migration history | high | mitigate | Pin the accepted migration hash, use Django migration APIs and exact reader commands, require zero drift, and prohibit alternate schema owners. |
| T-25-06 | Elevation of Privilege | Migration Job | high | mitigate | Use one bounded command, non-root security, disabled service-account token, exact Secret key, reviewed source allowlist, and exact run ownership. |
| T-25-07 | Tampering / Information Disclosure | Manifest rendering and Job logs | high | mitigate | Enforce exact tokens, 0600 temporary renders, strict server validation, credential redaction, stable readback, and deterministic deletion. |
</threat_model>

<verification>
Run both direct RED/GREEN pairs, fresh/current-head migration, immutable hash,
zero drift, exact manifests, strict server validation, two source Job Complete
runs, ready health, and exact cleanup.
</verification>

<success_criteria>
- D-03 makes the unchanged Django migration the exclusive replayable schema owner.
- D-06 supplies the future production Job and executes the source adapter twice.
- TDD-03 observes migration completion before readiness on fresh PostgreSQL.
- DJAN-02 has repeatable migration and Job contracts with zero owned residue.
</success_criteria>

<output>
Create `.planning/phases/25-django-postgresql-stage/25-03-SUMMARY.md` with both
RED/GREEN pairs, migration outputs, immutable hash, manifest validation, both
Job completion sequences, readiness ordering, and cleanup evidence.
</output>
