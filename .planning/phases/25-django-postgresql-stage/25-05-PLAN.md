---
phase: 25-django-postgresql-stage
plan: "05"
type: execute
wave: 5
depends_on:
  - 25-04
files_modified:
  - refs/heads/main
  - refs/tags/stage-1-deploy
  - refs/tags/stage-2-postgresql
  - https://github.com/yangchuansheng/sealos-django-tutorial
  - .planning/phases/25-django-postgresql-stage/evidence/django-source.txt
  - .planning/phases/25-django-postgresql-stage/evidence/fastapi-phase-gate.txt
  - .planning/phases/25-django-postgresql-stage/evidence/cleanup.txt
  - .planning/phases/25-django-postgresql-stage/evidence/checksums.txt
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - DJAN-02
must_haves:
  truths:
    - "D-06: Immutable public FastAPI Stage 2 independently passes its verified fresh migration, two-Job, readiness, public read/write, and cleanup gate."
    - "D-07: Public Django main equals the accepted 24-subject/32-file candidate, Stage 1 stays exact, and annotated Stage 2 peels to accepted main with message Django PostgreSQL stage."
    - "D-07: Existing ruleset 19014157 remains the one exact active refs/tags/stage-* update/deletion protection with empty exclude and bypass lists."
    - "D-07: A fresh public Django Stage 2 clone passes lock/export and the complete phase gate against a fresh real PostgreSQL database."
    - "D-08: Final evidence verifies sorted checksums and every Django/FastAPI resource, process, browser session, secure file, and replay clone is absent."
    - "D-09: Final source contains no Phase 26 runtime/image work or Phase 27 tutorial content."
  artifacts:
    - path: "refs/tags/stage-2-postgresql"
      provides: "Protected annotated accepted Django PostgreSQL source identity"
    - path: ".planning/phases/25-django-postgresql-stage/evidence/fastapi-phase-gate.txt"
      provides: "Redacted immutable FastAPI Stage 2 rerun proof"
    - path: ".planning/phases/25-django-postgresql-stage/evidence/django-source.txt"
      provides: "Accepted/public main, both tag identities, messages, and ruleset readback"
    - path: ".planning/phases/25-django-postgresql-stage/evidence/checksums.txt"
      provides: "Sorted SHA-256 manifest over every reviewed evidence input"
  key_links:
    - from: "FastAPI stage-2-postgresql^{}"
      to: "fresh real PostgreSQL phase gate"
      via: "public HTTPS clone at 2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3"
      pattern: "FASTAPI_PHASE_GATE_OK"
    - from: "refs/tags/stage-2-postgresql^{}"
      to: "refs/heads/main"
      via: "local and remote peeled commit equality"
      pattern: "Django PostgreSQL stage"
    - from: "fresh public Django Stage 2 clone"
      to: "scripts/test-postgres.sh --phase-gate"
      via: "locked sync and independent owned PostgreSQL run"
      pattern: "PHASE_GATE_OK"
---

<objective>
Close D-06 through D-09 by rerunning both immutable framework seams, publishing
protected Django Stage 2, replaying it publicly, finalizing evidence, and
proving zero residue.

Purpose: Make the accepted PostgreSQL source independently reproducible while
preserving Stage 1 and all unrelated external state.
Output: FastAPI rerun evidence, accepted public Django main, protected
annotated Stage 2, fresh public replay, final checksums, and complete cleanup.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-django-postgresql-stage/25-CONTEXT.md
@.planning/phases/25-django-postgresql-stage/25-RESEARCH.md
@.planning/phases/25-django-postgresql-stage/25-PATTERNS.md
@.planning/phases/25-django-postgresql-stage/25-VALIDATION.md
@.planning/phases/25-django-postgresql-stage/25-04-SUMMARY.md
@.planning/phases/22-fastapi-postgresql-stage/22-VERIFICATION.md
@.planning/phases/24-django-deploy-stage/24-VERIFICATION.md
@/Users/longnv/bin/repo/sealos-django-tutorial/README.md
@/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh

<interfaces>
- FastAPI Stage 2 direct object `b61254c237885744ae85cb6f81386f77f1e3ac09` peels to `2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3` with message `FastAPI PostgreSQL stage`.
- Django Stage 1 direct object `0d9254d37914976898039ff3c55f94399aa1d7c0` peels to `ca115bf21b599c14e667b336bd78e3c587c24208` with message `Django deploy stage`.
- Django Stage 2 message is `Django PostgreSQL stage`; accepted peeled commit is the final local `docs(25-04)` commit.
- Existing ruleset ID `19014157`, name `Protect tutorial stage tags`, target tag, active, include `refs/tags/stage-*`, empty exclude/bypass, exactly update and deletion.
- Final evidence inputs: `README.md`, `package-identity.txt`, `django-migrations.txt`, `django-jobs.txt`, `django-http.jsonl`, `django-source.txt`, `fastapi-phase-gate.txt`, and `cleanup.txt`; `checksums.txt` is generated last.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rerun immutable FastAPI Stage 2 independently</name>
  <files>.planning/phases/25-django-postgresql-stage/evidence/fastapi-phase-gate.txt, .planning/phases/25-django-postgresql-stage/evidence/cleanup.txt</files>
  <action>
Per D-06, create a new temporary root and clone public FastAPI
`stage-2-postgresql` over HTTPS. Require direct tag object, peeled commit,
annotated message, exact lock/export, and clean status. Front-load shell syntax
and the focused migration-Job/harness pytest-only check, then run its verified
`PHASE22_EVIDENCE_DIR=... ./scripts/test-postgres.sh --phase-gate` against an
independent fresh PostgreSQL service and run ID. Record only source identity,
fresh/repeat Alembic state, both source Job Complete states, ready 200, public
write/later read, checksum result, exact cleanup result, and run ID in
`fastapi-phase-gate.txt`. Scan credentials before retention. Remove the clone
and external evidence root, require no FastAPI run-labeled objects or owned
processes, and require the local FastAPI repository clean and unchanged.
  </action>
  <acceptance_criteria>
    - Public FastAPI Stage 2 identities match the accepted immutable values.
    - Its fresh database, repeat migration, two Jobs, readiness, public persistence, and checksums pass.
    - Retained output contains stable redacted facts only.
    - The FastAPI clone, labeled resources, port-forward, and other owned state are absent.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; tmp=$(mktemp -d); trap '\''rm -rf "$tmp"'\'' EXIT; git clone --branch stage-2-postgresql https://github.com/yangchuansheng/sealos-fastapi-tutorial.git "$tmp/repo"; cd "$tmp/repo"; test "$(git rev-parse stage-2-postgresql)" = b61254c237885744ae85cb6f81386f77f1e3ac09; test "$(git rev-parse HEAD)" = 2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3; test "$(git for-each-ref --format="%(contents:subject)" refs/tags/stage-2-postgresql)" = "FastAPI PostgreSQL stage"; uv sync --locked; bash -n scripts/test-postgres.sh; ./scripts/test-postgres.sh --pytest-only tests/test_migration_job.py tests/test_postgres_harness.py -q -x; PHASE22_EVIDENCE_DIR="$tmp/evidence" ./scripts/test-postgres.sh --phase-gate; git diff --exit-code; cd /Users/longnv/bin/repo/sealos-fastapi-tutorial; test -z "$(git status --porcelain --untracked-files=all)"'</automated>
  </verify>
  <done>The shared FastAPI migration/runtime seam passes independently and returns to zero state before Django publication.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze, publish, and protect accepted Django Stage 2</name>
  <files>refs/heads/main, refs/tags/stage-1-deploy, refs/tags/stage-2-postgresql, https://github.com/yangchuansheng/sealos-django-tutorial, .planning/phases/25-django-postgresql-stage/evidence/django-source.txt</files>
  <action>
Freeze clean `ACCEPTED_HEAD`; run the exact 24-subject, 32-file, lock/export,
migration-hash, test, phase-gate, and D-09 inventory assertions. Before public
mutation, run a no-local clone of the accepted commit through locked sync and
the complete phase gate against a fresh independently owned database, then
remove it and prove cleanup.

Read GitHub owner, public visibility, default main, remote main, Stage 1 direct
and peeled refs/message/type, and ruleset 19014157. Abort every mismatch. Accept
remote main only at Stage 1 peeled commit or exact `ACCEPTED_HEAD`; use a normal
fast-forward push when required. Resolve local/remote Stage 2 through the four
coherent absent/local-only/remote-only/both states used by Phase 22. Create the
annotated tag only when absent, validate exact message/type/peeled commit, and
push only the logical tag ref. Treat lone direct/peeled rows and every SHA,
message, type, owner, visibility, branch, or ruleset disagreement as conflict.
Use no force, replacement, or bypass.

Repeat full readback and write only accepted/public main, Stage 1 and Stage 2
direct/peeled identities, tag messages/types, and exact ruleset fields to
`django-source.txt` after credential scan.
  </action>
  <acceptance_criteria>
    - The final candidate passes exact history/inventory and a pre-public no-local fresh database replay.
    - Public main equals accepted HEAD and Stage 1 remains byte-identical.
    - Annotated Stage 2 has the exact message and peels to accepted public main.
    - Ruleset 19014157 retains exact target, enforcement, conditions, rules, and empty bypass.
    - Every matching recovery state is validated; every conflict aborts without forceful mutation.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ACCEPTED=$(git rev-parse HEAD) &amp;&amp; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$ACCEPTED" &amp;&amp; test "$(git rev-parse stage-1-deploy)" = 0d9254d37914976898039ff3c55f94399aa1d7c0 &amp;&amp; test "$(git rev-parse 'stage-1-deploy^{}')" = ca115bf21b599c14e667b336bd78e3c587c24208 &amp;&amp; test "$(git cat-file -t stage-2-postgresql)" = tag &amp;&amp; test "$(git rev-parse 'stage-2-postgresql^{}')" = "$ACCEPTED" &amp;&amp; test "$(git for-each-ref --format='%(contents:subject)' refs/tags/stage-2-postgresql)" = 'Django PostgreSQL stage' &amp;&amp; test "$(git ls-remote origin 'refs/tags/stage-2-postgresql^{}' | cut -f1)" = "$ACCEPTED" &amp;&amp; gh api repos/yangchuansheng/sealos-django-tutorial/rulesets/19014157 | jq -e '.name == "Protect tutorial stage tags" and .target == "tag" and .enforcement == "active" and .conditions.ref_name.include == ["refs/tags/stage-*"] and .conditions.ref_name.exclude == [] and ([.rules[].type] | sort) == ["deletion","update"] and .bypass_actors == []' &gt;/dev/null</automated>
  </verify>
  <done>Accepted Django Stage 2 is public, annotated, protected, exact, and recoverable without changing Stage 1.</done>
</task>

<task type="auto">
  <name>Task 3: Replay public Django Stage 2 and finalize zero-residue evidence</name>
  <files>.planning/phases/25-django-postgresql-stage/evidence/django-source.txt, .planning/phases/25-django-postgresql-stage/evidence/cleanup.txt, .planning/phases/25-django-postgresql-stage/evidence/checksums.txt</files>
  <action>
Clone public Django `stage-2-postgresql` over HTTPS into a new temporary root.
Require the accepted HEAD, annotated type/message, exact 24-subject history,
exact 32-file inventory, lock/export, immutable migration hash, and clean
status. Front-load shell syntax and the focused public/readiness pytest-only
check, then run the complete phase gate against a fresh real PostgreSQL service
with evidence outside the clone; require both Job completions, readiness,
public restart persistence, authenticated admin readback, and cleanup. Remove
the clone and all external output after extracting curated facts.

Per D-08, update `cleanup.txt` with independent FastAPI and Django run IDs,
zero exact-label resource counts, stopped supervisors/port-forwards/servers,
closed browser sessions, free ports, absent state/log/cookie/response paths,
absent clones, and clean source repositories. Recheck live prefix inventory
without deleting unrelated state. Scan all eight evidence inputs for secrets,
URLs with credentials, passwords, tokens, cookies, CSRF, Secret data,
unresolved tokens, and exception strings. Generate sorted `checksums.txt`
atomically over exactly those eight files last, then verify every checksum.
Finish with complete GitHub identity readback and `--assert-clean-all`.
  </action>
  <acceptance_criteria>
    - A fresh public Django Stage 2 clone passes the complete gate against a fresh database.
    - All eight reviewed evidence inputs are credential-free and checksums verify.
    - Both framework run-label inventories and every owned local/browser/clone path are absent.
    - Public main, both Django tags, messages, direct/peeled identities, and ruleset remain exact after replay.
    - D-09 source inventory contains only Phase 25 assets and accepted Stage 1 files.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; SOURCE=/Users/longnv/bin/repo/sealos-django-tutorial; E=/Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/25-django-postgresql-stage/evidence; tmp=$(mktemp -d); trap '\''rm -rf "$tmp"'\'' EXIT; git clone --branch stage-2-postgresql https://github.com/yangchuansheng/sealos-django-tutorial.git "$tmp/repo"; cd "$tmp/repo"; test "$(git rev-parse HEAD)" = "$(git -C "$SOURCE" rev-parse HEAD)"; uv sync --locked; bash -n scripts/test-postgres.sh; ./scripts/test-postgres.sh --pytest-only tests/test_public_http.py tests/test_postgresql.py -q -x; PHASE25_EVIDENCE_DIR="$tmp/public-evidence" ./scripts/test-postgres.sh --phase-gate; git diff --exit-code; cd "$SOURCE"; ./scripts/test-postgres.sh --assert-clean-all; sha256sum -c "$E/checksums.txt"; test -z "$(git status --porcelain --untracked-files=all)"'</automated>
  </verify>
  <done>Public Stage 2 reproduces from fresh real PostgreSQL and the phase closes with verified evidence plus zero owned residue.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Immutable public clones to local/Sealos execution | Remote source creates local dependencies and external resources. |
| Accepted Git to public GitHub refs/ruleset | Durable reader identity is mutated under authenticated authority. |
| External run output to planning evidence | Cross-framework state may contain credentials or unstable details. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-12 | Spoofing | FastAPI public replay | high | mitigate | Require exact direct/peeled tag identities, annotated message, lock/export, full gate, clean clone, and independent cleanup. |
| T-25-13 | Tampering / Elevation of Privilege | Django public main, tags, and ruleset | high | mitigate | Verify every field before/after mutation, fast-forward only, use a four-state tag recovery machine, preserve ruleset, and prohibit force/replacement/bypass. |
| T-25-14 | Denial of Service | Cross-framework external resources | high | mitigate | Use independent run IDs, bounded gates, exact-label cleanup, assert-clean-all, clone traps, and final live inventory readback. |
| T-25-15 | Information Disclosure | Final evidence | critical | mitigate | Curate stable fields, scan all inputs, create the sorted checksum manifest last, verify it, and retain no credential-bearing raw output. |
</threat_model>

<verification>
Run immutable FastAPI replay, pre-public Django replay, exact publication,
fresh public Django replay, final evidence scans/checksums, complete GitHub
readback, and cross-framework zero-residue assertions.
</verification>

<success_criteria>
- TDD-02 and DJAN-02 reproduce from protected public Django Stage 2.
- TDD-03 independently reproduces both framework migration/runtime seams.
- D-07 preserves Stage 1 and publishes exact protected Stage 2.
- D-08 finishes credential-free evidence and complete cleanup.
- D-09 keeps Phase 26/27 concerns outside the 32-file Stage 2 source.
</success_criteria>

<output>
Create `.planning/phases/25-django-postgresql-stage/25-05-SUMMARY.md` with
FastAPI/Django replay identities, accepted/public SHAs, both Django tags,
ruleset, exact history/inventory, evidence checksums, and final zero-residue
readback.
</output>
