---
phase: 25-django-postgresql-stage
plan: "04"
type: tdd
wave: 4
depends_on:
  - 25-03
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
  - /Users/longnv/bin/repo/sealos-django-tutorial/README.md
  - .planning/phases/25-django-postgresql-stage/evidence/README.md
  - .planning/phases/25-django-postgresql-stage/evidence/package-identity.txt
  - .planning/phases/25-django-postgresql-stage/evidence/django-migrations.txt
  - .planning/phases/25-django-postgresql-stage/evidence/django-jobs.txt
  - .planning/phases/25-django-postgresql-stage/evidence/django-http.jsonl
  - .planning/phases/25-django-postgresql-stage/evidence/cleanup.txt
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - DJAN-02
must_haves:
  truths:
    - "D-05: Process A creates a task through public CSRF POST/redirect/GET, process B reads the retained title from the same PostgreSQL database, and both are independently owned."
    - "D-05: Authenticated Django administration renders the retained Task after restart without exposing acceptance credentials."
    - "D-06: The complete Django phase gate orders fresh schema 503, two source Job completions, ready 200, public write, restart readback, and admin readback."
    - "D-08: Reader workflow and credential-free evidence cover package identity, migrations, Jobs, public HTTP, browser/admin behavior, and exact cleanup."
    - "D-08: --assert-clean-all is a tested read-only audit over both framework run-label inventories and every Phase 25 owned local path; it performs zero deletion."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      provides: "Complete bounded Django phase gate with two-process and browser acceptance"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      provides: "Stage 2 migration-before-readiness reader workflow"
    - path: ".planning/phases/25-django-postgresql-stage/evidence/django-http.jsonl"
      provides: "Redacted readiness, write, restart, and admin observations"
    - path: ".planning/phases/25-django-postgresql-stage/evidence/cleanup.txt"
      provides: "Local Django zero-residue proof"
  key_links:
    - from: "Process A public POST /tasks/"
      to: "Process B public GET /"
      via: "same migrated DATABASE_URL and later rendered title"
      pattern: "restart"
    - from: "Source Job Complete sequence 2"
      to: "GET /health 200"
      via: "phase-gate ordering"
      pattern: "status.*ok"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      via: "documented real-PostgreSQL acceptance command"
      pattern: "test-postgres.sh --phase-gate"
---

<objective>
Prove D-05 persistence across independent Django processes and close the local
D-06/D-08 reader, browser, evidence, and cleanup gate.

Purpose: Turn migration completion into observable persistent behavior before
public Stage 2 identity is mutated.
Output: Restart/admin RED/GREEN pair, complete local phase gate, Stage 2 README,
and the pre-public credential-free evidence package.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-django-postgresql-stage/25-CONTEXT.md
@.planning/phases/25-django-postgresql-stage/25-RESEARCH.md
@.planning/phases/25-django-postgresql-stage/25-PATTERNS.md
@.planning/phases/25-django-postgresql-stage/25-VALIDATION.md
@.planning/phases/25-django-postgresql-stage/25-03-SUMMARY.md
@/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
@/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
@/Users/longnv/bin/repo/sealos-django-tutorial/README.md

<interfaces>
- Phase-gate order: lock/export; missing/unreachable/fresh-schema 503; strict production validation; source Job Complete twice; migrated 200; fresh/repeat migration readback; full pytest; process A write/read; process B read/health; authenticated admin Task readback; evidence; cleanup.
- Runtime server command: `python manage.py runserver 127.0.0.1:PORT --noreload` on owned dynamic ports.
- Public task title derives from the run ID; credentials, cookies, CSRF tokens, response bodies, URLs, logs, and PIDs remain temporary.
- Evidence root is supplied only through `PHASE25_EVIDENCE_DIR`; disabled helpers return success.
- Evidence names are fixed by `25-PATTERNS.md`; final source identity, FastAPI replay, and checksums arrive in Plan 25-05.
- `--assert-clean-all` performs read-only `get`/process/session/path checks for both `tutorial-django-pg-test-*` and `tutorial-fastapi-pg-test-*`; it never invokes a deletion command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement public restart persistence</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py, /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh</files>
  <behavior>
    - Missing configuration, unreachable PostgreSQL, and fresh schema are exact bounded public 503 observations.
    - Process A receives 200 health, submits one CSRF-protected Task, follows PRG, and renders the title.
    - Process A is stopped and reaped before process B starts; process B returns 200 health and renders the retained title.
    - Ephemeral admin credentials authenticate a named browser session whose Task changelist renders the retained title; credentials never reach durable output.
    - Cleanup stops both servers and browser session and removes logs, cookies, responses, credentials, and labeled Kubernetes objects.
    - `--assert-clean-all` reads both framework run-label inventories plus the Phase 25 ownership ledger for servers, port-forwards, browser sessions, temporary roots, state files, and replay clones; it reports zero residue without deleting anything.
  </behavior>
  <action>
RED per D-05 and D-08: extend only `tests/test_postgres_harness.py` with source-contract
tests for owned server identity, bounded readiness polling, missing/unreachable
probes, CSRF form flow, explicit A-stop-before-B-start ordering, retained-title
readback, ephemeral superuser/browser flow, evidence redaction, and cleanup
inventory. Add executable source tests requiring `--assert-clean-all`, exact
Django and FastAPI prefix queries, ownership-ledger checks for every Phase 25
server/port-forward/browser/temp/state/clone path, and an absence of mutation
operations in that mode. Run after real database preflight; retain the absent
restart/cleanup-audit failure. Commit exactly `test(25-04): specify public
restart persistence`.

GREEN: extend the harness phase gate with the exact interface order. Use unique
ports, verified PID command identities, mode-0600 cookie/response/log files,
public curl requests, and one named agent-browser session. Create the
superuser through Django's management command from ephemeral environment
values; authenticate, open `/admin/tasks/task/`, and observe only the retained
title and route. Persist curated JSONL fields only. Call cleanup explicitly,
remove the trap, and assert dead PIDs, free ports, absent browser session and
temporary paths, and zero exact-label inventory. Commit exactly
`feat(25-04): prove PostgreSQL restart persistence` as RED's direct child,
changing only the harness. Implement `--assert-clean-all` in the same GREEN as
a read-only audit: query both framework run-label inventories, validate every
recorded Phase 25 PID/session/path/state/clone is absent, emit one stable result,
and call no delete/kill/close/remove operation. Run the focused harness tests,
then the complete phase gate without retained evidence, then the read-only
audit.
  </action>
  <acceptance_criteria>
    - RED fails for the missing independently-owned restart flow after database preflight.
    - GREEN observes the same public task before and after an explicit server stop/start and in authenticated admin.
    - The two exact commits are adjacent with test-only RED and harness-only GREEN scopes.
    - No credential, cookie, CSRF token, URL, exception string, PID, process, browser session, or labeled object survives.
    - `--assert-clean-all` checks both framework inventories and all owned local categories while performing zero deletion.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; bash -n scripts/test-postgres.sh &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_postgres_harness.py -q -x &amp;&amp; ./scripts/test-postgres.sh --phase-gate &amp;&amp; ./scripts/test-postgres.sh --assert-clean-all &amp;&amp; RED=$(git log --format='%H' --grep='^test(25-04): specify public restart persistence$' -1) &amp;&amp; GREEN=$(git log --format='%H' --grep='^feat(25-04): prove PostgreSQL restart persistence$' -1) &amp;&amp; test "$(git rev-parse "$GREEN^")" = "$RED" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$RED")" = tests/test_postgres_harness.py &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$GREEN")" = scripts/test-postgres.sh</automated>
  </verify>
  <done>The complete real Django runtime seam proves migration ordering, public restart persistence, admin readback, and exact cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Document Stage 2 and retain the final-source local gate</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/README.md, .planning/phases/25-django-postgresql-stage/evidence/README.md, .planning/phases/25-django-postgresql-stage/evidence/package-identity.txt, .planning/phases/25-django-postgresql-stage/evidence/django-migrations.txt, .planning/phases/25-django-postgresql-stage/evidence/django-jobs.txt, .planning/phases/25-django-postgresql-stage/evidence/django-http.jsonl, .planning/phases/25-django-postgresql-stage/evidence/cleanup.txt</files>
  <action>
Per D-07 and D-08, rewrite README's primary path for immutable
`stage-2-postgresql`: Python 3.12/uv, explicit credential handling, locked
sync/export, fresh and repeat `migrate --noinput`, applied-state and drift
readback, migration-before-readiness, exact public health responses, rendered
create/list/admin workflow, complete phase gate, production/source Job roles,
restart persistence, and exact-label cleanup. Keep Stage 1 historical and keep
all D-09 production/image/tutorial concerns in their named later stages.
Commit exactly `docs(25-04): document PostgreSQL reader workflow` after the
full source tests pass.

Create the evidence directory and schema README. Record the approved package
identity, immutable migration hash, fresh/repeat migrations, both Job
completion sequences, readiness states, process A/B public observations,
authenticated admin route/title, and local cleanup through one final-source
Front-load `bash -n` and the focused public/readiness pytest-only gate, then run
the multi-minute `PHASE25_EVIDENCE_DIR=... --phase-gate`. Retain stable statuses, hashes,
resource names, run ID, public routes, and task title only. Scan the package for
database URLs, credentials, password/token/cookie/CSRF fields, Secret data,
unresolved tokens, and exception strings. Defer checksums until Plan 25-05 adds
public identity and FastAPI replay. Finish with exact 24-subject history,
32-file inventory, lock/export, migration hash, clean source, free ports,
absent named browser sessions, and zero current Django run-label inventory.
  </action>
  <acceptance_criteria>
    - README reproduces the complete Stage 2 reader path and migration-before-readiness ordering.
    - The final-source local gate passes every migration, Job, public, restart, admin, evidence, and cleanup behavior.
    - Five evidence data files plus the schema README contain no sensitive values.
    - Exact history, inventory, source hash, lock/export, clean Git, browser, port, and Kubernetes zero-state checks pass.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; E=/Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/25-django-postgresql-stage/evidence &amp;&amp; bash -n scripts/test-postgres.sh &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_public_http.py tests/test_postgresql.py -q -x &amp;&amp; PHASE25_EVIDENCE_DIR="$E" ./scripts/test-postgres.sh --phase-gate &amp;&amp; ./scripts/test-postgres.sh --assert-clean-all &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'docs(25-04): document PostgreSQL reader workflow' &amp;&amp; uv lock --check &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code &amp;&amp; test "$(git ls-files | wc -l | tr -d ' ')" -eq 32 &amp;&amp; test "$(git log --format=%s | wc -l | tr -d ' ')" -eq 24 &amp;&amp; test -z "$(git status --porcelain --untracked-files=all)"</automated>
  </verify>
  <done>The exact final local candidate is documented, evidenced without credentials, fully accepted, and clean before publication.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public form and admin browser to Django/PostgreSQL | Untrusted input and ephemeral credentials cross CSRF, auth, sessions, ORM, and rendering. |
| Owned local processes to persistent database | Two independent servers share committed records and process-control authority. |
| Runtime output to retained evidence | Logs and HTTP state may contain credentials or volatile values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-08 | Spoofing / Tampering | Public form and admin | high | mitigate | Preserve CSRF, ModelForm, autoescaping, Django auth/session, ephemeral superuser credentials, and public readback. |
| T-25-09 | Tampering | Restart persistence | medium | mitigate | Verify PID identity, stop/reap A before B, use one explicit database, and prove persistence only through later HTTP/admin reads. |
| T-25-10 | Information Disclosure | Evidence package | critical | mitigate | Curate stable fields, scan forbidden credential/state patterns, keep sensitive files temporary, and generate final checksums only after review. |
| T-25-11 | Denial of Service | Servers, ports, and browser sessions | medium | mitigate | Use dynamic ports, bounded polling, explicit cleanup, trap removal, PID/session/path assertions, and exact-label inventory. |
</threat_model>

<verification>
Run restart RED/GREEN, complete phase gate, README reproduction, evidence
redaction, exact history/inventory, dependency/migration invariants, clean Git,
and local zero-residue assertions.
</verification>

<success_criteria>
- TDD-02 covers health, create/list, and administration on real PostgreSQL.
- TDD-03 orders two Job completions before readiness and public persistence.
- DJAN-02 data survives an owned Django restart and appears in admin.
- D-08 retains credential-free local evidence and complete cleanup.
</success_criteria>

<output>
Create `.planning/phases/25-django-postgresql-stage/25-04-SUMMARY.md` with the
restart RED/GREEN pair, final-source SHA, migrations, Jobs, public/admin
observations, evidence scan, exact history/inventory, and cleanup proof.
</output>
