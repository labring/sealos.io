---
phase: 25-django-postgresql-stage
plan: "02"
type: tdd
wave: 2
depends_on:
  - 25-01
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/conftest.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py
autonomous: true
requirements:
  - TDD-02
  - TDD-03
  - DJAN-02
must_haves:
  truths:
    - "D-01: Every focused database test owns a dedicated run-labeled PostgreSQL 17 service in ns-let51wad and cleanup selects only that run."
    - "D-02: Stage 2 accepts only explicit PostgreSQL URLs, uses a distinct explicit test database, and supplies no SQLite runtime path."
    - "D-04: Public /health returns exact 503 JSON until the configured PostgreSQL Task table exists and exact 200 JSON after migration."
    - "D-05: Exactly the five retained Stage 1 public behaviors pass through real Django collaborators and real PostgreSQL."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      provides: "Run-scoped PostgreSQL session, explicit state, pytest dispatch, and exact cleanup foundation"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py"
      provides: "Strict URL-to-Django database configuration with bounded connect timeout"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      provides: "Schema-aware public readiness"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py"
      provides: "URL and public PostgreSQL readiness behavior"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py"
      provides: "Exactly five retained public Django behaviors on PostgreSQL"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      to: "Sealos namespace ns-let51wad"
      via: "tutorial.sealos.io/run-id label, generated names, supervised port-forward, and cleanup trap"
      pattern: "tutorial-django-pg-test"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py"
      to: "Django PostgreSQL backend"
      via: "TEST_DATABASE_URL or DATABASE_URL converted to native fields"
      pattern: "django.db.backends.postgresql"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py"
      via: "Django connection introspection of Task._meta.db_table"
      pattern: "Task._meta.db_table"
---

<objective>
Move the accepted D-05 public seam onto owned real PostgreSQL and make the
D-04 health response depend on the migrated schema, implementing D-01, D-02,
D-04, and the first half of D-05.

Purpose: Prove the hardest runtime constraint early: explicit PostgreSQL
configuration and real schema state control public readiness while the
rendered Task Board contract remains stable.
Output: Owned PostgreSQL test foundation, strict settings conversion,
schema-aware health, retained five-function public seam, and one direct
readiness RED/GREEN pair.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-django-postgresql-stage/25-CONTEXT.md
@.planning/phases/25-django-postgresql-stage/25-RESEARCH.md
@.planning/phases/25-django-postgresql-stage/25-PATTERNS.md
@.planning/phases/25-django-postgresql-stage/25-VALIDATION.md
@.planning/phases/25-django-postgresql-stage/25-01-SUMMARY.md
@.planning/phases/24-django-deploy-stage/24-VERIFICATION.md
@/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
@/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py
@/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh

<interfaces>
- PostgreSQL image: `postgres:17.10-bookworm@sha256:4f736ae292687621d4dbe0d499ffd024a36bd2ee7d8ca6f2ccd4c800f047b394`.
- Namespace: `ns-let51wad`; prefix: `tutorial-django-pg-test`; ownership label: `tutorial.sealos.io/run-id`.
- State keys: `RUN_ID`, `DATABASE_URL`, `TEST_DATABASE_URL`, `SUPERVISOR_PID`, and `PORT_FORWARD_PID`; state mode 0600.
- Base and test database names are distinct; the PostgreSQL role can create and destroy the explicit pytest database.
- Public health: migrated `200 {"status":"ok"}`; missing, unreachable, or unmigrated `503 {"status":"unavailable"}`.
- Retain these exact five function names: `test_health_is_public`, `test_empty_task_board_is_rendered`, `test_task_creation_redirects_then_appears_on_board`, `test_invalid_task_title_shows_feedback_without_creating_task`, and `test_admin_login_is_public`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the owned real-PostgreSQL test foundation</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh, /Users/longnv/bin/repo/sealos-django-tutorial/tests/conftest.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/conftest.py`
    - `.planning/phases/25-django-postgresql-stage/25-PATTERNS.md` Real PostgreSQL lifecycle
  </read_first>
  <action>
Per D-01, adapt the verified FastAPI session envelope with Bash 3.2-compatible
syntax, the Django resource prefix, exact run labels, the pinned PostgreSQL
image, generated Secret, bounded rollout/in-pod readiness, an `emptyDir`, a
loopback port-forward supervised in a detached POSIX session, and one distinct
explicit pytest database name. Preflight the authenticated context, namespace,
required create/delete/get permissions, resource prefixes, and local port.

Implement `--session-start --state-file PATH`, `--session-stop --state-file
PATH`, `--assert-clean --state-file PATH`, and `--pytest-only [ARGS...]`.
Atomically write the five-key 0600 state file only after connectivity succeeds;
validate grammar, permissions, run-label coherence, and process command
identity before sourcing or signaling. Install cleanup immediately when the
first object or process is owned. Delete only exact names or the exact run
label, wait boundedly for dependent Pods, require zero labeled Deployment,
Pod, Service, Secret, Job, and ConfigMap objects, and propagate cleanup
failure. Redact both URLs from all output.

Add `tests/conftest.py` session checks requiring both explicit URLs,
PostgreSQL schemes, distinct database names, and the harness run. Let
pytest-django own test database creation, migrations, transaction isolation,
and destruction. Commit exactly `chore(25-02): add owned PostgreSQL test
foundation` with only these two files. Run a real session start/stop/assert
cycle and leave zero state before RED.
  </action>
  <acceptance_criteria>
    - A session reaches real PostgreSQL 17 and returns a valid five-key 0600 handle.
    - Runtime and test URLs identify distinct databases on the same owned service.
    - Successful and failed paths converge on exact-label cleanup and stopped owned processes.
    - The foundation commit has the exact subject, two-file scope, and clean status.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; cd /Users/longnv/bin/repo/sealos-django-tutorial; TMP_DIR=$(mktemp -d /tmp/sealos-django-phase25-02.XXXXXX); STATE_FILE=$TMP_DIR/state.env; trap '\''rm -rf "$TMP_DIR"'\'' EXIT; test "$(stat -f "%Lp" "$TMP_DIR")" = 700; bash -n scripts/test-postgres.sh; ./scripts/test-postgres.sh --session-start --state-file "$STATE_FILE"; test "$(stat -f "%Lp" "$STATE_FILE")" = 600; test "$(grep -Ec "^(RUN_ID|DATABASE_URL|TEST_DATABASE_URL|SUPERVISOR_PID|PORT_FORWARD_PID)=" "$STATE_FILE")" -eq 5; ./scripts/test-postgres.sh --session-stop --state-file "$STATE_FILE"; ./scripts/test-postgres.sh --assert-clean --state-file "$STATE_FILE"; test ! -e "$STATE_FILE"; test "$(git show -s --format=%s HEAD)" = "chore(25-02): add owned PostgreSQL test foundation"; test -z "$(git status --porcelain --untracked-files=all)"'</automated>
  </verify>
  <done>One fail-closed harness owns real PostgreSQL, explicit test isolation, state transfer, and exact cleanup before the first database behavior RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify and implement strict PostgreSQL readiness</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py</files>
  <behavior>
    - PostgreSQL URLs with either accepted scheme and encoded credentials produce native Django fields and a one-second connection timeout.
    - Unsupported, incomplete, queried, fragmented, multi-path, and malformed-port values fail configuration without exposing the input.
    - A reachable database with the Task migration at zero returns exact public 503 JSON; restoring `tasks.0001_initial` returns exact public 200 JSON.
    - The retained public health function runs as a database-marked public Client behavior.
  </behavior>
  <action>
Start one task session, source its validated handle, and prove PostgreSQL TCP
connectivity before RED. Per D-04 and D-05, add `tests/test_postgresql.py` with
subprocess configuration cases plus transactional public health cases that use
Django `MigrationExecutor` to move only `tasks` to zero and restore
`tasks.0001_initial` in `finally`. Mark the retained health function for
database access without renaming any of the five public functions. RED must
exit with pytest status 1 on the exact public mismatch `assert 200 == 503`
after the owned database preflight. Commit exactly `test(25-02): specify
PostgreSQL readiness`, changing only the two test files.

GREEN per D-02: in `taskboard/settings.py`, choose `TEST_DATABASE_URL` when
present and otherwise `DATABASE_URL`; use `urlsplit` and strict decoding;
accept only `postgresql` and `postgres`; require username, password, host, one
database segment, and a valid port; reject query/fragment complexity; and
build Django's PostgreSQL fields with `connect_timeout: 1`, `CONN_MAX_AGE: 0`,
and explicit `TEST.NAME`. Use Django's dummy backend plus a configuration flag
for the absent-URL health state, with no usable fallback database.

In `tasks/views.py`, keep board and creation behavior unchanged. Make health
return the locked 200 response only when the configuration flag is true,
PostgreSQL is reachable, and `Task._meta.db_table` appears through Django
introspection. Catch Django configuration/database exceptions, close failed
connections, log only a stable reason class, and return the locked 503
response. Run the named and accumulated files against the same task session,
then commit exactly `feat(25-02): require migrated PostgreSQL`, changing only
settings and views as RED's direct child. Stop/assert the session once after
GREEN.
  </action>
  <acceptance_criteria>
    - RED fails for the prior public 200 on an unmigrated schema after real database preflight.
    - GREEN passes strict URL cases and exact unmigrated/migrated public responses on PostgreSQL.
    - RED and GREEN are direct parent/child commits with exact test-only and two-file production scopes.
    - Board/form/admin files and the immutable migration remain byte-identical.
    - The shared run cleans to zero after GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_postgresql.py tests/test_public_http.py -q -x &amp;&amp; RED=$(git log --format='%H' --grep='^test(25-02): specify PostgreSQL readiness$' -1) &amp;&amp; GREEN=$(git log --format='%H' --grep='^feat(25-02): require migrated PostgreSQL$' -1) &amp;&amp; test "$(git rev-parse "$GREEN^")" = "$RED" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$RED" | sort)" = "$(printf '%s\n' tests/test_postgresql.py tests/test_public_http.py)" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r "$GREEN" | sort)" = "$(printf '%s\n' taskboard/settings.py tasks/views.py)" &amp;&amp; test "$(sha256sum tasks/migrations/0001_initial.py | cut -d' ' -f1)" = 745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3</automated>
  </verify>
  <done>Explicit PostgreSQL configuration and migrated schema state are the only ready path, with one retained public RED/GREEN pair.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the complete Stage 1 public seam on PostgreSQL</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py`
    - `.planning/phases/24-django-deploy-stage/24-VERIFICATION.md`
  </read_first>
  <action>
Per D-05 and TDD-02, require exactly the five accepted function names and run
them with the new readiness/configuration cases through `--pytest-only` on a
fresh owned PostgreSQL service. Preserve Django Client, routing, ORM,
ModelForm, templates, CSRF/session middleware, and admin without internal
mocking. Confirm valid POST/redirect/later GET, invalid-title later-empty read,
native admin login, and migrated health. Run `manage.py check`, lock/export,
migration hash, source scans for a PostgreSQL-only runtime backend, exact-label
cleanup, and clean Git status. This acceptance task creates no commit.
  </action>
  <acceptance_criteria>
    - Exactly five retained public behaviors pass against real PostgreSQL.
    - Public create/list persistence remains observable through POST, redirect, and later GET.
    - No internal collaborator mock or usable SQLite runtime backend exists.
    - The database run, port-forward, state file, and all run-labeled objects are absent afterward.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test "$(rg -c '^def test_[A-Za-z0-9_]+\(' tests/test_public_http.py)" -eq 5 &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_public_http.py tests/test_postgresql.py -q -x &amp;&amp; uv run python manage.py check &amp;&amp; uv lock --check &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code &amp;&amp; python -c "from pathlib import Path; s=Path('taskboard/settings.py').read_text(); assert 'django.db.backends.postgresql' in s; assert 'django.db.backends.sqlite3' not in s" &amp;&amp; test -z "$(git status --porcelain --untracked-files=all)"</automated>
  </verify>
  <done>TDD-02's exact public seam is green on real PostgreSQL and the Stage 2 runtime has one explicit database path.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local harness to Sealos Kubernetes | The script creates and deletes namespace resources under authenticated authority. |
| Environment URL to Django settings | Credentials and network identity become backend configuration. |
| Anonymous health/form/admin requests to Django/PostgreSQL | Public input and readiness checks cross framework and database boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-25-01 | Information Disclosure | URLs, state, and logs | high | mitigate | Keep URLs in 0600 state/child environments, redact output, log stable reason classes, and delete state/log files. |
| T-25-02 | Tampering / Denial of Service | Kubernetes cleanup | high | mitigate | Generate one run identity, verify every owned name, select exact labels, preserve unrelated resources, and fail when zero inventory is unproven. |
| T-25-03 | Denial of Service | Public readiness | medium | mitigate | Use one-second connection timeout, bounded server waits, and migrated-table introspection. |
| T-25-04 | Tampering | Task form input | medium | mitigate | Preserve CSRF middleware, title-only ModelForm validation, Django ORM parameterization, and autoescaped rendering. |
</threat_model>

<verification>
Verify the owned session lifecycle, expected public RED, direct GREEN,
configuration cases, exact five-function public suite, migration immutability,
PostgreSQL-only settings, lock/export, and exact cleanup.
</verification>

<success_criteria>
- D-01 and D-02 provide real owned PostgreSQL with strict explicit configuration.
- D-04 returns exact schema-aware public health responses.
- D-05 and TDD-02 preserve all five public Django behaviors on real PostgreSQL.
- DJAN-02 database-backed Task reads work without changing the reader-visible board contract.
</success_criteria>

<output>
Create `.planning/phases/25-django-postgresql-stage/25-02-SUMMARY.md` with the
foundation commit, RED failure, GREEN result, public suite, run IDs, exact
cleanup, migration hash, and commit ancestry.
</output>
