---
phase: 14-canonical-object-storage-start-here-page
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - content/docs/guides/object-storage/index.en.mdx
autonomous: true
requirements: [UPLD-01, UPLD-02]

must_haves:
  truths:
    - "A user can follow the guide to create a storage bucket with private permission"
    - "A user reads a brief explanation of all three permission levels (private, publicRead, publicReadWrite)"
    - "A user can find and retrieve Access Key, Secret Key, Internal endpoint, and External endpoint from one guide path"
    - "A user understands the difference between Internal and External endpoints"
  artifacts:
    - path: "content/docs/guides/object-storage/index.en.mdx"
      provides: "Create a Bucket and Get Your Credentials sections appended after overview"
      contains: "## Create a Bucket"
  key_links:
    - from: "content/docs/guides/object-storage/index.en.mdx"
      to: "images/object-storage-create-bucket-interface.zh-cn.png"
      via: "markdown image reference"
      pattern: "object-storage-create-bucket-interface"
---

<objective>
Append the bucket creation walkthrough and credential retrieval sections to the
English Object Storage page, covering the path from opening the Object Storage
app to having all four credential values ready.

Purpose: Fulfill UPLD-01 (create bucket with permission guidance) and UPLD-02
(obtain credentials from one guide path).

Output: `content/docs/guides/object-storage/index.en.mdx` with "Create a Bucket"
and "Get Your Credentials" sections appended after the overview from Plan 14-01.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-object-storage-audit.md
@.planning/phases/14-canonical-object-storage-start-here-page/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the bucket creation and credential retrieval sections</name>
  <files>content/docs/guides/object-storage/index.en.mdx</files>
  <read_first>
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md (heading order, MDX components, screenshot posture, writing guardrails)
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md (credential display rules, permission guidance contract, product-truth constraints)
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-object-storage-audit.md (Chinese source sections for factual input, screenshot inventory)
    - content/docs/guides/object-storage/index.en.mdx (current file as modified by Plan 14-01 -- read the ENTIRE file to understand where to append)
    - content/docs/guides/object-storage/index.zh-cn.mdx (Chinese source lines 18-97 for bucket creation, upload, and credential retrieval facts)
  </read_first>
  <action>
Append TWO new sections to the end of `content/docs/guides/object-storage/index.en.mdx` (after the existing "What Object Storage Is" section from Plan 14-01). Do NOT modify any existing content. Only append.

**## Create a Bucket** (per 13-page-outline-spec.md):

Opening: 1-2 sentences stating what the user will do in this section.

Use `<div className='fd-steps [&_h4]:fd-step'>` with `<h4>` sub-steps:

- **Step: Open the Object Storage app** -- Direct the user to sign in to Sealos and open the Object Storage app in the console. Per product-truth rules (D-10 unstable facts): use generic action language ("open the Object Storage app"), do NOT hardcode exact menu paths or console URLs.

- **Step: Create a new bucket** -- Direct the user to use the bucket creation action: enter a bucket name, set permission to `private` (per D-07), and apply. Per product-truth rules: use generic action language for button labels ("use the bucket creation action" or "create a bucket"), do NOT write exact button text as literal quotes.

Close the `</div>`.

Include a screenshot reference for the bucket creation interface (per outline spec screenshot posture):
```
![Sealos Object Storage -- bucket creation interface (Chinese console UI)](./images/object-storage-create-bucket-interface.zh-cn.png)
```

Include a `<Callout type="info">` block explaining the three permission levels (per D-09, per safe-example contract permission guidance):
- **`private`**: only authenticated users can access bucket contents. Recommended for the first bucket and any bucket containing sensitive data.
- **`publicRead`**: anyone can read objects without authentication; write operations still require credentials. Suitable for content delivery and static website hosting.
- **`publicReadWrite`**: fully open read and write access. Use with caution; production environments should avoid this level.

Frame `private` as the recommended starting point.

**## Get Your Credentials** (per 13-page-outline-spec.md):

Opening: 1-2 sentences stating that the user will retrieve the four values needed to access the bucket programmatically (even though the first upload uses the console).

Direct the user to click the credential retrieval action in the console (per D-05). Per product-truth rules: use generic action language ("open the credential display" or "use the access key action"), do NOT write exact button position or exact label text as literal quotes.

Describe what appears: Access Key, Secret Key, Internal endpoint, External endpoint (these are stable facts per D-10).

Include screenshot references for credential retrieval (per outline spec):
```
![Sealos Object Storage -- bucket list with credential access button (Chinese console UI)](./images/sealos-objectstorage-access-key-get-entry.zh-cn.png)
```
```
![Sealos Object Storage -- credential display showing Access Key, Secret Key, and endpoints (Chinese console UI)](./images/sealos-objectstorage-access-key-get-result.zh-cn.png)
```

Include a `<Callout type="tip">` explaining Internal vs External endpoints:
- Internal endpoint: for services running inside Sealos (lower latency, no external traffic)
- External endpoint: for access from outside the Sealos environment

Per safe-example contract credential display rules:
- Do NOT display placeholder credential values (no `xxxxxxxx`, no `your-access-key-here`, no fabricated endpoint URLs).
- Describe this as a console-guided retrieval action: the reader opens the credential display and reads values directly.
- The reader does NOT need to paste credentials into a terminal for the first-upload path.

**Writing rules (all sections):**
- English only, action-oriented, second person, present tense.
- Short paragraphs (3-4 sentences max).
- No code blocks, no Tabs/Tab components, no SDK examples.
- No Chinese text.
- No placeholder credential values.
- Screenshots are optional reinforcement; the text must stand on its own.
  </action>
  <verify>
    <automated>
grep -c "## Create a Bucket" content/docs/guides/object-storage/index.en.mdx && grep -c "## Get Your Credentials" content/docs/guides/object-storage/index.en.mdx && grep -c "fd-steps" content/docs/guides/object-storage/index.en.mdx && grep -c "Callout" content/docs/guides/object-storage/index.en.mdx && grep -c "private" content/docs/guides/object-storage/index.en.mdx && grep -c "publicRead" content/docs/guides/object-storage/index.en.mdx && ! grep -qP "[\x{4e00}-\x{9fff}]" content/docs/guides/object-storage/index.en.mdx && ! grep -q "xxxxxxxx" content/docs/guides/object-storage/index.en.mdx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - "## Create a Bucket" heading exists in the file
    - "## Get Your Credentials" heading exists in the file
    - Create a Bucket section uses `fd-steps` with `h4` sub-steps
    - Permission Callout explains all three levels: private, publicRead, publicReadWrite
    - Permission Callout recommends private as the starting point
    - Get Your Credentials section mentions all four values: Access Key, Secret Key, Internal endpoint, External endpoint
    - A Callout (type tip) explains Internal vs External endpoint difference
    - At least one screenshot reference to bucket creation interface exists
    - At least one screenshot reference to credential display exists
    - No placeholder credential values (no xxxxxxxx, no your-access-key-here)
    - No Chinese text anywhere in the file
    - No code blocks with language syntax highlighting
    - The existing overview section from Plan 14-01 is preserved unchanged
  </acceptance_criteria>
  <done>The English Object Storage page now covers bucket creation with permission guidance and credential retrieval. A reader can follow the guide to create a private bucket and find all four credential values. The file is ready for Plan 14-03 to append the upload and next-steps sections.</done>
</task>

</tasks>

<verification>
- `grep -c '## Create a Bucket' content/docs/guides/object-storage/index.en.mdx` returns 1
- `grep -c '## Get Your Credentials' content/docs/guides/object-storage/index.en.mdx` returns 1
- `grep -c 'fd-steps' content/docs/guides/object-storage/index.en.mdx` returns at least 1
- `grep -c 'Callout' content/docs/guides/object-storage/index.en.mdx` returns at least 2
- `grep -c 'publicRead' content/docs/guides/object-storage/index.en.mdx` returns at least 1
- `grep -c 'publicReadWrite' content/docs/guides/object-storage/index.en.mdx` returns at least 1
- `grep -cP '[\x{4e00}-\x{9fff}]' content/docs/guides/object-storage/index.en.mdx` returns 0
- `grep -c 'xxxxxxxx' content/docs/guides/object-storage/index.en.mdx` returns 0
- File still contains "## What Object Storage Is" from Plan 14-01
</verification>

<success_criteria>
The English Object Storage page has bucket creation and credential retrieval sections that fulfill UPLD-01 and UPLD-02. A reader can create a private bucket with clear permission guidance and retrieve all four credential values from one guide path.
</success_criteria>

<output>
After completion, create `.planning/phases/14-canonical-object-storage-start-here-page/14-02-SUMMARY.md`
</output>
