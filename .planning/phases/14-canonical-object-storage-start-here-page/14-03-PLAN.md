---
phase: 14-canonical-object-storage-start-here-page
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - content/docs/guides/object-storage/index.en.mdx
autonomous: true
requirements: [UPLD-03]

must_haves:
  truths:
    - "A user can follow the guide to upload a first file through the console"
    - "A user can verify the upload succeeded by seeing the file in the console list"
    - "A user understands what Object Storage capabilities exist beyond the first upload"
    - "The page stays a single canonical entry point with no child-page links"
  artifacts:
    - path: "content/docs/guides/object-storage/index.en.mdx"
      provides: "Complete English Object Storage start-here page with all 6 sections"
      contains: "## Upload Your First File"
  key_links:
    - from: "content/docs/guides/object-storage/index.en.mdx"
      to: "images/object-storage-upload-file-interface.zh-cn.png"
      via: "markdown image reference"
      pattern: "object-storage-upload-file-interface"
---

<objective>
Append the first file upload walkthrough, verification section, and scope boundary
("What's Next") to complete the English Object Storage start-here page.

Purpose: Fulfill UPLD-03 (upload a first file and confirm success without needing
a second guide page) and close the page with a clear scope boundary.

Output: `content/docs/guides/object-storage/index.en.mdx` as a complete
six-section start-here page.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-object-storage-audit.md
@.planning/phases/14-canonical-object-storage-start-here-page/14-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the upload walkthrough, verification, and scope boundary</name>
  <files>content/docs/guides/object-storage/index.en.mdx</files>
  <read_first>
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md (heading order, MDX components, writing guardrails)
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md (file type recommendation, verification signal contract, deferred boundary)
    - .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-object-storage-audit.md (Chinese source upload section lines 38-58, screenshot #3)
    - content/docs/guides/object-storage/index.en.mdx (current file as modified by Plan 14-02 -- read the ENTIRE file to know where to append)
    - content/docs/guides/object-storage/index.zh-cn.mdx (Chinese source lines 38-58 for upload step facts)
  </read_first>
  <action>
Append THREE new sections to the end of `content/docs/guides/object-storage/index.en.mdx` (after the existing "Get Your Credentials" section from Plan 14-02). Do NOT modify any existing content. Only append.

**## Upload Your First File** (per 13-page-outline-spec.md):

Opening: 1-2 sentences stating that the user will upload a test file through the console.

Use `<div className='fd-steps [&_h4]:fd-step'>` with `<h4>` sub-steps:

- **Step: Open the bucket** -- Direct the user to navigate to the bucket they just created. Per product-truth rules: use generic action language, do NOT hardcode exact navigation paths.

- **Step: Upload a file** -- Direct the user to use the upload action in the console toolbar to select and upload a file. Per safe-example contract file type recommendation: recommend a small test file such as a plain text file or a small image. Keep the recommendation generic (any OS, any file). Per product-truth rules: use generic action language for the upload control ("use the upload action"), do NOT write exact button text as literal quotes.

- **Step: Confirm the upload** -- Direct the user to wait for the upload to complete and see the file appear in the file list.

Close the `</div>`.

Include a screenshot reference for the upload interface (per outline spec):
```
![Sealos Object Storage -- file upload interface with toolbar and file list (Chinese console UI)](./images/object-storage-upload-file-interface.zh-cn.png)
```

Per safe-example contract: console-UI only, no code blocks, no CLI commands, no SDK imports.

**## Verify Your Upload** (per 13-page-outline-spec.md):

Per verification signal contract (D-08): first-success = the uploaded file appears in the console file list.

Describe what the user should see: the file name, size, and timestamp visible in the bucket's file list view.

State that this is a visual confirmation inside the console -- no commands, no URL access test needed (the bucket is private).

Note that Phase 15 will strengthen this section with failure checks. For now, keep it as a brief positive-path confirmation (2-3 sentences).

Per verification contract: do NOT include public URL access test, SDK-based verification, curl/wget test, or programmatic file-existence check.

**## What's Next** (per 13-page-outline-spec.md):

Brief scope boundary section (3-5 sentences).

Mention that Object Storage also supports:
- SDK access using MinIO-compatible client libraries (per stable facts)
- Static website hosting
- Resource monitoring

Frame these as capabilities the user can explore beyond the first upload. Per safe-example contract deferred boundary: do NOT provide walkthrough steps for any of them. Do NOT link to non-existent future Object Storage child pages.

Optionally mention the Chinese page as an additional resource for users who read Chinese.

**Writing rules (all sections):**
- English only, action-oriented, second person, present tense.
- Short paragraphs (3-4 sentences max).
- No code blocks, no Tabs/Tab components, no SDK examples, no CLI commands.
- No Chinese text.
- No placeholder credential values.
- No links to non-existent child pages.
- Screenshots are optional reinforcement; text stands on its own.
  </action>
  <verify>
    <automated>
grep -c "## Upload Your First File" content/docs/guides/object-storage/index.en.mdx && grep -c "## Verify Your Upload" content/docs/guides/object-storage/index.en.mdx && grep -c "## What's Next" content/docs/guides/object-storage/index.en.mdx && grep -c "fd-steps" content/docs/guides/object-storage/index.en.mdx && grep -c "## What Object Storage Is" content/docs/guides/object-storage/index.en.mdx && grep -c "## Create a Bucket" content/docs/guides/object-storage/index.en.mdx && grep -c "## Get Your Credentials" content/docs/guides/object-storage/index.en.mdx && ! grep -qP "[\x{4e00}-\x{9fff}]" content/docs/guides/object-storage/index.en.mdx && echo "PASS" || echo "FAIL"
    </automated>
  </verify>
  <acceptance_criteria>
    - "## Upload Your First File" heading exists
    - "## Verify Your Upload" heading exists
    - "## What's Next" heading exists
    - Upload section uses `fd-steps` with `h4` sub-steps
    - Upload section recommends a small test file (plain text or small image)
    - Upload section is console-UI only (no code blocks, no CLI commands)
    - Verify section describes file name, size, and timestamp in the console file list
    - Verify section does NOT include public URL test, SDK verification, or curl/wget
    - What's Next mentions SDK access, static hosting, and monitoring as future capabilities
    - What's Next does NOT provide walkthrough steps for deferred features
    - What's Next does NOT link to non-existent child pages
    - At least one screenshot reference to upload interface exists
    - No Chinese text anywhere in the file
    - All six h2 headings present in order: What Object Storage Is, Create a Bucket, Get Your Credentials, Upload Your First File, Verify Your Upload, What's Next
    - The existing sections from Plans 14-01 and 14-02 are preserved unchanged
  </acceptance_criteria>
  <done>The English Object Storage page is complete with all six sections. A user can follow the entire guide from product understanding through first file upload to verification, all on one page. The page stays a single canonical entry point with clear scope boundaries.</done>
</task>

</tasks>

<verification>
- All six h2 headings exist in order: "What Object Storage Is", "Create a Bucket", "Get Your Credentials", "Upload Your First File", "Verify Your Upload", "What's Next"
- `grep -c 'fd-steps' content/docs/guides/object-storage/index.en.mdx` returns at least 2 (bucket creation + upload)
- `grep -c 'Callout' content/docs/guides/object-storage/index.en.mdx` returns at least 2 (permission + endpoint tip)
- `grep -cP '[\x{4e00}-\x{9fff}]' content/docs/guides/object-storage/index.en.mdx` returns 0
- `grep -c 'xxxxxxxx' content/docs/guides/object-storage/index.en.mdx` returns 0
- No `<Tabs>`, no `<Tab>`, no code blocks with language syntax
- No links to non-existent Object Storage child pages
- File is a single complete page ready for Phase 15 verification hardening
</verification>

<success_criteria>
The English Object Storage page is a complete six-section start-here guide fulfilling UPLD-03. A user can upload a first file and confirm success without needing a second guide page. The page ends with a clear scope boundary that mentions future capabilities without providing walkthroughs.
</success_criteria>

<output>
After completion, create `.planning/phases/14-canonical-object-storage-start-here-page/14-03-SUMMARY.md`
</output>
