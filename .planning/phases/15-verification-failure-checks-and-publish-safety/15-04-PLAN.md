---
phase: 15-verification-failure-checks-and-publish-safety
plan: 04
type: execute
wave: 4
depends_on:
  - "15-01"
  - "15-02"
  - "15-03"
files_modified:
  - .planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md
  - .planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md
autonomous: true
requirements:
  - QLTY-03
user_setup: []
must_haves:
  truths:
    - "Phase 15 closes with real repo validation using both `npm run build` and `npm run lint`."
    - "Clearly unrelated existing repo failures are recorded separately as environmental or pre-existing blockers instead of being misclassified as Object Storage regressions."
    - "The phase ends with focused Object Storage UAT plus a validation artifact that records exact command evidence and the final verification report updated with outcomes."
  artifacts:
    - path: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"
      provides: "Phase 15 validation log mixing Object Storage shell assertions with repo build and lint commands"
      contains: "# Phase 15 — Validation Strategy"
    - path: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md"
      provides: "Phase-level verification report updated with final command outcomes and UAT result"
      contains: "Verification Commands"
    - path: "package.json"
      provides: "Authoritative source for repo build and lint commands"
      contains: "\"build\""
  key_links:
    - from: "package.json"
      to: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"
      via: "Phase 15 must record the existing repo build and lint commands exactly as the milestone closeout checks (D-13)."
      pattern: "\"build\":|\"lint\":"
    - from: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md"
      to: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"
      via: "Phase 15 must record unrelated existing repo failures separately from Object Storage regressions if they appear (D-14)."
      pattern: "environmental or pre-existing blocker"
---

<objective>
Define and complete the publish-safety validation closeout for Phase 15 using
real Object Storage assertions plus the repo's existing `npm run build` and
`npm run lint` commands. Finalize the verification report with outcomes and
honest blocker classification.

Purpose: Satisfy `QLTY-03` and close the Object Storage v1.3 milestone with
honest, worktree-local validation evidence.
Output: `.planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md` and final updates to `15-VERIFICATION.md`.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md
@.planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md
@.planning/phases/15-verification-failure-checks-and-publish-safety/15-03-UAT.md
@.planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md
@.planning/phases/12-verification-failure-checks-and-publish-safety/12-VALIDATION.md
@content/docs/guides/object-storage/index.en.mdx
@content/docs/guides/meta.en.json
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the Phase 15 validation strategy with Object Storage checks plus repo build and lint</name>
  <files>.planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md</files>
  <read_first>.planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md, .planning/phases/12-verification-failure-checks-and-publish-safety/12-VALIDATION.md, package.json</read_first>
  <action>
Create `.planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md` following the Phase 12 validation-log format.

Required structure:

1. Header: `# Phase 15 — Validation Strategy`

2. `## Quick run command` — a single paste-and-run block that executes the Object Storage shell assertions fast (the rg/test commands already defined in 15-01, 15-02, and 15-03). This is the fast-feedback loop.

3. `## Full suite command` — the explicit ordered sequence:
   - First: the Object Storage-specific shell assertions (same set as the quick run).
   - Then: `npm run build`.
   - Then: `npm run lint`.
   Document the exact ordering rationale: Object Storage assertions first (cheap, fast), build next (catches route/MDX/compile regressions), lint last (catches stylistic/type issues that should not block publish if unrelated).

4. `## Task Map` — table mapping `VRFY-01`, `VRFY-02`, `QLTY-01`, `QLTY-02`, `QLTY-03` to the specific commands that validate each.

5. `## Executed Command Order` — placeholder section for Task 2 to fill in after execution.

6. `## Execution Notes` — placeholder section for Task 2 to fill in with outcomes and any `environmental or pre-existing blocker` classification per D-14.

Implements D-13 from Phase 15 CONTEXT. The validation log stays worktree-local and Object Storage-specific; it does not widen into unrelated repo cleanup.
  </action>
  <verify>
    <automated>test -f ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md" && rg -n "# Phase 15 — Validation Strategy|## Quick run command|## Full suite command|## Task Map|## Executed Command Order|## Execution Notes|npm run build|npm run lint" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md" && rg -n "VRFY-01|VRFY-02|QLTY-01|QLTY-02|QLTY-03" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - `15-VALIDATION.md` exists with the six required sections
    - the quick run command runs the Object Storage shell assertions
    - the full suite command runs the Object Storage shell assertions first, then `npm run build`, then `npm run lint`
    - the task map covers `VRFY-01`, `VRFY-02`, `QLTY-01`, `QLTY-02`, and `QLTY-03`
    - Executed Command Order and Execution Notes sections exist as placeholders for Task 2
  </acceptance_criteria>
  <done>The phase has a concrete validation contract for Object Storage publish-safety execution.</done>
</task>

<task type="auto">
  <name>Task 2: Execute validation, record results, classify unrelated failures, and finalize the verification report</name>
  <files>.planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md</files>
  <read_first>.planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-03-UAT.md, package.json</read_first>
  <action>
Run the validation sequence in the exact order defined in 15-VALIDATION.md's `## Full suite command`:

1. Run the Object Storage-specific shell assertions (from 15-01, 15-02, 15-03). Capture the output of each.
2. Run `npm run build`. Capture stdout/stderr and exit code. If the command takes long, record the duration. Do not kill it prematurely.
3. Run `npm run lint`. Capture stdout/stderr and exit code.

Update `15-VALIDATION.md`:

- `## Executed Command Order`: list each command in order with exit code, runtime, and a one-line outcome.
- `## Execution Notes`: classify every failure. Object Storage regressions (anything triggered by `index.en.mdx`, `meta.en.json`, or the Phase 15 edits) are blockers. Clearly unrelated existing repo failures (e.g., issues in other docs, unrelated TypeScript modules, or test infrastructure that existed before Phase 14) are recorded as `environmental or pre-existing blocker` per D-14 with a one-line root-cause note. If the worktree is dirty with unrelated changes (Phase 15 CONTEXT notes the worktree is already dirty), record that as execution context, not as a Phase 15 regression.

Update `15-VERIFICATION.md`:

- Fill the `## Execution Notes` section with the build and lint outcomes, referencing `15-VALIDATION.md` for the full command evidence.
- Update `## Requirement Coverage` to mark `QLTY-03` with the validation result (pass if build and lint have no Object Storage-related failure; conditional pass if only unrelated blockers remain with explicit classification).
- Add a final `## Outcome` line stating whether Phase 15 is publish-safe for the Object Storage milestone and naming any unrelated blockers separately.

Stay worktree-local and Object Storage-specific. Do not attempt to fix unrelated repo failures inside this plan — record them and classify them.

Implements D-13 and D-14 from Phase 15 CONTEXT.
  </action>
  <verify>
    <automated>rg -n "Executed Command Order|Execution Notes|npm run build|npm run lint|environmental or pre-existing blocker" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md" && rg -n "Execution Notes|Outcome|npm run build|npm run lint" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md" && rg -n "QLTY-03" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md"</automated>
  </verify>
  <acceptance_criteria>
    - `15-VALIDATION.md` `## Executed Command Order` records each command with exit code and outcome
    - `15-VALIDATION.md` `## Execution Notes` classifies every failure as Object Storage regression or environmental/pre-existing blocker, with a one-line root-cause note for each unrelated failure
    - `15-VERIFICATION.md` `## Execution Notes` references the validation log and summarizes build and lint outcomes
    - `15-VERIFICATION.md` `## Requirement Coverage` marks `QLTY-03` with the validation result
    - `15-VERIFICATION.md` ends with an `## Outcome` line stating Phase 15 publish-safety status and naming unrelated blockers separately if any
    - no unrelated repo failures are misclassified as Object Storage regressions
    - no unrelated repo issues are fixed inside this plan (only recorded)
  </acceptance_criteria>
  <done>The phase closeout includes exact command evidence, honest blocker classification, and a finalized verification report.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"`
- [ ] `test -f ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md"`
- [ ] `rg -n "# Phase 15 — Validation Strategy|## Quick run command|## Full suite command|## Task Map|## Executed Command Order|## Execution Notes" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"`
- [ ] `rg -n "npm run build|npm run lint" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VALIDATION.md"`
- [ ] `rg -n "## Execution Notes|## Outcome" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md"`
- [ ] `rg -n "QLTY-03" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-VERIFICATION.md"`
</verification>

<success_criteria>

- All tasks completed
- Phase 15 ends with a real validation log including Object Storage assertions, `npm run build`, and `npm run lint`
- The verification report captures focused UAT and outcome evidence for the Object Storage milestone
- Any unrelated repo failures are recorded honestly as environmental or pre-existing blockers rather than being attributed to the Object Storage docs work

</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-failure-checks-and-publish-safety/15-04-SUMMARY.md`
</output>
