---
phase: 15-verification-failure-checks-and-publish-safety
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
files_modified:
  - content/docs/guides/object-storage/index.en.mdx
  - .planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md
autonomous: true
requirements:
  - QLTY-01
  - QLTY-02
user_setup: []
must_haves:
  truths:
    - "The English Object Storage page contains only English body, headings, alt text, and frontmatter with no Chinese residue."
    - "The page does not hardcode secrets, placeholder credential strings, fabricated endpoint URLs, exact console button labels, or specific console URLs."
    - "The v1.3 Object Storage surface remains exactly one leaf page: no `content/docs/guides/object-storage/meta.en.json` exists and no child-page links are introduced."
    - "Phase 15 produces a publish-safety audit record capturing the exact grep checks run and their results."
  artifacts:
    - path: "content/docs/guides/object-storage/index.en.mdx"
      provides: "English-only Object Storage leaf page with publish-safe wording and neutral UI-label framing"
      contains: "title: \"Object Storage\""
    - path: "content/docs/guides/meta.en.json"
      provides: "Existing top-level Guides registration exposing object-storage as one leaf entry"
      contains: "\"object-storage\""
    - path: ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md"
      provides: "Publish-safety audit record showing the grep checks run and their pass/fix outcome"
      contains: "# Phase 15 — Publish-Safety Audit"
  key_links:
    - from: ".planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md"
      to: "content/docs/guides/object-storage/index.en.mdx"
      via: "Phase 15 must preserve the single-leaf posture and avoid introducing Object Storage child-page navigation or a local meta.en.json."
      pattern: "must not create content/docs/guides/object-storage/meta.en.json|no new MDX files"
    - from: ".planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md"
      to: "content/docs/guides/object-storage/index.en.mdx"
      via: "Phase 15 must preserve neutral wording around unstable UI labels and must not reintroduce placeholder credentials or hardcoded console URLs."
      pattern: "generic action-oriented descriptions|no placeholder credential values|evidence inputs"
---

<objective>
Harden the Object Storage English page for publish safety: English-only
copy, no hardcoded secrets or placeholder credentials, no exact console
button labels or hardcoded console URLs, and the single-leaf route posture
preserved. Record the audit as evidence.

Purpose: Satisfy `QLTY-01` and the content/metadata side of `QLTY-02` before
Phase 15 records route integrity evidence (15-03) and runs repo build/lint
(15-04).
Output: `content/docs/guides/object-storage/index.en.mdx` (fixes if needed)
and `.planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md`.
</objective>

<execution_context>
@~/.codex/get-shit-done/workflows/execute-plan.md
@~/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md
@.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md
@content/docs/guides/object-storage/index.en.mdx
@content/docs/guides/meta.en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the publish-safety grep audit on the Object Storage page and fix any residue in place</name>
  <files>content/docs/guides/object-storage/index.en.mdx, .planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md</files>
  <read_first>.planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md, .planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-safe-example-contract.md, content/docs/guides/object-storage/index.en.mdx</read_first>
  <action>
Scan `content/docs/guides/object-storage/index.en.mdx` for publish-risk content (per D-16) and fix any residue in place. If the page is already clean, still record the audit.

Required grep checks (record the exact command and result for each in the audit log):

1. Chinese character residue in body, headings, alt text, and frontmatter:
   `rg -n "[一-龥]" "content/docs/guides/object-storage/index.en.mdx"`
   Expected: zero matches.

2. Hardcoded secret shapes and placeholder credential strings:
   `rg -n "xxxxxxxx|your-access-key|your-secret-key|AKIA[A-Z0-9]+|SK[A-Z0-9]+|objectstorageapi\\." "content/docs/guides/object-storage/index.en.mdx"`
   Expected: zero matches.

3. Hardcoded console URLs (region- or deployment-specific):
   `rg -n "cloud\\.sealos\\.run|objectstorage\\.[a-z]+\\.sealos\\.run|https?://[a-z]+\\.sealos\\.(io|run)" "content/docs/guides/object-storage/index.en.mdx"`
   Expected: zero matches in body prose. Image paths under `./images/...` are not URLs and do not count.

4. Exact console button label assertions framed as the literal button text (e.g., `the "Upload" button`, `the "Create Bucket" button`, `the "Access Key" button labeled "Access Key"`). Phase 13 locked these as unstable UI labels that must stay neutral:
   `rg -n "button labeled|\"Upload\" button|\"Create Bucket\" button|\"Access Key\" button|click \"Upload\"|click \"Create Bucket\"" "content/docs/guides/object-storage/index.en.mdx"`
   Expected: zero matches.

Note: the existing page already uses neutral phrasing such as "access key action", "upload action", "bucket creation action". The phrase "Access Key" naming the credential value (not the button) is allowed as a stable fact per the safe-example contract; only exact-button-label assertions are out of bounds.

If any check produces a match, fix the residue in place with neutral action-oriented language (per Phase 13 product-truth rules). Preserve the strengthened verification and first-failure sections added in 15-01. Do not widen the page, do not add screenshots, do not add child-page links, do not add code blocks or SDK content.

Create `.planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md` and record:
- Header: `# Phase 15 — Publish-Safety Audit`
- One section per check: the exact command, the expected result, the actual result, and any fix applied (with a one-line description of the edit).
- A final summary line stating whether the page is publish-safe or listing remaining blockers.

Implements D-16 from Phase 15 CONTEXT.
  </action>
  <verify>
    <automated>test -f ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md" && ! rg -n "[一-龥]" "content/docs/guides/object-storage/index.en.mdx" && ! rg -n "xxxxxxxx|your-access-key|your-secret-key|objectstorageapi\\." "content/docs/guides/object-storage/index.en.mdx" && ! rg -n "cloud\\.sealos\\.run|objectstorage\\.[a-z]+\\.sealos\\.run" "content/docs/guides/object-storage/index.en.mdx" && ! rg -n "button labeled|\"Upload\" button|\"Create Bucket\" button" "content/docs/guides/object-storage/index.en.mdx" && rg -n "# Phase 15 — Publish-Safety Audit" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `15-02-AUDIT.md` exists and records every grep check with command + expected + actual + fix status
    - the page contains no Chinese character residue anywhere (body, headings, alt text, frontmatter)
    - the page contains no hardcoded secret or placeholder credential patterns
    - the page contains no region- or deployment-specific console URL in prose
    - the page contains no exact-console-button-label assertions
    - if any check initially failed, the fix is applied in place and the audit records both the original match and the resulting edit
    - the page preserves all Phase 14 and 15-01 content structurally (no sections removed or reordered)
  </acceptance_criteria>
  <done>The Object Storage English page is publish-safe at the content level and the audit is captured as evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Preserve the single-leaf route posture in content and metadata</name>
  <files>.planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md</files>
  <read_first>.planning/phases/13-product-truth-safe-example-contract-and-page-outline/13-page-outline-spec.md, .planning/phases/15-verification-failure-checks-and-publish-safety/15-CONTEXT.md, content/docs/guides/object-storage/index.en.mdx, content/docs/guides/meta.en.json, .planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md</read_first>
  <action>
Verify the single-leaf route posture per D-15 and append the results to `15-02-AUDIT.md` as a dedicated `## Single-leaf posture` section.

Required checks (record command + expected + actual for each):

1. Top-level Guides nav still exposes Object Storage:
   `rg -n "\"object-storage\"" "content/docs/guides/meta.en.json"`
   Expected: at least one match in the `pages` array.

2. No local Object Storage `meta.en.json` exists:
   `test -f "content/docs/guides/object-storage/meta.en.json"`
   Expected: file does not exist (the test command should return non-zero).

3. No internal links pointing to non-existent local Object Storage child pages:
   `rg -n "/docs/guides/object-storage/[a-z][a-z0-9-]+/?" "content/docs/guides/object-storage/index.en.mdx"`
   Expected: zero matches. The root route `/docs/guides/object-storage/` is allowed but must not be followed by an additional path segment.

4. No additional MDX files were introduced under `content/docs/guides/object-storage/`:
   List siblings of `index.en.mdx` and confirm only the expected files exist (`index.en.mdx`, `index.zh-cn.mdx`, and the `images/` directory). Record the listing in the audit.

If any check fails, do not add features in this plan — record the finding in the audit and escalate via the audit summary. This task verifies and records; it does not expand the page.

Implements D-15 from Phase 15 CONTEXT.
  </action>
  <verify>
    <automated>rg -n "\"object-storage\"" "content/docs/guides/meta.en.json" && ! test -f "content/docs/guides/object-storage/meta.en.json" && ! rg -n "/docs/guides/object-storage/[a-z][a-z0-9-]+/?" "content/docs/guides/object-storage/index.en.mdx" && rg -n "## Single-leaf posture" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md"</automated>
  </verify>
  <acceptance_criteria>
    - `meta.en.json` still lists `object-storage` in the top-level `pages` array
    - no local `content/docs/guides/object-storage/meta.en.json` exists
    - the Object Storage page contains no links pointing to non-existent local child pages under `/docs/guides/object-storage/<segment>/`
    - no unexpected MDX files exist alongside `index.en.mdx` beyond `index.zh-cn.mdx`
    - `15-02-AUDIT.md` has a `## Single-leaf posture` section recording every check with its command, expected result, and actual result
  </acceptance_criteria>
  <done>The Object Storage surface stays flat, discoverable, and free of invented child-page routes, with the evidence captured in the audit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `test -f ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md"`
- [ ] `! rg -n "[一-龥]" "content/docs/guides/object-storage/index.en.mdx"`
- [ ] `! rg -n "xxxxxxxx|your-access-key|your-secret-key|objectstorageapi\\." "content/docs/guides/object-storage/index.en.mdx"`
- [ ] `! rg -n "cloud\\.sealos\\.run|objectstorage\\.[a-z]+\\.sealos\\.run" "content/docs/guides/object-storage/index.en.mdx"`
- [ ] `! rg -n "button labeled|\"Upload\" button|\"Create Bucket\" button" "content/docs/guides/object-storage/index.en.mdx"`
- [ ] `rg -n "\"object-storage\"" "content/docs/guides/meta.en.json"`
- [ ] `! test -f "content/docs/guides/object-storage/meta.en.json"`
- [ ] `! rg -n "/docs/guides/object-storage/[a-z][a-z0-9-]+/?" "content/docs/guides/object-storage/index.en.mdx"`
- [ ] `rg -n "# Phase 15 — Publish-Safety Audit|## Single-leaf posture" ".planning/phases/15-verification-failure-checks-and-publish-safety/15-02-AUDIT.md"`
</verification>

<success_criteria>

- All tasks completed
- The Object Storage English page is English-only and free of unsafe concrete publish claims (per D-16)
- The top-level Guides entry remains intact while the local Object Storage surface stays one leaf page (per D-15)
- `15-02-AUDIT.md` records the exact grep commands, their expected results, their actual results, and any fixes applied

</success_criteria>

<output>
After completion, create `.planning/phases/15-verification-failure-checks-and-publish-safety/15-02-SUMMARY.md`
</output>
