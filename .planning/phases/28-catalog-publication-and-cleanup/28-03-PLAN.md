---
phase: 28-catalog-publication-and-cleanup
plan: "03"
type: execute
wave: 3
depends_on: ["28-02"]
files_modified:
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/README.md
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/build.txt
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/http.jsonl
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/browser.txt
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/cleanup.jsonl
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/review.txt
  - .planning/phases/28-catalog-publication-and-cleanup/evidence/checksums.txt
autonomous: true
requirements: [SHOT-03, PUB-01, PUB-02, PUB-03, OPS-02]
decisions: [D-09, D-10, D-11, D-12, D-13, D-14]
must_haves:
  truths:
    - "The production static export serves the tutorial index, six new pages, and 24 new WebPs with exact content types."
    - "The tutorial index visibly exposes five complete paths at desktop and mobile without overflow or overlap."
    - "All external and local practice/publication residue is zero after server and browser cleanup."
    - "Phase 27 evidence remains byte-identical and read-only."
  artifacts:
    - path: ".planning/phases/28-catalog-publication-and-cleanup/evidence/http.jsonl"
      provides: "Exact 31-path static HTTP results"
    - path: ".planning/phases/28-catalog-publication-and-cleanup/evidence/cleanup.jsonl"
      provides: "Final external and local zero-residue audit"
    - path: ".planning/phases/28-catalog-publication-and-cleanup/evidence/checksums.txt"
      provides: "Checksum-last publication seal"
---

<objective>
Build, serve, inspect, clean, and seal the complete 15-page tutorial catalog.

Purpose: Prove the reader-visible production surface and empty practice
footprint before closing the milestone.
Output: Static export acceptance, responsive index review, final cleanup, and a
read-only Phase 28 evidence package.
</objective>

<context>
@.planning/phases/28-catalog-publication-and-cleanup/28-CONTEXT.md
@.planning/phases/28-catalog-publication-and-cleanup/28-02-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-06-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/evidence/checksums.txt
@next.config.mjs
@scripts/normalize-root-locale.js
@scripts/check-static-export-routes.js
@scripts/check-static-output.js
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build production output and accept the exact static HTTP surface</name>
  <files>.planning/phases/28-catalog-publication-and-cleanup/evidence/README.md, .planning/phases/28-catalog-publication-and-cleanup/evidence/build.txt, .planning/phases/28-catalog-publication-and-cleanup/evidence/http.jsonl</files>
  <action>
Snapshot Phase 27 evidence bytes/modes and the final source commit. Run the
focused tests, production validator, lint, `npm run build`, static-output check,
and static-routes check. Record curated commands, durations, exit status,
tutorial page count, and output inventory without local absolute paths.

Allocate an unused loopback port, install cleanup traps, start a run-owned
static server for `out/`, and poll `/tutorials/` with a bounded timeout. Request
exactly one index, six new trailing-slash detail URLs, and the 24 new WebP URLs.
Record route, status, normalized content type, bytes, and SHA-256 in deterministic
JSONL order. Require 31 unique 200 responses, seven HTML responses, 24
`image/webp` responses, all 15 tutorial links in index HTML, and no error export.
  </action>
  <verify>
    <automated>npm run validate-tutorials &amp;&amp; npm run lint &amp;&amp; test -d out/tutorials &amp;&amp; jq -e -s 'length == 31 and (map(.path) | unique | length == 31) and all(.[]; .status == 200) and (map(select(.content_type == "image/webp")) | length == 24) and (map(select(.content_type == "text/html")) | length == 7)' .planning/phases/28-catalog-publication-and-cleanup/evidence/http.jsonl</automated>
  </verify>
  <acceptance_criteria>
    - Production build and established static checks pass.
    - Exact 31-path HTTP set passes status, type, bytes, digest, and inventory gates.
    - Phase 27 evidence bytes and modes remain unchanged.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Review the published catalog at desktop and mobile</name>
  <files>.planning/phases/28-catalog-publication-and-cleanup/evidence/browser.txt</files>
  <action>
Open `/tutorials/` from the run-owned static server in one uniquely named browser
session. At 1440x900 and 390x844 assert the final route, title, body, FastAPI and
Django text, six new direct matrix links, 15 total available cells, status
counts, `scrollWidth &lt;= innerWidth`, stable overlap zero, and clipped labels
zero. Capture temporary full-page screenshots, inspect both with `view_image` at
original detail, record curated pass results, then close the session and remove
the captures.
  </action>
  <verify>
    <automated>rg -F 'desktop=pass' .planning/phases/28-catalog-publication-and-cleanup/evidence/browser.txt &amp;&amp; rg -F 'mobile=pass' .planning/phases/28-catalog-publication-and-cleanup/evidence/browser.txt &amp;&amp; test -z "$(agent-browser session list --json | jq -r '.data.sessions[]?' | rg '^p28-catalog-' || true)"</automated>
  </verify>
  <acceptance_criteria>
    - Both viewports pass behavior and visual review.
    - FastAPI/Django links and catalog counts are reader-visible and coherent.
    - Browser session and review captures are absent afterward.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove final cleanup and seal publication evidence</name>
  <files>.planning/phases/28-catalog-publication-and-cleanup/evidence/cleanup.jsonl, .planning/phases/28-catalog-publication-and-cleanup/evidence/review.txt, .planning/phases/28-catalog-publication-and-cleanup/evidence/checksums.txt</files>
  <action>
Stop and reap the exact static server PID, require its port free, and remove its
scratch. Run the protected global cleanup harness plus namespaced exact-name and
label queries for Instance, App, Deployment, StatefulSet, ReplicaSet, Pod,
Service, Ingress, Job, PostgreSQL Cluster, PVC, Secret, and ConfigMap. Recheck
processes, browsers, paths, state, logs, render data, ledgers, registry scratch,
and public temporary Phase 27 image tags. Record one final all-zero row.

Verify Phase 27's nine checksums, ten byte snapshots, and `0444` modes. Write the
Phase 28 review summary, run a credential scan, generate `checksums.txt` last
through a mode-0600 temporary file and atomic rename, verify every entry, rerun
the complete validator/test/lint/cleanup suite read-only, require evidence byte
equality, and set all retained Phase 28 evidence files to `0444`. Commit only the
evidence package as `docs(28-03): seal tutorial publication evidence`.
  </action>
  <verify>
    <automated>root=.planning/phases/28-catalog-publication-and-cleanup/evidence &amp;&amp; test "$(find "$root" -maxdepth 1 -type f | wc -l | tr -d ' ')" -eq 7 &amp;&amp; test "$(wc -l &lt; "$root/checksums.txt" | tr -d ' ')" -eq 6 &amp;&amp; test "$(LC_ALL=C sort "$root/checksums.txt")" = "$(cat "$root/checksums.txt")" &amp;&amp; (cd "$root" &amp;&amp; shasum -a 256 -c checksums.txt) &amp;&amp; test "$(find "$root" -maxdepth 1 -type f ! -perm 0444 | wc -l | tr -d ' ')" -eq 0 &amp;&amp; /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh --assert-clean-all</automated>
  </verify>
  <acceptance_criteria>
    - Static server, port, browser, scratch, external resources, and local footprint are zero.
    - Phase 27 remains checksum-valid, byte-identical, and mode 0444.
    - Seven Phase 28 files and six checksum inputs verify and remain read-only.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Require focused tests, 15-page validator, TypeScript lint, production build,
existing static checks, exact 31-path HTTP acceptance, two responsive visual
reviews, Phase 27 immutability, final global cleanup, and the Phase 28 evidence
seal.
</verification>

<success_criteria>
- All 15 tutorial pages are direct catalog links and validator-approved.
- Six new pages and 24 assets succeed through production static HTTP.
- Desktop and mobile catalog review pass with no overflow or overlap.
- All practice and publication residue is zero.
- Phase 27 and Phase 28 evidence packages are checksum-valid and read-only.
</success_criteria>

<output>
Create `28-03-SUMMARY.md` with build and HTTP results, responsive review,
cleanup inventory, evidence checksums/modes, Phase 27 equality, commits,
deviations, self-check, and milestone handoff.
</output>
