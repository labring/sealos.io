---
phase: 28-catalog-publication-and-cleanup
plan: "02"
type: tdd
wave: 2
depends_on: ["28-01"]
files_modified:
  - scripts/validate-tutorials.test.mjs
  - scripts/validate-tutorials.mjs
  - app/[lang]/(home)/tutorials/tutorial-growth-data.ts
  - app/[lang]/(home)/tutorials/page.tsx
  - app/[lang]/(home)/tutorials/TutorialFrameworkMatrix.tsx
autonomous: true
requirements: [TDD-04, PUB-01, PUB-02]
decisions: [D-01, D-02, D-03, D-06]
must_haves:
  truths:
    - "FastAPI and Django are Available Framework Paths with three direct links each."
    - "The visible index and metadata name all five complete public framework paths."
    - "The production validator fails when availability or discoverability drifts."
  artifacts:
    - path: "app/[lang]/(home)/tutorials/tutorial-growth-data.ts"
      provides: "Exact five-framework available state"
    - path: "app/[lang]/(home)/tutorials/page.tsx"
      provides: "Five-framework metadata and hero copy"
    - path: "scripts/validate-tutorials.mjs"
      provides: "Catalog availability and discoverability gate"
---

<objective>
Promote FastAPI and Django through the existing tutorial catalog and bind that
reader-visible state to the production validator through one RED/GREEN cycle.

Purpose: Make all 15 pages discoverable as five complete framework paths.
Output: Exact available state, accurate index copy, and fail-closed catalog tests.
</objective>

<context>
@.planning/phases/28-catalog-publication-and-cleanup/28-CONTEXT.md
@.planning/phases/28-catalog-publication-and-cleanup/28-01-SUMMARY.md
@app/[lang]/(home)/tutorials/tutorial-growth-data.ts
@app/[lang]/(home)/tutorials/page.tsx
@app/[lang]/(home)/tutorials/TutorialFrameworkMatrix.tsx
@scripts/validate-tutorials.mjs
@scripts/validate-tutorials.test.mjs
</context>

<feature>
  <name>Five available framework paths and catalog discoverability</name>
  <files>scripts/validate-tutorials.test.mjs, scripts/validate-tutorials.mjs, app/[lang]/(home)/tutorials/tutorial-growth-data.ts, app/[lang]/(home)/tutorials/page.tsx, app/[lang]/(home)/tutorials/TutorialFrameworkMatrix.tsx</files>
  <behavior>
    Case 1: exact available keys are Next.js, React, Node.js, FastAPI, and Django
    -> validator exit 0; matrix derives 15 available cells.

    Case 2: copied catalog data omits Django from the available set -> exit 1
    with a stable exact-availability diagnostic.

    Case 3: copied index source omits FastAPI or Django from metadata/hero copy
    -> exit 1 with a stable discoverability diagnostic.
  </behavior>
  <implementation>
    RED: extend the fixture test with catalog data and index-copy mutations.
    Commit only the failing tests after proving the current validator ignores
    both invalid inputs.

    GREEN: require the catalog data and exact key sets in the validator; update
    available/coming sets; update FastAPI/Django path notes, metadata description
    and keywords, hero copy, and matrix explanation. Preserve layout, default
    Next.js action, slug derivation, request behavior, and total inventory.

    REFACTOR: keep copy constants and derived state simple. Commit cleanup only
    when it improves clarity without changing the accepted behavior.
  </implementation>
</feature>

<verification>
Run the focused test, public validator, TypeScript lint, and development HTML
smoke. Require counts 15 available, six coming-next, and 33 planned.
</verification>

<success_criteria>
- RED invalid catalog cases fail before production changes.
- GREEN exposes six new direct matrix links and all 15 available cells.
- Metadata and visible copy include FastAPI and Django.
- The current layout and Phase 27 source/asset/evidence bytes remain unchanged.
</success_criteria>

<output>
Create `28-02-SUMMARY.md` with RED/GREEN commits, exact key sets, copy changes,
derived counts, verification commands, deviations, and self-check.
</output>
