---
phase: 28-catalog-publication-and-cleanup
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/validate-tutorials.test.mjs
  - scripts/validate-tutorials.mjs
autonomous: true
requirements: [TDD-04, SHOT-03, PUB-02]
decisions: [D-04, D-05, D-07, D-08]
must_haves:
  truths:
    - "The public validator accepts exactly 15 English tutorial sources."
    - "All six Python pages have exact stage, source, series, CTA, related-link, terminology, and strict image contracts."
    - "A missing Python evidence asset produces a stable CLI failure."
  artifacts:
    - path: "scripts/validate-tutorials.test.mjs"
      provides: "CLI-level valid and invalid publication cases"
    - path: "scripts/validate-tutorials.mjs"
      provides: "Exact 15-page production source and asset contract"
  key_links:
    - from: "scripts/validate-tutorials.test.mjs"
      to: "scripts/validate-tutorials.mjs"
      via: "spawned CLI with fixture cwd"
      pattern: "validate-tutorials"
---

<objective>
Expand the public tutorial validator from nine to 15 source and asset contracts
through one CLI-level RED/GREEN cycle.

Purpose: Make the accepted Phase 27 pages eligible for production validation
while preserving exact fail-closed behavior.
Output: Focused behavior tests and a passing 15-page source/asset validator.
</objective>

<context>
@.planning/phases/28-catalog-publication-and-cleanup/28-CONTEXT.md
@.planning/phases/28-catalog-publication-and-cleanup/28-RESEARCH.md
@.planning/phases/28-catalog-publication-and-cleanup/28-PATTERNS.md
@.planning/phases/28-catalog-publication-and-cleanup/28-VALIDATION.md
@.planning/phases/27-practice-backed-tutorial-series/27-06-SUMMARY.md
@scripts/validate-tutorials.mjs
@AGENTS.md
</context>

<feature>
  <name>Exact 15-page tutorial source and evidence-asset validation</name>
  <files>scripts/validate-tutorials.test.mjs, scripts/validate-tutorials.mjs</files>
  <behavior>
    Case 1: the committed exact 15-page catalog -> exit 0 and
    `validate-tutorials passed: 15 tutorial pages checked.`

    Case 2: a copied fixture with one required FastAPI evidence WebP removed ->
    exit 1 and a stable error naming the page and missing public asset.

    Case 3: any six-page directory remains outside the exact contract -> exit 1
    with the existing extra-directory diagnostic.
  </behavior>
  <implementation>
    RED: create the focused Node test and fixture copier. Copy exact tutorial
    sources, their local image references, and the validator integration-file
    allowlist. Commit only the test after both new cases fail for the absent
    15-page behavior.

    GREEN: add the six FastAPI/Django contracts, their protected tag/source
    fields, stage-specific phrases, exact CTA/related relationships, and strict
    image policies. Extend the strict slug set, require four unique images for
    every strict page, close each strict directory against references, and keep
    the existing byte budget. Commit only the validator implementation.

    REFACTOR: keep contract helpers shared and diagnostics stable. Make a third
    commit only when behavior-neutral cleanup is necessary.
  </implementation>
</feature>

<verification>
Run the focused test, public validator, Phase 27 bundle gate, and TypeScript
lint. Require one RED commit followed directly by one GREEN commit.
</verification>

<success_criteria>
- RED test exists, observes CLI behavior, and fails before implementation.
- GREEN accepts exactly 15 sources and all 12 strict tutorial asset folders.
- Missing Python assets fail with the expected stable diagnostic.
- Phase 27 evidence and catalog availability files remain byte-identical.
</success_criteria>

<output>
Create `28-01-SUMMARY.md` with RED/GREEN commits, fixture contract, 15-page and
strict-asset results, complete commands, deviations, and self-check.
</output>
