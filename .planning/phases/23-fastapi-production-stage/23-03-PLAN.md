---
phase: 23-fastapi-production-stage
plan: "03"
type: execute
wave: 3
depends_on: ["23-02"]
files_modified:
  - .planning/phases/23-fastapi-production-stage/evidence/workflow.txt
  - .planning/phases/23-fastapi-production-stage/evidence/images.txt
  - .planning/phases/23-fastapi-production-stage/evidence/migration.txt
  - .planning/phases/23-fastapi-production-stage/evidence/runtime.txt
  - .planning/phases/23-fastapi-production-stage/evidence/logs.txt
  - .planning/phases/23-fastapi-production-stage/evidence/http.jsonl
  - .planning/phases/23-fastapi-production-stage/evidence/rollback.txt
  - .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt
  - .planning/phases/23-fastapi-production-stage/evidence/checksums.txt
  - .planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md
autonomous: true
requirements: [FAST-03]
must_haves:
  truths:
    - "D-02: Both baseline and final states run two Ready replicas as UID/GID 10001 with one Uvicorn application process, read-only root, writable bounded /tmp, dropped capabilities, no privilege escalation, and RuntimeDefault seccomp."
    - "D-03: Each accepted state correlates source SHA, Deployment digest, Pod imageID, release environment, and startup log without credentials."
    - "D-05: Baseline and final application rollouts begin only after the same-digest migration Job completes at Alembic revision 0001."
    - "D-06: One persistent task survives baseline deployment, final update, kubectl rollout undo to baseline, and explicit recovery to final."
    - "D-09: The run leaves zero owned Kubernetes objects, processes, state, rendered files, and clones, while curated evidence remains redacted and checksum-valid."
    - "The exact live order is baseline migration/deploy -> final migration/update -> rollout undo baseline -> explicit final recovery."
  artifacts:
    - path: ".planning/phases/23-fastapi-production-stage/evidence/runtime.txt"
      provides: "Two-replica image, UID/GID, process, port, filesystem, and security evidence for every state"
      contains: "state=final-recovered"
    - path: ".planning/phases/23-fastapi-production-stage/evidence/http.jsonl"
      provides: "Public health, docs, CRUD, and cross-rollout persistence evidence"
      contains: "final-recovered"
    - path: ".planning/phases/23-fastapi-production-stage/evidence/rollback.txt"
      provides: "Controller revision and digest sequence for baseline/final/undo/final"
      contains: "rollout_undo=passed"
    - path: ".planning/phases/23-fastapi-production-stage/evidence/cleanup.txt"
      provides: "Exact-label zero-resource and stopped-process proof"
      contains: "owned_processes=0"
    - path: ".planning/phases/23-fastapi-production-stage/evidence/checksums.txt"
      provides: "SHA-256 integrity for every reviewed evidence file"
  key_links:
    - from: ".planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh"
      via: "accepted baseline/final digest and source arguments"
      pattern: "baseline.*sha256|final.*sha256"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml"
      via: "same rendered image digest and database Secret before each rollout"
      pattern: "__IMAGE_REFERENCE__"
    - from: ".planning/phases/23-fastapi-production-stage/evidence/rollback.txt"
      to: ".planning/phases/23-fastapi-production-stage/evidence/http.jsonl"
      via: "same persistent task ID and payload across all four controller states"
      pattern: "task_id"
---

<objective>
Execute and retain the complete real Sealos production rollout, rollback, and recovery proof.

Purpose: Convert the two frozen public image identities into direct Kubernetes, PostgreSQL, HTTP, process, security, log, rollback, cleanup, and integrity evidence.
Output: One exact-run evidence package proving baseline -> final -> rollout undo baseline -> explicit final recovery, followed by complete live-footprint cleanup.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-fastapi-production-stage/23-CONTEXT.md
@.planning/phases/23-fastapi-production-stage/23-VALIDATION.md
@.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
@.planning/phases/22-fastapi-postgresql-stage/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run the real baseline, final, rollback, and recovery state machine</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    .planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
    .planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
    .planning/phases/23-fastapi-production-stage/23-CONTEXT.md
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/evidence/workflow.txt, .planning/phases/23-fastapi-production-stage/evidence/images.txt, .planning/phases/23-fastapi-production-stage/evidence/migration.txt, .planning/phases/23-fastapi-production-stage/evidence/runtime.txt, .planning/phases/23-fastapi-production-stage/evidence/logs.txt, .planning/phases/23-fastapi-production-stage/evidence/http.jsonl, .planning/phases/23-fastapi-production-stage/evidence/rollback.txt, .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt, .planning/phases/23-fastapi-production-stage/evidence/checksums.txt</files>
  <action>
    Resolve BASELINE_SHA/BASELINE_DIGEST and FINAL_SHA/FINAL_DIGEST from the accepted summaries, then independently re-read public main, both exact workflow runs, public package visibility, anonymous full-SHA tag digests, linux/amd64 manifests, and OCI revision/source labels. Require two distinct image digests and public main equal FINAL_SHA. Require the Reference Application clean at FINAL_SHA. External recovery is four-state and fail-closed: exact success accepts; queued/in-progress waits boundedly; an absent exact run/tag executes `gh workflow run publish-image.yml --repo yangchuansheng/sealos-fastapi-tutorial --ref main -f source_sha="$TARGET_SHA"` with TARGET_SHA set to the missing BASELINE_SHA or FINAL_SHA; one infrastructure-only failed run may use `gh run rerun "$RUN_ID" --repo yangchuansheng/sealos-fastapi-tutorial`; any source/test failure, unexpected main, mutable-only image, digest mismatch, or OCI mismatch halts before Kubernetes mutation. For dispatch recovery, record public main as expected API head SHA, require event workflow_dispatch, and require run logs/summary `target_sha=$TARGET_SHA`; this is the executable baseline rebuild route after main equals FINAL_SHA. For push recovery, require API head SHA and target SHA both equal the candidate.

    Execute each anonymous baseline/final gate with a separate newly created empty mode-0700 DOCKER_CONFIG. Before crane, clear GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH; set only DOCKER_CONFIG; run digest, config, and manifest; validate digest, linux/amd64, OCI source/revision; then remove the directory and trap. Ambient Docker keychain or credential-helper state is outside the accepted gate.

    Preflight authenticated context `dn9ue3wz@sealos`, namespace `ns-let51wad`, required create/get/log/exec/patch/delete permissions, current exact-prefix inventory, and an empty new evidence directory. Generate one 12-hex run ID and require every temporary object name under `tutorial-fastapi-pg-test-${RUN_ID}-*` plus label `tutorial.sealos.io/run-id=${RUN_ID}`. If an interrupted prior run exists, accept only a complete mode-0600 state whose run label, prefix, PIDs, and object names agree; use exact names/that one label to clean and prove zero before generating a fresh run. A partial, mismatched, or unlabeled prior state halts for forensic review. Never query Secret data or delete by a broad namespace selector.

    Run the frozen production harness with the four accepted identities and the Phase 23 planning evidence directory. The retained state order is mandatory: (1) create run-scoped PostgreSQL and Secret; baseline-digest migration Job reaches Complete and logs revision `0001`; apply baseline Deployment/Service, wait for 2/2 Ready, and create the persistent primary task plus a disposable CRUD task; (2) final-digest migration Job reaches Complete at `0001`; apply final digest and FINAL_SHA environment in one Pod-template update, wait 2/2 Ready, read and update the primary task; (3) execute `kubectl rollout undo`, wait 2/2 Ready, require baseline digest/source on Deployment, ReplicaSets, Pods, and logs, then read the updated primary task; (4) explicitly apply the final digest/FINAL_SHA tuple, wait 2/2 Ready, require final identities, read the same task, and finish the disposable delete/404 check.

    At every application state, require Available=True, two Ready Pods, expected imageID on both, UID/GID 10001, exactly one Uvicorn application command per Pod, PID 1 command bound to 0.0.0.0:8000 with one worker, rejected root-filesystem write, successful write/remove under mounted `/tmp`, effective security context, health 200, docs HTML, expected startup log tuple, and public persistent task payload. Capture only curated public fields. Let the installed EXIT trap clean every exact application/PostgreSQL Deployment, ReplicaSet, Pod, Service, migration Job, Secret, ConfigMap, state/render file, port-forward, and temporary clone on success or failure; wait for deletion and record final exact inventories plus process checks before writing checksums.
  </action>
  <verify>
    <automated>cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; EVIDENCE="$PWD/.planning/phases/23-fastapi-production-stage/evidence" &amp;&amp; RELEASES="$PWD/.planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md" &amp;&amp; BASELINE_IMAGE=$(sed -n 's/^baseline_image=//p' "$RELEASES") &amp;&amp; BASELINE_SOURCE=$(sed -n 's/^baseline_source=//p' "$RELEASES") &amp;&amp; FINAL_IMAGE=$(sed -n 's/^final_image=//p' "$RELEASES") &amp;&amp; FINAL_SOURCE=$(sed -n 's/^final_source=//p' "$RELEASES") &amp;&amp; test -n "$BASELINE_IMAGE" &amp;&amp; test -n "$BASELINE_SOURCE" &amp;&amp; test -n "$FINAL_IMAGE" &amp;&amp; test -n "$FINAL_SOURCE" &amp;&amp; cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; ./scripts/test-production.sh --run --baseline-image "$BASELINE_IMAGE" --baseline-source "$BASELINE_SOURCE" --final-image "$FINAL_IMAGE" --final-source "$FINAL_SOURCE" --evidence-dir "$EVIDENCE"</automated>
  </verify>
  <acceptance_criteria>
    - The two public images pass exact source/workflow/package/digest/OCI preflight before cluster mutation.
    - Baseline and final migration Jobs each reach Complete from the same digest used by the corresponding Deployment and record Alembic `0001`.
    - Each baseline, final, undone-baseline, and recovered-final state passes two-replica, image, release-log, UID/GID, one-process, port, filesystem, security, readiness, docs, and HTTP checks.
    - `kubectl rollout undo` is the transition to the retained baseline state; final recovery is one explicit immutable tuple apply.
    - The same primary task ID and updated payload survive every transition.
    - The trap finishes with zero exact-run resources and owned processes even when an intermediate assertion fails.
  </acceptance_criteria>
  <done>One real PostgreSQL/Kubernetes run proves production behavior, immutable rollback, explicit recovery, and exact cleanup across all four states.</done>
</task>

<task type="auto">
  <name>Task 2: Audit the curated evidence, checksums, and zero footprint</name>
  <read_first>
    .planning/phases/23-fastapi-production-stage/evidence/workflow.txt
    .planning/phases/23-fastapi-production-stage/evidence/images.txt
    .planning/phases/23-fastapi-production-stage/evidence/migration.txt
    .planning/phases/23-fastapi-production-stage/evidence/runtime.txt
    .planning/phases/23-fastapi-production-stage/evidence/logs.txt
    .planning/phases/23-fastapi-production-stage/evidence/http.jsonl
    .planning/phases/23-fastapi-production-stage/evidence/rollback.txt
    .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt
    .planning/phases/23-fastapi-production-stage/evidence/checksums.txt
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/evidence/checksums.txt, .planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md</files>
  <action>
    Run `scripts/test-production.sh --verify-evidence live --evidence-dir <path>` from the Sealos.io evidence directory. The live semantic mode must require exactly the eight reviewed data files plus checksums.txt and reject publication.txt; parse every line and JSON object; require both workflow head/target identities, both image mappings, two migration completions, four ordered runtime states, four release-log states, the persistent task sequence, controller revisions, `rollout_undo=passed`, explicit final recovery, and final zero inventories. Independently query the exact recorded run label across Deployment, ReplicaSet, Pod, Service, Job, Secret, and ConfigMap and require zero; inspect recorded PIDs/commands and exact prefix files/directories and require none. Verify the Reference Application remains byte-for-byte at FINAL_SHA with a clean tree and public main unchanged.

    Run the credential scan before accepting checksums. Scan for PostgreSQL userinfo URLs, DATABASE_URL assignment/value output, password/Secret data, GitHub or bearer tokens, Kubernetes service-account tokens, private registry credentials, and raw kubeconfig content; report only file and rule on failure. Require checksums.txt to contain one repository-relative SHA-256 entry for each of the other eight evidence files, then verify every digest. Do not rewrite any reviewed evidence after this verification. Create 23-03-SUMMARY.md with run ID, namespace/context, exact state sequence, two Job names/conditions/revisions, Deployment/ReplicaSet revisions, Pod image IDs, runtime/process/security results, HTTP task continuity, evidence checksum, source-clean proof, and zero cleanup inventory. Commit the nine evidence files and summary in Sealos.io as `docs(23-03): record FastAPI rollback and recovery`; create no Reference Application commit.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; ./scripts/test-production.sh --verify-evidence live --evidence-dir /Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/23-fastapi-production-stage/evidence &amp;&amp; cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; sha256sum -c .planning/phases/23-fastapi-production-stage/evidence/checksums.txt &amp;&amp; test "$(wc -l &lt; .planning/phases/23-fastapi-production-stage/evidence/checksums.txt | tr -d ' ')" = 8</automated>
  </verify>
  <acceptance_criteria>
    - Live semantic mode rejects publication.txt and requires exactly eight nonempty, parseable, internally coherent data files in the eight-entry checksum manifest.
    - Credential and secret scans pass before checksum acceptance.
    - Cleanup evidence and live readback both show zero exact-run resources, owned processes, state, render, and clone residue.
    - Runtime/rollback/HTTP records prove the mandatory four-state sequence and one task's continuity.
    - The Reference Application remains clean at public FINAL_SHA and receives no Plan 23-03 source commit.
  </acceptance_criteria>
  <done>The live proof is durable, credential-free, checksum-valid, source-coherent, and closed with an empty run footprint.</done>
</task>

</tasks>

## Artifacts this phase produces

- Evidence records `workflow.txt`, `images.txt`, `migration.txt`, `runtime.txt`, `logs.txt`, `http.jsonl`, `rollback.txt`, and `cleanup.txt`
- Eight-entry `evidence/checksums.txt`
- Four named runtime states `baseline`, `final`, `baseline-rollback`, and `final-recovered`
- Persistent task continuity record keyed by `task_id`
- `.planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Authenticated client to shared Sealos namespace | Exact-run mutations coexist with unrelated user resources. |
| Public GHCR to Kubernetes runtime | External immutable bytes execute with an internal database Secret. |
| Service HTTP and Pod logs to retained evidence | Runtime output crosses into durable repository artifacts. |
| Deployment controller history to rollback | Controller revisions choose which immutable source/image tuple runs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-03-S | Spoofing | Pod image/release identity | high | mitigate | Correlate Deployment digest, both Pod imageIDs, source env, OCI revision, and application startup log at every state. |
| T-23-03-T | Tampering | Rollout history and task continuity | high | mitigate | Record controller revisions and immutable tuples; verify the same task only through public HTTP after every transition. |
| T-23-03-I | Information Disclosure | Database Secret and evidence | high | mitigate | Never read Secret values; curate fields, redact temp output, scan credentials, then checksum reviewed evidence. |
| T-23-03-D | Denial of Service | Shared namespace and PostgreSQL | high | mitigate | Use bounded resources/waits, one exact run label/prefix, and cleanup installed before mutation. |
| T-23-03-E | Elevation of Privilege | Running Pods | high | mitigate | Live-prove numeric non-root identity, effective restricted security context, read-only root, and one process. |
</threat_model>

<verification>
1. Independently resolve both public image/source/workflow identities before mutation.
2. Execute the exact four-state migration/rollout/undo/recovery chain on one owned PostgreSQL database.
3. Verify two Pods, process/security/filesystem/log/image/readiness/HTTP behavior at every state.
4. Verify curated evidence, credential scan, eight checksums, source freeze, and zero exact-run footprint.
</verification>

<success_criteria>
- Real Sealos evidence proves every FAST-03 runtime, migration, logging, scaling, rollback, and recovery behavior.
- The final recovered state runs the final digest on two Ready Pods and retains the task updated before rollback.
- All temporary cluster/local resources are absent and the immutable public source/images plus redacted evidence are the only durable artifacts.
</success_criteria>

<output>
Create `.planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md` with the complete four-state runtime proof, evidence manifest, source freeze, deviations, and exact cleanup audit.
</output>
