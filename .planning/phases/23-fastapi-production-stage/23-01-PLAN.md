---
phase: 23-fastapi-production-stage
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/.dockerignore
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
  - .planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
autonomous: true
requirements: [FAST-03, FAST-04]
must_haves:
  truths:
    - "D-01: The production image resolves the accepted lock with pinned official Python 3.12.13 and uv 0.10.9 image digests."
    - "D-02: The runtime image has fixed UID/GID 10001 and one exec-form Uvicorn process on 0.0.0.0:8000."
    - "D-03: Application startup emits one stable source-release and expected-image-reference record while all public response bodies remain unchanged."
    - "D-04: Public GitHub Actions normalizes one validated TARGET_SHA from push or workflow_dispatch and uses it for checkout, tests, build context, image tag, build revision, OCI revision, concurrency, summary, and public digest with no mutable image tag."
    - "D-08: The container and release-log contract retains one test-only RED followed immediately by its minimum production-only GREEN."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py"
      provides: "Public/static production container and release-log behavior contract"
      contains: "test_hardened_container_contract"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile"
      provides: "Pinned multi-stage non-root Python runtime"
      contains: "USER 10001:10001"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml"
      provides: "Pinned test-before-publish GHCR workflow"
      contains: "TARGET_SHA"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      provides: "Deterministic production startup identity log"
      contains: "event=service_start"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile"
      via: "uv sync --locked --no-dev in the builder"
      pattern: "uv sync.*--locked.*--no-dev"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml"
      to: "ghcr.io/yangchuansheng/sealos-fastapi-tutorial"
      via: "full-SHA tag plus build-push digest output"
      pattern: "sha-.*target_sha"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      via: "baked SOURCE_RELEASE consumed by lifespan startup logging"
      pattern: "SOURCE_RELEASE"
---

<objective>
Specify and publish the first production-capable FastAPI image as the immutable rollback baseline.

Purpose: Establish a source-identifiable, locked, non-root, single-process image before workload contracts add the final release source.
Output: A direct RED/GREEN pair, accepted baseline source on public main, one successful exact-SHA workflow run, and one anonymous-resolvable public baseline GHCR digest.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-fastapi-production-stage/23-CONTEXT.md
@.planning/phases/23-fastapi-production-stage/23-RESEARCH.md
@.planning/phases/23-fastapi-production-stage/23-PATTERNS.md
@.planning/phases/23-fastapi-production-stage/23-VALIDATION.md
@.planning/phases/22-fastapi-postgresql-stage/22-VERIFICATION.md
@.planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - specify the hardened image, publisher, and startup identity</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/uv.lock
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    .planning/phases/23-fastapi-production-stage/23-CONTEXT.md
    .planning/phases/23-fastapi-production-stage/23-VALIDATION.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py</files>
  <behavior>
    - The repository exposes a pinned, allowlist-copy, multi-stage image using Python 3.12.13 and uv 0.10.9 official index digests.
    - The runtime image declares numeric user/group 10001, port 8000, and one exec-form Uvicorn command with an explicit single worker.
    - The build context and runtime copy set exclude tests, evidence, VCS data, caches, development dependencies, and repository-wide copies.
    - The publisher uses only the six approved full Action SHAs, accepts workflow_dispatch input source_sha constrained to 40 lowercase hex, normalizes push github.sha or validated dispatch input to one TARGET_SHA, and exposes that value as a prepare-job output.
    - Test checkout ref, verified checkout HEAD, build context, linux/amd64 sha-${TARGET_SHA} tag, SOURCE_RELEASE build argument, OCI revision, publish concurrency key, digest output, logs, and summary all derive from the same TARGET_SHA.
    - A TestClient lifespan startup emits exactly one record with event=service_start, source_release=<40 hex>, and image_reference=<digest reference> from SOURCE_RELEASE and IMAGE_REFERENCE.
  </behavior>
  <action>
    In the Reference Application repository, first run the accepted Stage 2 lock/export and real PostgreSQL phase gate. Add tests/test_production.py using pathlib and public TestClient/caplog behavior only; do not mock FastAPI, Uvicorn, the lifespan, or internal collaborators. Give the missing-image assertion the exact message `Dockerfile must exist`, ensure it is the first failing production assertion, and cover the concrete D-01, D-02, D-03, D-04, and D-08 values listed in behavior. Run the focused test and retain pytest status 1 with `AssertionError: Dockerfile must exist`. Commit only tests/test_production.py as `test(23-01): specify hardened FastAPI container contract`; confirm the commit has one file and its parent is the accepted Stage 2 commit `2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3`.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d')" = "tests/test_production.py" &amp;&amp; test "$(git log -1 --format=%s)" = "test(23-01): specify hardened FastAPI container contract" &amp;&amp; test "$(git rev-parse HEAD^)" = "2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3"</automated>
  </verify>
  <acceptance_criteria>
    - `uv lock --check` and the exact runtime export diff pass before RED is committed.
    - The inherited real PostgreSQL gate passes before the new test is added.
    - Focused pytest exits 1 for the intended missing Dockerfile assertion after test collection and imports succeed.
    - The RED commit changes exactly tests/test_production.py and has the exact subject and parent above.
  </acceptance_criteria>
  <done>The committed RED reaches the confirmed static/public seam and fails for the absent production artifact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - add the minimum reproducible production image and publisher</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/migrations/env.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    .planning/phases/23-fastapi-production-stage/23-RESEARCH.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile, /Users/longnv/bin/repo/sealos-fastapi-tutorial/.dockerignore, /Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <behavior>
    - The complete focused production test turns green without modifying the RED test.
    - All inherited health, docs, CRUD, migration, readiness, and migration-Job tests remain green against real PostgreSQL.
  </behavior>
  <action>
    Re-resolve the official image references immediately before editing and require Python index digest `sha256:d50fb7611f86d04a3b0471b46d7557818d88983fc3136726336b2a4c657aa30b` plus uv index digest `sha256:10902f58a1606787602f303954cea099626a4adb02acbac4c69920fe9d278f82`; stop on drift and update research before using a different digest. Add a multi-stage Dockerfile that resolves `.venv` from committed pyproject.toml and uv.lock, copies only `.venv`, app, alembic.ini, and migrations into the runtime, creates UID/GID 10001, declares SOURCE_RELEASE from build arg, exposes 8000, and execs one Uvicorn process with `--workers 1`. Add the explicit build-context exclusions required by the test. Extend the existing lifespan to log one stable `event=service_start source_release=%s image_reference=%s` record before yield through the existing module logger while preserving disposal and every HTTP body.

    Add `.github/workflows/publish-image.yml` with push-to-main and workflow_dispatch triggers. Define required dispatch input `source_sha` and validate it against `^[0-9a-f]{40}$`. A prepare job must select push `${{ github.sha }}` or dispatch `${{ inputs.source_sha }}`, normalize it to one lowercase `TARGET_SHA`, print `target_sha=<value>`, and expose `target_sha` as its job output; invalid input exits before checkout or login. Every downstream job consumes only `needs.prepare.outputs.target_sha`: checkout `ref`, an immediate `git rev-parse HEAD` equality gate, source used by build context `.`, the full image tag `ghcr.io/yangchuansheng/sealos-fastapi-tutorial:sha-${{ needs.prepare.outputs.target_sha }}`, SOURCE_RELEASE build argument, explicit `org.opencontainers.image.revision` label/annotation, publish-job concurrency group, digest output, and summary `target_sha=<value> image_digest=<value>`. This exact-SHA dispatch path must work for the baseline after public main advances to final.

    Grant only `contents: read` and `packages: write`; use PostgreSQL `17.10-bookworm@sha256:4f736ae292687621d4dbe0d499ffd024a36bd2ee7d8ca6f2ccd4c800f047b394`, Python 3.12.13, uv 0.10.9, lock/export, two upgrades, current revision, and full pytest before linux/amd64 publish. Pin checkout `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`, setup-uv `11f9893b081a58869d3b5fccaea48c9e9e46f990`, login `af1e73f918a031802d376d3c8bbc3fe56130a9b0`, setup-buildx `bb05f3f5519dd87d3ba754cc423b652a5edd6d2c`, metadata `dc802804100637a589fabce1cb79ff13a1411302`, and build-push `53b7df96c91f9c12dcc8a07bcb9ccacbed38856a`. Derive SOURCE_DATE_EPOCH and OCI created time from the TARGET_SHA commit so a dispatch rebuild has stable source metadata. Before associating the full-SHA tag, inspect an existing tag through the isolated anonymous gate: absent permits creation, exact candidate digest permits idempotent acceptance, and any different digest fails before retagging. D-04 governs the exact single tag set; the workflow emits no branch, stage, date, short-SHA, main, or latest image tag.

    Run focused tests, lock/export, shell/YAML source checks, credential scans, and the complete real PostgreSQL gate. Commit only the four production files as `feat(23-01): add hardened FastAPI production image`; require it to be the direct child of RED and leave the source tree clean.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_production.py -q -x &amp;&amp; uv lock --check &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- requirements.txt &amp;&amp; ./scripts/test-postgres.sh --phase-gate &amp;&amp; test "$(git log -1 --format=%s)" = "feat(23-01): add hardened FastAPI production image" &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "test(23-01): specify hardened FastAPI container contract"</automated>
  </verify>
  <acceptance_criteria>
    - The Dockerfile contains both verified official index digests, fixed user 10001:10001, allowlisted copies, and one explicit Uvicorn worker on port 8000.
    - The workflow references only the six approved Action SHAs and produces one full-commit SHA tag plus digest.
    - tests/test_production.py, the complete inherited suite, lock/export, and the real PostgreSQL phase gate pass.
    - GREEN changes exactly Dockerfile, .dockerignore, publish-image.yml, and app/main.py and directly follows RED.
  </acceptance_criteria>
  <done>The first production-capable source commit is reproducible, one-process, non-root, test-green, and ready to publish as the rollback baseline.</done>
</task>

<task type="auto">
  <name>Task 3: Fast-forward and accept the public baseline workflow and digest</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile
    .planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md
    .planning/phases/23-fastapi-production-stage/23-CONTEXT.md
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md</files>
  <action>
    Treat public GitHub, workflow, and package state as fail-closed state machines. For public main, accept only Stage 2 `2b256b3...` before a normal fast-forward push or the exact local GREEN commit after recovery; any other SHA halts without force-push. For an absent baseline run, execute `gh workflow run publish-image.yml --repo yangchuansheng/sealos-fastapi-tutorial --ref main -f source_sha="$BASELINE_SHA"`, select the newly created workflow_dispatch run after the recorded dispatch timestamp, and retain its run ID. Wait boundedly when queued/in_progress; accept only conclusion success; permit at most one `gh run rerun "$RUN_ID" --repo yangchuansheng/sealos-fastapi-tutorial` after an infrastructure failure while retaining both attempts; a source/test failure halts. Verify the run API `head_sha` equals public main at dispatch time, event is `workflow_dispatch`, and `gh run view "$RUN_ID" --repo yangchuansheng/sealos-fastapi-tutorial --log` contains the normalized `target_sha=$BASELINE_SHA`; for a push run require both head and target equal BASELINE_SHA. This target check enables an exact baseline rebuild after main advances. For the GHCR full-SHA tag, accept absent state before the successful workflow or an exact digest whose linux/amd64 manifest and OCI revision/source match; any mismatched existing tag halts.

    Push the GREEN commit to public main by fast-forward. Read back workflow event, API head SHA, normalized target SHA from logs/summary, workflow path, conclusion, and digest output. Read the owner package API. When the first exact package exists with private visibility and repository linkage is correct, PATCH `/user/packages/container/sealos-fastapi-tutorial` to `visibility=public`; skip mutation when already public and halt for any wrong owner/source package.

    For every anonymous registry readback, create a fresh empty directory with `ANON_DOCKER_CONFIG=$(mktemp -d)`, run `chmod 700 "$ANON_DOCKER_CONFIG"`, verify it has no entries, and install a trap that removes it. Invoke each of `crane digest`, `crane config`, and `crane manifest` with `DOCKER_CONFIG="$ANON_DOCKER_CONFIG"` and `env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH`; never reuse the directory between gates. Remove the directory and clear its trap immediately after the three reads. Require anonymous full-SHA resolution, linux/amd64 manifest, OCI revision/source equality, and digest equality with the workflow output. Record the immutable `ghcr.io/yangchuansheng/sealos-fastapi-tutorial@sha256:<digest>` baseline, architecture, OCI revision, source label, workflow run URL/ID/head/target, and baseline source SHA in 23-01-SUMMARY.md. Preserve both protected source tags and ruleset 18970425 unchanged. Commit only the Sealos.io summary/tracking artifact as `docs(23-01): record FastAPI production image baseline`.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; BASELINE_SHA=$(git rev-parse HEAD) &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-fastapi-tutorial/commits/main --jq .sha)" = "$BASELINE_SHA" &amp;&amp; test "$(gh api /user/packages/container/sealos-fastapi-tutorial --jq .visibility)" = public &amp;&amp; ANON_DOCKER_CONFIG=$(mktemp -d) &amp;&amp; chmod 700 "$ANON_DOCKER_CONFIG" &amp;&amp; test -z "$(find "$ANON_DOCKER_CONFIG" -mindepth 1 -print -quit)" &amp;&amp; trap 'rm -rf "$ANON_DOCKER_CONFIG"' EXIT &amp;&amp; BASELINE_DIGEST=$(env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane digest "ghcr.io/yangchuansheng/sealos-fastapi-tutorial:sha-${BASELINE_SHA}") &amp;&amp; test -n "$BASELINE_DIGEST" &amp;&amp; env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane config "ghcr.io/yangchuansheng/sealos-fastapi-tutorial@${BASELINE_DIGEST}" | jq -e --arg sha "$BASELINE_SHA" '.config.Labels["org.opencontainers.image.revision"] == $sha and .config.Labels["org.opencontainers.image.source"] == "https://github.com/yangchuansheng/sealos-fastapi-tutorial"' &gt;/dev/null &amp;&amp; env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane manifest "ghcr.io/yangchuansheng/sealos-fastapi-tutorial@${BASELINE_DIGEST}" | jq -e . &gt;/dev/null &amp;&amp; rm -rf "$ANON_DOCKER_CONFIG" &amp;&amp; trap - EXIT</automated>
  </verify>
  <acceptance_criteria>
    - Public main equals the local baseline GREEN through a normal fast-forward.
    - One successful workflow run records API head SHA and normalized target SHA; target equals BASELINE_SHA for push and dispatch paths.
    - `gh workflow run ... --ref main -f source_sha="$BASELINE_SHA"` can rebuild the baseline after public main advances.
    - The full-SHA baseline tag resolves anonymously to a recorded public digest and correct OCI identity.
    - No latest, main, date, short-SHA, or stage image tag is published by this workflow.
    - Stage 1, Stage 2, and ruleset 18970425 read back unchanged.
  </acceptance_criteria>
  <done>The public baseline source and its immutable public GHCR digest form a coherent rollback input, and the execution summary records all identities.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_production.py::test_hardened_container_contract`
- `tests/test_production.py::test_startup_log_identifies_release`
- `Dockerfile`
- `.dockerignore`
- `.github/workflows/publish-image.yml`
- Workflow input `source_sha` and normalized job output `target_sha`
- Environment contracts `SOURCE_RELEASE` and `IMAGE_REFERENCE`
- Structured log fields `event`, `source_release`, and `image_reference`
- Public baseline image tag `sha-<40-character-commit>` and immutable GHCR digest
- `.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub source to Actions runner | Untrusted source state enters a privileged package-publish job. |
| Actions runner to GHCR | Workflow token can create or update package versions. |
| Image build context to runtime | Repository files cross into deployable bytes. |
| Environment to application logs | Release identity values enter retained runtime logs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-01-S | Spoofing | GHCR source identity | high | mitigate | Full-SHA tag, digest readback, OCI revision/source equality, and anonymous resolution are all required. |
| T-23-01-T | Tampering | GitHub Actions supply chain | high | mitigate | Pin every Action and both build images by verified full digest; grant only contents read and packages write. |
| T-23-01-I | Information Disclosure | Build context and startup log | high | mitigate | Allowlist runtime COPY inputs, exclude VCS/tests/evidence, log only public source and digest reference, and run credential scans. |
| T-23-01-E | Elevation of Privilege | Runtime image | high | mitigate | Fixed UID/GID 10001 and direct one-process command establish the image-level least-privilege boundary. |
| T-23-01-SC | Tampering | Official images and Actions | high | mitigate | Re-resolve the CLEAR audited image digests and official Action SHAs immediately before use; halt on drift. |
</threat_model>

<verification>
1. Prove RED failure reason, exact scope, unique subject, and Stage 2 parent.
2. Prove GREEN direct adjacency, exact production scope, focused tests, inherited real-PostgreSQL suite, lock, and export.
3. Prove public main fast-forward, exact successful workflow SHA, public package visibility, anonymous digest resolution, linux/amd64, and OCI labels.
4. Prove Stage 1/2 direct and peeled objects plus the existing ruleset remain unchanged.
</verification>

<success_criteria>
- The rollback baseline has one immutable source SHA, one full-SHA public image tag, and one recorded public digest.
- Python 3.12.13, uv 0.10.9, the exact runtime lock, UID/GID 10001, one Uvicorn process, and port 8000 are source-testable.
- The TDD pair and external publication states are fail-closed and fully recorded in 23-01-SUMMARY.md.
</success_criteria>

<output>
Create `.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md` with RED/GREEN commit identities, exact scopes, workflow run, public package state, baseline tag/digest/OCI mapping, preserved source refs, deviations, and cleanup state.
</output>
