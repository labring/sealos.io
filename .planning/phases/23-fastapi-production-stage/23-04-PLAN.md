---
phase: 23-fastapi-production-stage
plan: "04"
type: execute
wave: 4
depends_on: ["23-03"]
files_modified:
  - .planning/phases/23-fastapi-production-stage/evidence/publication.txt
  - .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt
  - .planning/phases/23-fastapi-production-stage/evidence/checksums.txt
  - .planning/phases/23-fastapi-production-stage/23-04-SUMMARY.md
autonomous: true
requirements: [FAST-03, FAST-04]
must_haves:
  truths:
    - "D-04: The final full-SHA GHCR tag resolves anonymously to the live-accepted final digest, linux/amd64 manifest, and exact OCI source revision without a mutable image tag."
    - "D-07: Protected Stage 1 and Stage 2 direct/peeled objects remain exact, protected annotated stage-3-production peels to public main, and main equals the final production source."
    - "D-07: Publication accepts only absent or exact-matching GitHub main, tag, workflow, package, and image states and uses no force update."
    - "D-09: A fresh public clone reproduces Stage 3 and its image identity, then leaves no cluster, process, state, render, or clone footprint."
    - "Every source stage retains a reproducible uv.lock and exact runtime requirements.txt compatibility export."
  artifacts:
    - path: ".planning/phases/23-fastapi-production-stage/evidence/publication.txt"
      provides: "Public main, all direct/peeled tags, ruleset, workflow, package, image, and clone replay identities"
      contains: "stage_3_message=FastAPI production stage"
    - path: ".planning/phases/23-fastapi-production-stage/evidence/checksums.txt"
      provides: "Final nine-entry SHA-256 manifest generated only after checksum-independent publication preflight passes"
    - path: ".planning/phases/23-fastapi-production-stage/23-04-SUMMARY.md"
      provides: "Fail-closed publication and public replay handoff for independent verification"
  key_links:
    - from: "https://github.com/yangchuansheng/sealos-fastapi-tutorial/tree/stage-3-production"
      to: "https://github.com/yangchuansheng/sealos-fastapi-tutorial/tree/main"
      via: "annotated Stage 3 peeled commit equals public main"
      pattern: "stage-3-production"
    - from: "stage-3-production peeled commit"
      to: "ghcr.io/yangchuansheng/sealos-fastapi-tutorial@sha256:<final>"
      via: "sha-<peeled commit> tag resolution plus exact OCI revision"
      pattern: "org.opencontainers.image.revision"
    - from: ".planning/phases/23-fastapi-production-stage/evidence/publication.txt"
      to: ".planning/phases/23-fastapi-production-stage/evidence/checksums.txt"
      via: "publication is finalized before the checksum manifest is regenerated"
      pattern: "publication.txt"
---

<objective>
Publish protected Stage 3 only after live acceptance, replay it from public HTTPS, and close Phase 23 with final coherent evidence and cleanup.

Purpose: Make all source and image identities durable for readers while fail-closing every ambiguous recovery state.
Output: Protected annotated stage-3-production, exact public main alignment, fresh public-clone and GHCR replay proof, final nine-file evidence checksum manifest, and zero temporary footprint.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-fastapi-production-stage/23-CONTEXT.md
@.planning/phases/23-fastapi-production-stage/23-VALIDATION.md
@.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/evidence/checksums.txt
@.planning/phases/22-fastapi-postgresql-stage/22-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish protected Stage 3 through exact recovery states</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml
    .planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
    .planning/phases/23-fastapi-production-stage/23-03-SUMMARY.md
    .planning/phases/23-fastapi-production-stage/evidence/images.txt
    .planning/phases/23-fastapi-production-stage/evidence/rollback.txt
    .planning/phases/23-fastapi-production-stage/23-CONTEXT.md
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/evidence/publication.txt</files>
  <action>
    Load BASELINE_SHA/BASELINE_DIGEST and FINAL_SHA/FINAL_DIGEST from accepted Phase 23 evidence and require the local Reference Application clean at FINAL_SHA. Read public repository owner/visibility/default branch/main, Actions workflow/runs, GHCR package visibility/source linkage, anonymous full-SHA image identities, and ruleset before mutating a ref. Public main must equal FINAL_SHA. For each missing baseline or final exact-SHA run, record a UTC dispatch timestamp and execute `gh workflow run publish-image.yml --repo yangchuansheng/sealos-fastapi-tutorial --ref main -f source_sha="$TARGET_SHA"` with TARGET_SHA set to BASELINE_SHA or FINAL_SHA. Select only the workflow_dispatch run created after that timestamp, wait boundedly, require API event `workflow_dispatch`, API `head_sha` equal FINAL_SHA because ref main already points to final, successful conclusion, and run logs/summary `target_sha=$TARGET_SHA`; this explicitly rebuilds the baseline after main advances while keeping checkout/build/tag/revision tied to BASELINE_SHA. For an accepted push run, require API head SHA and normalized target SHA both equal the candidate. An active exact run waits; one infrastructure-only failure may use `gh run rerun "$RUN_ID" --repo yangchuansheng/sealos-fastapi-tutorial`; source/test failure, another API head, another normalized target, or digest drift halts. Record head and target separately for both images.

    Package state is accepted when public and exact; when private, permit the authenticated owner visibility PATCH only after package source plus final/baseline versions and digests match; absent package or any identity mismatch halts. For every anonymous baseline or final read, create a new empty `ANON_DOCKER_CONFIG=$(mktemp -d)`, chmod it 0700, verify it contains no entry, and install removal before crane starts. Run `crane digest`, `crane config`, and `crane manifest` with that DOCKER_CONFIG while clearing GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH; validate digest, linux/amd64 manifest, OCI source, and OCI revision, then remove the directory and clear its trap. Use a different fresh directory for the other image. Require the final digest equal the live-recovered digest from Plan 23-03, the rebuilt baseline digest equal the accepted baseline digest, and zero mutable image versions produced by publish-image.yml.

    Read and freeze Stage 1 direct `77e57a281ecc087041b54273c1bfc63b66f13d1a`, peeled `276aa00e4d5bb7a0d5e375fee530cde3240b2ce8`, message `FastAPI deploy stage`; Stage 2 direct `b61254c237885744ae85cb6f81386f77f1e3ac09`, peeled `2b256b3dfc2a7d2a4b930c9970becca8c6da8cd3`, message `FastAPI PostgreSQL stage`; and active ruleset `18970425` targeting only `refs/tags/stage-*` with update/deletion rules, empty exclude, and empty bypass. Any drift halts before Stage 3 mutation.

    Recover stage-3-production through exactly four coherent local/remote states. Absent/absent: create one annotated tag at FINAL_SHA with exact message `FastAPI production stage`, validate locally, then push only `refs/tags/stage-3-production`. Exact-local/absent-remote: validate type, message, peeled FINAL_SHA, then push only that ref. Absent-local/exact-remote: fetch that exact ref and validate its direct object, type, message, and peeled FINAL_SHA. Exact-local/exact-remote: require identical direct tag object and peeled FINAL_SHA. Any fifth state, differing direct object, lightweight tag, message drift, or peeled mismatch halts without delete/update/force. After publication, re-read main, all three local/remote direct and peeled refs/messages, ruleset, workflow, package, and both image digests. Write only curated values to publication.txt.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; FINAL_SHA=$(git rev-parse HEAD) &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-fastapi-tutorial/commits/main --jq .sha)" = "$FINAL_SHA" &amp;&amp; test "$(git cat-file -t stage-3-production)" = tag &amp;&amp; test "$(git rev-parse stage-3-production^{})" = "$FINAL_SHA" &amp;&amp; test "$(git for-each-ref refs/tags/stage-3-production --format='%(contents:subject)')" = "FastAPI production stage" &amp;&amp; test "$(git ls-remote origin refs/tags/stage-3-production^{} | awk '{print $1}')" = "$FINAL_SHA"</automated>
  </verify>
  <acceptance_criteria>
    - Public main, exact final workflow, package, final digest, live-recovered digest, and OCI identity agree before tag mutation.
    - Stage 1/2 direct objects, peeled commits, messages, and ruleset 18970425 agree with the accepted constants before and after publication.
    - Stage 3 is one protected annotated tag with the exact message and peeled FINAL_SHA; local and remote direct objects agree.
    - Publication uses normal ref creation/push only and leaves all existing tags unchanged.
    - publication.txt contains curated identities and no credential-bearing values.
  </acceptance_criteria>
  <done>Public main, three protected source stages, two immutable images, workflow, package, and the live final digest form one coherent publication state.</done>
</task>

<task type="auto">
  <name>Task 2: Replay public Stage 3 source and image identity from clean clones</name>
  <read_first>
    .planning/phases/23-fastapi-production-stage/evidence/publication.txt
    .planning/phases/23-fastapi-production-stage/evidence/images.txt
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/evidence/publication.txt, .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt</files>
  <action>
    Create three fresh temporary directories with mode-safe generated names outside both repositories and install one trap before cloning. Clone each public HTTPS source tag independently. For Stage 1, require checkout peeled `276aa00...`, Python 3.12 lock validity, exact runtime export, and its public HTTP suite. For Stage 2, require checkout peeled `2b256b3...`, lock validity, and exact runtime export. For Stage 3, require checkout FINAL_SHA, clean tree, lock/export, all static production/migration tests, shell syntax/help, Dockerfile official pins, workflow Action SHAs and exact tag set, manifests, README, and the complete real PostgreSQL phase gate with evidence written outside the clone. The Stage 3 real gate must begin from a fresh run-scoped PostgreSQL database, reach migration `0001`, pass all public HTTP behavior, and prove its own exact cleanup.

    From the Stage 3 clone's peeled source SHA, derive only `sha-${FINAL_SHA}`. Create a fresh empty mode-0700 DOCKER_CONFIG with removal trap, clear GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH, and run final `crane digest`, `crane config`, and `crane manifest` with only that isolated config. Require digest equal FINAL_DIGEST, linux/amd64, OCI revision FINAL_SHA, exact source URL, title/version labels, and anonymous manifest/config access; remove the config before continuing. Create a second fresh isolated config with the same cleared environment and run all three crane operations for the baseline; require BASELINE_DIGEST and BASELINE_SHA, then remove it. Ambient Docker keychain and credential-helper state cannot satisfy either replay. Read remote Stage 3 direct and peeled refs independently of the local implementation checkout. Append the three clone commits, lock/export outcomes, Stage 1 test result, Stage 3 static/real PostgreSQL outcomes, both isolated GHCR identity replays, workflow API head/normalized target pairs, and public URL boundaries to publication.txt. Stop the owned database session, query its exact run label to zero across all kinds, remove all clone/evidence temp directories, and append only the final zero counts to cleanup.txt.
  </action>
  <verify>
    <automated>cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; rg -q '^public_stage_3_clone=passed ' .planning/phases/23-fastapi-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_final_image_replay=passed ' .planning/phases/23-fastapi-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_clone_cleanup=passed ' .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt</automated>
  </verify>
  <acceptance_criteria>
    - Three independent public clones peel to the exact accepted Stage 1, Stage 2, and Stage 3 commits.
    - Every stage reproduces its lock/export; Stage 1 behavior and Stage 3 static plus real PostgreSQL gates pass.
    - The Stage 3 clone-derived full-SHA tag anonymously resolves to the live-accepted final digest and matching OCI identity.
    - Baseline and final digests remain public and distinct.
    - Every clone, external replay directory, exact-run Kubernetes object, and owned process is removed before acceptance.
  </acceptance_criteria>
  <done>A reader starting only from public source tags and GHCR can reproduce all source locks and the complete Stage 3 runtime identity.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize publication evidence, checksums, cleanup, and handoff</name>
  <read_first>
    .planning/phases/23-fastapi-production-stage/evidence/workflow.txt
    .planning/phases/23-fastapi-production-stage/evidence/images.txt
    .planning/phases/23-fastapi-production-stage/evidence/migration.txt
    .planning/phases/23-fastapi-production-stage/evidence/runtime.txt
    .planning/phases/23-fastapi-production-stage/evidence/logs.txt
    .planning/phases/23-fastapi-production-stage/evidence/http.jsonl
    .planning/phases/23-fastapi-production-stage/evidence/rollback.txt
    .planning/phases/23-fastapi-production-stage/evidence/publication.txt
    .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt
    .planning/phases/23-fastapi-production-stage/evidence/checksums.txt
  </read_first>
  <files>.planning/phases/23-fastapi-production-stage/evidence/checksums.txt, .planning/phases/23-fastapi-production-stage/23-04-SUMMARY.md</files>
  <action>
    Finish every publication.txt and cleanup.txt field from Tasks 1-2 before checksum work. Run `scripts/test-production.sh --preflight-evidence publication --evidence-dir <path>` first. This checksum-independent preflight must require exactly the nine data files workflow.txt, images.txt, migration.txt, runtime.txt, logs.txt, http.jsonl, rollback.txt, publication.txt, and cleanup.txt; include all live semantics plus coherent baseline/final workflow API-head and normalized-target identities, image mappings, mandatory four-state runtime order, persistent task continuity, exact Stage 1/2/3 direct/peeled identities and messages, ruleset 18970425, public package, isolated anonymous baseline/final digest/config/manifest replay, three fresh-clone outcomes, final cleanup, and the credential scan. It may ignore only stale or missing checksums.txt and must reject any missing/extra data file or semantic/redaction failure.

    After preflight succeeds, generate checksums.txt atomically through a temporary file with exactly nine repository-relative SHA-256 entries in this order: workflow.txt, images.txt, migration.txt, runtime.txt, logs.txt, http.jsonl, rollback.txt, publication.txt, cleanup.txt. Rename the completed temporary manifest to checksums.txt, then run `scripts/test-production.sh --verify-evidence publication --evidence-dir <path>` exactly once. Full publication verification repeats all semantics/redaction checks, requires exactly nine checksum entries and exact paths, and verifies every digest. From that successful full verification onward, evidence-directory operations are read-only: checksum verification, file counts, and parsing are allowed; evidence regeneration, append, touch, rename, or rewrite is prohibited.

    Re-read public main, all three direct/peeled refs/messages, ruleset, exact final workflow, package visibility, anonymous baseline/final digests, OCI revisions, and final Reference Application clean status. Re-query every exact recorded run label/prefix across Deployment, ReplicaSet, Pod, Service, Job, Secret, and ConfigMap plus owned process/state/render/clone paths and require zero. These checks are read-only after full publication verification. Create 23-04-SUMMARY.md outside the evidence directory with publication recovery branch, accepted constants, final tag object and peeled commit, main equality, image mappings, public clone gates, final evidence manifest, zero-footprint proof, and any deviations. Commit the already-final publication.txt, cleanup.txt, checksums.txt, and the summary in Sealos.io as `docs(23-04): publish FastAPI production stage`; create no Reference Application commit and perform no tag/image mutation after final readback.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; EVIDENCE=/Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/23-fastapi-production-stage/evidence &amp;&amp; ./scripts/test-production.sh --preflight-evidence publication --evidence-dir "$EVIDENCE" &amp;&amp; CHECKSUM_TMP=$(mktemp "$EVIDENCE/.checksums.XXXXXX") &amp;&amp; trap 'rm -f "$CHECKSUM_TMP"' EXIT &amp;&amp; cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; sha256sum .planning/phases/23-fastapi-production-stage/evidence/workflow.txt .planning/phases/23-fastapi-production-stage/evidence/images.txt .planning/phases/23-fastapi-production-stage/evidence/migration.txt .planning/phases/23-fastapi-production-stage/evidence/runtime.txt .planning/phases/23-fastapi-production-stage/evidence/logs.txt .planning/phases/23-fastapi-production-stage/evidence/http.jsonl .planning/phases/23-fastapi-production-stage/evidence/rollback.txt .planning/phases/23-fastapi-production-stage/evidence/publication.txt .planning/phases/23-fastapi-production-stage/evidence/cleanup.txt &gt; "$CHECKSUM_TMP" &amp;&amp; mv "$CHECKSUM_TMP" "$EVIDENCE/checksums.txt" &amp;&amp; trap - EXIT &amp;&amp; cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; ./scripts/test-production.sh --verify-evidence publication --evidence-dir "$EVIDENCE" &amp;&amp; cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; sha256sum -c .planning/phases/23-fastapi-production-stage/evidence/checksums.txt &amp;&amp; test "$(wc -l &lt; .planning/phases/23-fastapi-production-stage/evidence/checksums.txt | tr -d ' ')" = 9 &amp;&amp; cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; test -z "$(git status --short)" &amp;&amp; test "$(git rev-parse HEAD)" = "$(git rev-parse stage-3-production^{})"</automated>
  </verify>
  <acceptance_criteria>
    - Publication preflight validates the exact nine data files, all live/publication semantics, and redaction while allowing only checksums.txt to be missing or stale.
    - Full publication verification runs exactly once after atomic nine-entry checksum generation and includes source-tag, ruleset, package, workflow head/target, isolated anonymous image, and clone replay checks.
    - Credential scans pass before the final nine-entry checksum manifest is generated.
    - Exactly nine checksum entries verify, extra/missing reviewed files fail, and no evidence file changes afterward.
    - Public main, three protected annotated stages, exact ruleset, workflow, package, baseline/final images, and fresh-clone results remain coherent.
    - Exact-run resource/process/state/render/clone inventories are all zero.
    - The Reference Application remains clean at FINAL_SHA and receives no post-freeze commit.
  </acceptance_criteria>
  <done>FAST-03/FAST-04 verification inputs are complete, immutable identities remain public, and every temporary footprint is gone.</done>
</task>

</tasks>

## Artifacts this phase produces

- Protected annotated source tag `stage-3-production`
- Exact tag message `FastAPI production stage`
- `evidence/publication.txt`
- Checksum-independent publication preflight followed by final nine-entry `evidence/checksums.txt`
- Updated final `evidence/cleanup.txt`
- `.planning/phases/23-fastapi-production-stage/23-04-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Git to public protected refs | A local annotated object becomes durable reader source. |
| Public source clone to local execution | Unauthenticated repository bytes enter lock, test, and Kubernetes gates. |
| Public GHCR to clone-derived identity check | Registry metadata must match independently cloned source. |
| Final evidence to future tutorial phases | Retained identities become the source for claims and screenshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-04-S | Spoofing | Source tag and image mapping | high | mitigate | Verify direct and peeled refs, annotated type/message, main equality, full-SHA tag, digest, and OCI revision from a public clone. |
| T-23-04-T | Tampering | Existing protected stages | high | mitigate | Freeze exact Stage 1/2 constants and ruleset before/after; accept only four coherent Stage 3 states and use no update/delete/force. |
| T-23-04-R | Repudiation | Publication recovery | high | mitigate | Record the chosen recovery branch plus every local/remote object, API readback, workflow run, and checksum. |
| T-23-04-I | Information Disclosure | Clone/runtime evidence | high | mitigate | Use public-only curated fields, external temp evidence, credential scan, and exact cleanup before final checksums. |
| T-23-04-D | Denial of Service | Shared namespace replay | medium | mitigate | Reuse bounded run-scoped Phase 22 lifecycle and exact cleanup; halt on ambiguous state before mutation. |
</threat_model>

<verification>
1. Verify public main/final workflow/package/image and frozen Stage 1/2/ruleset before Stage 3 creation.
2. Recover/publish Stage 3 through one of four coherent states and re-read every identity.
3. Reproduce all stage locks and Stage 3 source/runtime/image from fresh public clones.
4. Finalize credential-free nine-file evidence checksums and prove zero cluster/local footprint.
</verification>

<success_criteria>
- `stage-1-deploy`, `stage-2-postgresql`, and `stage-3-production` are protected annotated public refs at the accepted commits, and public main equals Stage 3.
- Baseline and final public image digests resolve anonymously from their exact full-source-SHA tags; the final digest equals the live recovered image.
- Public clone gates, final checksums, and cleanup pass with no post-freeze source mutation.
</success_criteria>

<output>
Create `.planning/phases/23-fastapi-production-stage/23-04-SUMMARY.md` with source/tag/ruleset/image/workflow/package identities, public clone results, final checksum manifest, exact cleanup, and FAST-03/FAST-04 verifier handoff.
</output>
