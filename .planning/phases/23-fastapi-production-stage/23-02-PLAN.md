---
phase: 23-fastapi-production-stage
plan: "02"
type: tdd
wave: 2
depends_on: ["23-01"]
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
  - .planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
autonomous: true
requirements: [FAST-03, FAST-04]
must_haves:
  truths:
    - "D-02: The workload contract runs two replicas under the complete restricted security context with a read-only root and bounded writable /tmp."
    - "D-03: Deployment image digest, source release environment, Pod image ID, and startup log are one verifiable identity tuple."
    - "D-04: The final frozen source produces only its full-SHA public image tag and immutable digest through the pinned workflow."
    - "D-05: The same immutable application image runs a hardened one-shot migration Job before the two-replica Deployment is accepted."
    - "D-08: The production workload and migration contract has one test-only RED directly followed by its minimum manifest-and-harness GREEN."
    - "The final README and source tree are frozen before the successful final image workflow, with no later Reference Application commit in this phase."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml"
      provides: "Parameterized two-replica Deployment and Service"
      contains: "__IMAGE_REFERENCE__"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml"
      provides: "Parameterized hardened application-image migration Job"
      contains: "alembic"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh"
      provides: "Run-scoped registry, migration, rollout, rollback, evidence, and cleanup harness"
      contains: "kubectl rollout undo"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md"
      provides: "Immutable production reader and rollback workflow"
      contains: "stage-3-production"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml"
      via: "identical rendered digest and DATABASE_URL Secret before rollout"
      pattern: "__IMAGE_REFERENCE__"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      via: "SOURCE_RELEASE and IMAGE_REFERENCE environment values consumed by startup logging"
      pattern: "SOURCE_RELEASE|IMAGE_REFERENCE"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh"
      via: "owned session-start/state-file/session-stop lifecycle"
      pattern: "test-postgres.sh.*--session-start"
---

<objective>
Specify the production Kubernetes contract, freeze the complete reader source, and publish the final immutable FastAPI image.

Purpose: Make the final source tree executable by the real Sealos rollback harness while preserving a separate baseline digest from Plan 23-01.
Output: A workload RED/GREEN pair, hardened parameterized manifests, a complete run-scoped production harness, final README/source commit, successful final workflow, and public final digest.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-fastapi-production-stage/23-CONTEXT.md
@.planning/phases/23-fastapi-production-stage/23-RESEARCH.md
@.planning/phases/23-fastapi-production-stage/23-PATTERNS.md
@.planning/phases/23-fastapi-production-stage/23-VALIDATION.md
@.planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
@.planning/phases/22-fastapi-postgresql-stage/22-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - specify the hardened application and migration workload</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/Dockerfile
    .planning/phases/23-fastapi-production-stage/23-CONTEXT.md
    .planning/phases/23-fastapi-production-stage/23-VALIDATION.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py</files>
  <behavior>
    - deploy/application.yaml contains a parameterized Deployment and Service with run, app, digest, source, and Secret inputs.
    - The Deployment has two replicas, RollingUpdate maxUnavailable 0/maxSurge 1, revisionHistoryLimit 3, progress deadline 180, named port http at 8000, and readiness GET /health.
    - Pod and container policy fixes UID/GID 10001, disables service-account token and privilege escalation, drops ALL capabilities, sets RuntimeDefault seccomp and read-only root, and mounts a size-limited emptyDir at /tmp.
    - Resources are bounded, DATABASE_URL comes from Secret key url, and SOURCE_RELEASE plus IMAGE_REFERENCE match the rendered image tuple.
    - deploy/migration-job.yaml uses the same digest and Secret tokens, one alembic upgrade head command, bounded execution, and the same non-root/read-only/tmp hardening.
    - scripts/test-production.sh exposes a bounded noninteractive run interface and contains strict render, server validation, baseline/final migration, rollout undo, explicit recovery, exact cleanup, checksum-independent `--preflight-evidence publication`, and full semantic modes `--verify-evidence live` plus `--verify-evidence publication` without running live infrastructure from pytest.
  </behavior>
  <action>
    Extend tests/test_production.py with a public static contract for deploy/application.yaml and scripts/test-production.sh. Update tests/test_migration_job.py before implementation so the accepted public migration contract expects the parameterized hardened Job. Use pathlib and deterministic source assertions; keep kubectl, subprocess, registry calls, and recursive harness execution outside pytest. Assert the application manifest exists first with exact message `deploy/application.yaml must exist`. Run only the new application contract to retain pytest status 1 and that exact assertion after successful imports. Commit exactly the two test files as `test(23-02): specify FastAPI production workload contract`. Require the parent to be the accepted Plan 23-01 GREEN baseline source, the subject to be unique, and no manifest or script to enter RED.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; test "$(git log -1 --format=%s)" = "test(23-02): specify FastAPI production workload contract" &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d' | sort | tr '\n' ' ')" = "tests/test_migration_job.py tests/test_production.py " &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "feat(23-01): add hardened FastAPI production image"</automated>
  </verify>
  <acceptance_criteria>
    - The accepted baseline production test and inherited suite pass before the new workload RED.
    - The focused workload test exits 1 with `AssertionError: deploy/application.yaml must exist`.
    - RED changes exactly tests/test_production.py and tests/test_migration_job.py.
    - RED directly follows the Plan 23-01 GREEN source commit and contains no production artifact.
  </acceptance_criteria>
  <done>The committed workload RED describes the complete static manifest/harness boundary and fails for the absent application contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - add parameterized workloads and the complete production harness</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    .planning/phases/23-fastapi-production-stage/23-PATTERNS.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/application.yaml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh</files>
  <behavior>
    - Static workload and migration tests turn green without changing either RED test.
    - The harness can consume two image@digest references and two 40-character source releases and execute the D-05/D-06 sequence in one owned database run.
  </behavior>
  <action>
    Add deploy/application.yaml with only `__RUN_ID__`, `__APP_NAME__`, `__IMAGE_REFERENCE__`, `__SOURCE_RELEASE__`, and `__SECRET_NAME__` tokens. Put the exact run label on Deployment, Pod template, and Service. Configure two replicas, one atomic Pod-template release tuple, revision history 3, the D-02 security controls, a Memory-backed sizeLimit 64Mi `/tmp`, requests `cpu: 100m, memory: 128Mi`, limits `cpu: 500m, memory: 512Mi`, readiness `/health` on named port `http`, and a Service port/targetPort of 8000/http. Evolve deploy/migration-job.yaml to tokens `__RUN_ID__`, `__JOB_NAME__`, `__IMAGE_REFERENCE__`, and `__SECRET_NAME__`; retain backoff 1, deadline 300, workingDir /app, and the exact Alembic command; add the same identity/security/tmp/resources boundary. Both image fields accept only an image name plus sha256 digest after rendering.

    Add scripts/test-production.sh as a strict Bash harness with `--run`, `--preflight-evidence publication`, `--verify-evidence live`, `--verify-evidence publication`, and `--help`; required `--baseline-image`, `--baseline-source`, `--final-image`, `--final-source`, and `--evidence-dir` arguments; 40-hex/digest validation; bounded waits; a mode-0600 state file; and one EXIT trap installed before mutation. Reuse `scripts/test-postgres.sh --session-start --state-file` and its validated run ID/Secret ownership, then render only the allowlisted tokens into temporary manifests, reject any unresolved token, and execute strict server-side dry runs. Implement the exact runtime sequence for Plan 23-03: baseline migration Job Complete, baseline Deployment/Service two Ready, task create; final migration Job Complete, apply the final image/source tuple in one template update, two Ready and task update; `kubectl rollout undo` to baseline and persisted read; explicit final manifest apply and full recovery read. For every application state collect Deployment image, ReplicaSet revision, Pod imageID, Ready count, UID/GID, one Uvicorn application command, port 8000, rejected root write, successful bounded /tmp write/remove, startup identity log, health, docs, and public task result. Cleanup exact label/name ownership across Deployment, ReplicaSet, Pod, Service, Job, Secret, ConfigMap, state/render files, all owned port-forwards, and clone directories; call the Phase 22 session stop and assert-clean paths.

    Implement semantic verification as read-only branches before any cluster preflight. Live mode requires exactly workflow.txt, images.txt, migration.txt, runtime.txt, logs.txt, http.jsonl, rollback.txt, cleanup.txt, and checksums.txt; publication.txt must be absent and checksums.txt must have exactly eight entries covering the eight data files. Full publication verification requires those files plus publication.txt and exactly nine checksum entries. Both full modes parse required key/value and JSONL records; prove two exact workflow target SHAs/conclusions, distinct source-to-digest mappings, two `0001` migration completions, ordered runtime states baseline/final/baseline-rollback/final-recovered with two Pods and security/process checks, matching release logs, one persistent task payload across all states, rollout undo plus explicit final recovery, and zero cleanup. Publication mode additionally proves public main/Stage 1/2/3 direct and peeled identities/messages, ruleset 18970425, public package, baseline/final anonymous image replay, three public clone results, and final cleanup. Each full mode rejects missing/extra reviewed files, malformed/duplicate records, wrong order/count/identity, credential patterns, checksum path drift, and checksum mismatch. The semantic pass runs before returning success; the verifier never regenerates evidence.

    `--preflight-evidence publication` shares the complete publication file-set, credential-scan, parsing, ordering, identity, tag/ruleset/package/image/clone, and cleanup semantics, while deliberately skipping only checksum presence/content validation. It requires exactly the nine publication data files and permits checksums.txt to be absent or stale; if checksums.txt exists it is ignored solely by this preflight. Any tenth data file, missing data file, semantic failure, or credential pattern fails. No other mode allows a missing or stale checksum, and preflight never creates or updates checksums.txt.

    Implement every harness-owned anonymous registry gate through one function that creates a fresh empty `mktemp -d` directory, chmods it 0700, installs removal before crane starts, clears GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH, sets only DOCKER_CONFIG to that directory, and runs digest, config, and manifest. Remove the directory after each image gate so baseline and final never share credential/config state; any cleanup failure fails the gate. Curate the eight live data files, redact before display, write cleanup last, and generate the live eight-entry checksum file last. The script must never print or retain DATABASE_URL, Secret payloads, tokens, raw namespace logs, or registry credentials.

    Run focused static tests, bash syntax, help, lock/export, strict server dry-run using safe substituted public values, and the inherited real PostgreSQL phase gate. Commit only application.yaml, migration-job.yaml, and test-production.sh as `feat(23-02): add FastAPI production workload contracts`; require direct adjacency to RED and a clean tree.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; uv run pytest tests/test_production.py tests/test_migration_job.py -q -x &amp;&amp; bash -n scripts/test-production.sh &amp;&amp; ./scripts/test-production.sh --help &gt;/dev/null &amp;&amp; ./scripts/test-postgres.sh --phase-gate &amp;&amp; test "$(git log -1 --format=%s)" = "feat(23-02): add FastAPI production workload contracts" &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "test(23-02): specify FastAPI production workload contract"</automated>
  </verify>
  <acceptance_criteria>
    - Both manifests have the exact bounded token vocabulary, immutable-image contract, run ownership, and complete security/resources/readiness fields.
    - The production script passes bash syntax/help, validates zero unresolved tokens, uses strict server dry-run, and contains the exact baseline-final-undo-final state sequence.
    - `--verify-evidence live` enforces the eight-file semantics and exact eight-entry checksum contract.
    - `--preflight-evidence publication` enforces nine data-file semantics and redaction while allowing only checksums.txt to be stale or absent; `--verify-evidence publication` then enforces the same semantics plus the exact nine-entry checksum contract.
    - The script installs cleanup before mutation and audits ReplicaSet plus every Phase 22 object/process/state kind.
    - The focused static suite and inherited real PostgreSQL gate pass.
    - GREEN changes exactly the two manifests and production harness and directly follows RED.
  </acceptance_criteria>
  <done>The complete workload source is statically green and the real-collaborator harness is ready to execute without further source edits.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze reader source and accept the successful final image</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/.github/workflows/publish-image.yml
    .planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md, .planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md</files>
  <action>
    Update README.md with Python 3.12 locked install/export, exact full-SHA image lookup, digest-only Job and Deployment rendering, migration-before-readiness, two replicas, release-log/process/security checks, `kubectl rollout undo`, explicit digest recovery, all three source-stage meanings, production harness use, and exact cleanup. Dynamic digest values remain in Phase 23 evidence so the README commit can be the final image source. Run all static tests, lock/export, the inherited real PostgreSQL phase gate, shell syntax, diff check, secret scan, and a no-local clean-clone source replay. Commit only README.md as `docs(23-02): document immutable FastAPI production release`; record this as FINAL_SHA and prohibit every later tracked Reference Application write in Phase 23.

    Recover public main through two accepted states: the Plan 23-01 baseline SHA before a normal fast-forward, or FINAL_SHA after a prior successful push. Halt on any third SHA and never force. Push FINAL_SHA. If the exact final workflow run is absent, execute `gh workflow run publish-image.yml --repo yangchuansheng/sealos-fastapi-tutorial --ref main -f source_sha="$FINAL_SHA"`, select the workflow_dispatch run created after the recorded timestamp, and wait boundedly. Require API event/head SHA, log/summary `target_sha=$FINAL_SHA`, successful conclusion, and digest output; for a push run require head and target both equal FINAL_SHA. Permit one exact-run infrastructure rerun and halt on source/test failure. Resolve only `sha-${FINAL_SHA}`. Accept an absent tag before publish or exact public digest/OCI identity after publish; any existing identity mismatch halts. Require package visibility public and a digest distinct from the accepted baseline digest.

    Perform final anonymous GHCR acceptance with a newly created empty mode-0700 DOCKER_CONFIG and a removal trap. Clear GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH for each `crane digest`, `crane config`, and `crane manifest`; set only the fresh DOCKER_CONFIG, require digest/manifest/OCI revision/source equality, then remove it. Repeat with another fresh directory for the baseline readback. Preserve the baseline tag/digest as durable rollback input and verify the workflow publishes no mutable tag.

    Write 23-02-SUMMARY.md with RED/GREEN/final commits and scopes, final source tree checksum, final workflow URL/ID/conclusion, baseline and final SHA/tag/digest mapping, package visibility, anonymous identity readback, clean-clone result, and cleanup. Include one machine-readable release block with exactly `baseline_source=<40 hex>`, `baseline_image=ghcr.io/yangchuansheng/sealos-fastapi-tutorial@sha256:<digest>`, `final_source=<40 hex>`, and `final_image=ghcr.io/yangchuansheng/sealos-fastapi-tutorial@sha256:<digest>` on separate lines so the frozen live harness has an unambiguous input contract. Commit only the Sealos.io summary as `docs(23-02): record FastAPI production workload execution`.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; FINAL_SHA=$(git rev-parse HEAD) &amp;&amp; test "$(git log -1 --format=%s)" = "docs(23-02): document immutable FastAPI production release" &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-fastapi-tutorial/commits/main --jq .sha)" = "$FINAL_SHA" &amp;&amp; ANON_DOCKER_CONFIG=$(mktemp -d) &amp;&amp; chmod 700 "$ANON_DOCKER_CONFIG" &amp;&amp; test -z "$(find "$ANON_DOCKER_CONFIG" -mindepth 1 -print -quit)" &amp;&amp; trap 'rm -rf "$ANON_DOCKER_CONFIG"' EXIT &amp;&amp; FINAL_DIGEST=$(env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane digest "ghcr.io/yangchuansheng/sealos-fastapi-tutorial:sha-${FINAL_SHA}") &amp;&amp; test -n "$FINAL_DIGEST" &amp;&amp; env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane config "ghcr.io/yangchuansheng/sealos-fastapi-tutorial@${FINAL_DIGEST}" | jq -e --arg sha "$FINAL_SHA" '.config.Labels["org.opencontainers.image.revision"] == $sha and .config.Labels["org.opencontainers.image.source"] == "https://github.com/yangchuansheng/sealos-fastapi-tutorial"' &gt;/dev/null &amp;&amp; env -u GH_TOKEN -u GITHUB_TOKEN -u GHCR_TOKEN -u REGISTRY_TOKEN -u DOCKER_AUTH_CONFIG -u REGISTRY_AUTH_FILE -u CRANE_AUTH DOCKER_CONFIG="$ANON_DOCKER_CONFIG" crane manifest "ghcr.io/yangchuansheng/sealos-fastapi-tutorial@${FINAL_DIGEST}" | jq -e . &gt;/dev/null &amp;&amp; rm -rf "$ANON_DOCKER_CONFIG" &amp;&amp; trap - EXIT &amp;&amp; test -z "$(git status --short)"</automated>
  </verify>
  <acceptance_criteria>
    - The final README documents immutable lookup, digest deployment, migration order, hardening, rollback, recovery, and cleanup without embedding a post-build digest.
    - FINAL_SHA is a clean, public, normal fast-forward descendant of the baseline SHA.
    - The exact final workflow records API head SHA and normalized target FINAL_SHA, succeeds, and its public full-SHA tag resolves anonymously to a distinct immutable digest with matching manifest and OCI revision/source.
    - Baseline and final image identities are both retained, and no mutable image tag is published.
    - Stage 1, Stage 2, and ruleset identities remain unchanged.
  </acceptance_criteria>
  <done>The complete production source is frozen on public main and maps coherently to a successful public final image digest, ready for the live gate.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_production.py::test_production_workload_contract`
- Updated `tests/test_migration_job.py::test_production_migration_job_contract`
- `deploy/application.yaml`
- Hardened parameterized `deploy/migration-job.yaml`
- `scripts/test-production.sh` commands `--run`, `--preflight-evidence publication`, `--verify-evidence live`, `--verify-evidence publication`, and `--help`
- Render tokens `__RUN_ID__`, `__APP_NAME__`, `__IMAGE_REFERENCE__`, `__SOURCE_RELEASE__`, `__SECRET_NAME__`, and `__JOB_NAME__`
- Final source commit and public `sha-<40-character-commit>` GHCR tag/digest
- `.planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template input to Kubernetes API | Run IDs, names, image refs, and Secret names become cluster objects. |
| GHCR to migration/runtime Pods | Public image bytes execute with database credentials. |
| Kubernetes workload to PostgreSQL | Application and migration Job cross the Secret-authenticated database boundary. |
| Live command output to evidence | Logs and HTTP output become durable planning artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-23-02-T | Tampering | Manifest rendering | high | mitigate | Validate an allowlisted token set, reject unresolved tokens, enforce digest refs, and use strict server validation. |
| T-23-02-I | Information Disclosure | Secret/log/evidence path | high | mitigate | Use Secret references only, curate allowlisted output, scan credentials before display and checksum, and remove raw temp logs. |
| T-23-02-E | Elevation of Privilege | Pod and migration Job | high | mitigate | UID/GID 10001, no service token, no privilege escalation, ALL capabilities dropped, RuntimeDefault seccomp, and read-only root. |
| T-23-02-D | Denial of Service | Rollout and resources | medium | mitigate | Bounded CPU/memory/tmp, Job deadline, rollout progress deadline, readiness, and bounded waits. |
| T-23-02-R | Repudiation | Final source/image publication | high | mitigate | Record exact commits, workflow runs, digest outputs, anonymous OCI readback, and no-later-source-write freeze. |
</threat_model>

<verification>
1. Prove workload RED failure, exact test scope, direct baseline parent, and unique subject.
2. Prove GREEN direct adjacency, exact implementation scope, focused static suite, shell contract, strict render validation, and inherited PostgreSQL gate.
3. Prove README-only final freeze and no-local clone reproduction before publication.
4. Prove final main/workflow/package/tag/digest/OCI identity and baseline preservation without mutable image tags.
</verification>

<success_criteria>
- The final frozen source tree contains executable hardened Job, Deployment, Service, production harness, and reader instructions.
- Two distinct public immutable digests map to the baseline and final full source SHAs.
- Plan 23-03 can execute the entire live sequence using only frozen public inputs and an empty evidence directory.
</success_criteria>

<output>
Create `.planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md` with TDD proof, manifest/harness contracts, final freeze identity, workflow/package/image readback, baseline preservation, and source-clean state.
</output>
