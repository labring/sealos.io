---
phase: 24-django-deploy-stage
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/.gitignore
  - /Users/longnv/bin/repo/sealos-django-tutorial/.python-version
  - /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
  - /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt
  - /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock
autonomous: false
requirements:
  - DJAN-01
must_haves:
  truths:
    - "D-04 package installation starts after a human confirms the exact Django 5.2.16, pytest 9.1.1, and pytest-django 4.12.0 identities from official sources."
    - "The Python 3.12 dependency baseline reproduces from uv.lock and an exact runtime-only requirements.txt export."
    - "The first commit tracks exactly five dependency/configuration files and has subject chore(24-01): lock approved Django dependencies."
    - "The uv application samples are absent and the committed dependency tree has a clean Git status before D-03 Django generation begins."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml"
      provides: "D-04 exact Python, runtime, development, and pytest configuration"
      contains: "DJANGO_SETTINGS_MODULE = \"taskboard.settings\""
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      provides: "Locked Python 3.12 dependency graph"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt"
      provides: "Exact runtime-only compatibility export"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/.gitignore"
      provides: "D-07 exclusion of environments, caches, coverage, and mutable SQLite state"
  key_links:
    - from: "Human package identity checkpoint"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml"
      via: "blocking approval before package operations"
      pattern: "Django==5.2.16"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      via: "uv lock and locked sync"
      pattern: "pytest-django==4.12.0"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt"
      via: "locked runtime-only uv export"
      pattern: "Django==5.2.16"
---

<objective>
Approve and lock the exact D-04 Python dependency baseline before Django source
generation, while preserving the D-07 Stage 1 source fence.

Purpose: Resolve the only supply-chain checkpoint and give every later plan one
clean, reproducible dependency commit.
Output: Human-approved package identities, five tracked baseline files, exact
lock/export, and commit `chore(24-01): lock approved Django dependencies`.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-django-deploy-stage/24-CONTEXT.md
@.planning/phases/24-django-deploy-stage/24-RESEARCH.md
@.planning/phases/24-django-deploy-stage/24-PATTERNS.md
@.planning/phases/24-django-deploy-stage/24-VALIDATION.md
@AGENTS.md

<interfaces>
- Implementation repository: `/Users/longnv/bin/repo/sealos-django-tutorial`, branch `main`.
- Runtime versions: Python 3.12, Django 5.2.16, pytest 9.1.1, pytest-django 4.12.0, and uv 0.10.9.
- Python constraint: `requires-python = ">=3.12,<3.13"`.
- pytest configuration: `DJANGO_SETTINGS_MODULE = "taskboard.settings"`, `pythonpath = ["."]`, and `testpaths = ["tests"]`.
- Exact Plan 24-01 inventory: `.gitignore`, `.python-version`, `pyproject.toml`, `requirements.txt`, and `uv.lock`.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Confirm exact package identities before installation</name>
  <files>.planning/phases/24-django-deploy-stage/24-01-SUMMARY.md</files>
  <read_first>
    - `.planning/phases/24-django-deploy-stage/24-RESEARCH.md` Package Legitimacy Audit and Open Questions (RESOLVED)
    - `.planning/phases/24-django-deploy-stage/24-CONTEXT.md` D-04
    - `https://pypi.org/project/Django/5.2.16/`
    - `https://pypi.org/project/pytest/9.1.1/`
    - `https://pypi.org/project/pytest-django/4.12.0/`
    - `https://www.djangoproject.com/download/`
    - `https://github.com/django/django/tree/5.2.16`
    - `https://github.com/pytest-dev/pytest/tree/9.1.1`
    - `https://github.com/pytest-dev/pytest-django/tree/v4.12.0`
  </read_first>
  <action>
Retrieve the official PyPI JSON records and official repository release refs
for the D-04 versions. Present package name, exact release, Python requirement,
official repository URL, official tag, and release date. Record the GSD SUS
classification reasons and the resolved pytest-django PyPI/tag versus changelog
discrepancy. The locked patch selections remain Django 5.2.16, pytest 9.1.1,
and pytest-django 4.12.0. Begin all package operations after the approval
signal.
  </action>
  <acceptance_criteria>
    - Official PyPI and source refs resolve Django 5.2.16 from `github.com/django/django` at tag `5.2.16`.
    - Official PyPI and source refs resolve pytest 9.1.1 from `github.com/pytest-dev/pytest` at tag `9.1.1`.
    - Official PyPI and source refs resolve pytest-django 4.12.0 from `github.com/pytest-dev/pytest-django` at tag `v4.12.0`.
    - The checkpoint records the resolved changelog discrepancy and applicable SUS reasons.
    - Human approval confirms the three exact package identities.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; d=$(mktemp); p=$(mktemp); pd=$(mktemp); trap '\''rm -f "$d" "$p" "$pd"'\'' EXIT; curl -fsSL https://pypi.org/pypi/Django/5.2.16/json &gt;"$d"; curl -fsSL https://pypi.org/pypi/pytest/9.1.1/json &gt;"$p"; curl -fsSL https://pypi.org/pypi/pytest-django/4.12.0/json &gt;"$pd"; jq -e '\''.info.version == "5.2.16" and (.info.requires_python | contains("&gt;=3.10")) and ([.info.project_urls[]] | any(test("github.com/django/django/?$")))'\'' "$d" &gt;/dev/null; jq -e '\''.info.version == "9.1.1" and (.info.requires_python | contains("&gt;=3.10")) and ([.info.project_urls[]] | any(test("github.com/pytest-dev/pytest/?$")))'\'' "$p" &gt;/dev/null; jq -e '\''.info.version == "4.12.0" and (.info.requires_python | contains("&gt;=3.10")) and ([.info.project_urls[]] | any(test("github.com/pytest-dev/pytest-django/?$")))'\'' "$pd" &gt;/dev/null; git ls-remote --exit-code --tags https://github.com/django/django.git refs/tags/5.2.16 &gt;/dev/null; git ls-remote --exit-code --tags https://github.com/pytest-dev/pytest.git refs/tags/9.1.1 &gt;/dev/null; git ls-remote --exit-code --tags https://github.com/pytest-dev/pytest-django.git refs/tags/v4.12.0 &gt;/dev/null'</automated>
  </verify>
  <done>The human approves all three official package identities and the exact D-04 versions are ready for locking.</done>
  <what-built>An official-source identity record for the three SUS packages and the resolved pytest-django metadata discrepancy.</what-built>
  <how-to-verify>
    1. Open each PyPI page and confirm exact name, version, Python requirement, and repository URL.
    2. Open each official source tag and confirm repository ownership.
    3. Confirm the checkpoint records the pytest-django changelog discrepancy.
  </how-to-verify>
  <resume-signal>Type "approved" after confirming the identities, or report the exact mismatched field.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Lock the exact dependency baseline</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/.gitignore, /Users/longnv/bin/repo/sealos-django-tutorial/.python-version, /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml, /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt, /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock</files>
  <read_first>
    - `.planning/phases/24-django-deploy-stage/24-CONTEXT.md` D-04 and D-07
    - `.planning/phases/24-django-deploy-stage/24-RESEARCH.md` Standard Stack, Package Legitimacy Audit, and TDD Commit Sequence
    - `.planning/phases/24-django-deploy-stage/24-PATTERNS.md` Python 3.12 and uv Foundation
    - `.planning/phases/24-django-deploy-stage/24-VALIDATION.md` 24-01-DEPS
  </read_first>
  <action>
After Task 1 approval, require the target path to be absent, create it,
initialize Git branch `main`, and run `uv init --app --python 3.12`. Delete the
generated `README.md` and `main.py` sample files immediately. Pin
`Django==5.2.16` in runtime dependencies and `pytest==9.1.1` plus
`pytest-django==4.12.0` in the development group. Set the exact Python range
and pytest settings from the interface contract. Populate `.gitignore` with
`.venv/`, Python/pytest caches, coverage outputs, `.DS_Store`, and
`db.sqlite3`. Generate `uv.lock`, perform a locked sync, and export the
runtime-only `requirements.txt` with `uv export --locked --no-dev
--no-emit-project --no-hashes --format requirements.txt --output-file
requirements.txt`.

Before committing, require both sample paths absent and compare `git ls-files
--cached --others --exclude-standard` after staging against the exact five-file
Plan 24-01 inventory. Commit exactly `chore(24-01): lock approved Django
dependencies`. Re-run lock, sync, version, export, exact inventory, absent-path,
commit-subject, and clean-status assertions after the commit. Django project
and app generation belongs to Plan 24-02.
  </action>
  <acceptance_criteria>
    - The repository is on `main` with exactly one commit carrying the required subject.
    - `.python-version` selects 3.12 and `pyproject.toml` pins all three approved versions in their required groups.
    - `uv lock --check`, `uv sync --locked`, and Django version 5.2.16 pass.
    - `requirements.txt` regenerates from the lock and excludes development packages.
    - `README.md` and `main.py` are absent before and after the commit.
    - Git tracks exactly the five Plan 24-01 files and `git status --porcelain` is empty.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv lock --check &amp;&amp; uv sync --locked &amp;&amp; uv run python -m django --version | grep -E '^5\.2\.16$' &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- uv.lock requirements.txt &amp;&amp; test ! -e main.py &amp;&amp; test ! -e README.md &amp;&amp; test "$(git ls-files | sort)" = "$(printf '%s\n' .gitignore .python-version pyproject.toml requirements.txt uv.lock)" &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'chore(24-01): lock approved Django dependencies' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The approved D-04 dependencies reproduce from a clean exact five-file commit and preserve the D-07 Stage 1 fence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Official package metadata to local dependency graph | External names, distributions, repositories, and release refs become executable dependencies. |
| uv-generated application baseline to accepted Git source | Generated samples and dependency metadata cross into reader-tracked source. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | Django, pytest, and pytest-django installation | high | mitigate | Block package operations on exact PyPI/repository/tag verification, then lock only the approved versions. |
| T-24-01 | Information Disclosure | Generated local files | medium | mitigate | Track the exact five-file allowlist, exclude environment/cache/database state, remove both uv samples, and require clean status. |
</threat_model>

<verification>
Approve official package identities, then verify exact Python constraints,
dependency groups, lock/export stability, absent sample files, exact five-file
inventory, required commit subject, and clean Git status.
</verification>

<success_criteria>
- Human approval records official identity for every SUS package before installation.
- The local repository contains one clean, reproducible D-04 dependency commit.
- The runtime export derives from the exact lock and excludes development packages.
- Django source generation has a clean five-file parent commit.
- D-07 generated and mutable state remains outside tracked source.
</success_criteria>

## Artifacts this phase produces

- Exact dependency/configuration files `.gitignore`, `.python-version`, `pyproject.toml`, `requirements.txt`, and `uv.lock`.
- Commit `chore(24-01): lock approved Django dependencies`.
- `.planning/phases/24-django-deploy-stage/24-01-SUMMARY.md` with package approval, inventory, lock/export, and cleanliness evidence.

## Source Audit Coverage

| Source | ID | Feature or constraint | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Cloneable locked Python foundation for the public Task Board | 24-01, 24-02, 24-05 | COVERED |
| REQ | DJAN-01 | Locked reader-installable source stage | 24-01, 24-05 | COVERED |
| RESEARCH | - | SUS package identity checkpoint before installation | 24-01 | COVERED |
| RESEARCH | - | Exact uv lock and runtime-only export | 24-01, 24-05 | COVERED |
| CONTEXT | D-04 | Exact Python and package versions | 24-01 | COVERED |
| CONTEXT | D-07 | Stage 1 dependency and generated-state fence | 24-01, 24-05 | COVERED |

<output>
Create `.planning/phases/24-django-deploy-stage/24-01-SUMMARY.md` with the
package checkpoint, exact source identities, five-file inventory, lock/export
results, commit identity, absent samples, and clean-status evidence.
</output>
