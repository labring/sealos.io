---
phase: 24-django-deploy-stage
plan: "03"
type: tdd
wave: 3
depends_on: ["24-02"]
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/static/tasks/styles.css
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
autonomous: true
requirements: [DJAN-01]
must_haves:
  truths:
    - "D-01 GET / renders one accessible empty Task Board with inline title form, count, empty state, CSRF field, local CSS, and zero scripts."
    - "D-03 Task has an integer primary key, required 200-character title, completed false default, stable ID ordering, and committed initial migration."
    - "The empty-board RED commit is the direct parent of feat(24-03): render empty task board."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py"
      provides: "D-03 ordered Task model"
      contains: "ordering = ['id']"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py"
      provides: "Committed Stage 1 schema owner"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py"
      provides: "D-01 title-only ModelForm"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html"
      provides: "Server-rendered board, form, count, and empty state"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/static/tasks/styles.css"
      provides: "Local responsive work-focused presentation"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py"
      via: "Django migration state"
      pattern: "max_length=200"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html"
      via: "render with TaskForm and ordered tasks"
      pattern: "tasks/board.html"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html"
      to: "/static/tasks/styles.css"
      via: "Django static tag"
      pattern: "tasks/styles.css"
---

<objective>
Deliver the D-01 rendered empty board and D-03 schema owner as one focused
D-05 RED/GREEN vertical slice.

Purpose: Establish the complete server-rendered read surface before task
mutation and administration behavior.
Output: Task model/migration, title form, board route/view/template/CSS, second
public behavior, and exact Plan 24-03 RED/GREEN commits.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-django-deploy-stage/24-CONTEXT.md
@.planning/phases/24-django-deploy-stage/24-RESEARCH.md
@.planning/phases/24-django-deploy-stage/24-PATTERNS.md
@.planning/phases/24-django-deploy-stage/24-VALIDATION.md
@.planning/phases/24-django-deploy-stage/24-02-SUMMARY.md
@AGENTS.md

<interfaces>
- Model: auto integer primary key, `title = CharField(max_length=200)`, `completed = BooleanField(default=False)`, `Meta.ordering = ['id']`.
- Form: `TaskForm` exposes only `title`.
- Route: `GET /` named `tasks:board`; existing `GET /health` remains green.
- Rendered copy: `Task Board`, `Task title`, `Add task`, `0 tasks`, and `No tasks yet.`.
- Asset set: one `/static/tasks/styles.css` link and zero script elements.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the empty Task Board</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py</files>
  <behavior>
    - `GET /` returns 200 HTML containing the fixed board heading, visible title label, `/tasks/` form action, CSRF hidden field, `0 tasks`, and `No tasks yet.`.
    - The response links only `/static/tasks/styles.css` as its frontend asset and contains zero script elements.
    - The board reads tasks through the committed migration and stable ID ordering.
  </behavior>
  <action>
Add only `test_empty_task_board_is_rendered` with `@pytest.mark.django_db`, run
its node, retain the missing-board failure, and commit exactly
`test(24-03): specify empty task board`. Confirm health remains green and the
new node fails after successful Django loading for the named absent behavior.
  </action>
  <acceptance_criteria>
    - RED reaches Django and fails on the absent board behavior.
    - The test specifies every fixed board observation through public HTTP.
    - The named node fails for the absent board after successful Django loading.
    - Health remains green and the RED commit has the exact subject.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py::test_health_is_public -x &amp;&amp; ! uv run pytest -q tests/test_public_http.py::test_empty_task_board_is_rendered -x &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'test(24-03): specify empty task board'</automated>
  </verify>
  <done>The exact D-01 board behavior is retained as the Plan 24-03 RED commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the empty Task Board</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/0001_initial.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/static/tasks/styles.css</files>
  <behavior>
    - The retained RED node becomes green through the exact model, migration, form, route, template, and CSS contract.
    - Existing health behavior remains green.
  </behavior>
  <action>
Implement the Task model and generate `tasks/migrations/0001_initial.py`; add
title-only `TaskForm`; add the board route/view; render the fixed accessible
copy through one autoescaped template with CSRF and one local stylesheet. Keep
the form action at `/tasks/` for Plan 24-04. Run named and accumulated tests,
system check, fresh migration/readback, and drift check; commit exactly
`feat(24-03): render empty task board`. Require direct RED/GREEN ancestry,
generated-database cleanup, and clean status.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py -x &amp;&amp; uv run python manage.py check &amp;&amp; rm -f db.sqlite3 &amp;&amp; uv run python manage.py migrate --noinput &amp;&amp; uv run python manage.py showmigrations tasks | grep -F '[X] 0001_initial' &gt;/dev/null &amp;&amp; uv run python manage.py makemigrations --check --dry-run &amp;&amp; rm -f db.sqlite3 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(24-03): render empty task board' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(24-03): specify empty task board' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The D-01 board and D-03 migration owner are green through the direct Plan 24-03 pair.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQLite migration to reader database | Tracked model state creates local schema. |
| Server data to rendered HTML | Task/form state crosses into a public autoescaped template. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | Locked Django runtime | high | mitigate | Use the approved lock and run lock/system/migration checks around the slice. |
| T-24-03 | Tampering | Task schema and form | medium | mitigate | Commit the generated migration, expose only title in ModelForm, and require zero migration drift. |
| T-24-03A | Information Disclosure | Rendered task content | medium | mitigate | Preserve Django autoescaping, local static assets, and the absence of script execution. |
</threat_model>

<verification>
Run the named and accumulated public tests, fresh migration, migration readback,
drift check, system check, direct ancestry, generated-database cleanup, and
clean-status gate.
</verification>

<success_criteria>
- D-01 empty board is accessible, responsive, server-rendered, CSRF-bearing, and script-free.
- D-03 schema reproduces from the committed migration on fresh SQLite.
- D-05 retains exact direct RED/GREEN ancestry.
- DJAN-01 has a stable rendered read surface while health remains green.
</success_criteria>

## Artifacts this phase produces

- `Task`, `TaskForm`, and `tasks.0001_initial`.
- Board route/view/template and local CSS.
- Empty-board public test and exact Plan 24-03 RED/GREEN pair.
- `.planning/phases/24-django-deploy-stage/24-03-SUMMARY.md` with behavior, migration, and history evidence.

## Source Audit Coverage

| Source | ID | Feature or constraint | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Rendered Task Board read surface | 24-03, 24-04, 24-05 | COVERED |
| REQ | DJAN-01 | Rendered task page | 24-03, 24-04 | COVERED |
| RESEARCH | - | Model, migration, ModelForm, CSRF, autoescape, template, and CSS patterns | 24-03 | COVERED |
| CONTEXT | D-01 | Single rendered board and empty state | 24-03 | COVERED |
| CONTEXT | D-03 | SQLite ORM and committed migration | 24-03 | COVERED |
| CONTEXT | D-05 | Public HTTP RED/GREEN slice | 24-03 | COVERED |
| CONTEXT | D-07 | Local CSS and Stage 1 source fence | 24-03 | COVERED |

<output>
Create `.planning/phases/24-django-deploy-stage/24-03-SUMMARY.md` with the
empty-board RED/GREEN pair, rendered observations, migration identity/replay,
security review, cleanup, and clean-tree evidence.
</output>
