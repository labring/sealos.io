---
phase: 24-django-deploy-stage
plan: "04"
type: tdd
wave: 4
depends_on: ["24-03"]
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/admin.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
autonomous: true
requirements: [DJAN-01]
must_haves:
  truths:
    - "D-01 valid POST /tasks/ creates an incomplete task, redirects to /, and a separate GET renders the title, count, and Open state."
    - "D-05 whitespace-only titles render Enter a task title. and a separate GET proves the board remains empty."
    - "D-02 GET /admin/login/ renders Django's native login while Task is registered behind Django authentication."
    - "Creation, invalid-title, and admin behaviors each retain an exact direct RED/GREEN pair under 24-04."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      provides: "ModelForm POST/redirect/get workflow"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py"
      provides: "Stable required-title feedback"
      contains: "Enter a task title."
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/admin.py"
      provides: "Task registration with Django admin"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py"
      provides: "Exactly five D-05 public behaviors"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html"
      to: "POST /tasks/"
      via: "CSRF form action"
      pattern: "tasks:create-task"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py"
      via: "bound TaskForm validation and save"
      pattern: "TaskForm(request.POST)"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py"
      to: "/admin/login/"
      via: "admin.site.urls"
      pattern: "path('admin/'"
---

<objective>
Complete the rendered creation, invalid-title, and framework-native admin
workflow through three sequential D-05 RED/GREEN slices.

Purpose: Finish every reader-visible D-01/D-02 behavior while preserving real
Django routing, ORM, ModelForm, templates, middleware, admin, and test database.
Output: Three public behaviors, six exact commits, five passing tests, and a
clean migration-backed source tree.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-django-deploy-stage/24-CONTEXT.md
@.planning/phases/24-django-deploy-stage/24-RESEARCH.md
@.planning/phases/24-django-deploy-stage/24-PATTERNS.md
@.planning/phases/24-django-deploy-stage/24-VALIDATION.md
@.planning/phases/24-django-deploy-stage/24-03-SUMMARY.md
@AGENTS.md

<interfaces>
- Routes: `GET /`, `POST /tasks/`, `GET /health`, and Django admin at `/admin/`.
- Form: title-only `TaskForm`; required copy `Enter a task title.`.
- PRG: valid POST returns 302 with `Location: /`; persistence proof uses a later GET.
- Admin: public `/admin/login/` controls with authenticated administration content.
- Exact subjects and ordering come from 24-RESEARCH.md TDD Commit Sequence.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create and list tasks through rendered pages</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html</files>
  <behavior>
    - Valid POST with `Write the tutorial` returns 302 and `Location: /`.
    - A separate GET renders that title, `1 task`, and `Open`.
    - Persistence assertions use only public responses.
  </behavior>
  <action>
RED: add only `test_task_creation_redirects_then_appears_on_board`, retain the
missing POST-route failure, and commit exactly `test(24-04): specify task
creation and listing`. GREEN: add named `/tasks/`, bind `TaskForm(request.POST)`,
save valid input, redirect to `tasks:board`, and render incomplete tasks as
`Open`; commit exactly `feat(24-04): create tasks through rendered workflow`
after the named, accumulated, system, and migration-drift checks. Require
direct ancestry and clean status.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py::test_task_creation_redirects_then_appears_on_board -x &amp;&amp; uv run pytest -q tests/test_public_http.py -x &amp;&amp; uv run python manage.py check &amp;&amp; uv run python manage.py makemigrations --check --dry-run &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(24-04): create tasks through rendered workflow' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(24-04): specify task creation and listing'</automated>
  </verify>
  <done>Valid D-01 creation and later rendered readback pass through one exact adjacent pair.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render bounded invalid-title feedback</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/forms.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/templates/tasks/board.html</files>
  <behavior>
    - Whitespace-only POST returns 200 with `Enter a task title.`.
    - A separate GET renders `0 tasks` and `No tasks yet.`.
    - The form exposes title as its sole writable model field.
  </behavior>
  <action>
RED: add only `test_invalid_task_title_shows_feedback_without_creating_task`,
retain the error-copy failure, and commit exactly `test(24-04): specify invalid
task feedback`. GREEN: set the ModelForm required copy, render the bound form
and ordered tasks with HTTP 200, then commit exactly `feat(24-04): render task
form errors` after named, accumulated, system, and drift checks. Require direct
ancestry and public later-GET proof.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py::test_invalid_task_title_shows_feedback_without_creating_task -x &amp;&amp; uv run pytest -q tests/test_public_http.py -x &amp;&amp; uv run python manage.py check &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(24-04): render task form errors' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(24-04): specify invalid task feedback'</automated>
  </verify>
  <done>D-05 invalid input renders stable feedback and leaves the public board empty.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose the native administration login</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/admin.py</files>
  <behavior>
    - `GET /admin/login/` returns 200 with Django administration, username, password, CSRF, and login controls.
    - Task is registered behind Django's authentication/session stack.
  </behavior>
  <action>
RED: add only `test_admin_login_is_public`, retain the missing-login failure,
and commit exactly `test(24-04): specify administration login`. GREEN: mount
`admin.site.urls` and register `Task`; prove the registry wiring with
`uv run python manage.py shell -c "from django.contrib import admin; from
tasks.models import Task; assert admin.site.is_registered(Task)"`; commit
exactly `feat(24-04): expose Django administration` after the named and
complete five-test suite, registry assertion, system, fresh-migration, drift,
lock/export, six-subject Plan 24-04 order, generated database cleanup, and
clean-status gates. D-02 Stage 1 acceptance ends at the login surface;
administrator data work remains Phase 25.
  </action>
  <acceptance_criteria>
    - The admin RED node fails on the absent framework-native login surface.
    - `GET /admin/login/` renders the expected Django login controls through public HTTP.
    - `admin.site.is_registered(Task)` evaluates true in an executable Django shell assertion.
    - The test file contains exactly five behavior functions and the full suite passes.
    - The admin RED commit is the direct parent of the exact admin GREEN commit.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py::test_admin_login_is_public -x &amp;&amp; test "$(rg -c '^def test_[A-Za-z0-9_]+\(' tests/test_public_http.py)" -eq 5 &amp;&amp; uv run pytest -q &amp;&amp; uv run python manage.py shell -c "from django.contrib import admin; from tasks.models import Task; assert admin.site.is_registered(Task)" &amp;&amp; uv run python manage.py check &amp;&amp; rm -f db.sqlite3 &amp;&amp; uv run python manage.py migrate --noinput &amp;&amp; uv run python manage.py makemigrations --check --dry-run &amp;&amp; uv lock --check &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code &amp;&amp; rm -f db.sqlite3 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(24-04): expose Django administration' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(24-04): specify administration login' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>All five DJAN-01 public behaviors pass with exact sequential D-05 history and fresh D-03 migration state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser form to ModelForm and ORM | Untrusted titles cross CSRF, validation, persistence, and rendering. |
| Public admin login to Django auth/session | Anonymous users reach login while administration remains protected. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | Approved package graph | high | mitigate | Require locked sync/export and full accumulated suite. |
| T-24-04 | Tampering | `POST /tasks/` | medium | mitigate | Retain CSRF, title-only ModelForm, required/max-length validation, and public PRG proof. |
| T-24-05 | Elevation of Privilege | `/admin/` | medium | mitigate | Use Django admin/auth/session and verify only public login access. |
</threat_model>

<verification>
Run every named node and accumulated suite, then fresh migration, zero drift,
lock/export stability, exact adjacency, five-test count, generated database
cleanup, and clean status.
</verification>

<success_criteria>
- D-01 creation/listing and D-05 invalid-title behavior pass through public pages.
- D-02 native admin login renders with protected administration ownership.
- Exactly five named tests pass through real Django collaborators.
- Three exact 24-04 RED/GREEN pairs are sequential and direct.
- D-03 migration and D-07 source fence remain clean.
</success_criteria>

## Artifacts this phase produces

- Task creation/listing, stable validation feedback, and Django admin wiring.
- Three public tests and six exact 24-04 commits.
- `.planning/phases/24-django-deploy-stage/24-04-SUMMARY.md` with history, behavior, migration, security, and cleanliness evidence.

## Source Audit Coverage

| Source | ID | Feature or constraint | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Usable rendered workflow and admin entry | 24-04, 24-05 | COVERED |
| REQ | DJAN-01 | Rendered creation/listing and Django administration | 24-04 | COVERED |
| RESEARCH | - | ModelForm PRG, invalid feedback, native admin, sequential TDD | 24-04 | COVERED |
| CONTEXT | D-01 | Create/list rendered tasks | 24-04 | COVERED |
| CONTEXT | D-02 | Native administration login | 24-04 | COVERED |
| CONTEXT | D-03 | ORM-backed SQLite writes | 24-03, 24-04 | COVERED |
| CONTEXT | D-05 | Public behavior-only RED/GREEN slices | 24-04 | COVERED |
| CONTEXT | D-07 | Later-stage fence | 24-04, 24-05 | COVERED |

<output>
Create `.planning/phases/24-django-deploy-stage/24-04-SUMMARY.md` with all
three RED/GREEN pairs, named/accumulated results, rendered observations,
migration/export checks, security review, and clean-tree evidence.
</output>
