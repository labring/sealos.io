---
phase: 24-django-deploy-stage
plan: "05"
type: execute
wave: 5
depends_on: ["24-04"]
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/README.md
  - refs/tags/stage-1-deploy
  - https://github.com/yangchuansheng/sealos-django-tutorial
autonomous: true
requirements: [DJAN-01]
must_haves:
  truths:
    - "D-04 README commands reproduce locked sync, migration, tests, runserver on 0.0.0.0:8000, health, board creation/listing, and admin login."
    - "Local and fresh-clone acceptance each assert the exact 13-subject history and exact 24-file PATTERNS inventory."
    - "D-06 public owner, visibility, default branch, main, active ruleset, annotated type/message, direct tag, and peeled commit all read back exactly."
    - "An existing empty GitHub repository is accepted only with zero refs, canonical origin, a push of accepted main, and complete post-push readback; every non-empty mismatch aborts."
    - "D-07 explicit cleanup followed by trap removal proves stopped PIDs, free port, absent browser sessions/paths/logs/responses, and absent source/clone db.sqlite3."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      provides: "D-04 Stage 1 reader workflow"
    - path: "refs/tags/stage-1-deploy"
      provides: "D-06 annotated accepted source identity"
    - path: "https://github.com/yangchuansheng/sealos-django-tutorial"
      provides: "Public main plus protected Stage 1 source"
    - path: ".planning/phases/24-django-deploy-stage/24-05-SUMMARY.md"
      provides: "Redacted local, publication, replay, cleanup, and final-readback evidence"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      to: "manage.py"
      via: "sync, migrate, test, and runserver commands"
      pattern: "runserver 0.0.0.0:8000 --noreload"
    - from: "public main"
      to: "refs/tags/stage-1-deploy^{}"
      via: "ACCEPTED_HEAD equality"
      pattern: "stage-1-deploy^{}"
    - from: "refs/tags/stage-* ruleset"
      to: "refs/tags/stage-1-deploy"
      via: "active update/deletion rules with empty bypass"
      pattern: "refs/tags/stage-*"
    - from: "fresh public tag clone"
      to: "uv.lock, tasks.0001_initial, tests/test_public_http.py"
      via: "locked sync, fresh migration, five tests, exact history/inventory, and browser runtime"
      pattern: "uv sync --locked"
---

<objective>
Document, accept, publish, protect, replay, and explicitly clean the complete
Django deploy stage per D-01 through D-07 and DJAN-01.

Purpose: Freeze one reader-reproducible local source identity and prove the same
identity from public GitHub after every temporary runtime artifact is removed.
Output: README, exact local acceptance, public main, protected annotated tag,
fresh public replay, final platform readback, and zero-residue evidence.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-django-deploy-stage/24-CONTEXT.md
@.planning/phases/24-django-deploy-stage/24-RESEARCH.md
@.planning/phases/24-django-deploy-stage/24-PATTERNS.md
@.planning/phases/24-django-deploy-stage/24-VALIDATION.md
@.planning/phases/24-django-deploy-stage/24-04-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/23-VERIFICATION.md
@AGENTS.md

<interfaces>
- Source: `/Users/longnv/bin/repo/sealos-django-tutorial`; public repository: `yangchuansheng/sealos-django-tutorial`.
- Accepted branch: public `main` at `ACCEPTED_HEAD`.
- Stage ref: annotated `refs/tags/stage-1-deploy`, message `Django deploy stage`.
- Ruleset: `Protect tutorial stage tags`, active tag target, include `refs/tags/stage-*`, update/deletion, empty exclude/bypass.
- Runtime: owned `runserver 0.0.0.0:8000 --noreload` and named agent-browser sessions.
- Exact 13 subjects and 24 files are listed in 24-VALIDATION.md and repeated in executable checks below.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document and accept the local reader workflow</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/README.md</files>
  <action>
Create English README guidance for prerequisites, immutable tag clone, locked
sync, migration, drift check, tests, exact runserver, health/board/admin HTTP,
visible task creation, and SQLite lifecycle. Keep Phase 25/26 boundaries
explicit. Commit exactly `docs(24-05): document Stage 1 reader workflow`.

From deleted `db.sqlite3`, run lock/sync, migration/readback/drift, five tests,
runtime export, clean diff/status, exact 13-subject history, and exact 24-file
inventory. Require port 8000 free. Create `LOCAL_TMP`, `LOCAL_LOG`,
`LOCAL_RESPONSE_DIR`, `LOCAL_PID`, and `LOCAL_SESSION=phase24-local-acceptance`;
install `cleanup` immediately. Launch owned runserver, poll boundedly, assert
health/board/admin, then use agent-browser to submit `Verify browser workflow`,
observe `1 task`, and open admin login. Call `cleanup` explicitly while all
variables remain in the same shell scope, run `trap - EXIT INT TERM`, then
assert the local PID stopped, port 8000 is clear, `agent-browser session list`
succeeds and its captured output lacks `LOCAL_SESSION`, every temporary/log/
response path is absent, source `db.sqlite3` is absent, and
`git status --porcelain --untracked-files=all` is empty. Record every result
before publication.
  </action>
  <acceptance_criteria>
    - The exact 13-subject history and exact 24-file tracked inventory pass before local runtime acceptance.
    - The owned local runserver returns exact health JSON and supports the real agent-browser CSRF creation/readback plus admin-login workflow.
    - `cleanup; trap - EXIT INT TERM` runs in the same shell lifetime that defines the local PID, session, temporary, log, and response variables.
    - A successful `agent-browser session list` readback lacks `phase24-local-acceptance`.
    - The local PID, port listener, temporary paths, source database, and all tracked or untracked Git changes are absent after cleanup.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; SOURCE=/Users/longnv/bin/repo/sealos-django-tutorial; LOCAL_TMP=$(mktemp -d "${TMPDIR:-/tmp}/phase24-local.XXXXXX"); LOCAL_LOG="$LOCAL_TMP/local.log"; LOCAL_RESPONSE_DIR="$LOCAL_TMP/responses"; LOCAL_PID=0; LOCAL_SESSION=phase24-local-acceptance; mkdir -p "$LOCAL_RESPONSE_DIR"; cleanup(){ agent-browser --session "$LOCAL_SESSION" close &gt;/dev/null 2&gt;&amp;1 || true; if test "$LOCAL_PID" -gt 0; then kill "$LOCAL_PID" 2&gt;/dev/null || true; wait "$LOCAL_PID" 2&gt;/dev/null || true; fi; rm -f "$SOURCE/db.sqlite3"; rm -rf "$LOCAL_TMP"; }; trap cleanup EXIT INT TERM; cd "$SOURCE"; rm -f db.sqlite3; uv sync --locked; uv lock --check; uv run python manage.py migrate --noinput; uv run python manage.py showmigrations tasks | grep -F "[X] 0001_initial" &gt;/dev/null; uv run python manage.py makemigrations --check --dry-run; uv run pytest -q; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt; git diff --exit-code; actual=$(git log --reverse --format=%s); expected=$(printf "%s\n" "chore(24-01): lock approved Django dependencies" "chore(24-02): initialize Django project" "test(24-02): specify public health" "feat(24-02): add public health endpoint" "test(24-03): specify empty task board" "feat(24-03): render empty task board" "test(24-04): specify task creation and listing" "feat(24-04): create tasks through rendered workflow" "test(24-04): specify invalid task feedback" "feat(24-04): render task form errors" "test(24-04): specify administration login" "feat(24-04): expose Django administration" "docs(24-05): document Stage 1 reader workflow"); test "$actual" = "$expected"; actual=$(git ls-files | sort); expected=$(printf "%s\n" .gitignore .python-version README.md manage.py pyproject.toml requirements.txt taskboard/__init__.py taskboard/asgi.py taskboard/settings.py taskboard/urls.py taskboard/wsgi.py tasks/__init__.py tasks/admin.py tasks/apps.py tasks/forms.py tasks/migrations/0001_initial.py tasks/migrations/__init__.py tasks/models.py tasks/static/tasks/styles.css tasks/templates/tasks/board.html tasks/urls.py tasks/views.py tests/test_public_http.py uv.lock); test "$actual" = "$expected"; test -z "$(git status --porcelain --untracked-files=all)"; ! lsof -nP -iTCP:8000 -sTCP:LISTEN &gt;/dev/null 2&gt;&amp;1; uv run python manage.py runserver 0.0.0.0:8000 --noreload &gt;"$LOCAL_LOG" 2&gt;&amp;1 &amp; LOCAL_PID=$!; ready=0; for _ in {1..40}; do kill -0 "$LOCAL_PID"; if curl -fsS http://127.0.0.1:8000/health &gt;"$LOCAL_RESPONSE_DIR/health.json"; then ready=1; break; fi; sleep 0.25; done; test "$ready" -eq 1; jq -e '\''. == {"status":"ok"}'\'' "$LOCAL_RESPONSE_DIR/health.json" &gt;/dev/null; curl -fsS http://127.0.0.1:8000/ &gt;"$LOCAL_RESPONSE_DIR/board.html"; curl -fsS http://127.0.0.1:8000/admin/login/ &gt;"$LOCAL_RESPONSE_DIR/admin.html"; rg -F "Task Board" "$LOCAL_RESPONSE_DIR/board.html" &gt;/dev/null; rg -F "Django administration" "$LOCAL_RESPONSE_DIR/admin.html" &gt;/dev/null; AGENT_BROWSER_ALLOWED_DOMAINS=127.0.0.1 agent-browser --session "$LOCAL_SESSION" open http://127.0.0.1:8000/; agent-browser --session "$LOCAL_SESSION" find label "Task title" fill "Verify browser workflow"; agent-browser --session "$LOCAL_SESSION" find role button click --name "Add task"; agent-browser --session "$LOCAL_SESSION" wait --text "Verify browser workflow"; agent-browser --session "$LOCAL_SESSION" wait --text "1 task"; agent-browser --session "$LOCAL_SESSION" open http://127.0.0.1:8000/admin/login/; agent-browser --session "$LOCAL_SESSION" wait --text "Django administration"; cleanup; trap - EXIT INT TERM; ! kill -0 "$LOCAL_PID" 2&gt;/dev/null; ! lsof -nP -iTCP:8000 -sTCP:LISTEN &gt;/dev/null 2&gt;&amp;1; session_list=$(agent-browser session list); if printf "%s\n" "$session_list" | rg -F -- "$LOCAL_SESSION" &gt;/dev/null; then exit 1; fi; test ! -e "$LOCAL_TMP"; test ! -e "$LOCAL_LOG"; test ! -e "$LOCAL_RESPONSE_DIR"; test ! -e "$SOURCE/db.sqlite3"; test -z "$(git status --porcelain --untracked-files=all)"' </automated>
  </verify>
  <done>The exact local source, reader commands, browser workflow, history, inventory, and post-cleanup zero state pass before publication.</done>
</task>

<task type="auto">
  <name>Task 2: Publish and protect the accepted Stage 1 identity</name>
  <files>refs/tags/stage-1-deploy, https://github.com/yangchuansheng/sealos-django-tutorial</files>
  <action>
Freeze `ACCEPTED_HEAD` from Task 1 and verify authenticated owner
`yangchuansheng`. Query GitHub before mutation. Preserve an existing `origin`
only when its normalized URL is the canonical repository; add it when absent.

For an absent repository, create it public, attach canonical origin, push
accepted `main`, then read back every repository field. For an existing empty
repository, require owner/public metadata and exactly zero refs from
`git ls-remote`, attach or preserve canonical origin, push accepted `main`, then
require owner, public visibility, default `main`, and remote main at
`ACCEPTED_HEAD`. For an existing non-empty repository, require canonical owner,
public visibility, default `main`, remote main at `ACCEPTED_HEAD`, and only
coherent allowed main/tag refs; every mismatch aborts.

Create or replace exactly one complete ruleset payload named `Protect tutorial
stage tags`: target tag, active, empty bypass/exclude, include
`refs/tags/stage-*`, exactly update/deletion. Duplicate names abort. Resolve
local/remote direct and peeled `refs/tags/stage-1-deploy` states. Apply the four
coherent absent/remote-only/both/local-only paths; require annotated type,
message `Django deploy stage`, equal direct objects, and every peeled commit at
`ACCEPTED_HEAD`. Push only the logical tag ref when needed. Finish with exact
owner/visibility/default/main/ruleset/direct/peeled/type/message readback.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; accepted=$(git rev-parse HEAD) &amp;&amp; test "$(gh api user --jq .login)" = yangchuansheng &amp;&amp; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json nameWithOwner --jq .nameWithOwner)" = yangchuansheng/sealos-django-tutorial &amp;&amp; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json isPrivate --jq .isPrivate)" = false &amp;&amp; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json defaultBranchRef --jq .defaultBranchRef.name)" = main &amp;&amp; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$accepted" &amp;&amp; test "$(git cat-file -t refs/tags/stage-1-deploy)" = tag &amp;&amp; test "$(git for-each-ref refs/tags/stage-1-deploy --format='%(contents:subject)')" = 'Django deploy stage' &amp;&amp; test "$(git rev-parse 'refs/tags/stage-1-deploy^{}')" = "$accepted" &amp;&amp; test "$(git ls-remote --tags origin refs/tags/stage-1-deploy | cut -f1)" = "$(git rev-parse refs/tags/stage-1-deploy)" &amp;&amp; test "$(git ls-remote --tags origin 'refs/tags/stage-1-deploy^{}' | cut -f1)" = "$accepted" &amp;&amp; ids=$(gh api repos/yangchuansheng/sealos-django-tutorial/rulesets --jq '.[] | select(.name == "Protect tutorial stage tags") | .id') &amp;&amp; test "$(printf '%s\n' "$ids" | sed '/^$/d' | wc -l | tr -d ' ')" -eq 1 &amp;&amp; gh api "repos/yangchuansheng/sealos-django-tutorial/rulesets/$ids" | jq -e '.name == "Protect tutorial stage tags" and .target == "tag" and .enforcement == "active" and .conditions.ref_name.exclude == [] and .conditions.ref_name.include == ["refs/tags/stage-*"] and ([.rules[].type] | sort) == ["deletion","update"] and (.bypass_actors | length == 0)' &gt;/dev/null</automated>
  </verify>
  <done>D-06 public repository, main, annotated tag, and exact active protection form one accepted identity graph with empty-repository recovery.</done>
</task>

<task type="auto">
  <name>Task 3: Replay the public tag, clean explicitly, and read back final state</name>
  <files>.planning/phases/24-django-deploy-stage/24-05-SUMMARY.md</files>
  <action>
Create `PHASE_TMP`, `CLONE_DIR`, `CLONE_LOG`, `CLONE_RESPONSE_DIR`, `CLONE_DB`,
`CLONE_PID`, and `CLONE_SESSION=phase24-public-replay`; install `cleanup`
immediately. Clone public `stage-1-deploy` over HTTPS. Assert clone HEAD equals
`ACCEPTED_HEAD`, local clone tag type is `tag`, and message is `Django deploy
stage`. Run the exact 13-subject and 24-file assertions from Task 1 in the clone,
then locked sync, fresh migration/readback/drift, five tests, zero-diff export,
and `test -z "$(git status --porcelain --untracked-files=all)"` before the
owned clone server and browser creation/admin flow.

Return to `SOURCE`, call `cleanup` explicitly while all clone variables remain
in the same shell scope, and run `trap - EXIT INT TERM`. Assert the clone PID
stopped, port 8000 is clear, `agent-browser session list` succeeds and its
captured output lacks `CLONE_SESSION`, clone/temp/log/response paths are
absent, both source/clone `db.sqlite3` paths are absent, and
`test -z "$(git status --porcelain --untracked-files=all)"` passes again in
the source repository. After cleanup, repeat Task 2's exact owner, visibility,
default, main, ruleset, direct, peeled, type, and message readback. Record
redacted outcomes in the summary; retain public source, protected tag, and
planning evidence only.
  </action>
  <acceptance_criteria>
    - The public HTTPS tag clone matches `ACCEPTED_HEAD`, annotated type/message, exact 13-subject history, and exact 24-file inventory.
    - Locked sync, fresh migration, five tests, runtime export, and `git status --porcelain --untracked-files=all` pass before clone runtime acceptance.
    - The owned clone runserver supports the real agent-browser CSRF creation/readback plus admin-login workflow.
    - `cleanup; trap - EXIT INT TERM` runs in the same shell lifetime that defines the clone PID, session, temporary, log, response, clone, and database variables.
    - A successful `agent-browser session list` readback lacks `phase24-public-replay`.
    - Clone and source Git status, clone PID, port listener, clone/temp/log/response paths, and both databases are absent after cleanup.
    - Final GitHub owner, visibility, default branch, main, ruleset, direct tag, peeled commit, object type, and tag message read back exactly.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'set -euo pipefail; SOURCE=/Users/longnv/bin/repo/sealos-django-tutorial; ACCEPTED_HEAD=$(git -C "$SOURCE" rev-parse HEAD); PHASE_TMP=$(mktemp -d "${TMPDIR:-/tmp}/phase24.XXXXXX"); CLONE_DIR="$PHASE_TMP/repo"; CLONE_LOG="$PHASE_TMP/clone.log"; CLONE_RESPONSE_DIR="$PHASE_TMP/responses"; CLONE_DB="$CLONE_DIR/db.sqlite3"; CLONE_SESSION=phase24-public-replay; CLONE_PID=0; mkdir -p "$CLONE_RESPONSE_DIR"; cleanup(){ agent-browser --session "$CLONE_SESSION" close &gt;/dev/null 2&gt;&amp;1 || true; if test "$CLONE_PID" -gt 0; then kill "$CLONE_PID" 2&gt;/dev/null || true; wait "$CLONE_PID" 2&gt;/dev/null || true; fi; rm -f "$SOURCE/db.sqlite3" "$CLONE_DB"; rm -rf "$PHASE_TMP"; }; trap cleanup EXIT INT TERM; git clone --branch stage-1-deploy https://github.com/yangchuansheng/sealos-django-tutorial.git "$CLONE_DIR"; cd "$CLONE_DIR"; test "$(git rev-parse HEAD)" = "$ACCEPTED_HEAD"; test "$(git cat-file -t refs/tags/stage-1-deploy)" = tag; test "$(git for-each-ref refs/tags/stage-1-deploy --format="%(contents:subject)")" = "Django deploy stage"; actual=$(git log --reverse --format=%s); expected=$(printf "%s\n" "chore(24-01): lock approved Django dependencies" "chore(24-02): initialize Django project" "test(24-02): specify public health" "feat(24-02): add public health endpoint" "test(24-03): specify empty task board" "feat(24-03): render empty task board" "test(24-04): specify task creation and listing" "feat(24-04): create tasks through rendered workflow" "test(24-04): specify invalid task feedback" "feat(24-04): render task form errors" "test(24-04): specify administration login" "feat(24-04): expose Django administration" "docs(24-05): document Stage 1 reader workflow"); test "$actual" = "$expected"; actual=$(git ls-files | sort); expected=$(printf "%s\n" .gitignore .python-version README.md manage.py pyproject.toml requirements.txt taskboard/__init__.py taskboard/asgi.py taskboard/settings.py taskboard/urls.py taskboard/wsgi.py tasks/__init__.py tasks/admin.py tasks/apps.py tasks/forms.py tasks/migrations/0001_initial.py tasks/migrations/__init__.py tasks/models.py tasks/static/tasks/styles.css tasks/templates/tasks/board.html tasks/urls.py tasks/views.py tests/test_public_http.py uv.lock); test "$actual" = "$expected"; uv sync --locked; uv lock --check; rm -f db.sqlite3; uv run python manage.py migrate --noinput; uv run python manage.py showmigrations tasks | grep -F "[X] 0001_initial" &gt;/dev/null; uv run python manage.py makemigrations --check --dry-run; uv run pytest -q; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt; git diff --exit-code; test -z "$(git status --porcelain --untracked-files=all)"; ! lsof -nP -iTCP:8000 -sTCP:LISTEN &gt;/dev/null 2&gt;&amp;1; uv run python manage.py runserver 0.0.0.0:8000 --noreload &gt;"$CLONE_LOG" 2&gt;&amp;1 &amp; CLONE_PID=$!; ready=0; for _ in {1..40}; do kill -0 "$CLONE_PID"; if curl -fsS http://127.0.0.1:8000/health &gt;"$CLONE_RESPONSE_DIR/health.json"; then ready=1; break; fi; sleep 0.25; done; test "$ready" -eq 1; jq -e '\''. == {"status":"ok"}'\'' "$CLONE_RESPONSE_DIR/health.json" &gt;/dev/null; AGENT_BROWSER_ALLOWED_DOMAINS=127.0.0.1 agent-browser --session "$CLONE_SESSION" open http://127.0.0.1:8000/; agent-browser --session "$CLONE_SESSION" find label "Task title" fill "Replay public stage"; agent-browser --session "$CLONE_SESSION" find role button click --name "Add task"; agent-browser --session "$CLONE_SESSION" wait --text "Replay public stage"; agent-browser --session "$CLONE_SESSION" open http://127.0.0.1:8000/admin/login/; agent-browser --session "$CLONE_SESSION" wait --text "Django administration"; cd "$SOURCE"; cleanup; trap - EXIT INT TERM; ! kill -0 "$CLONE_PID" 2&gt;/dev/null; ! lsof -nP -iTCP:8000 -sTCP:LISTEN &gt;/dev/null 2&gt;&amp;1; session_list=$(agent-browser session list); if printf "%s\n" "$session_list" | rg -F -- "$CLONE_SESSION" &gt;/dev/null; then exit 1; fi; test ! -e "$CLONE_DIR"; test ! -e "$PHASE_TMP"; test ! -e "$CLONE_LOG"; test ! -e "$CLONE_RESPONSE_DIR"; test ! -e "$SOURCE/db.sqlite3"; test ! -e "$CLONE_DB"; test -z "$(git status --porcelain --untracked-files=all)"; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json nameWithOwner --jq .nameWithOwner)" = yangchuansheng/sealos-django-tutorial; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json isPrivate --jq .isPrivate)" = false; test "$(gh repo view yangchuansheng/sealos-django-tutorial --json defaultBranchRef --jq .defaultBranchRef.name)" = main; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$ACCEPTED_HEAD"; test "$(git cat-file -t refs/tags/stage-1-deploy)" = tag; test "$(git for-each-ref refs/tags/stage-1-deploy --format="%(contents:subject)")" = "Django deploy stage"; test "$(git ls-remote --tags origin refs/tags/stage-1-deploy | cut -f1)" = "$(git rev-parse refs/tags/stage-1-deploy)"; test "$(git ls-remote --tags origin "refs/tags/stage-1-deploy^{}" | cut -f1)" = "$ACCEPTED_HEAD"; ids=$(gh api repos/yangchuansheng/sealos-django-tutorial/rulesets --jq '\''.[] | select(.name == "Protect tutorial stage tags") | .id'\''); test "$(printf "%s\n" "$ids" | sed "/^$/d" | wc -l | tr -d " ")" -eq 1; gh api "repos/yangchuansheng/sealos-django-tutorial/rulesets/$ids" | jq -e '\''.name == "Protect tutorial stage tags" and .target == "tag" and .enforcement == "active" and .conditions.ref_name.exclude == [] and .conditions.ref_name.include == ["refs/tags/stage-*"] and ([.rules[].type] | sort) == ["deletion","update"] and (.bypass_actors | length == 0)'\'' &gt;/dev/null' </automated>
  </verify>
  <done>The public tag reproduces DJAN-01 and final explicit cleanup plus platform readback satisfy D-06/D-07.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Accepted local Git tree to public GitHub | Reader source and exact history become public. |
| Ruleset/tag to future readers | Mutable platform state controls immutable stage identity. |
| Public clone/browser to local execution | Remote source and input cross into Django and SQLite. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | Public-clone dependencies | high | mitigate | Use approved lock, exact export, full suite, history, and inventory. |
| T-24-06 | Repudiation | Local/browser acceptance | medium | mitigate | Record exact commands, subjects, inventory, public responses, and explicit cleanup readback. |
| T-24-07 | Spoofing | GitHub repository | medium | mitigate | Verify owner, visibility, canonical origin, default branch, refs, and main before/after mutation. |
| T-24-08 | Tampering | Stage tag/ruleset | high | mitigate | Require annotated type/message, direct/peeled identity, and exact active update/deletion protection. |
| T-24-09 | Information Disclosure | Published inventory/evidence | high | mitigate | Publish exactly 24 allowlisted files and retain redacted evidence outside the tag. |
| T-24-10 | Denial of Service | Owned local processes | medium | mitigate | Use bounded polling, explicit cleanup calls, trap removal, and PID/port/path zero-state assertions. |
</threat_model>

<verification>
Run exact 13-subject and 24-file assertions locally and in the fresh clone;
verify lock/export/migration/tests/browser runtime; recover only absent,
zero-ref-empty, or exact existing GitHub state; call cleanup explicitly; assert
all runtime residue absent; finish with complete public identity readback.
</verification>

<success_criteria>
- D-01 through D-05 reproduce locally and from the public tag on port 8000.
- D-06 owner, visibility, default, main, ruleset, tag type/message/direct/peeled fields are exact.
- Empty GitHub repositories recover only from zero refs; non-empty mismatches abort.
- Exact 13-subject history and exact 24-file inventory pass twice.
- D-07 cleanup assertions prove zero PID, port, browser, clone/temp/log/response, and SQLite residue.
</success_criteria>

## Artifacts this phase produces

- README with the complete Stage 1 reader workflow.
- Public `main`, annotated logical ref `refs/tags/stage-1-deploy`, and exact active tag ruleset.
- Local and public-clone history/inventory/runtime evidence.
- `.planning/phases/24-django-deploy-stage/24-05-SUMMARY.md` with redacted cleanup and final readback.

## Source Audit Coverage

| Source | ID | Feature or constraint | Plans | Status |
|--------|----|-----------------------|-------|--------|
| GOAL | - | Clone, run, and use the first public Task Board stage | 24-01..24-05 | COVERED |
| REQ | DJAN-01 | Public health, rendered tasks, Django admin, and port 8000 | 24-02..24-05 | COVERED |
| RESEARCH | - | Exact dependencies, five RED/GREEN slices, README, publication, replay, cleanup | 24-01..24-05 | COVERED |
| CONTEXT | D-01 | Rendered board, form, PRG, and list | 24-03, 24-04, 24-05 | COVERED |
| CONTEXT | D-02 | Health and native admin login | 24-02, 24-04, 24-05 | COVERED |
| CONTEXT | D-03 | SQLite ORM, migration, fresh replay, cleanup | 24-02, 24-03, 24-05 | COVERED |
| CONTEXT | D-04 | Python 3.12 lock/export and reader commands | 24-01, 24-05 | COVERED |
| CONTEXT | D-05 | Public HTTP tests and exact RED/GREEN history | 24-02, 24-03, 24-04, 24-05 | COVERED |
| CONTEXT | D-06 | Public repository, protected annotated tag, public replay | 24-05 | COVERED |
| CONTEXT | D-07 | Stage fence and complete cleanup | 24-01, 24-05 | COVERED |

<output>
Create `.planning/phases/24-django-deploy-stage/24-05-SUMMARY.md` with README
acceptance, exact history/inventory, GitHub recovery/readback, ruleset/tag
identities, public replay, explicit cleanup call, and zero-state assertions.
</output>
