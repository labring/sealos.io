---
phase: 24-django-deploy-stage
plan: "02"
type: tdd
wave: 2
depends_on: ["24-01"]
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/manage.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/__init__.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/asgi.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/__init__.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/admin.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/apps.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/__init__.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
autonomous: true
requirements: [DJAN-01]
must_haves:
  truths:
    - "D-03 Django-generated project and tasks app load under Python 3.12 with conventional SQLite settings and an ignored db.sqlite3."
    - "The scaffold commit has exact subject chore(24-02): initialize Django project and starts with custom public and admin routes absent."
    - "D-02 GET /health returns HTTP 200 and exactly {\"status\": \"ok\"} through the D-05 public HTTP seam."
    - "The health RED commit is the direct parent of its GREEN commit and the first four phase subjects are exact."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py"
      provides: "D-03 generated Django settings with SQLite and tasks app"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py"
      provides: "Application URL namespace and D-02 health route"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      provides: "Exact public health JSON response"
      exports: ["health"]
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py"
      provides: "D-05 public behavior seam"
      contains: "def test_health_is_public"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py"
      via: "Django include at the root path"
      pattern: "include('tasks.urls')"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py"
      via: "named health route"
      pattern: "path('health'"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py"
      to: "GET /health"
      via: "pytest-django client"
      pattern: "client.get('/health')"
---

<objective>
Generate the D-03 Django project/app scaffold and deliver the D-02 health
behavior through one exact D-05 RED/GREEN pair.

Purpose: Separate framework generation from dependency bootstrap and establish
the public test seam used by every later slice.
Output: Django project/app source, scaffold commit, health test/route/view, and
the first adjacent RED/GREEN pair.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-django-deploy-stage/24-CONTEXT.md
@.planning/phases/24-django-deploy-stage/24-RESEARCH.md
@.planning/phases/24-django-deploy-stage/24-PATTERNS.md
@.planning/phases/24-django-deploy-stage/24-VALIDATION.md
@.planning/phases/24-django-deploy-stage/24-01-SUMMARY.md
@AGENTS.md

<interfaces>
- Project package: `taskboard`; application package: `tasks`.
- Root URLconf includes `tasks.urls`; custom patterns and `admin.site.urls` start absent.
- SQLite path: `BASE_DIR / "db.sqlite3"`; installed app: `tasks.apps.TasksConfig`.
- Public health contract: `GET /health` -> HTTP 200 and `{"status": "ok"}`.
- Exact subjects: scaffold, health RED, health GREEN as listed in 24-RESEARCH.md.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the Django project package</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/manage.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/__init__.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/asgi.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py</files>
  <read_first>
    - `.planning/phases/24-django-deploy-stage/24-CONTEXT.md` D-02, D-03, D-05, and D-07
    - `.planning/phases/24-django-deploy-stage/24-PATTERNS.md` Generated Project Files
    - `.planning/phases/24-django-deploy-stage/24-VALIDATION.md` 24-02-SCAFFOLD
  </read_first>
  <action>
From the clean Plan 24-01 commit, run Django 5.2.16 `startproject taskboard .`
to create the six project-package files owned by this task. Add
`tasks.apps.TasksConfig` to installed apps, keep generated middleware/templates
and SQLite, and replace the generated root admin pattern with
`include('tasks.urls')`. Leave these six paths deliberately uncommitted while
Task 2 creates the app half of the same scaffold. Require the only pending
paths to be this task's six-file allowlist, keep `db.sqlite3` absent, and keep
the Plan 24-01 dependency commit at HEAD. The uncommitted interval ends inside
this plan when Task 2 creates the single scaffold commit.
  </action>
  <acceptance_criteria>
    - Exactly six project-package files are generated by this task.
    - Settings use SQLite and reference `tasks.apps.TasksConfig`; root routing references `tasks.urls`.
    - The `tasks` and `tests` directories remain absent until Task 2.
    - The six project paths are the complete untracked allowlist and remain deliberately uncommitted only until Task 2.
    - `db.sqlite3` is absent and the Plan 24-01 dependency commit remains at HEAD.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test -f manage.py &amp;&amp; test -f taskboard/__init__.py &amp;&amp; test -f taskboard/asgi.py &amp;&amp; test -f taskboard/settings.py &amp;&amp; test -f taskboard/urls.py &amp;&amp; test -f taskboard/wsgi.py &amp;&amp; test ! -e tasks &amp;&amp; test ! -e tests &amp;&amp; test ! -e db.sqlite3 &amp;&amp; test "$(git ls-files --others --exclude-standard | sort)" = "$(printf '%s\n' manage.py taskboard/__init__.py taskboard/asgi.py taskboard/settings.py taskboard/urls.py taskboard/wsgi.py)" &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'chore(24-01): lock approved Django dependencies'</automated>
  </verify>
  <done>The six-file project package is ready as the deliberately pending first half of the same-plan scaffold commit.</done>
</task>

<task type="auto">
  <name>Task 2: Generate the Django app scaffold and commit both groups</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tasks/__init__.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/admin.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/apps.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/migrations/__init__.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/models.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py</files>
  <read_first>
    - `/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py` and `/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/urls.py` from Task 1
    - `.planning/phases/24-django-deploy-stage/24-CONTEXT.md` D-02, D-03, D-05, and D-07
    - `.planning/phases/24-django-deploy-stage/24-PATTERNS.md` Generated Project Files
    - `.planning/phases/24-django-deploy-stage/24-VALIDATION.md` 24-02-SCAFFOLD
  </read_first>
  <action>
Run Django 5.2.16 `startapp tasks` to create the app-package group. Remove
generated `tasks/tests.py`; retain behavior-free `admin.py`, `models.py`, and
`views.py`; create `tasks/urls.py` with empty `urlpatterns`; and create
`tests/test_public_http.py` with imports only. Run `manage.py check` and pytest
collection, compare the complete staged tree with the exact 19-file scaffold
inventory, then stage both Task 1 and Task 2 groups and create the single
commit `chore(24-02): initialize Django project`. Require clean status and
absent `db.sqlite3` after the commit. No intermediate scaffold commit is
created.
  </action>
  <acceptance_criteria>
    - Task 2 owns exactly eight app/test scaffold files and removes generated `tasks/tests.py`.
    - Generated project and app entry points import successfully under the approved lock.
    - Root routing includes an empty `tasks.urls`; custom health, board, task, and admin behavior remain absent.
    - `tests/test_public_http.py` collects with zero behavior functions.
    - One scaffold commit covers both task groups with the exact required subject, exact 19-file inventory, and clean status.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run python manage.py check &amp;&amp; uv run pytest --collect-only -q &amp;&amp; test ! -e tasks/tests.py &amp;&amp; test ! -e db.sqlite3 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'chore(24-02): initialize Django project' &amp;&amp; test "$(git ls-files | sort)" = "$(printf '%s\n' .gitignore .python-version manage.py pyproject.toml requirements.txt taskboard/__init__.py taskboard/asgi.py taskboard/settings.py taskboard/urls.py taskboard/wsgi.py tasks/__init__.py tasks/admin.py tasks/apps.py tasks/migrations/__init__.py tasks/models.py tasks/urls.py tasks/views.py tests/test_public_http.py uv.lock)" &amp;&amp; test -z "$(git status --porcelain --untracked-files=all)"</automated>
  </verify>
  <done>The complete D-03 project/app scaffold is committed once with empty custom behavior and a clean public test seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Deliver the public health tracer</name>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/views.py, /Users/longnv/bin/repo/sealos-django-tutorial/tasks/urls.py</files>
  <behavior>
    - `client.get('/health')` returns HTTP 200.
    - `response.json()` equals the independent literal `{"status": "ok"}`.
    - The test observes only Django's public client and response.
  </behavior>
  <action>
RED: add only `test_health_is_public`, run its node, retain the missing-route
failure, and commit exactly `test(24-02): specify public health`. GREEN: add
the `JsonResponse` view and named `/health` route, run the named test,
accumulated file, and `manage.py check`, then commit exactly
`feat(24-02): add public health endpoint`. Require RED as GREEN's direct parent
and compare the complete four-subject history with 24-RESEARCH.md.
  </action>
  <acceptance_criteria>
    - RED fails on missing public health after successful Django loading.
    - GREEN returns the exact D-02 status and JSON payload.
    - The test file contains exactly one behavior function.
    - The scaffold, health RED, and health GREEN subjects occupy their exact ordered positions.
    - The repository is clean and `db.sqlite3` remains absent.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; uv run pytest -q tests/test_public_http.py::test_health_is_public -x &amp;&amp; uv run pytest -q tests/test_public_http.py -x &amp;&amp; uv run python manage.py check &amp;&amp; test "$(rg -c '^def test_[A-Za-z0-9_]+\(' tests/test_public_http.py)" -eq 1 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(24-02): add public health endpoint' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(24-02): specify public health' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The D-02 health contract is green through real routing with exact D-05 ancestry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Approved lock to generated Django source | Framework generators create tracked reader-executable files. |
| Django test client to public URLconf | An HTTP request crosses routing into the health view. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-24-SC | Tampering | Generated source dependencies | high | mitigate | Use Plan 24-01's approved lock and require locked sync plus exact scaffold inventory. |
| T-24-01 | Information Disclosure | Stage 1 settings | medium | mitigate | Keep credentials absent, use explicit development settings, and reserve production secrets for Phase 26. |
| T-24-02 | Spoofing | `/health` | low | accept | The public endpoint exposes one fixed status field and its test asserts the complete payload. |
</threat_model>

<verification>
Verify generated settings, route-empty scaffold, exact inventory and subject,
then verify named health behavior, direct RED/GREEN ancestry, accumulated suite,
system check, clean status, and absent mutable database.
</verification>

<success_criteria>
- D-03 generated project/app source loads from the approved dependency commit.
- D-02 health succeeds through D-05 public behavior testing.
- Exact scaffold and health subjects form the first four ordered phase commits.
- D-07 keeps generated database state outside the tracked tree.
</success_criteria>

## Artifacts this phase produces

- Generated `taskboard` project and `tasks` app scaffold.
- Public `GET /health` route and exact JSON response.
- Scaffold commit plus one health RED/GREEN pair.
- `.planning/phases/24-django-deploy-stage/24-02-SUMMARY.md` with scaffold, history, health, and cleanliness evidence.

## Source Audit Coverage

| Source | ID | Feature or constraint | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | - | Runnable Django foundation with public health | 24-02, 24-05 | COVERED |
| REQ | DJAN-01 | `/health` and port-8000 Django source | 24-02, 24-05 | COVERED |
| RESEARCH | - | Generated project/app boundary and public test seam | 24-02 | COVERED |
| CONTEXT | D-02 | Exact public health JSON | 24-02 | COVERED |
| CONTEXT | D-03 | Conventional SQLite Django scaffold | 24-02, 24-03 | COVERED |
| CONTEXT | D-05 | Public HTTP RED/GREEN behavior | 24-02 | COVERED |
| CONTEXT | D-07 | Stage 1 generated-state fence | 24-02, 24-05 | COVERED |

<output>
Create `.planning/phases/24-django-deploy-stage/24-02-SUMMARY.md` with the exact
scaffold inventory/commit, health RED/GREEN outputs and ancestry, system checks,
and clean generated-state evidence.
</output>
