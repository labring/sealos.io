---
phase: 27-practice-backed-tutorial-series
plan: "02"
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/source-identities.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/timing.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/commands.txt
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/claims.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/practice-events.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/screenshots.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/cleanup.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/review.txt
autonomous: true
requirements: [CONT-01, CONT-03, CONT-04, SHOT-01, SHOT-02, OPS-01]
must_haves:
  truths:
    - "D-18 produces one accepted clean FastAPI reader attempt with a fresh Stage 1 timing interval, public HTTPS domain, current Sealos analysis/template output, and browser proof."
    - "All three protected FastAPI stages are observed from run-owned clones while the public repository, tags, ruleset, accepted images, and sealed Phase 23 evidence remain byte-identical."
    - "Stage 2 proves Alembic 0001 completion, schema-aware readiness, public CRUD, and process-restart persistence with real PostgreSQL."
    - "Stage 3 proves migration-first two-replica runtime, current HTTPS/docs/log presentation, and evidence-backed rollback/recovery continuity."
    - "Exactly 12 FastAPI screenshot specifications join D-11 filenames to protected source and reviewed real observations."
    - "D-24 cleanup records every accepted and failed attempt and ends with zero FastAPI-owned Sealos, Kubernetes, process, browser, clone, state, render, image, and ledger residue."
  artifacts:
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/timing.jsonl"
      provides: "Every FastAPI beginner attempt and one accepted complete timing record"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/practice-events.jsonl"
      provides: "Local, Sealos, migration, readiness, HTTP, browser, log, rollout, rollback, and recovery observations"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/screenshots.jsonl"
      provides: "Twelve evidence-joined FastAPI render specifications"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/cleanup.jsonl"
      provides: "Per-attempt terminal cleanup and independent zero-state readback"
  key_links:
    - from: "FastAPI stage-1-deploy clone"
      to: "timing.jsonl"
      via: "monotonic clock from native Sealos deploy request to generated HTTPS /health success"
      pattern: "elapsed_ms"
    - from: "practice-events.jsonl"
      to: "screenshots.jsonl"
      via: "evidence_ids and source_record_id joins"
      pattern: "evidence_ids"
    - from: "screenshots.jsonl"
      to: "scripts/python-tutorial-assets.mjs"
      via: "SCREENSHOT_CONTRACT record IDs and required tokens"
      pattern: "deploy-fastapi-sealos"
---

<objective>
Execute a fresh, fully owned FastAPI three-stage reader practice and retain the
credential-free evidence needed by all 12 FastAPI cards and three tutorials.

Purpose: Turn protected source and real current Sealos behavior into accepted
timing, domain, migration, browser, runtime, rollback, and cleanup authority.
Output: Eight framework-owned staging evidence files, one accepted clean
timing attempt, 12 render specifications, and zero run residue.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-RESEARCH.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/evidence/
@.planning/phases/25-django-postgresql-stage/evidence/fastapi-phase-gate.txt
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-production.sh
@/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md
@AGENTS.md

<interfaces>
- Framework staging root: `.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/`; Plan 27-03 owns only the sibling `django/` root.
- Attempt ID and every mutable collaborator use one lowercase DNS-safe `p27-fastapi-<UTC>-<hex>` run ID.
- Raw clone, `.sealos/`, credentials, cookies, captures, render scratch, and ownership ledger live below one mode-0700 `mktemp -d` root; state and credential files use mode 0600.
- All Kubernetes calls use `KUBECONFIG="$HOME/.sealos/kubeconfig" kubectl --insecure-skip-tls-verify` and one exact namespace read from authenticated context without retaining the namespace value.
- Timing begins immediately before the native Sealos DEPLOY request and ends immediately after the generated public HTTPS `/health` returns 200 and the exact Stage 1 payload.
- A failed or incomplete attempt is retained as a rejected timing/cleanup record, fully cleaned, and retried with a new run ID.
- Host execution uses `PLUGIN_DIR=/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy`; because an executor cannot invoke the interactive `$sealos` composer trigger, it runs this same plugin's scoring, artifact-validation, Template API, footprint, smoke, and log scripts directly and records that execution path truthfully.
- Stage 1 deploys the protected tag as a deterministic run-owned bootstrap ConfigMap archive on pinned `docker.io/library/python:3.12.13-slim-bookworm@sha256:d50fb7611f86d04a3b0471b46d7557818d88983fc3136726336b2a4c657aa30b`: an initContainer extracts with Python's `tarfile` data filter and installs tracked `requirements.txt` into a one-GiB StatefulSet `volumeClaimTemplates` volume; the main container sets `PYTHONPATH` to that volume and runs the tracked Uvicorn command. The generated Template also owns Service, standard Sealos HTTPS Ingress, and App resources.
- Wave-2 ownership is framework-scoped: this plan may read the sibling Django evidence and run prefixes, but it never writes, deletes, closes, or globally asserts their live resources while Plan 27-03 may still be active. Plan 27-06 owns the cross-framework zero-state audit after the wave barrier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture FastAPI source, local, beginner timing, and public browser evidence</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/source-identities.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/timing.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/commands.txt, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/practice-events.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/cleanup.jsonl</files>
  <read_first>
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-03, D-07 through D-10, and D-18 through D-24
    - `.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md` Claim-to-Source Map and FastAPI Practice
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md` including kubeconfig, log, Runtime Truth Pass, Instance, and cleanup rules
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/modules/pipeline.md`, `scripts/score-model.mjs`, `scripts/validate-artifacts.mjs`, `scripts/deploy-template.mjs`, `scripts/sealos-footprint.mjs`, `scripts/sealos-live-smoke.mjs`, and `scripts/sealos-log-scan.mjs`
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/docker-to-sealos/SKILL.md`, `references/sealos-specs.md`, and `scripts/quality_gate.py`
    - `git -C /Users/longnv/bin/repo/sealos-fastapi-tutorial show stage-1-deploy:README.md`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md` and the three local annotated-tag identities
    - `.planning/phases/23-fastapi-production-stage/evidence/publication.txt` and `checksums.txt`
    - `scripts/python-tutorial-assets.mjs` from Plan 27-01
  </read_first>
  <action>
Hash and record the Phase 23 sealed evidence tree, all three local/public
annotated tag objects and peeled commits, ruleset `18970425`, both accepted
image digests, the two reference-repository statuses, and the Phase 28 boundary
files before mutation. Stop on identity drift. Create a fresh mode-0700 root
with BSD-compatible `mktemp -d`, install traps before starting any process,
create a mode-0600 ownership ledger, and generate one unique FastAPI run ID.
Preflight exact proposed names and the run label across Instance, App,
Deployment, StatefulSet, ReplicaSet, Pod, Service, Ingress, Job, PostgreSQL cluster, PVC,
Secret, and ConfigMap kinds; require absence before creation.

Clone the public `stage-1-deploy` tag by HTTPS into the owned root. Require the
peeled commit, clean status, `uv sync --locked`, all 12 public tests, and a
real owned-port Uvicorn start. Record `/health` 200 with `{"status":"ok"}`,
`/docs` Swagger UI, and task create/list behavior. Set `PLUGIN_DIR` to the exact
installed 1.1.0 skill path. Run its real `score-model.mjs`, write
`.sealos/analysis.json` from that measured result and the plugin schema, and
validate it with `validate-artifacts.mjs`. Generate
`.sealos/template/index.yaml` from the co-installed docker-to-sealos rules with
one run-owned source-archive bootstrap ConfigMap, a one-GiB
`volumeClaimTemplates` dependency/source volume, bounded install initContainer,
one-replica StatefulSet, Service, standard
`${{ defaults.app_host }}.${{ SEALOS_CLOUD_DOMAIN }}` TLS Ingress, and App.
Use the pinned Python 3.12 digest from the interface; extract the protected-tag
archive through `tarfile.extractall(..., filter="data")`, install the tracked
requirements into the claimed volume, and start the tracked Uvicorn entry with
`PYTHONPATH` pointing at source plus installed packages. Run the co-installed
`quality_gate.py` and plugin artifact validator. Retain the one raw plugin log
only in scratch.

Run the exact plugin `deploy-template.mjs` once with `--dry-run`, require its
structured preview, then start both clocks immediately before the real
`deploy-template.mjs` request. Derive the Instance/App identity and public host
from the real response/live resources, write `.sealos/state.json` only from
those observed values, and validate it against the installed state schema.

Start UTC and monotonic clocks immediately before the DEPLOY request and stop
them only after the generated public HTTPS `/health` returns the exact payload.
Record D-21 fields, response-body hashes, protected tag, context hash, run ID,
and complete-evidence flag in `timing.jsonl`; retain no raw workspace,
namespace, API server, credential, Secret, cookie, or private URL. Verify the
generated domain, rollout, Instance/App/workload footprint, public `/docs`, and
public task create/list in a uniquely named `agent-browser --session` session.
Record final URL, title, language, body selectors, network status, and capture
hash, then close only that session. Mark the attempt accepted only after its
four beginner screenshot evidence cells are sourceable. Execute the plugin
Runtime Truth Pass with `sealos-footprint.mjs`, live `kubectl rollout status`,
exact Instance/App/Deployment/ReplicaSet/Pod/Service/Ingress/endpoint/log reads,
`sealos-live-smoke.mjs`, and public curl/browser checks. Then clean this exact
attempt immediately, close/reap every owned process/session, delete only the
ledger/label intersection, and write its terminal zero row. Mark the timing row
accepted and `evidence_complete=true` only after that terminal exists; otherwise
retain it rejected, finish cleanup, and use a new run ID.
  </action>
  <acceptance_criteria>
    - Three FastAPI protected source identities and ruleset 18970425 are read back without mutating either reference repository or public tag.
    - Stage 1 locked sync, 12 tests, local port-8000-equivalent start, health, Swagger, and task HTTP behaviors are observed from a fresh public clone.
    - One accepted timing record has UTC start/end, monotonic endpoints, elapsed milliseconds, run ID, protected source, context hash, exact payload hashes, status 200, and `evidence_complete=true`.
    - Real analysis/template summaries, generated HTTPS domain, rollout, health, Swagger, and task browser records exist and contain no sensitive field.
    - The named browser session, run-owned resources, source archive, `.sealos` state, log, and scratch are absent before the accepted timing row is finalized; its terminal cleanup row reports zero.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi &amp;&amp; node --test scripts/python-tutorial-assets.test.mjs &amp;&amp; jq -e -s 'length == 3 and (map(.tag) | unique | length == 3) and all(.[]; .framework == "FastAPI" and .protected == true)' "$root/source-identities.jsonl" &amp;&amp; jq -e -s 'map(select(.framework == "fastapi" and .accepted == true and .evidence_complete == true and .http_status == 200 and (.elapsed_ms | type == "number") and .elapsed_ms &gt;= 0 and .cleanup_record_id != null)) | length == 1' "$root/timing.jsonl" &amp;&amp; jq -e -s 'any(.[]; .terminal == true and .zero_residue == true)' "$root/cleanup.jsonl" &amp;&amp; jq -e -s 'any(.[]; .event == "plugin-analysis") and any(.[]; .event == "template-dry-run") and any(.[]; .event == "runtime-truth" and .health_status == 200)' "$root/practice-events.jsonl" &amp;&amp; browser_json="$(agent-browser session list --json)" &amp;&amp; test -z "$(printf '%s' "$browser_json" | jq -r '.data.sessions[]?' | grep '^p27-fastapi-' || true)"</automated>
  </verify>
  <done>FastAPI Stage 1 has one accepted clean timing/domain/browser attempt plus protected-source and local validation authority.</done>
</task>

<task type="auto">
  <name>Task 2: Capture FastAPI PostgreSQL and production stage evidence</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/claims.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/practice-events.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/screenshots.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/cleanup.jsonl</files>
  <read_first>
    - Task 1 FastAPI staging records and ownership ledger
    - `git -C /Users/longnv/bin/repo/sealos-fastapi-tutorial show stage-2-postgresql:README.md`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md`, `app/main.py`, `app/database.py`, `app/models.py`, `deploy/migration-job.yaml`, and `deploy/application.yaml`
    - `/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh` and `scripts/test-production.sh`
    - `.planning/phases/23-fastapi-production-stage/evidence/` and `.planning/phases/25-django-postgresql-stage/evidence/fastapi-phase-gate.txt` as immutable accepted provenance
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-11 FastAPI rows and D-19 through D-20
  </read_first>
  <action>
Use separate owned Stage 2 and Stage 3 HTTPS clones and exact run identities
under the same framework attempt family. For Stage 2, verify protected source,
locked export, SQLAlchemy 2/psycopg configuration, Alembic `0001`, and the
tracked production/source Job contracts. Provision a fresh run-owned
PostgreSQL 17 service and Secret. Deploy the Stage 2 protected source through
the same deterministic ConfigMap archive, pinned Python digest, bounded
initContainer/claimed-volume install, StatefulSet, Service, and run-owned public HTTPS Ingress
mechanism established by Task 1; keep all generated files in scratch. Apply the
migration Job twice with strict
server validation, require `Complete=True` and current `0001`, then accept
schema-aware `/health` 200. Create a task through public HTTP in process A,
stop and reap A, start process B against the same database, and read the exact
task through public HTTP. Retain the generated app/PostgreSQL/Secret/Service/
Ingress/Job plan with all values redacted.

For Stage 3, verify the protected source, final immutable digest, same-image
migration relation, two Ready replicas, UID/GID 10001, one Uvicorn worker per
Pod, `/health`, `/docs`, and credential-free `event=service_start` logs. Use
the accepted Phase 23 checksums as authority for the immutable A/B images and
A-B-A-B historical transition, and run the current-product domain/browser/log
observations under this plan's exact ownership. Exercise baseline, final,
rollout undo, and explicit final recovery when the fresh adapter owns those
states; preserve the same Task witness across all four. Every assertion gets
a `practice-events.jsonl` record with source/evidence selectors.

Create exactly the 12 FastAPI D-11 screenshot records with their locked page,
filename, adjacent step, protected tag, authority path/record, evidence IDs,
capture session, redactions, layout, required/forbidden tokens, and pending
asset metadata. Create material `claims.jsonl` rows before prose authoring;
each route, version, command, migration, digest, runtime identity, log, and
rollback statement resolves to a protected source or checksum-valid evidence
record. No WebP is generated in this plan.

After each Stage 2 or Stage 3 observation set is frozen and sanitized, close
its exact browser/process/port-forward handles and clean its ledger/label
intersection immediately. Append one terminal cleanup row before continuing;
any retry uses a new run ID. Stage 3 uses the protected immutable image, while
Stage 2's source archive/ConfigMap and all generated Ingress/state/log scratch
are removed with that attempt.
  </action>
  <acceptance_criteria>
    - Stage 2 records show fresh and repeated Alembic 0001 completion, schema-aware health 200, process A public write, and process B readback on real PostgreSQL.
    - Stage 3 records show migration-first two-replica readiness, immutable digest, UID/GID 10001, one Uvicorn worker, public HTTPS health/docs, and release logs.
    - Rollback/recovery claims resolve to either the fresh exact run or the unmodified checksum-valid Phase 23 record with no mixed-source ambiguity.
    - `claims.jsonl` contains one reviewed selector for every material FastAPI tutorial fact planned for all three pages.
    - `screenshots.jsonl` contains exactly 12 unique D-11 FastAPI page/filename records and every record joins source plus real evidence.
    - Every Stage 2/3 run ID has its own terminal zero cleanup row before Task 2 finishes.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi &amp;&amp; jq -e -s 'length == 12 and (map(.page + "/" + .filename) | unique | length == 12) and all(.[]; (.source_artifact | length) &gt; 0 and (.evidence_ids | length) &gt; 0 and .framework == "FastAPI")' "$root/screenshots.jsonl" &amp;&amp; jq -e -s 'any(.[]; .event == "migration" and .revision == "0001" and .condition == "Complete") and any(.[]; .event == "restart-readback" and .status == 200) and any(.[]; .event == "rollout" and .ready == 2) and any(.[]; .event == "public-docs" and .status == 200)' "$root/practice-events.jsonl" &amp;&amp; jq -e -s 'length &gt;= 3 and all(.[]; .terminal == true and .zero_residue == true)' "$root/cleanup.jsonl"</automated>
  </verify>
  <done>All three FastAPI stages and all 12 card cells have protected-source and reviewed real-observation authority.</done>
</task>

<task type="auto">
  <name>Task 3: Review FastAPI evidence and prove exact cleanup</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/cleanup.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi/review.txt</files>
  <read_first>
    - Every FastAPI staging record created by Tasks 1 and 2
    - Each attempt's retained terminal row and sanitized ownership selectors after immediate cleanup
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md` cleanup order and Instance requirement
    - `/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh` read-only `--assert-clean-all` implementation
    - Original Phase 23 sealed-evidence hashes and reference-repository statuses captured in Task 1
  </read_first>
  <action>
For every accepted and rejected FastAPI attempt, compare its existing terminal
with fresh exact-name and exact-label readback and halt on ambiguous ownership.
Apply the user's confirmed D-24 cleanup decision to any exact owned residue,
including
Instance, App, Deployment, StatefulSet, ReplicaSet, Pod, Service, Ingress, Job, PostgreSQL
cluster, PVC, Secret, and ConfigMap resources plus temporary image versions.
Stop only ledger-recorded port-forwards, local servers, and render servers;
close only exact named browser sessions; remove only run roots, `.sealos/`
clones, raw captures, HTML/PNG/image scratch, state, credentials, logs, and
ownership ledgers. Require exactly one existing terminal cleanup object per
attempt and never reuse a failed run ID; append only the separate framework
final-audit record from this task.

Run a separate read-only query over every exact FastAPI name and run label, the
FastAPI tutorial prefix, ownership roots, process inventory, listening ports,
browser sessions, clone paths, generated state, render/image scratch, and
temporary registry/package state. Keep this audit scoped to `p27-fastapi-*` so
the sibling wave plan can remain live; Plan 27-06 runs the inherited
cross-framework `--assert-clean-all` check after both plans finish. Require all
FastAPI-owned counts zero. Re-hash the target repositories,
ruleset readback, accepted images, sealed Phase 23 evidence, and Phase 28
boundary files; require byte/identity equality.

Credential-scan all eight FastAPI staging files for URLs with credentials,
tokens, authorization headers, cookies, CSRF, Secret values, namespaces,
workspace names, API servers, private endpoints, admin identity, and unresolved
redaction markers. Record semantic, source, timing, browser, 12-spec,
credential, and cleanup acceptance in `review.txt`. Keep these framework-owned
records writable for Plans 27-05/27-06 and create no site-source commit.
  </action>
  <acceptance_criteria>
    - Every accepted and failed attempt has one terminal cleanup row and a unique run ID.
    - Exact-name, exact-label, process, port, browser, path, registry, and ledger readbacks all report zero FastAPI-owned residue.
    - All eight FastAPI staging files pass the credential and unresolved-redaction scan.
    - Reference repositories are clean; tags, ruleset 18970425, accepted image identities, sealed Phase 23 bytes, and Phase 28 boundary bytes match their preflight values.
    - `review.txt` records 12/12 specifications sourceable and the accepted timing attempt as complete.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/fastapi &amp;&amp; jq -e -s 'length &gt;= 3 and (map(.run_id) | unique | length) == length and all(.[]; .terminal == true and .zero_residue == true and ([.instance,.app,.deployment,.statefulset,.replicaset,.pod,.service,.ingress,.job,.postgresql_cluster,.pvc,.secret,.configmap,.port_forward,.local_server,.browser_session,.clone_path,.sealos_path,.state_path,.log_path,.capture_path,.render_path,.image_scratch,.ownership_ledger,.temporary_image,.credential_path,.registry_path] | all(. == 0)))' "$root/cleanup.jsonl" &amp;&amp; grep -F 'screenshots=12/12' "$root/review.txt" &gt;/dev/null &amp;&amp; grep -F 'credentials=passed' "$root/review.txt" &gt;/dev/null &amp;&amp; grep -F 'cleanup=zero' "$root/review.txt" &gt;/dev/null</automated>
  </verify>
  <done>The complete FastAPI practice is reviewed, credential-free, sourceable for 12 cards, and leaves zero owned footprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Git and sealed evidence to practice | Immutable source and historical evidence become claims without receiving writes. |
| Authenticated workstation to Sealos | The deployment plugin, Template API, kubeconfig, and namespace create run-owned resources. |
| Public HTTPS/browser to evidence | Current domains, application responses, and browser surfaces enter sanitized staging records. |
| Ownership ledger to cleanup | Destructive cleanup is authorized only for exact run-owned identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-05 | Spoofing | Public tag and runtime identity | high | mitigate | Require direct/peeled tag, ruleset, image digest, Pod imageID, release log, and source selectors before acceptance. |
| T-27-06 | Information Disclosure | Logs, `.sealos`, credentials, browser state | high | mitigate | Keep raw material in mode-0700 scratch, state in mode 0600, sanitize by field, scan records, and delete raw state before acceptance. |
| T-27-07 | Tampering | Timing and browser observations | high | mitigate | Use wall plus monotonic clocks, exact payload hashes, named sessions, final URL/title/body/network records, and immutable event IDs. |
| T-27-08 | Denial of Service | Run-owned Sealos resources | medium | mitigate | Use bounded resources/timeouts, one exact run identity, cleanup traps, and zero-state readback after every attempt. |
| T-27-09 | Elevation of Privilege | Cleanup selectors | high | mitigate | Intersect exact ledger names with exact labels, halt on ambiguity, preserve unrelated resources, and audit deletion independently. |
</threat_model>

<verification>
Validate all eight staging schemas, one accepted complete timing attempt, 12
unique screenshot specifications, protected source joins, real Stage 2/3
events, credential scans, per-attempt cleanup terminals, inherited read-only
cleanup, and pre/post immutable-source hashes.
</verification>

<success_criteria>
- FastAPI Stage 1 supplies an accepted measured public-domain workflow.
- FastAPI Stages 2 and 3 supply current migration, persistence, runtime, browser, log, and domain observations.
- Exactly 12 locked screenshot records have real source/evidence joins.
- Every accepted and failed attempt is cleaned to an independently verified zero state.
</success_criteria>

## Artifacts this phase produces

- Framework staging files under `evidence/work/fastapi/`: `source-identities.jsonl`, `timing.jsonl`, `commands.txt`, `claims.jsonl`, `practice-events.jsonl`, `screenshots.jsonl`, `cleanup.jsonl`, `review.txt`.
- Run-owned external artifacts during execution: protected-tag clones, `.sealos/` state, Sealos Instance/App objects, workloads, PostgreSQL, Ingress/domain, browser sessions, timing state, render/image scratch, and ownership ledgers; all are removed before completion.
- Twelve pending FastAPI screenshot provenance records using the exact D-11 filenames.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-02-SUMMARY.md`
with accepted/rejected run IDs, timing, source identities, Stage 1/2/3 outcomes,
12-spec inventory, immutable hashes, and zero-residue proof. Do not record raw
workspace, namespace, credential, cookie, Secret, or private endpoint values.
</output>
