---
phase: 27-practice-backed-tutorial-series
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/python-tutorial-assets.test.mjs
  - scripts/python-tutorial-assets.mjs
autonomous: true
requirements: [CONT-03, CONT-04, SHOT-01, SHOT-02, OPS-01]
must_haves:
  truths:
    - "D-01 through D-17 are represented by one exact 24-record screenshot contract and one read-only six-draft validation seam."
    - "The coordinator RED commit changes only its test file and fails after fixture creation with Node ERR_MODULE_NOT_FOUND for the absent implementation."
    - "The direct GREEN commit changes only the implementation file and makes the unchanged focused suite pass."
    - "D-22 applies one shared title decision to both beginner pages from two accepted clean timing records."
    - "D-25 keeps every Phase 28 publication surface byte-identical and reports publication drift as a stable semantic failure."
  artifacts:
    - path: "scripts/python-tutorial-assets.test.mjs"
      provides: "Public coordinator behavior specification and deterministic fixture bundle"
    - path: "scripts/python-tutorial-assets.mjs"
      provides: "Draft validator, evidence-card renderer, image validator, and three-mode CLI"
      exports: ["SCREENSHOT_CONTRACT", "validateDraftBundle", "renderEvidenceCard", "validateEvidenceCard"]
  key_links:
    - from: "scripts/python-tutorial-assets.test.mjs"
      to: "scripts/python-tutorial-assets.mjs"
      via: "dynamic public-module import after fixture setup"
      pattern: "python-tutorial-assets\\.mjs"
    - from: "scripts/python-tutorial-assets.mjs"
      to: "content/tutorials/<locked-slug>/index.en.mdx"
      via: "validateDraftBundle exact six-page contract"
      pattern: "validateDraftBundle"
    - from: "scripts/python-tutorial-assets.mjs"
      to: "public/images/<locked-slug>/<locked-filename>.webp"
      via: "SCREENSHOT_CONTRACT path allowlist and validateEvidenceCard"
      pattern: "SCREENSHOT_CONTRACT"
---

<objective>
Build the two-file Phase 27 draft and evidence-asset coordinator through one
strict RED -> GREEN cycle.

Purpose: Establish a deterministic public seam for the six draft pages, 24
evidence cards, evidence joins, shared title gate, and Phase 28 boundary before
live practice or content authoring begins.
Output: One test-only RED commit and its direct implementation-only GREEN
commit, with no dependency or package-script changes.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.codex/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-RESEARCH.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-VALIDATION.md
@AGENTS.md
@CONTEXT.md
@package.json
@source.config.ts
@scripts/validate-tutorials.mjs
@app/[lang]/(home)/tutorials/tutorial-growth-data.ts
@app/[lang]/(home)/tutorials/page.tsx
@/Users/longnv/.agents/skills/tdd/SKILL.md

<interfaces>
- `SCREENSHOT_CONTRACT` is an immutable exact 24-record array keyed by the D-11 page and filename matrix.
- `validateDraftBundle({ repoRoot, evidenceRoot, phaseBase })` performs read-only semantic validation and returns `{ ok, issues, counts }` with sorted issue objects.
- `renderEvidenceCard({ spec, evidence, browserCapture, outputPath })` is the sole mutating export and writes one contract-owned 1440x900 WebP after evidence, redaction, and path checks pass.
- `validateEvidenceCard({ path, record })` returns structured format, dimension, byte, pixel, digest, and provenance findings without mutating the asset.
- CLI modes are `--check-drafts`, `--render <record-id>`, and `--check-bundle`; all accept explicit `--repo-root`, `--evidence-root`, and `--phase-base` pairs, while `--render` also requires `--scratch-root` and `--check-bundle` alone accepts optional `--framework FastAPI|Django`. Exit codes are 0 pass, 1 semantic failure, 2 usage failure, and 3 internal failure.
- Render/image work uses Node built-ins plus the installed `agent-browser`, `cwebp`, `ffprobe`/FFmpeg, and Tesseract CLIs. Every subprocess receives a literal argument array; no shell command string, repository image library, ImageMagick, Playwright, dependency, or package script is allowed.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify the Python tutorial asset coordinator</name>
  <files>scripts/python-tutorial-assets.test.mjs</files>
  <read_first>
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-01 through D-17 and D-21 through D-26
    - `.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md` Draft-Only Coordinator, Exact Output Map, and TDD Pair
    - `/Users/longnv/.agents/skills/tdd/SKILL.md`
    - `package.json` for the existing `js-yaml` and Node test environment; probe `agent-browser`, `cwebp`, `ffprobe`, `ffmpeg`, and `tesseract` from PATH
    - `scripts/validate-tutorials.mjs` for structured frontmatter and image checks that remain Phase 28-owned
    - Historical pattern only: `git show 9edd81e:scripts/python-tutorial-assets.test.mjs` and `git show 9edd81e:scripts/python-tutorial-assets.mjs`; do not restore or cherry-pick either file
  </read_first>
  <behavior>
    - A mode-0700 temporary repository and mode-0600 fixture evidence set are complete before the coordinator module is dynamically imported.
    - With the implementation absent, the focused command exits 1 and reports `ERR_MODULE_NOT_FOUND` for `scripts/python-tutorial-assets.mjs` after the fixture-ready assertion has passed.
    - After GREEN, the unchanged suite requires exactly 24 immutable screenshot records and the exact four public exports.
    - Fixture mutations produce the stable codes `DRAFT_PAGE_SET_MISMATCH`, `DRAFT_FRONTMATTER_MISMATCH`, `DRAFT_SERIES_LINK_MISMATCH`, `DRAFT_SOURCE_TAG_MISMATCH`, `DRAFT_IMAGE_MATRIX_MISMATCH`, `DRAFT_IMAGE_REFERENCE_MISMATCH`, `EVIDENCE_JOIN_MISSING`, `EVIDENCE_SECRET_MATCH`, `IMAGE_FORMAT_MISMATCH`, `IMAGE_DIMENSION_MISMATCH`, `IMAGE_SIZE_LIMIT`, `IMAGE_BLANK_CANVAS`, `IMAGE_DIGEST_MISMATCH`, `CHECKSUM_ORDER_INVALID`, and `PUBLICATION_BOUNDARY_CHANGED`.
    - Both accepted timing rows at or below 300000 ms enable both five-minute titles; every other complete timing combination selects both evergreen titles.
    - With only the three FastAPI drafts present, `--check-drafts` exits 1 with exactly one `DRAFT_PAGE_SET_MISMATCH` listing the three locked Django paths and the summary `drafts_valid=3 drafts_pending=3 assets_pending=24 issues=1`; after all six drafts it exits 0 with `drafts_valid=6 drafts_pending=0 assets_pending=24 issues=0`.
    - CLI parsing rejects missing, repeated, unknown, wrong-mode, path-escape, and invalid-framework arguments with usage exit 2 and passes every browser/codec argument as a literal array element.
    - Named-session inventory is read from `agent-browser session list --json` at `.data.sessions[]`; only the exact coordinator session is closed and unrelated sessions survive fixtures unchanged.
    - All validation modes preserve fixture bytes and directory entries; only one allowlisted render operation may write an output.
  </behavior>
  <action>
Per D-11 and the confirmed TDD seam, create only
`scripts/python-tutorial-assets.test.mjs` using `node:test`,
`node:assert/strict`, temporary filesystem fixtures, Node built-ins, and the
real installed browser/WebP/FFmpeg/Tesseract CLIs. Build the complete six-page,
24-record, evidence, timing, checksum, and publication-boundary fixture before
dynamically importing the module under test. Keep expected values independent
literals copied from D-01 through D-26. Create fixture PNG/WebP corruption
cases with FFmpeg and `cwebp`, and exercise one real named-browser render; mock
none of `fs`, `crypto`, `js-yaml`, subprocess execution, image decoding, or OCR.
Snapshot unrelated browser sessions before/after and assert their preservation.

Specify the exact three-FastAPI-page intermediate outcome and the complete
six-page draft outcome above. Cover optional `--framework` only for
`--check-bundle`, all required argument pairs, literal handling of shell
metacharacters, `.data.sessions[]` parsing, exact-session close, server/PID
cleanup, and refusal to write outside the contract path.

Run one focused import test first and retain its authentic missing-module
failure. Require the captured output to include `ERR_MODULE_NOT_FOUND` and the
implementation path, and require the failure to occur after the fixture-ready
assertion. Then run the full file to show the same missing implementation is
the RED cause. Commit exactly `test(27-01): specify Python tutorial asset
coordinator`; the commit tree contains only the test file, while `package.json`
and `package-lock.json` remain byte-identical.
  </action>
  <acceptance_criteria>
    - The fixture establishes all six pages, 24 screenshot records, timing rows, evidence joins, image corruption cases, and three publication-boundary snapshots before import.
    - The focused RED command exits 1 with `ERR_MODULE_NOT_FOUND` naming `scripts/python-tutorial-assets.mjs`.
    - No production implementation, dependency, package script, MDX, image, or planning-evidence file appears in the RED commit.
    - The RED commit subject is exactly `test(27-01): specify Python tutorial asset coordinator`.
  </acceptance_criteria>
  <verify>
    <automated>tmp=$(mktemp -d "${TMPDIR:-/tmp}/p27-red.XXXXXX") &amp;&amp; chmod 700 "$tmp" &amp;&amp; trap 'rm -rf "$tmp"' EXIT HUP INT TERM &amp;&amp; if node --test --test-name-pattern='imports the coordinator after fixture setup' scripts/python-tutorial-assets.test.mjs &gt;"$tmp/red.log" 2&gt;&amp;1; then exit 1; fi &amp;&amp; grep -F 'ERR_MODULE_NOT_FOUND' "$tmp/red.log" &gt;/dev/null &amp;&amp; grep -F 'python-tutorial-assets.mjs' "$tmp/red.log" &gt;/dev/null &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d')" = 'scripts/python-tutorial-assets.test.mjs' &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'test(27-01): specify Python tutorial asset coordinator'</automated>
  </verify>
  <done>The coordinator contract exists as one behavior-level RED file, and its failure proves fixture setup reached the absent public module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Python tutorial asset coordinator</name>
  <files>scripts/python-tutorial-assets.mjs</files>
  <read_first>
    - `scripts/python-tutorial-assets.test.mjs` from Task 1
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-01 through D-26
    - `.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md` Draft-Only Coordinator, Evidence Package Pattern, Screenshot Pipeline, and Phase 27 Publication Boundary
    - `source.config.ts`, `lib/utils/tutorial-utils.ts`, and `app/[lang]/(home)/tutorials/[slug]/page.tsx`
    - `app/[lang]/(home)/tutorials/tutorial-growth-data.ts`, `app/[lang]/(home)/tutorials/page.tsx`, and `scripts/validate-tutorials.mjs` as read-only Phase 28 boundaries
  </read_first>
  <action>
Implement only `scripts/python-tutorial-assets.mjs`. Export the exact frozen
24-record D-11 `SCREENSHOT_CONTRACT`, `validateDraftBundle`,
`renderEvidenceCard`, and `validateEvidenceCard`. Use built-in Node modules,
the existing `js-yaml` parser, and literal argument-array calls to the installed
`agent-browser`, `cwebp`, `ffprobe`/FFmpeg, and Tesseract CLIs. Reject path
escape, symlinked inputs, unknown slugs or filenames, duplicate records,
unescaped evidence, credential patterns, missing authority selectors, and
unsorted checksum entries. Return deduplicated issues sorted by code, path,
record ID, and message.

`validateDraftBundle` must parse the complete D-04 frontmatter contract,
framework-local D-02/D-05 ordering, protected D-03 tag URLs, D-09 vocabulary,
the shared D-22 title gate, exact four-reference D-11 matrix, descriptive D-15
alt text, evidence joins, and the exact retained-evidence schema. Its
`phaseBase` argument is a required Git object; compare the Phase 28 boundary
paths at that object with the worktree and emit
`PUBLICATION_BOUNDARY_CHANGED` on any byte or inventory drift. A successful
check snapshots inputs before and after and proves read-only behavior.

`renderEvidenceCard` must generate escaped self-contained HTML, bind an owned
Node HTTP server to `127.0.0.1`, open one exact named `agent-browser` session at
1440x900/DPR 1, verify fonts/images/critical rectangles/no overlap, capture a
PNG, close that session, stop/reap the owned server, and encode through
`cwebp -quiet -q <quality> -m 6 -sharp_yuv -metadata none`. Product-surface
records embed only the sanitized real capture selected by their evidence row.
`validateEvidenceCard` must use Node for RIFF/WEBP signature, bytes, digest, and
path/provenance; `ffprobe` for codec/dimensions; FFmpeg for decode and nonblank
pixel statistics; and Tesseract for required/forbidden OCR tokens. Add the
three CLI modes and stable exit codes 0/1/2/3, strict paired-argument parsing,
and the optional FastAPI/Django bundle filter. `--check-drafts` implements the
exact intermediate/full summaries specified by Task 1; `--check-bundle`
requires all selected images and final evidence. Add no package dependency or
script.

Run the unchanged test file, then commit exactly
`feat(27-01): add Python tutorial asset coordinator`. Require the GREEN commit
to change only the implementation file and to have the RED commit as its
direct parent. Complete review after GREEN; create no refactor commit unless
review changes behavior-neutral implementation and the entire suite remains
green.
  </action>
  <acceptance_criteria>
    - The module exports exactly the four confirmed public coordinator symbols and an exact 24-record contract.
    - The unchanged test file passes all semantic, real-render, image, timing, credential, checksum, argument-safety, session-preservation, CLI, and publication-boundary cases.
    - `--check-drafts`, `--render`, and `--check-bundle` use stable 0/1/2/3 exits and deterministic sorted output.
    - Validator modes preserve all input bytes and entries; rendering can write only one locked target below the repository image root.
    - The direct GREEN commit changes only `scripts/python-tutorial-assets.mjs` and has the exact required subject.
    - `package.json`, `package-lock.json`, Phase 28 boundary files, tutorial sources, and public images are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/python-tutorial-assets.test.mjs &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d')" = 'scripts/python-tutorial-assets.mjs' &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'feat(27-01): add Python tutorial asset coordinator' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'test(27-01): specify Python tutorial asset coordinator' &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD^)" = 'scripts/python-tutorial-assets.test.mjs' &amp;&amp; git diff --exit-code HEAD^^ -- package.json package-lock.json app/'[lang]'/'(home)'/tutorials/tutorial-growth-data.ts app/'[lang]'/'(home)'/tutorials/page.tsx scripts/validate-tutorials.mjs</automated>
  </verify>
  <done>The same coordinator specification is green through the four public exports and three CLI modes with exact RED/GREEN ancestry and intact Phase 28 boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Evidence and MDX to coordinator | Untrusted JSONL, YAML, paths, prose, and browser-capture metadata enter validators and rendering. |
| Coordinator to public image tree | One render mode writes reader-visible WebP bytes. |
| Phase 27 to Phase 28 | Draft checks must preserve catalog, validator, metadata, and publication ownership. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-01 | Tampering | Draft/evidence validation | high | mitigate | Parse structured inputs, require immutable authority selectors and checksums, sort stable findings, and prove validators are read-only. |
| T-27-02 | Information Disclosure | Evidence-card rendering | high | mitigate | Reject credential patterns and forbidden values before composition, escape all text, strip metadata, and constrain output to the exact 24 paths. |
| T-27-03 | Elevation of Privilege | Filesystem paths and subprocess arguments | high | mitigate | Resolve canonical roots, reject symlinks/path escape, pass subprocess arguments as arrays, and bind any local renderer to loopback. |
| T-27-04 | Repudiation | Shared title and image claims | medium | mitigate | Require accepted timing records, source/evidence record IDs, digests, and deterministic issue output for every decision. |
| T-27-SC | Tampering | Package supply chain | low | accept | Add no packages; use the existing lock, built-in Node APIs, existing `js-yaml`, and installed system CLIs with lockfiles byte-identical. |
</threat_model>

<verification>
Replay the RED commit from an isolated Git worktree and require the post-fixture
`ERR_MODULE_NOT_FOUND`; replay GREEN with the unchanged test. Verify exact
commit parents and scopes, 24 contract records, stable CLI exits, read-only
validation, and byte-identical Phase 28 boundaries.
</verification>

<success_criteria>
- The TDD history is exactly one test-only RED followed by one implementation-only GREEN.
- The coordinator exposes the confirmed draft, render, and final-bundle seams without adding dependencies.
- All D-01 through D-17 and D-21 through D-26 deterministic contracts have stable fixture coverage.
- Phase 28 publication files remain untouched and detectable as a fail-closed boundary.
</success_criteria>

## Artifacts this phase produces

- Files: `scripts/python-tutorial-assets.test.mjs`, `scripts/python-tutorial-assets.mjs`.
- Exports: `SCREENSHOT_CONTRACT`, `validateDraftBundle`, `renderEvidenceCard`, `validateEvidenceCard`.
- CLI modes: `--check-drafts`, `--render <record-id>`, `--check-bundle`.
- Exit-code contract: pass 0, semantic failure 1, usage failure 2, internal failure 3.
- Stable issue-code vocabulary enumerated in Task 1.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md`
with RED replay, GREEN replay, exact commit scope, test count, coordinator
interfaces, and Phase 28 boundary hashes. Include the exact machine-readable
line `phase27_base=<40-hex base before the RED commit>` for every downstream
source-boundary check.
</output>
