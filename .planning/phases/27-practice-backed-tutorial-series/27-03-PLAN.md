---
phase: 27-practice-backed-tutorial-series
plan: "03"
type: execute
wave: 2
depends_on: ["27-01"]
files_modified:
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/source-identities.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/timing.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/commands.txt
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/claims.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/practice-events.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/screenshots.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/cleanup.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/review.txt
autonomous: true
requirements: [CONT-02, CONT-03, CONT-04, SHOT-01, SHOT-02, OPS-01]
must_haves:
  truths:
    - "D-18 produces one accepted clean Django reader attempt with a fresh Stage 1 timing interval, public HTTPS domain, current Sealos analysis/template output, Task Board, and native admin surface."
    - "All three protected Django stages are observed from run-owned clones while the public repository, tags, ruleset, accepted images, and sealed Phase 25/26 evidence remain byte-identical."
    - "The run-owned public edge rewrites upstream Host to localhost and proves health, board, admin login, and hashed static assets where applicable."
    - "Stage 2 proves tasks.0001_initial completion, schema-aware readiness, public CSRF write, process-restart persistence, and authenticated admin readback with real PostgreSQL."
    - "Stage 3 proves same-image migration-first two-replica Gunicorn/WhiteNoise runtime and evidence-backed rollback/recovery Task continuity."
    - "Exactly 12 Django screenshot specifications join D-11 filenames to protected source and reviewed real observations, followed by zero owned residue."
  artifacts:
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/django/timing.jsonl"
      provides: "Every Django beginner attempt and one accepted complete timing record"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/django/practice-events.jsonl"
      provides: "Local, host-edge, Sealos, migration, readiness, HTTP, admin, static, log, rollout, rollback, and recovery observations"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/django/screenshots.jsonl"
      provides: "Twelve evidence-joined Django render specifications"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/work/django/cleanup.jsonl"
      provides: "Per-attempt terminal cleanup and independent zero-state readback"
  key_links:
    - from: "run-owned public Ingress or edge"
      to: "Django Stage 1/2/3 Service"
      via: "verified upstream Host localhost rewrite"
      pattern: "upstream-vhost.*localhost"
    - from: "Django stage-1-deploy clone"
      to: "timing.jsonl"
      via: "monotonic clock from native Sealos deploy request to generated HTTPS /health success"
      pattern: "elapsed_ms"
    - from: "practice-events.jsonl"
      to: "screenshots.jsonl"
      via: "evidence_ids and source_record_id joins"
      pattern: "evidence_ids"
---

<objective>
Execute a fresh, fully owned Django three-stage reader practice and retain the
credential-free evidence needed by all 12 Django cards and three tutorials.

Purpose: Turn protected source and real current Sealos behavior into accepted
timing, host-rewritten domain, migration, board/admin, static, runtime,
rollback, and cleanup authority.
Output: Eight Django-owned staging evidence files, one accepted clean timing
attempt, 12 render specifications, and zero run residue.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-RESEARCH.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md
@.planning/phases/25-django-postgresql-stage/evidence/
@.planning/phases/26-django-production-stage/evidence/
@/Users/longnv/bin/repo/sealos-django-tutorial/README.md
@/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
@/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
@/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md
@AGENTS.md

<interfaces>
- Framework staging root: `.planning/phases/27-practice-backed-tutorial-series/evidence/work/django/`; Plan 27-02 owns only the sibling `fastapi/` root.
- Attempt ID and every mutable collaborator use one lowercase DNS-safe `p27-django-<UTC>-<hex>` run ID distinct from every FastAPI identity.
- Raw clone, `.sealos/`, credentials, CSRF/cookies, admin state, captures, render scratch, and ownership ledger live below one mode-0700 root; state and credential files use mode 0600.
- Django keeps the frozen `DJANGO_ALLOWED_HOSTS=localhost` source contract; a run-owned Ingress with `nginx.ingress.kubernetes.io/upstream-vhost: localhost` or bounded run-owned edge sets the upstream Host.
- Timing begins immediately before the native Sealos DEPLOY request and ends immediately after generated public HTTPS `/health` returns 200 with `{"status":"ok"}` through the verified host rewrite.
- A failed or incomplete attempt is retained as a rejected timing/cleanup record, fully cleaned, and retried with a new run ID.
- Host execution uses `PLUGIN_DIR=/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy`; the executor runs that installed plugin's scoring, artifact validation, Template API, footprint, smoke, and log scripts directly and records this truthful fallback path.
- Stage 1 uses the same pinned Python 3.12 digest and one-GiB StatefulSet `volumeClaimTemplates` source/dependency volume as FastAPI, with a separate run-owned bootstrap ConfigMap. The initContainer installs dependencies and applies the SQLite migration; the main container only execs the tracked `runserver --noreload` command.
- Wave-2 ownership is framework-scoped: this plan may read the sibling FastAPI evidence and run prefixes, but it never writes, deletes, closes, or globally asserts their live resources while Plan 27-02 may still be active. Plan 27-06 owns the cross-framework zero-state audit after the wave barrier.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture Django source, local, beginner timing, host edge, and browser evidence</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/django/source-identities.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/timing.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/commands.txt, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/practice-events.jsonl</files>
  <read_first>
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-03, D-07 through D-10, and D-18 through D-24
    - `.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md` Claim-to-Source Map and Django Practice
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md` including kubeconfig, log, Runtime Truth Pass, Instance, and cleanup rules
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/modules/pipeline.md`, `scripts/score-model.mjs`, `scripts/validate-artifacts.mjs`, `scripts/deploy-template.mjs`, `scripts/sealos-footprint.mjs`, `scripts/sealos-live-smoke.mjs`, and `scripts/sealos-log-scan.mjs`
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/docker-to-sealos/SKILL.md`, `references/sealos-specs.md`, and `scripts/quality_gate.py`
    - `git -C /Users/longnv/bin/repo/sealos-django-tutorial show stage-1-deploy:README.md`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/README.md`, `taskboard/settings.py`, `tasks/views.py`, and `tasks/templates/tasks/board.html`
    - `.planning/phases/25-django-postgresql-stage/evidence/checksums.txt` and `.planning/phases/26-django-production-stage/evidence/checksums.txt`
    - `scripts/python-tutorial-assets.mjs` from Plan 27-01
  </read_first>
  <action>
Hash and record the Phase 25/26 sealed evidence trees, all three local/public
annotated tag objects and peeled commits, ruleset `19014157`, both accepted
image digests, reference-repository statuses, and Phase 28 boundary files.
Stop on identity drift. Create a separate mode-0700 root and mode-0600
ownership ledger with BSD-compatible `mktemp -d`, install cleanup traps before
process startup, generate one unique Django run ID, and require all proposed
exact names plus its run label absent across the complete D-24 resource set.

Clone public `stage-1-deploy` by HTTPS. Require protected commit, clean status,
`uv sync --locked`, `uv lock --check`, two migration commands, `[X]
0001_initial`, no drift, all five public tests, and a real owned-port
`runserver --noreload`. Record local health, empty board, CSRF-protected task
create/later read, and `/admin/login/` through public HTTP. Invoke the installed
Sealos deployment workflow against this clone: run the installed scorer, write
and validate real `.sealos/analysis.json`, then generate and quality-gate a
Template with the deterministic protected-tag bootstrap ConfigMap, pinned
Python digest, bounded install initContainer, one-GiB StatefulSet claimed
volume, Service, standard TLS Ingress, and App. The initContainer applies the
tracked migration against the claimed source tree; the main container uses that
tree and execs the tracked Django server command.
Sanitize the real `.sealos/template/index.yaml` only after structural checks.

Before public acceptance, create a run-owned Ingress using the current Sealos
NGINX conventions and exact upstream-vhost `localhost` annotation. If the
cluster rejects or ignores the annotation, use a bounded run-owned edge proxy
that sets `Host: localhost`; record the selected adapter and live request proof.
Start UTC and monotonic clocks immediately before DEPLOY and stop after public
HTTPS `/health` returns exactly 200 `{"status":"ok"}` through that edge.
Record all D-21 fields and hashes while excluding raw workspace/namespace/API
server/credentials.

Use a uniquely named browser session to create and list a real Task Board item,
open the native admin login, and record final URL, title, language, body
selectors, CSS/network state, and capture hash. Close only that session. Mark
the attempt accepted after the four beginner card cells and cleanup are
complete; retain incomplete attempts as rejected records and retry with a new
run ID only after exact cleanup.
  </action>
  <acceptance_criteria>
    - Six protected source identities and Django ruleset 19014157 are read back without target mutation.
    - Stage 1 locked sync, migration/current/drift, five tests, local server, health, Task Board write/read, and admin login are observed from a fresh clone.
    - A real public edge proves Host rewrite to `localhost`, generated HTTPS health 200, board, and admin login while retaining the external public host.
    - One accepted timing record contains all D-21 fields, exact payload hashes, complete evidence, and a unique run ID.
    - Real analysis/template and Task Board/admin browser records exist, the named session is closed, and sensitive values remain only in owned scratch.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/django &amp;&amp; jq -e -s 'map(select(.framework == "django" and .accepted == true and .evidence_complete == true and .http_status == 200 and (.elapsed_ms | type == "number") and .elapsed_ms &gt;= 0)) | length == 1' "$root/timing.jsonl" &amp;&amp; jq -e -s 'any(.[]; .event == "host-rewrite" and .upstream_host == "localhost" and .health_status == 200) and any(.[]; .event == "board-task" and .status == 200) and any(.[]; .event == "admin-login" and .status == 200)' "$root/practice-events.jsonl" &amp;&amp; test -z "$(agent-browser session list --json | jq -r '.data.sessions[]?' | grep '^p27-django-' || true)"</automated>
  </verify>
  <done>Django Stage 1 has one accepted clean timing/domain/host/browser attempt plus protected-source and local validation authority.</done>
</task>

<task type="auto">
  <name>Task 2: Capture Django PostgreSQL and production stage evidence</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/django/claims.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/practice-events.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/screenshots.jsonl</files>
  <read_first>
    - Task 1 Django staging records, host adapter, and ownership ledger
    - `git -C /Users/longnv/bin/repo/sealos-django-tutorial show stage-2-postgresql:README.md`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/README.md`, `taskboard/settings.py`, `tasks/views.py`, `tasks/admin.py`, `deploy/migration-job.yaml`, and `deploy/application.yaml`
    - `/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh` and `scripts/test-production.sh`
    - `.planning/phases/25-django-postgresql-stage/evidence/` and `.planning/phases/26-django-production-stage/evidence/` as immutable accepted provenance
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-11 Django rows and D-19 through D-20
  </read_first>
  <action>
Use separate owned Stage 2 and Stage 3 public clones and exact run identities.
For Stage 2, verify protected source, lock/export, psycopg configuration,
immutable `tasks.0001_initial`, dummy unconfigured backend, and application/
source Job contracts. Provision fresh PostgreSQL and run-owned Secret, apply
the source migration Job twice under strict validation, require
`Complete=True`, `[X] 0001_initial`, zero drift, and schema-aware health 200.
Use a mode-0600 cookie jar for a CSRF-protected public write in process A; stop
and reap A; start process B on the same database and read the exact title.
Create an ephemeral superuser without retaining its values, authenticate in a
run-owned browser session, and read the Task from the native changelist and
change form. Close the session and delete credential state.

For Stage 3, verify protected source, final digest, same-image migration,
two Ready replicas, UID/GID 10001, one Gunicorn master/worker pair, manifest
WhiteNoise CSS, public health/board/admin, and credential-free startup logs
through the same run-owned host-rewrite edge. Use checksum-valid Phase 26
records as immutable authority for Images A/B and historical A-B-A-B
transitions; run current domain/browser/static/log observations and preserve
one Task witness for any fresh baseline/final/undo/recovery states.

Create exactly the 12 Django D-11 screenshot records with locked filename,
adjacent step, protected tag, source artifact/record, evidence IDs, capture
session, redactions, layout, required/forbidden tokens, and pending asset
metadata. Build reviewed `claims.jsonl` selectors for versions, commands,
migration, Host contract, health, board/admin, Gunicorn, WhiteNoise, digest,
logs, and rollback facts. Keep credentials, cookies, CSRF, Secret data,
workspace identity, and private endpoints outside retained files. Generate no
WebP in this plan.
  </action>
  <acceptance_criteria>
    - Stage 2 records show two migration Job completions, current `tasks.0001_initial`, health 200, process A CSRF write, process B board readback, and authenticated admin change-form readback.
    - Stage 3 records show same-image migration-first two-replica readiness, Gunicorn identity, health/board/admin, hashed WhiteNoise CSS, and release logs through the public host adapter.
    - Rollback/recovery claims resolve explicitly to a fresh run or unmodified checksum-valid Phase 26 evidence.
    - `claims.jsonl` contains reviewed selectors for every material Django fact planned for all three pages.
    - `screenshots.jsonl` contains exactly 12 unique D-11 Django page/filename records and every record joins protected source plus real evidence.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/django &amp;&amp; jq -e -s 'length == 12 and (map(.page + "/" + .filename) | unique | length == 12) and all(.[]; (.source_artifact | length) &gt; 0 and (.evidence_ids | length) &gt; 0 and .framework == "Django")' "$root/screenshots.jsonl" &amp;&amp; jq -e -s 'any(.[]; .event == "migration" and .revision == "tasks.0001_initial" and .condition == "Complete") and any(.[]; .event == "restart-readback" and .status == 200) and any(.[]; .event == "admin-readback" and .status == 200) and any(.[]; .event == "static" and .content_type == "text/css") and any(.[]; .event == "rollout" and .ready == 2)' "$root/practice-events.jsonl"</automated>
  </verify>
  <done>All three Django stages and all 12 card cells have protected-source and reviewed real-observation authority.</done>
</task>

<task type="auto">
  <name>Task 3: Review Django evidence and prove exact cleanup</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/work/django/cleanup.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/work/django/review.txt</files>
  <read_first>
    - Every Django staging record created by Tasks 1 and 2
    - Each attempt's mode-0600 ownership ledger before cleanup
    - `/Users/longnv/.codex/plugins/cache/sealos/sealos/1.1.0/skills/sealos-deploy/SKILL.md` cleanup order and Instance requirement
    - `/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh` read-only `--assert-clean-all` implementation
    - Original Phase 25/26 evidence hashes and reference-repository statuses captured in Task 1
  </read_first>
  <action>
For every accepted and rejected Django attempt, intersect exact ledger names
with exact run-label results and halt on ambiguity. Apply the confirmed D-24
cleanup only to that owned set: Instance, App, Deployment, StatefulSet, ReplicaSet, Pod,
Service, Ingress/edge, Job, PostgreSQL cluster, PVC, Secret, ConfigMap, and
temporary image versions. Stop only owned processes and port-forwards; close
only run-named browser sessions; remove exact clones, `.sealos/`, raw captures,
cookies, CSRF/admin credentials, state, HTML/PNG/image scratch, logs, and
ledgers. Record one terminal per attempt and never reuse a failed run ID.

Run a second read-only exact-name/label audit across the Django tutorial prefix,
all Django-owned resource kinds, ownership roots, processes, listening ports,
browser sessions, state/replay/render/image paths, and registry/package scratch.
Keep this audit scoped to `p27-django-*` while the sibling wave plan may remain
live; Plan 27-06 runs the protected Stage 3 cross-framework
`scripts/test-production.sh --assert-clean-all` after both plans finish. Require
zero Django-owned counts. Re-hash both target repositories, public
tag/ruleset identities, accepted images, sealed Phase 25/26 evidence, and
Phase 28 boundary bytes.

Credential-scan all eight Django staging files for database URLs, raw hosts
other than the public domain/localhost contract, tokens, authorization,
cookies, CSRF, Secret/admin values, namespaces, workspace/API identity,
private endpoints, and unresolved redaction markers. Record semantic, host,
timing, browser/admin, 12-spec, credential, and cleanup acceptance in
`review.txt`. Preserve these staging records for Plans 27-05/27-06 and create
no site-source commit.
  </action>
  <acceptance_criteria>
    - Every accepted and failed attempt has one terminal cleanup row and a unique run ID.
    - Exact-name, exact-label, process, port, browser, path, registry, credential, and ledger readbacks all report zero Django-owned residue.
    - All eight Django staging files pass credential and unresolved-redaction scans.
    - Reference repository status, tags, ruleset 19014157, accepted image identities, sealed Phase 25/26 bytes, and Phase 28 boundaries equal preflight.
    - `review.txt` records host rewrite accepted, 12/12 specifications sourceable, accepted timing complete, and cleanup zero.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence/work/django &amp;&amp; jq -e -s 'length &gt;= 1 and all(.[]; .terminal == true and .zero_residue == true and ([.instance,.app,.deployment,.statefulset,.replicaset,.pod,.service,.ingress,.job,.postgresql_cluster,.pvc,.secret,.configmap,.port_forward,.local_server,.browser_session,.state_path,.render_path,.image_scratch,.ownership_ledger,.temporary_image,.credential_path] | all(. == 0)))' "$root/cleanup.jsonl" &amp;&amp; grep -F 'host_rewrite=passed' "$root/review.txt" &gt;/dev/null &amp;&amp; grep -F 'screenshots=12/12' "$root/review.txt" &gt;/dev/null &amp;&amp; grep -F 'credentials=passed' "$root/review.txt" &gt;/dev/null &amp;&amp; grep -F 'cleanup=zero' "$root/review.txt" &gt;/dev/null</automated>
  </verify>
  <done>The complete Django practice is reviewed, credential-free, sourceable for 12 cards, and leaves zero owned footprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Git and sealed evidence to practice | Immutable Django source and historical evidence become claims without receiving writes. |
| Authenticated workstation to Sealos | The plugin, Template API, kubeconfig, namespace, and host edge create run-owned resources. |
| Public board/admin to evidence | CSRF, cookies, administrator state, static assets, and rendered Task data cross a sensitive browser boundary. |
| Ownership ledger to cleanup | Destructive cleanup is authorized only for exact run-owned identities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-10 | Spoofing | Host rewrite and public Django surface | high | mitigate | Inspect live Ingress/edge, require upstream localhost, and verify public health, board, admin, and static responses. |
| T-27-11 | Information Disclosure | Admin, CSRF, cookies, Secret and logs | high | mitigate | Use mode-0600 state, named sessions, field redaction, credential/OCR scans, and exact scratch deletion. |
| T-27-12 | Tampering | Migration and Task continuity | high | mitigate | Require immutable migration checksum, two Job completions, process A/B public readback, authenticated admin readback, and evidence selectors. |
| T-27-13 | Denial of Service | Run-owned Sealos resources | medium | mitigate | Bound replicas/resources/timeouts, install cleanup traps, and clean every accepted or failed attempt by exact identity. |
| T-27-14 | Elevation of Privilege | Cleanup and ephemeral superuser | high | mitigate | Scope admin credentials to one run, delete them after readback, intersect ledger/labels before deletion, and preserve unrelated resources. |
</threat_model>

<verification>
Validate all eight Django staging schemas, one accepted complete timing attempt,
host rewrite, 12 unique screenshot specifications, Stage 2/3 events,
credential scans, per-attempt cleanup terminals, inherited read-only cleanup,
and pre/post immutable-source hashes.
</verification>

<success_criteria>
- Django Stage 1 supplies an accepted measured host-rewritten public workflow.
- Django Stages 2 and 3 supply current migration, persistence, admin, static, runtime, log, and domain observations.
- Exactly 12 locked screenshot records have real source/evidence joins.
- Every accepted and failed attempt is cleaned to an independently verified zero state.
</success_criteria>

## Artifacts this phase produces

- Framework staging files under `evidence/work/django/`: `source-identities.jsonl`, `timing.jsonl`, `commands.txt`, `claims.jsonl`, `practice-events.jsonl`, `screenshots.jsonl`, `cleanup.jsonl`, `review.txt`.
- Run-owned external artifacts during execution: protected-tag clones, `.sealos/` state, host edge, Sealos Instance/App objects, workloads, PostgreSQL, Ingress/domain, browser/admin sessions, timing state, render/image scratch, credentials, and ownership ledgers; all are removed before completion.
- Twelve pending Django screenshot provenance records using the exact D-11 filenames.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-03-SUMMARY.md`
with accepted/rejected run IDs, timing, host adapter, source identities, Stage
1/2/3 outcomes, 12-spec inventory, immutable hashes, and zero-residue proof.
Exclude raw workspace, namespace, credential, cookie, CSRF, Secret, admin, or
private endpoint values.
</output>
