---
phase: 27-practice-backed-tutorial-series
plan: "06"
type: execute
wave: 5
depends_on: ["27-05"]
files_modified:
  - .planning/phases/27-practice-backed-tutorial-series/evidence/README.md
  - .planning/phases/27-practice-backed-tutorial-series/evidence/source-identities.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/timing.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/commands.txt
  - .planning/phases/27-practice-backed-tutorial-series/evidence/claims.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/practice-events.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/screenshots.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/cleanup.jsonl
  - .planning/phases/27-practice-backed-tutorial-series/evidence/review.txt
  - .planning/phases/27-practice-backed-tutorial-series/evidence/checksums.txt
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, SHOT-01, SHOT-02, OPS-01]
must_haves:
  truths:
    - "D-26 closes every page-to-tag, claim-to-source, screenshot-to-evidence, timing, redaction, and adjacent-step join with no unresolved record."
    - "The exact retained evidence package contains nine reviewed checksum inputs plus one checksum manifest generated last."
    - "All accepted and failed D-24 attempts have terminal cleanup records and a final read-only cross-framework zero-residue audit."
    - "The final draft coordinator passes read-only with six pages, 24 images, 24 unique references/digests, complete evidence, and valid checksums."
    - "The complete non-planning Phase 27 diff is exactly 32 files: two scripts, six MDX sources, and 24 WebPs in six exact source commits."
    - "D-19 and D-25 preserve target repositories, public tags/rulesets/images, sealed Phase 23/25/26 evidence, catalog status, global validator, index metadata, build, and static publication surfaces."
  artifacts:
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/README.md"
      provides: "Schema, authority, replay, redaction, ownership, and checksum-last contract"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/screenshots.jsonl"
      provides: "Exactly 24 final image provenance records"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/cleanup.jsonl"
      provides: "Every attempt terminal plus final zero-residue audit"
    - path: ".planning/phases/27-practice-backed-tutorial-series/evidence/checksums.txt"
      provides: "Sorted SHA-256 seal over the previous nine evidence inputs"
  key_links:
    - from: "claims.jsonl"
      to: "six MDX sources"
      via: "page, section, normalized claim hash, source artifact, record selector"
      pattern: "claim_text_sha256"
    - from: "screenshots.jsonl"
      to: "24 public WebPs"
      via: "exact page/filename, adjacent step, source/evidence IDs, dimensions, bytes, and SHA-256"
      pattern: "sha256"
    - from: "checksums.txt"
      to: "nine evidence inputs"
      via: "sorted checksum-last manifest and immediate verification"
      pattern: "README.md"
---

<objective>
Consolidate, audit, seal, and independently verify the complete Phase 27
tutorial source, assets, evidence, cleanup, and publication boundary.

Purpose: Produce the durable handoff that lets Phase 28 promote the catalog
only after every practice-backed claim and image is reproducible and clean.
Output: One exact read-only evidence package, passing final draft gate, exact
source inventory/history, and zero temporary residue.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-02-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-03-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-04-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-05-SUMMARY.md
@.planning/phases/23-fastapi-production-stage/evidence/
@.planning/phases/25-django-postgresql-stage/evidence/
@.planning/phases/26-django-production-stage/evidence/
@scripts/python-tutorial-assets.mjs
@AGENTS.md

<interfaces>
- Exact retained package: `README.md`, `source-identities.jsonl`, `timing.jsonl`, `commands.txt`, `claims.jsonl`, `practice-events.jsonl`, `screenshots.jsonl`, `cleanup.jsonl`, `review.txt`, `checksums.txt`.
- The first nine files are checksum inputs; `checksums.txt` is created atomically after semantic, source, credential, visual, and cleanup review and contains exactly nine sorted relative entries.
- Final evidence files are mode 0444 after sealing; any later byte change invalidates acceptance and requires review plus reseal.
- Site-source subjects, in order when filtering Git history to the 32 source paths: the two exact 27-01 subjects, two exact 27-04 subjects, and two exact 27-05 subjects.
- Phase 28 remains intentionally blocked: the current global validator reports the six extra draft directories while the Phase 27 `--check-bundle` gate passes.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate and audit source identities, timings, commands, claims, and practice events</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/README.md, .planning/phases/27-practice-backed-tutorial-series/evidence/source-identities.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/timing.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/commands.txt, .planning/phases/27-practice-backed-tutorial-series/evidence/claims.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/practice-events.jsonl</files>
  <read_first>
    - All FastAPI and Django staging files under `evidence/work/`
    - All six MDX sources and every material command, version, response, migration, runtime, image, log, and rollback claim
    - Protected-tag source and current public tag/ruleset readbacks
    - Sealed Phase 23/25/26 evidence and checksum manifests
    - `27-CONTEXT.md` D-01 through D-10 and D-18 through D-26
  </read_first>
  <action>
Create README schema documentation first, recording `phase27_base` from the
Plan 27-01 summary, authority precedence, exact retained file set, JSONL stable
keys, mode rules, replay commands, redaction vocabulary, attempt ownership,
retry semantics, and checksum-last procedure. Merge FastAPI and Django source
records into sorted `source-identities.jsonl`; require six protected tag URLs,
direct objects, peeled commits, messages, local/public equality, and the two
unchanged rulesets. Reference sealed evidence by path plus verified checksum
and preserve its bytes.

Merge every timing attempt with unique IDs and terminal cleanup linkage.
Require one accepted complete attempt per framework and recompute the shared
title decision independently from elapsed milliseconds; compare both beginner
frontmatter titles and body durations with that result. Curate `commands.txt`
from protected READMEs and native Sealos entry vocabulary, using public
placeholders only.

Merge claim and practice-event records with deterministic ordering and unique
IDs. Normalize each material MDX claim, verify its hash and source selector,
and require every referenced event or sealed record to exist and remain
checksum-valid. Require current domain/browser records, migrations, readiness,
public reads/writes, logs, host rewrite, static, rollback/recovery, and all
three stages for both frameworks. Scan all six outputs for credentials, raw
context identity, private endpoints, unresolved placeholders, unsupported
claims, and stale/contradictory stages.
  </action>
  <acceptance_criteria>
    - README defines the exact ten-file package, nine checksum inputs, source authority, schemas, replay, redaction, retry, cleanup, and seal rules.
    - Six source identities and two rulesets match local/public protected state and sealed evidence checksums remain valid.
    - Timing has one accepted complete attempt per framework; the independently recomputed shared title result matches both beginner pages.
    - Every material claim in all six pages maps to one existing protected source or reviewed evidence selector.
    - Commands and practice events contain all required three-stage facts and pass credential/stage consistency scans.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence &amp;&amp; jq -e -s 'length == 6 and (map(.framework + "/" + .tag) | unique | length == 6) and all(.[]; .protected == true and (.tag_object | test("^[0-9a-f]{40}$")) and (.peeled_commit | test("^[0-9a-f]{40}$")))' "$root/source-identities.jsonl" &amp;&amp; jq -e -s '(map(select(.accepted == true and .evidence_complete == true)) | group_by(.framework) | length == 2) and all(.[]; .run_id != null and .cleanup_record_id != null)' "$root/timing.jsonl" &amp;&amp; jq -e -s 'all(.[]; .reviewed == true and (.source_artifact | length) &gt; 0 and (.record_selector | length) &gt; 0)' "$root/claims.jsonl"</automated>
  </verify>
  <done>The source, timing, command, claim, and practice half of the final evidence package is complete, consistent, and credential-free.</done>
</task>

<task type="auto">
  <name>Task 2: Audit screenshot provenance, visual review, and final cleanup</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/screenshots.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/cleanup.jsonl, .planning/phases/27-practice-backed-tutorial-series/evidence/review.txt</files>
  <read_first>
    - Both framework staging screenshot/cleanup/review records
    - All 24 final WebPs and their six adjacent MDX locations
    - Plan 27-05 per-image and desktop/mobile results
    - Every accepted/rejected attempt ownership terminal
    - `scripts/python-tutorial-assets.mjs` final image and bundle validators
  </read_first>
  <action>
Merge exactly 24 screenshot records in D-11 order. For every row require page,
filename, adjacent step, framework, stage, protected tag, source artifact and
record, evidence IDs, capture session/hash, redactions, layout, required and
forbidden tokens, width 1440, height 900, format WEBP, byte size below 200000,
unique SHA-256, nonblank status, metadata stripped, semantic acceptance,
`view_image` acceptance, and desktop/mobile review. Compare each record to the
actual asset and its one MDX reference; require exact four-name directory
allowlists and exact captions beside the matching step.

Merge every per-attempt cleanup terminal and add one final read-only audit
record. Query exact names and labels across Instance, App, Deployment,
ReplicaSet, Pod, Service, Ingress, Job, PostgreSQL cluster, PVC, Secret, and
ConfigMap; query both ownership roots, processes, port-forwards, local servers,
ports, browser sessions, clones, `.sealos`, state, logs, credentials, render,
image scratch, registry configs, temporary image versions, and ledgers. Run
the protected Django Stage 3 `--assert-clean-all`. Require zero in all final
fields while retaining public repositories, protected tags, accepted images,
final WebPs, and Phase 27 evidence.

Write `review.txt` with explicit source, claim, timing, 24 machine, 24 visual,
six desktop, six mobile, credential, cleanup, boundary, and final inventory
results. Remove `evidence/work/` only after its records are represented and
verified in the top-level files. Run a final credential scan over all nine
checksum inputs plus decoded/OCR text and fail on any sensitive or unresolved
value.
  </action>
  <acceptance_criteria>
    - `screenshots.jsonl` has exactly 24 unique page/filename and SHA-256 records matching actual bytes and one MDX reference each.
    - All 24 machine, semantic, redaction, provenance, `view_image`, desktop, and mobile statuses pass.
    - `cleanup.jsonl` contains every accepted/rejected attempt terminal and one cross-framework final audit with all defined counts zero.
    - `review.txt` records complete acceptance for all required review categories.
    - `evidence/work/`, raw capture, credential, clone, render, image, process, browser, and ledger paths are absent.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence &amp;&amp; jq -e -s 'length == 24 and (map(.page + "/" + .filename) | unique | length == 24) and (map(.sha256) | unique | length == 24) and all(.[]; .width == 1440 and .height == 900 and .format == "WEBP" and .bytes &lt; 200000 and .nonblank == true and .metadata_stripped == true and .desktop_review == "pass" and .mobile_review == "pass")' "$root/screenshots.jsonl" &amp;&amp; jq -e -s 'any(.[]; .scope == "phase27-final" and .zero_residue == true and ([.instance,.app,.deployment,.statefulset,.replicaset,.pod,.service,.ingress,.job,.postgresql_cluster,.pvc,.secret,.configmap,.process,.browser_session,.path,.ledger,.temporary_image] | all(. == 0)))' "$root/cleanup.jsonl" &amp;&amp; test ! -e "$root/work" &amp;&amp; /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh --assert-clean-all</automated>
  </verify>
  <done>All 24 screenshots and every practice attempt are fully traceable, visually accepted, credential-free, and cleaned to zero.</done>
</task>

<task type="auto">
  <name>Task 3: Seal checksums and verify exact source and publication boundaries</name>
  <files>.planning/phases/27-practice-backed-tutorial-series/evidence/checksums.txt</files>
  <read_first>
    - The nine reviewed top-level evidence inputs from Tasks 1 and 2
    - `phase27_base` and all five prior plan summaries
    - Current Git log and complete Phase 27 non-planning diff
    - Phase 28 boundary hashes, reference repository statuses, public identities, and sealed-evidence preflight hashes
    - `scripts/python-tutorial-assets.test.mjs` and `scripts/python-tutorial-assets.mjs`
  </read_first>
  <action>
After all semantic, source, credential, screenshot, visual, responsive, and
cleanup reviews pass, generate `checksums.txt` through a mode-0600 temporary
file and atomic rename. Hash exactly the previous nine relative paths in
LC_ALL=C sorted order, exclude the manifest itself, require nine entries, and
verify every checksum immediately. Run the coordinator `--check-bundle`,
snapshot all ten evidence files before/after, and require read-only equality.
Set all ten retained files to mode 0444 only after the final pass.

From the recorded `phase27_base`, compare the sorted non-planning diff with the
exact 32-path allowlist: two coordinator scripts, six locked MDX files, and 24
locked WebPs. Filter Git history to those paths and require exactly these six
subjects in order: `test(27-01): specify Python tutorial asset coordinator`,
`feat(27-01): add Python tutorial asset coordinator`,
`docs(27-04): add FastAPI tutorial series`,
`docs(27-04): add Django tutorial series`,
`docs(27-05): add FastAPI tutorial evidence cards`, and
`docs(27-05): add Django tutorial evidence cards`. Replay the RED missing-module
failure and GREEN suite from isolated committed worktrees.

Require zero diff for `tutorial-growth-data.ts`, `scripts/validate-tutorials.mjs`,
tutorial index metadata/page, source schema/loader, tutorial utilities,
metadata helper, detail route/layout, and build/static publication surfaces.
Run the current global validator and accept only its deterministic extra-six-
draft fail-closed result; run no Phase 28 catalog promotion, production build,
or static HTTP acceptance. Recheck target repositories, public tag/ruleset/
image identities, sealed Phase 23/25/26 bytes, final cleanup, source tree
status, focused coordinator tests, and TypeScript lint. Force-stage only the
ten evidence files in a separate planning/evidence commit
`docs(27-06): seal practice-backed tutorial evidence`; add no site source.
  </action>
  <acceptance_criteria>
    - `checksums.txt` is the last generated evidence file, has exactly nine sorted relative entries, and verifies all inputs.
    - The final coordinator bundle check passes and preserves all ten read-only evidence files.
    - The non-planning diff has exactly 32 expected paths and filtered source history has exactly six ordered subjects/scopes.
    - RED and GREEN replays pass their expected missing-module and green outcomes from isolated committed trees.
    - All Phase 28 boundaries are byte-identical and the current production validator remains deterministically fail-closed on the six drafts.
    - Target sources/tags/rulesets/images and sealed evidence are unchanged; cleanup is zero; tests and `npm run lint` pass.
  </acceptance_criteria>
  <verify>
    <automated>root=.planning/phases/27-practice-backed-tutorial-series/evidence &amp;&amp; test "$(wc -l &lt; "$root/checksums.txt" | tr -d ' ')" -eq 9 &amp;&amp; test "$(LC_ALL=C sort "$root/checksums.txt")" = "$(cat "$root/checksums.txt")" &amp;&amp; (cd "$root" &amp;&amp; shasum -a 256 -c checksums.txt) &amp;&amp; node --test scripts/python-tutorial-assets.test.mjs &amp;&amp; node scripts/python-tutorial-assets.mjs --check-bundle --repo-root "$PWD" --evidence-root "$root" --phase-base "$(sed -n 's/^phase27_base=//p' .planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md | head -1)" &amp;&amp; npm run lint &amp;&amp; /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh --assert-clean-all</automated>
  </verify>
  <done>Phase 27 is sealed with exact source/history, complete read-only evidence, zero residue, and an intact fail-closed Phase 28 publication boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Staging evidence to sealed package | Mutable per-framework records become durable review authority. |
| Source/assets to evidence joins | Public tutorial bytes must match claims and screenshot provenance. |
| Cleanup state to phase completion | Live and local resource absence determines whether evidence may be sealed. |
| Phase 27 to Phase 28 | Exact source completion must coexist with intentionally closed publication contracts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-25 | Tampering | Evidence package | high | mitigate | Normalize schemas, review semantics first, atomically generate sorted checksums last, verify, and seal read-only. |
| T-27-26 | Repudiation | Claim/image traceability | high | mitigate | Require hashes and selectors for every claim, 24 image records, source commits, captures, and adjacent steps. |
| T-27-27 | Information Disclosure | Final retained files | high | mitigate | Scan structured values, text, decoded/OCR pixels, and forbidden raw identities before checksum creation. |
| T-27-28 | Elevation of Privilege | Final cleanup | high | mitigate | Use exact name/label/ledger audits, inherited read-only cleanup, and preserve unrelated/public immutable resources. |
| T-27-29 | Tampering | Publication boundary | high | mitigate | Compare phase-base bytes, require expected global-validator failure, and prohibit all Phase 28-owned source changes. |
</threat_model>

<verification>
Run the complete coordinator and Node tests, nine-entry checksum verification,
credential and semantic scans, 32-path/6-subject inventory, isolated TDD replay,
immutable-source checks, read-only zero cleanup, TypeScript lint, and expected
global-validator fail-closed assertion.
</verification>

<success_criteria>
- All seven requirements and D-01 through D-26 are proven by retained artifacts.
- The evidence package has exactly ten files, nine checksum entries, and read-only verified bytes.
- Source contains exactly 32 new files in six exact source commits.
- Phase 28 promotion remains closed and receives a complete, clean handoff.
</success_criteria>

## Artifacts this phase produces

- Exact evidence package: nine checksum inputs plus `checksums.txt` at `.planning/phases/27-practice-backed-tutorial-series/evidence/`.
- Separate evidence commit: `docs(27-06): seal practice-backed tutorial evidence`.
- Final Phase 27 inventory/replay/cleanup handoff for Phase 28.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-06-SUMMARY.md`
with the ten evidence files, nine checksums, seven-requirement audit, D-01..D-26
coverage, exact 32 paths/six source subjects, TDD replay, boundary hashes,
immutable target proof, final cleanup, and Phase 28 handoff.
</output>
