---
phase: 27-practice-backed-tutorial-series
plan: "04"
type: execute
wave: 3
depends_on: ["27-02", "27-03"]
files_modified:
  - content/tutorials/deploy-fastapi-sealos/index.en.mdx
  - content/tutorials/fastapi-postgresql-sealos/index.en.mdx
  - content/tutorials/fastapi-production-deployment-sealos/index.en.mdx
  - content/tutorials/deploy-django-sealos/index.en.mdx
  - content/tutorials/django-postgresql-sealos/index.en.mdx
  - content/tutorials/django-production-deployment-sealos/index.en.mdx
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04, SHOT-01, SHOT-02]
must_haves:
  truths:
    - "D-01 through D-05 produce exactly two framework-local three-page English series with complete frontmatter, correct order, protected tag links, related links, and stage CTA."
    - "D-06 through D-10 make every stage-specific command and claim describe the continuous FastAPI Tasks API or Django Task Board at the matching protected source stage."
    - "D-22 applies one shared timing result to both beginner titles and each body reports its own observed duration and exact clock boundary."
    - "Each page contains exactly four unique D-11 image references with descriptive D-15 captions immediately adjacent to the proving step."
    - "All material claims join reviewed protected source or evidence records before prose is committed."
    - "D-25 keeps the catalog, global validator, index metadata, runtime helpers, and production publication surfaces unchanged."
  artifacts:
    - path: "content/tutorials/deploy-fastapi-sealos/index.en.mdx"
      provides: "FastAPI beginner deployment tutorial"
    - path: "content/tutorials/fastapi-postgresql-sealos/index.en.mdx"
      provides: "FastAPI PostgreSQL tutorial"
    - path: "content/tutorials/fastapi-production-deployment-sealos/index.en.mdx"
      provides: "FastAPI production tutorial"
    - path: "content/tutorials/deploy-django-sealos/index.en.mdx"
      provides: "Django beginner deployment tutorial"
    - path: "content/tutorials/django-postgresql-sealos/index.en.mdx"
      provides: "Django PostgreSQL tutorial"
    - path: "content/tutorials/django-production-deployment-sealos/index.en.mdx"
      provides: "Django production tutorial"
  key_links:
    - from: "each tutorial frontmatter series/seriesOrder"
      to: "lib/utils/tutorial-utils.ts"
      via: "existing series equality and ascending order"
      pattern: "seriesOrder"
    - from: "each material tutorial claim"
      to: "evidence/work/<framework>/claims.jsonl"
      via: "page, section, claim hash, authority selector"
      pattern: "claim_id"
    - from: "each Markdown image reference"
      to: "SCREENSHOT_CONTRACT"
      via: "exact page/filename and adjacent-step matrix"
      pattern: "/images/"
---

<objective>
Author the complete FastAPI and Django Framework Tutorial Series from reviewed
protected source and fresh/sealed practice evidence.

Purpose: Deliver all six reader journeys with current Sealos language, exact
stage continuity, shared measured-title logic, and locked image placement while
the Phase 28 publication gate remains closed.
Output: Six English MDX sources in two exact source commits.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-02-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-03-SUMMARY.md
@content/tutorials/deploy-nextjs-sealos/index.en.mdx
@content/tutorials/nextjs-postgresql-sealos/index.en.mdx
@content/tutorials/nextjs-production-deployment-sealos/index.en.mdx
@content/tutorials/deploy-react-sealos/index.en.mdx
@content/tutorials/nodejs-postgresql-sealos/index.en.mdx
@content/tutorials/nodejs-production-deployment-sealos/index.en.mdx
@source.config.ts
@lib/utils/tutorial-utils.ts
@app/[lang]/(home)/tutorials/[slug]/page.tsx
@AGENTS.md

<interfaces>
- FastAPI series: `sealos-skills-fastapi`; Django series: `sealos-skills-django`; orders beginner 1, advanced 2, production 3.
- Deploy CTA is `Start free on Sealos` -> `https://os.sealos.io`; later-stage CTA is `Open Sealos Skills` -> `/sealos-skills`.
- Protected reader links use `https://github.com/yangchuansheng/<repo>/tree/<stage-tag>` and clone commands use the tag name.
- `--check-drafts` requires six semantic drafts and reports `assets_pending=24`; image bytes become mandatory only under `--check-bundle` after Plan 27-05.
- Source commits are exactly `docs(27-04): add FastAPI tutorial series` and `docs(27-04): add Django tutorial series`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the FastAPI Framework Tutorial Series</name>
  <files>content/tutorials/deploy-fastapi-sealos/index.en.mdx, content/tutorials/fastapi-postgresql-sealos/index.en.mdx, content/tutorials/fastapi-production-deployment-sealos/index.en.mdx</files>
  <read_first>
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-01 through D-16 and D-21 through D-26
    - All eight reviewed `evidence/work/fastapi/` files and `27-02-SUMMARY.md`
    - The three matching protected-tag READMEs via `git -C /Users/longnv/bin/repo/sealos-fastapi-tutorial show <tag>:README.md`
    - The three Next.js stage templates and exact-four React/Node.js analogs listed in context
    - `source.config.ts`, `lib/utils/tutorial-utils.ts`, and `app/[lang]/(home)/tutorials/[slug]/page.tsx`
  </read_first>
  <action>
Create exactly the three D-01 FastAPI English sources. Use every D-04
frontmatter field, framework `FastAPI`, lowercase `fastapi` tag, exact series,
order, same-series related links, stage CTA, two to four evidence-backed FAQ
items, and HowTo steps matching body order. Compute the beginner title from
both accepted timing records: use `How to Deploy FastAPI on Sealos in 5
Minutes` only when both elapsed values are at most 300000 ms; otherwise use
`How to Deploy FastAPI on Sealos`. Report the observed FastAPI elapsed value
and the deploy-request-to-public-health clock boundary in the body.

Follow D-06 stage spines. Deploy covers native plugin entry, Stage 1 clone and
12 tests, Uvicorn/port 8000, analysis/template, DEPLOY, Runtime Truth Pass,
`.sealos/`, public health/docs/tasks, and next stage. PostgreSQL covers
SQLAlchemy 2, psycopg 3, Alembic `0001`, generated resources, Secret-backed
`DATABASE_URL`, migration before schema-aware readiness, public CRUD, process
restart persistence, and next stage. Production covers protected Stage 3,
immutable digest, same-image migration, two hardened replicas, UID/GID 10001,
one Uvicorn worker, HTTPS/docs/logs, monitoring/backup/update, A-B-A-B rollback
and explicit recovery, runbook, failures, and checklist.

Copy commands from protected source or minimally parameterize them with public
placeholders. Cite each protected tag URL and ensure every material statement
resolves to a reviewed claim row. Insert exactly the four locked Markdown image
references per page immediately after the proving step, each with descriptive
observed-result alt text. All 24 assets may still be absent. Before the Django
drafts exist, require `--check-drafts` to exit 1 with exactly one
`DRAFT_PAGE_SET_MISMATCH` listing the three locked Django paths and summary
`drafts_valid=3 drafts_pending=3 assets_pending=24 issues=1`. Stage and commit only these
three MDX files as `docs(27-04): add FastAPI tutorial series`.
  </action>
  <acceptance_criteria>
    - Exactly three FastAPI MDX files exist with full frontmatter, correct series/order, related links, CTA, FAQ, and HowTo.
    - The shared timing gate chooses the same title mode later used by Django and the FastAPI body reports its observed duration and clock boundary.
    - Each page links its matching protected FastAPI tag and every material claim has a reviewed source/evidence selector.
    - Each page has exactly four unique locked image references and useful visible alt captions adjacent to proving steps.
    - The intermediate coordinator result has exactly the expected missing-Django issue; the commit changes only the three FastAPI MDX files and has the exact required subject.
  </acceptance_criteria>
  <verify>
    <automated>set +e; output=$(node scripts/python-tutorial-assets.mjs --check-drafts --repo-root "$PWD" --evidence-root .planning/phases/27-practice-backed-tutorial-series/evidence --phase-base "$(sed -n 's/^phase27_base=//p' .planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md | head -1)" 2&gt;&amp;1); status=$?; set -e; test "$status" -eq 1 &amp;&amp; test "$(printf '%s\n' "$output" | rg -c '^DRAFT_PAGE_SET_MISMATCH')" -eq 1 &amp;&amp; printf '%s\n' "$output" | rg -F 'content/tutorials/deploy-django-sealos/index.en.mdx' &amp;&amp; printf '%s\n' "$output" | rg -F 'content/tutorials/django-postgresql-sealos/index.en.mdx' &amp;&amp; printf '%s\n' "$output" | rg -F 'content/tutorials/django-production-deployment-sealos/index.en.mdx' &amp;&amp; printf '%s\n' "$output" | rg -F 'drafts_valid=3 drafts_pending=3 assets_pending=24 issues=1' &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d' | sort)" = "$(printf '%s\n' content/tutorials/deploy-fastapi-sealos/index.en.mdx content/tutorials/fastapi-postgresql-sealos/index.en.mdx content/tutorials/fastapi-production-deployment-sealos/index.en.mdx | sort)" &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'docs(27-04): add FastAPI tutorial series'</automated>
  </verify>
  <done>The complete FastAPI series is source-grounded, title-gated, image-wired, and committed in one exact three-file source commit.</done>
</task>

<task type="auto">
  <name>Task 2: Author the Django Framework Tutorial Series</name>
  <files>content/tutorials/deploy-django-sealos/index.en.mdx, content/tutorials/django-postgresql-sealos/index.en.mdx, content/tutorials/django-production-deployment-sealos/index.en.mdx</files>
  <read_first>
    - `.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md` D-01 through D-16 and D-21 through D-26
    - All eight reviewed `evidence/work/django/` files and `27-03-SUMMARY.md`
    - The three matching protected-tag READMEs via `git -C /Users/longnv/bin/repo/sealos-django-tutorial show <tag>:README.md`
    - `taskboard/settings.py`, `tasks/views.py`, `tasks/templates/tasks/board.html`, production manifests, and the three Next.js stage templates
    - The Task 1 FastAPI beginner title to require shared gate parity
  </read_first>
  <action>
Create exactly the three D-01 Django English sources with full D-04
frontmatter, framework `Django`, lowercase `django` tag, exact series/order,
framework-local related links, CTAs, evidence-backed FAQ, and matching HowTo.
Use `How to Deploy Django on Sealos in 5 Minutes` only when the same shared
gate selected FastAPI's five-minute title; otherwise use `How to Deploy Django
on Sealos`. Report Django's own observed elapsed value and clock boundary.

Deploy covers native plugin entry, Stage 1 clone, SQLite migration/current/
drift, five tests, Task Board/admin, analysis/template, run-owned Host rewrite,
DEPLOY, Runtime Truth Pass, `.sealos/`, public health/board/admin, and next
stage. PostgreSQL covers psycopg 3, `tasks.0001_initial`, generated resources,
Secret-backed `DATABASE_URL`, repeatable Job, schema-aware readiness, CSRF
write, process restart, authenticated admin readback, and next stage.
Production covers protected Stage 3/digests, same-image migration, two hardened
Gunicorn replicas, Host edge, WhiteNoise/collectstatic hashed CSS, HTTPS,
board/admin/logs, monitoring/backup/update, A-B-A-B rollback/recovery, runbook,
failures, and checklist.

Use protected source commands and public placeholders, link each protected tag,
and resolve every material claim to reviewed authority. Insert exactly four
locked D-11 references per page with descriptive captions beside the proving
steps. Run the shared draft semantic gate while assets remain pending. Commit
only the three Django MDX files as
`docs(27-04): add Django tutorial series`, directly after the FastAPI source
commit.
  </action>
  <acceptance_criteria>
    - Exactly three Django MDX files exist with full frontmatter, correct series/order, related links, CTA, FAQ, and HowTo.
    - Both beginner pages use the same five-minute or evergreen title mode; Django reports its independent observed duration.
    - Each page links its matching protected Django tag and every material claim has reviewed authority.
    - Each page has exactly four unique locked image references and descriptive adjacent captions.
    - The commit changes only the three Django MDX files, has the exact subject, and directly follows the FastAPI tutorial commit.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/python-tutorial-assets.mjs --check-drafts --repo-root "$PWD" --evidence-root .planning/phases/27-practice-backed-tutorial-series/evidence --phase-base "$(sed -n 's/^phase27_base=//p' .planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md | head -1)" &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d' | sort)" = "$(printf '%s\n' content/tutorials/deploy-django-sealos/index.en.mdx content/tutorials/django-postgresql-sealos/index.en.mdx content/tutorials/django-production-deployment-sealos/index.en.mdx | sort)" &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'docs(27-04): add Django tutorial series' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'docs(27-04): add FastAPI tutorial series'</automated>
  </verify>
  <done>The complete Django series is source-grounded, title-parity-checked, image-wired, and committed in one exact three-file source commit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Protected source/evidence to prose | Operational claims and commands become public reader guidance. |
| Evidence paths to MDX | Screenshot references and captions bind future public assets to adjacent steps. |
| Draft content to publication | New discoverable MDX exists while catalog and global validation remain fail-closed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-15 | Tampering | Tutorial claims and commands | high | mitigate | Require protected tag URLs, reviewed claim selectors, source hashes, and draft semantic validation before each commit. |
| T-27-16 | Information Disclosure | Public prose and code blocks | high | mitigate | Allow only public placeholders and safe identities; scan out credentials, namespaces, cookies, Secret values, and private endpoints. |
| T-27-17 | Repudiation | Five-minute title | high | mitigate | Compute one shared result from two accepted complete timing records and report each measured interval in its page. |
| T-27-18 | Spoofing | Image captions and adjacency | medium | mitigate | Use the exact D-11 filename matrix and reviewed evidence record IDs before image generation. |
| T-27-19 | Elevation of Privilege | Phase 28 publication boundary | high | mitigate | Hash and compare every forbidden publication file and accept only the draft coordinator during Phase 27. |
</threat_model>

<verification>
Run the draft semantic gate, parse all frontmatter structurally, verify exact
series and CTA contracts, protected tag URLs, shared title mode, 24 unique
image references/captions, claim joins, credential scans, exact two commit
scopes, and unchanged Phase 28 boundaries.
</verification>

<success_criteria>
- All six English pages implement D-01 through D-10 and the matching stage journey.
- Shared title eligibility is evidence-derived and symmetric across frameworks.
- Every page owns four exact pending asset references and reviewed adjacent-step provenance.
- Only six MDX source paths changed in the two exact source commits.
</success_criteria>

## Artifacts this phase produces

- FastAPI MDX files: `deploy-fastapi-sealos`, `fastapi-postgresql-sealos`, `fastapi-production-deployment-sealos`.
- Django MDX files: `deploy-django-sealos`, `django-postgresql-sealos`, `django-production-deployment-sealos`.
- Source commits: `docs(27-04): add FastAPI tutorial series`, `docs(27-04): add Django tutorial series`.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-04-SUMMARY.md`
with the shared title decision, six-page metadata matrix, protected links,
claim/reference counts, exact commits, expected pending assets, and unchanged
Phase 28 hashes.
</output>
