---
phase: 27-practice-backed-tutorial-series
plan: "05"
type: execute
wave: 4
depends_on: ["27-04"]
files_modified:
  - public/images/deploy-fastapi-sealos/local-stage-validation.webp
  - public/images/deploy-fastapi-sealos/sealos-analysis-template.webp
  - public/images/deploy-fastapi-sealos/sealos-deployment-health.webp
  - public/images/deploy-fastapi-sealos/swagger-tasks-api.webp
  - public/images/fastapi-postgresql-sealos/database-ready-source.webp
  - public/images/fastapi-postgresql-sealos/sealos-postgresql-plan.webp
  - public/images/fastapi-postgresql-sealos/alembic-migration-complete.webp
  - public/images/fastapi-postgresql-sealos/persistent-crud-readback.webp
  - public/images/fastapi-production-deployment-sealos/production-state-redacted.webp
  - public/images/fastapi-production-deployment-sealos/immutable-rollout-health.webp
  - public/images/fastapi-production-deployment-sealos/domain-runtime-logs.webp
  - public/images/fastapi-production-deployment-sealos/rollback-recovery.webp
  - public/images/deploy-django-sealos/local-stage-validation.webp
  - public/images/deploy-django-sealos/sealos-analysis-template.webp
  - public/images/deploy-django-sealos/sealos-deployment-health.webp
  - public/images/deploy-django-sealos/task-board-admin.webp
  - public/images/django-postgresql-sealos/database-ready-source.webp
  - public/images/django-postgresql-sealos/sealos-postgresql-plan.webp
  - public/images/django-postgresql-sealos/django-migration-complete.webp
  - public/images/django-postgresql-sealos/persistent-board-admin.webp
  - public/images/django-production-deployment-sealos/production-state-redacted.webp
  - public/images/django-production-deployment-sealos/immutable-rollout-health.webp
  - public/images/django-production-deployment-sealos/domain-static-logs.webp
  - public/images/django-production-deployment-sealos/rollback-recovery.webp
autonomous: true
requirements: [SHOT-01, SHOT-02, CONT-01, CONT-02, CONT-03]
must_haves:
  truths:
    - "D-11 produces exactly the locked 24 unique filenames, four in each page-owned directory and one MDX reference per file."
    - "D-12 through D-14 render real reviewed evidence inside one consistent 1440x900 dark evidence-card system with deterministic redaction."
    - "D-16 accepts only RIFF/WEBP assets at exactly 1440x900 and strictly below 200000 bytes."
    - "D-17 verifies signature, dimensions, byte size, nonblank pixels, required visible tokens, redaction, provenance, digest, and per-image visual quality."
    - "All six draft pages render without horizontal overflow or incoherent overlap at 1440x900 and 390x844 development viewports."
    - "Two exact image-only source commits add FastAPI then Django assets while Phase 28 publication files remain unchanged."
  artifacts:
    - path: "public/images/deploy-fastapi-sealos/"
      provides: "Four FastAPI deploy evidence cards"
    - path: "public/images/fastapi-postgresql-sealos/"
      provides: "Four FastAPI PostgreSQL evidence cards"
    - path: "public/images/fastapi-production-deployment-sealos/"
      provides: "Four FastAPI production evidence cards"
    - path: "public/images/deploy-django-sealos/"
      provides: "Four Django deploy evidence cards"
    - path: "public/images/django-postgresql-sealos/"
      provides: "Four Django PostgreSQL evidence cards"
    - path: "public/images/django-production-deployment-sealos/"
      provides: "Four Django production evidence cards"
  key_links:
    - from: "evidence/work/<framework>/screenshots.jsonl"
      to: "renderEvidenceCard"
      via: "reviewed record ID, source/evidence joins, redactions, and browser capture"
      pattern: "record_id"
    - from: "each generated WebP"
      to: "validateEvidenceCard"
      via: "path, metadata, pixel, digest, and provenance record"
      pattern: "validateEvidenceCard"
    - from: "each MDX image"
      to: "tutorial detail ImageZoom"
      via: "root-relative exact path and descriptive alt caption"
      pattern: "image-caption"
---

<objective>
Generate, inspect, and commit all 24 practice-backed FastAPI and Django WebP
evidence cards from reviewed records.

Purpose: Make every tutorial step visually inspectable through the established
dark screenshot form while preserving real product/runtime truth and a strict
asset budget.
Output: Twelve FastAPI and twelve Django WebPs in two exact source commits,
plus completed screenshot provenance and six-page desktop/mobile review.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/27-practice-backed-tutorial-series/27-CONTEXT.md
@.planning/phases/27-practice-backed-tutorial-series/27-PATTERNS.md
@.planning/phases/27-practice-backed-tutorial-series/27-02-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-03-SUMMARY.md
@.planning/phases/27-practice-backed-tutorial-series/27-04-SUMMARY.md
@scripts/python-tutorial-assets.mjs
@public/images/nextjs-postgresql-sealos/
@public/images/nextjs-production-deployment-sealos/
@public/images/deploy-react-sealos/
@public/images/nodejs-production-deployment-sealos/
@app/[lang]/(home)/tutorials/[slug]/page.tsx
@app/[lang]/(home)/tutorials/[slug]/layout.tsx
@/Users/longnv/.agents/skills/agent-browser/SKILL.md
@AGENTS.md

<interfaces>
- `renderEvidenceCard` consumes one reviewed screenshot record, joined evidence, and optional real sanitized browser capture; it writes one allowlisted WebP.
- Real application cards retain a capture whose named session record includes final URL, title, language, body selector, capture digest, and closed state.
- Render sessions use exact `p27-render-<framework>-<record-id>` names, viewport 1440x900 at DPR 1, network-idle plus font/image readiness, no root scrollbar, and close after capture.
- FastAPI accent is restrained `#009688`; Django accent is `#44b78b` with a muted blue secondary; neutral dark visual grammar follows inspected Next.js/React/Node.js assets.
- Source commits are exactly `docs(27-05): add FastAPI tutorial evidence cards` and `docs(27-05): add Django tutorial evidence cards`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate and visually accept the 12 FastAPI evidence cards</name>
  <files>public/images/deploy-fastapi-sealos/local-stage-validation.webp, public/images/deploy-fastapi-sealos/sealos-analysis-template.webp, public/images/deploy-fastapi-sealos/sealos-deployment-health.webp, public/images/deploy-fastapi-sealos/swagger-tasks-api.webp, public/images/fastapi-postgresql-sealos/database-ready-source.webp, public/images/fastapi-postgresql-sealos/sealos-postgresql-plan.webp, public/images/fastapi-postgresql-sealos/alembic-migration-complete.webp, public/images/fastapi-postgresql-sealos/persistent-crud-readback.webp, public/images/fastapi-production-deployment-sealos/production-state-redacted.webp, public/images/fastapi-production-deployment-sealos/immutable-rollout-health.webp, public/images/fastapi-production-deployment-sealos/domain-runtime-logs.webp, public/images/fastapi-production-deployment-sealos/rollback-recovery.webp</files>
  <read_first>
    - The three FastAPI MDX files and adjacent D-11 steps from Plan 27-04
    - All reviewed `evidence/work/fastapi/` records, especially 12 screenshot specs and source/evidence selectors
    - `scripts/python-tutorial-assets.mjs` render and validation interfaces
    - Canonical visual analogs named in `27-PATTERNS.md`
    - `/Users/longnv/.agents/skills/agent-browser/SKILL.md` named-session, viewport, capture, evaluation, and cleanup rules
  </read_first>
  <action>
For each exact FastAPI record, verify source/evidence checksums and completed
redactions before rendering. Use terminal panels only with curated real command
and output lines; use browser panels only with the real sanitized public
Swagger/application capture from the accepted run. Escape all visible values,
retain safe public tag/domain/digest/revision/status facts, and show an explicit
omission marker for shortened output. Create a fixed no-network 1440x900 card
with stable dimensions, Arial/Menlo fonts, dark neutral surface, thin chrome,
small evidence eyebrow, concise heading, readable source panel, and FastAPI
teal accent.

Serve each local card from an owned loopback port. Open a fresh named
`agent-browser` session, set 1440x900 at DPR 1, wait for network idle, images,
and fonts, record final URL/title/body, and evaluate exact viewport, no root
scrollbar, all critical rectangles inside the canvas, and zero marked overlap.
Capture PNG, close the exact session, stop the server, then encode WebP with
metadata removed. Descend the documented quality ladder only as needed; keep
exact dimensions and target at most 150000 bytes while enforcing below 200000.

Run `validateEvidenceCard`, decode/pixel checks, expected-token and forbidden
credential scans, and SHA-256. Inspect every final WebP with `view_image` at
original detail; reject blank, clipped, overlapped, unreadable, awkwardly
wrapped, weakly framed, or semantically mismatched output and regenerate from
the same reviewed source. Update the 12 staging records with final metadata and
visual result. Commit only the 12 FastAPI WebPs as
`docs(27-05): add FastAPI tutorial evidence cards`.
  </action>
  <acceptance_criteria>
    - All 12 exact FastAPI paths exist once, have unique content digests, and match their adjacent MDX steps.
    - Every asset is metadata-free RIFF/WEBP, 1440x900, below 200000 bytes, nonblank, readable, and visibly uses the established dark-card grammar.
    - Required source tokens match reviewed evidence; forbidden credential and raw context scans pass.
    - Every render/browser session and local server is closed with no scratch retained.
    - `view_image` review passes 12/12 and the exact image-only commit subject/scope passes.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/python-tutorial-assets.mjs --check-bundle --repo-root "$PWD" --evidence-root .planning/phases/27-practice-backed-tutorial-series/evidence --framework FastAPI --phase-base "$(sed -n 's/^phase27_base=//p' .planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md | head -1)" &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d' | wc -l | tr -d ' ')" -eq 12 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'docs(27-05): add FastAPI tutorial evidence cards' &amp;&amp; test -z "$(agent-browser session list --json | jq -r '.data.sessions[]?' | grep '^p27-render-fastapi-' || true)"</automated>
  </verify>
  <done>All 12 FastAPI cards pass evidence, machine, visual, redaction, and exact source-commit gates.</done>
</task>

<task type="auto">
  <name>Task 2: Generate Django cards and inspect all six draft pages at desktop and mobile</name>
  <files>public/images/deploy-django-sealos/local-stage-validation.webp, public/images/deploy-django-sealos/sealos-analysis-template.webp, public/images/deploy-django-sealos/sealos-deployment-health.webp, public/images/deploy-django-sealos/task-board-admin.webp, public/images/django-postgresql-sealos/database-ready-source.webp, public/images/django-postgresql-sealos/sealos-postgresql-plan.webp, public/images/django-postgresql-sealos/django-migration-complete.webp, public/images/django-postgresql-sealos/persistent-board-admin.webp, public/images/django-production-deployment-sealos/production-state-redacted.webp, public/images/django-production-deployment-sealos/immutable-rollout-health.webp, public/images/django-production-deployment-sealos/domain-static-logs.webp, public/images/django-production-deployment-sealos/rollback-recovery.webp</files>
  <read_first>
    - The three Django MDX files and adjacent D-11 steps from Plan 27-04
    - All reviewed `evidence/work/django/` records, including board/admin and host/static captures
    - The Task 1 accepted FastAPI cards for cross-framework visual consistency
    - `scripts/python-tutorial-assets.mjs`, the canonical visual analogs, tutorial detail page/layout, and agent-browser skill
  </read_first>
  <action>
Generate and validate the 12 exact Django cards through the same reviewed
source, loopback render, uniquely named browser, fixed canvas, compression,
machine, credential, provenance, and `view_image` flow from Task 1. Use Django
green with muted blue secondary and preserve the same neutral visual grammar.
Use real Task Board/admin captures in the confirmed two-panel cards and real
host rewrite, migration, Gunicorn, WhiteNoise CSS, log, and rollback records;
retain no raw cookie, CSRF, administrator, Secret, or workspace value. Update
the 12 Django staging records and commit only the Django WebPs as
`docs(27-05): add Django tutorial evidence cards`, directly after the FastAPI
image commit.

Allocate an unused loopback port with a short Node `listen(0)` probe, install a
cleanup trap before launch, and start
`npm run dev -- --hostname 127.0.0.1 --port "$PORT"` in the background while
recording its PID and log under mode-0700 scratch. Poll one tutorial route with
`curl --fail --max-time 5` for at most 120 seconds; on timeout, reap the PID,
remove scratch, and fail the task. In one uniquely named page-review session per page, inspect all six
`/tutorials/<slug>` routes at 1440x900 and 390x844. At each viewport record the
final route/title/body, assert all four images load, captions equal useful alt
text, `scrollWidth <= innerWidth`, no fixed/stable element overlap, no clipped
long token, and coherent surrounding prose/adjacency. Capture review-only
screenshots in scratch, inspect them with `view_image`, record outcomes in each
framework `review.txt`, then close sessions, stop the server, and remove
review scratch. Require the recorded server PID to be reaped and its exact port
to have no listener before accepting the task. Run the complete draft bundle/image gate. Preserve the Phase
28 production validator, catalog status, index metadata, build, and static HTTP
ownership unchanged.
  </action>
  <acceptance_criteria>
    - All 12 exact Django paths pass unique digest, WEBP, 1440x900, byte, pixel, text, redaction, provenance, and visual checks.
    - Board/admin two-panel cards and production static/log cards display only reviewed real observations.
    - All six routes pass desktop and mobile no-overflow/no-overlap, four-image, caption, readability, and adjacency checks in development mode.
    - All page-review and render sessions, local servers, and scratch paths are absent afterward.
    - The exact Django image commit changes only 12 locked WebPs, directly follows the FastAPI image commit, and Phase 28 paths are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/python-tutorial-assets.mjs --check-bundle --repo-root "$PWD" --evidence-root .planning/phases/27-practice-backed-tutorial-series/evidence --phase-base "$(sed -n 's/^phase27_base=//p' .planning/phases/27-practice-backed-tutorial-series/27-01-SUMMARY.md | head -1)" &amp;&amp; npm run lint &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d' | wc -l | tr -d ' ')" -eq 12 &amp;&amp; test "$(git show -s --format=%s HEAD)" = 'docs(27-05): add Django tutorial evidence cards' &amp;&amp; test "$(git show -s --format=%s HEAD^)" = 'docs(27-05): add FastAPI tutorial evidence cards' &amp;&amp; test -z "$(agent-browser session list --json | jq -r '.data.sessions[]?' | grep '^p27-' || true)"</automated>
  </verify>
  <done>All 24 cards and all six development pages pass machine, semantic, visual, desktop, mobile, and cleanup review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Reviewed evidence to visual card | Operational text and real browser captures become public image pixels. |
| Local render server to browser | Generated HTML and captures cross a loopback browser boundary. |
| Public assets to tutorial layout | Dense 1440x900 cards render inside responsive reader pages. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-27-20 | Information Disclosure | Render specs and pixels | high | mitigate | Redact before render, scan source and decoded pixels/OCR, strip metadata, and delete raw capture/browser state. |
| T-27-21 | Tampering | Evidence-card content | high | mitigate | Join reviewed record IDs and checksums, compare required tokens, and reject any visually edited unsupported fact. |
| T-27-22 | Elevation of Privilege | Local renderer and paths | high | mitigate | Bind loopback, reject path escape/symlinks, use exact output allowlist, and close server/session after every card. |
| T-27-23 | Denial of Service | Oversized or unstable assets | medium | mitigate | Fix dimensions, bound byte size, use deterministic fonts/layout, and assert no scroll/overlap at capture time. |
| T-27-24 | Repudiation | Visual acceptance | medium | mitigate | Record machine results, `view_image` review, desktop/mobile review, SHA-256, and capture session for each asset. |
</threat_model>

<verification>
Require 24 exact unique assets, complete coordinator gate, per-image metadata/
pixel/token/digest checks, 24 `view_image` approvals, six desktop/mobile route
reviews, closed sessions/servers, exact two source commits, and byte-identical
Phase 28 boundaries.
</verification>

<success_criteria>
- Every D-11 output exists at its exact path and proves its adjacent step.
- Every asset meets D-12 through D-17 hard and visual contracts.
- Six tutorial pages remain professional and readable at desktop and mobile widths.
- Only 24 WebP source files changed in the two exact image commits.
</success_criteria>

## Artifacts this phase produces

- Twenty-four exact WebP assets listed in frontmatter.
- Twelve finalized FastAPI and twelve finalized Django screenshot provenance records.
- Source commits: `docs(27-05): add FastAPI tutorial evidence cards`, `docs(27-05): add Django tutorial evidence cards`.
- Development-only desktop/mobile review records; Phase 28 retains production build/static HTTP ownership.

<output>
Create `.planning/phases/27-practice-backed-tutorial-series/27-05-SUMMARY.md`
with 24 paths/digests/sizes, machine checks, 24 visual reviews, six desktop and
mobile results, exact commit scopes, and session/server/scratch cleanup.
</output>
