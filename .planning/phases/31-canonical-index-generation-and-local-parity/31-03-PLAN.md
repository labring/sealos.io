---
phase: 31-canonical-index-generation-and-local-parity
plan: "03"
type: tdd
wave: 3
depends_on: ["31-01", "31-02"]
files_modified:
  - scripts/verify-ai-faq-index.mjs
  - scripts/ai-faq-index.mjs
  - scripts/ai-faq-index.test.mjs
  - scripts/measure-build-pipeline.js
  - scripts/measure-build-pipeline.test.mjs
  - package.json
autonomous: true
requirements: [PARITY-01, DELIVERY-01]
requirements_addressed: [PARITY-01, DELIVERY-01]
decisions: [D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-16]
must_haves:
  truths:
    - "D-05 through D-08: read-only verification compares both directions, exact fields, relative order, and canonical bytes through shared seams."
    - "D-09 through D-12: stale page-index data fails before Next.js in build, analyzer, and direct timed-build paths with grouped actionable diagnostics."
    - "D-10: npm run build and npm run build:analyze invoke verify:ai-faq-index before their Next.js command, and inherited Vercel, Cloudflare, and Docker package paths receive the same preflight."
    - "D-13 through D-16: Node tests, package commands, legacy verifier preservation, and fast preflight commands form the maintainer contract."
  artifacts:
    - path: scripts/verify-ai-faq-index.mjs
      provides: Read-only source/index parity CLI with injected paths and streams.
    - path: package.json
      provides: generate:ai-faq-index, verify:ai-faq-index, and test:ai-faq-index scripts plus build/analyzer preflight.
    - path: scripts/measure-build-pipeline.js
      provides: AI FAQ parity as the first direct build and analyzer stage.
    - path: scripts/measure-build-pipeline.test.mjs
      provides: stage-order and fail-fast coverage for the parity preflight.
  key_links:
    - from: package.json
      to: scripts/verify-ai-faq-index.mjs
      via: build and build:analyze prefix the Next command with npm run verify:ai-faq-index.
      pattern: verify:ai-faq-index
    - from: scripts/measure-build-pipeline.js
      to: scripts/verify-ai-faq-index.mjs
      via: first measured stage executes npm run verify:ai-faq-index before direct Next invocation.
      pattern: AI FAQ parity
---

<objective>
Expose read-only source/index parity and make it the shared preflight before
every repository-owned static-export entry point.

Purpose: Stop stale committed AI FAQ index data before Next.js consumes static
export time while keeping mutation under the explicit generator command.
Output: A deterministic verifier CLI, package scripts, timed-build stage wiring,
and complete source-audit evidence.
</objective>

<context>
@.planning/phases/31-canonical-index-generation-and-local-parity/31-CONTEXT.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-RESEARCH.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-VALIDATION.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-PATTERNS.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-01-SUMMARY.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-02-SUMMARY.md
@scripts/ai-faq-index.mjs
@scripts/ai-faq-index.test.mjs
@scripts/verify-ai-faq-slugs.mjs
@scripts/measure-build-pipeline.js
@scripts/measure-build-pipeline.test.mjs
@package.json
@.github/workflows/deploy.yml
@.github/workflows/preview.yml
@.github/workflows/deploy-cloudflare.yml
@.github/workflows/preview-cloudflare.yml
@Dockerfile
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Define the read-only verifier CLI and failure contract</name>
  <files>scripts/verify-ai-faq-index.mjs, scripts/ai-faq-index.test.mjs, scripts/ai-faq-index.mjs</files>
  <read_first>
    - scripts/ai-faq-index.mjs from Plan 31-01 for comparison and formatter seams.
    - scripts/generate-ai-faq-index.mjs from Plan 31-02 for injected CLI path and stream conventions.
    - scripts/verify-ai-faq-slugs.mjs for current source-wide verifier availability.
    - scripts/check-deployment-parity.js for returned exit-code and stream-capture shape.
    - public/ai-faqs.en.json for the tracked target that must remain read-only during verification.
  </read_first>
  <behavior>
    - RED case: valid temporary source and index exit 0 with a concise English success count.
    - RED case: each membership, duplicate, identifier, schema, ordering, field, and canonical-byte mutation exits 1, names public/ai-faqs.en.json, keeps every repository file byte-identical, prints category totals/details, and ends with npm run generate:ai-faq-index.
    - GREEN case: runVerifyFAQIndex accepts sourceDirectory, indexPath, stdout, and stderr and delegates all semantics and formatting to ai-faq-index.mjs.
  </behavior>
  <action>
    RED: add process-level and direct injected-boundary cases to scripts/ai-faq-index.test.mjs. Snapshot fixture index bytes before each verifier call and assert unchanged bytes afterward. Require D-05 ID alignment, D-06 category coverage, D-07 stable grouped detail output, D-08 canonical-byte comparison, and D-12 output naming public/ai-faqs.en.json plus npm run generate:ai-faq-index.

    GREEN: create scripts/verify-ai-faq-index.mjs with runVerifyFAQIndex. Load source through loadCanonicalFAQSource, parse the requested index, call compareFAQIndexRecords and formatFAQIndexReport, write success to stdout and failures to stderr, and return 0 or 1 through the main entrypoint. Keep this command read-only for D-09 and keep the current test:ai-faq-slugs and verify:ai-faq-routes commands available for their established and Phase 32 contracts.

    REFACTOR: retain thin CLI control flow and export only testable path/stream seams. Preserve the source/index semantic boundary and leave sitemap/detail-route checks with Phase 32.
  </action>
  <acceptance_criteria>
    - npm run verify:ai-faq-index can report every D-06 category and returns 1 for representative stale fixture data.
    - Verification reads source and index only; byte snapshots before and after failure remain equal.
    - Failure diagnostics follow the D-07 order, cap each category at 20 details, include record data, and provide the exact regeneration command.
    - Valid real corpus plus regenerated asset returns zero findings and exit 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ai-faq-index &amp;&amp; npm run verify:ai-faq-index</automated>
  </verify>
  <done>The repository has a source-to-index read-only parity command with stable semantic and byte diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Wire parity before package and timed Next builds</name>
  <files>package.json, scripts/measure-build-pipeline.js, scripts/measure-build-pipeline.test.mjs, scripts/ai-faq-index.test.mjs</files>
  <read_first>
    - package.json for current build, build:analyze, build:timed, build:analyze:timed, test:ai-faq-slugs, and verify:ai-faq-routes command placement.
    - scripts/measure-build-pipeline.js for stage factory, direct Next command, and fail-fast loop.
    - scripts/measure-build-pipeline.test.mjs for exact stage-list and failed-stage assertions.
    - scripts/verify-ai-faq-index.mjs from Task 1 for the command that runs before static export.
    - .github/workflows/deploy.yml, .github/workflows/preview.yml, .github/workflows/deploy-cloudflare.yml, .github/workflows/preview-cloudflare.yml, and Dockerfile for inherited package-build acceptance surfaces.
  </read_first>
  <action>
    Add package commands exactly named generate:ai-faq-index, verify:ai-faq-index, and test:ai-faq-index with Node entrypoints. Prefix build and build:analyze with npm run verify:ai-faq-index && before their existing Next command; preserve normalize-root-locale ordering. Preserve test:ai-faq-slugs and verify:ai-faq-routes as separate commands under D-11 and D-16.

    Add an AI FAQ parity stage as the first build and analyze stage in scripts/measure-build-pipeline.js, ahead of pre generated diff guard and direct Next invocation. Update scripts/measure-build-pipeline.test.mjs exact stage arrays and add a parity-failure test that observes no Next binary spawn and only completed stage timing output. Extend focused index tests with package-string assertions that build and build:analyze run the fast read-only verifier first.

    Treat Vercel, Cloudflare, and Docker files as acceptance surfaces: preserve their package build paths and record one Node.js 20 CI run plus one Vercel build log demonstrating verifier completion before Next. The package preflight must leave the checkout unchanged when it succeeds or fails.
  </action>
  <acceptance_criteria>
    - package.json exposes all three D-16 commands and retains test:ai-faq-slugs plus verify:ai-faq-routes.
    - npm run build and npm run build:analyze execute verify:ai-faq-index before Next.js starts.
    - Timed build and analyzer modes list AI FAQ parity first and stop before Next after a non-zero parity result.
    - A deliberately stale temporary index emits actionable diagnostics, exits non-zero, and leaves its fixture and checkout bytes unchanged.
    - Node.js 20 CI and Vercel build-log evidence are recorded in the execution summary before Phase 31 acceptance.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:ai-faq-index &amp;&amp; node --test scripts/measure-build-pipeline.test.mjs &amp;&amp; npm run verify:ai-faq-index &amp;&amp; npm run lint</automated>
  </verify>
  <done>Every repository-owned static-export route receives the same read-only parity gate before Next.js work.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: build preflight and diagnostic integrity.

| Threat | Severity | Disposition | Planned control |
|---|---|---|---|
| T-31-01 stale, malformed, duplicate, or drifted index reaches static export | medium | mitigate | D-05/D-06 verifier runs before Next and exits non-zero with structured findings. |
| T-31-03 diagnostics hide detail or terminal control values obscure actionable data | medium | mitigate | D-07 shared formatter totals every class, caps details, and JSON-stringifies values. |
| Direct timed builds bypass package scripts | medium | mitigate | D-10 first timing stage invokes the same verifier and tests assert fail-fast behavior. |
| Deployment path bypasses package preflight | low | mitigate | D-10 acceptance records Node 20 CI and Vercel package-build log evidence; Cloudflare and Docker inherit npm run build. |
</threat_model>

## Artifacts this phase produces

- `scripts/verify-ai-faq-index.mjs` — `runVerifyFAQIndex` plus read-only CLI entrypoint.
- `package.json` — `generate:ai-faq-index`, `verify:ai-faq-index`, and `test:ai-faq-index` package commands with build/analyze preflight order.
- `scripts/measure-build-pipeline.js` — AI FAQ parity measured stage.
- `scripts/measure-build-pipeline.test.mjs` — first-stage and fail-fast regression coverage.

## Four-Source Coverage Audit

| Source | ID | Feature or constraint | Plan task | Status |
|---|---|---|---|---|
| GOAL | Phase 31 | deterministic regeneration and precise mismatch failure | 31-01 Tasks 1-2; 31-02 Tasks 1-2; 31-03 Tasks 1-2 | COVERED |
| REQ | SOURCE-01 | source collection is the only hand-edited input | 31-01 Task 1; 31-02 Tasks 1-2 | COVERED |
| REQ | SOURCE-02 | 2,000 exact projected numeric-order records and drift repair | 31-01 Task 1; 31-02 Task 2 | COVERED |
| REQ | PARITY-01 | bidirectional local parity with record-level detail | 31-01 Task 2; 31-03 Task 1 | COVERED |
| REQ | DELIVERY-01 | build and CI preflight before static export | 31-03 Task 2 | COVERED |
| RESEARCH | 32-read bound and numeric source order | 31-01 Task 1 | COVERED |
| RESEARCH | 16-category controlled-cascade comparison and 20-detail display cap | 31-01 Task 2; 31-03 Task 1 | COVERED |
| RESEARCH | same-directory atomic rename and exact cleanup | 31-02 Task 1 | COVERED |
| RESEARCH | FAQSearch four-field contract and asset compactness | 31-01 Task 1; 31-02 Task 2 | COVERED |
| RESEARCH | direct timed-build parity stage | 31-03 Task 2 | COVERED |
| RESEARCH | Node.js 20 CI and Vercel package-build evidence | 31-03 Task 2 | COVERED |
| CONTEXT | D-01 | numeric filename identity, contiguous IDs, source uniqueness, numeric order | 31-01 Task 1 | COVERED |
| CONTEXT | D-02 | exact four-field projection and input validation | 31-01 Task 1 | COVERED |
| CONTEXT | D-03 | compact deterministic bytes and no trailing newline | 31-01 Task 1; 31-02 Tasks 1-2 | COVERED |
| CONTEXT | D-04 | explicit atomic generator and concise summary | 31-02 Task 1 | COVERED |
| CONTEXT | D-05 | numeric-ID alignment plus independent uniqueness and relative order | 31-01 Task 2; 31-03 Task 1 | COVERED |
| CONTEXT | D-06 | complete mismatch-class taxonomy | 31-01 Task 2; 31-03 Task 1 | COVERED |
| CONTEXT | D-07 | stable totals, 20-detail cap, serialized values, regeneration guidance | 31-01 Task 2; 31-03 Task 1 | COVERED |
| CONTEXT | D-08 | shared source/projection/serialization boundary and read-only verifier | 31-01 Tasks 1-2; 31-02 Task 1; 31-03 Task 1 | COVERED |
| CONTEXT | D-09 | parity preflight is read-only before static export | 31-03 Tasks 1-2 | COVERED |
| CONTEXT | D-10 | build, analyzer, and inherited CI/package gate order | 31-03 Task 2 | COVERED |
| CONTEXT | D-11 | Phase 31 source-index focus and preserved slug/route commands | 31-03 Tasks 1-2 | COVERED |
| CONTEXT | D-12 | actionable stale preflight output and unchanged files | 31-03 Task 1 | COVERED |
| CONTEXT | D-13 | node:test and node:assert/strict | 31-01 Task 1; 31-02 Task 1; 31-03 Task 1 | COVERED |
| CONTEXT | D-14 | unique temporary fixtures and full-corpus smoke | 31-01 Task 1; 31-02 Task 1 | COVERED |
| CONTEXT | D-15 | determinism, malformed/duplicate/drift, bytes, status, and diagnostics matrix | 31-01 Tasks 1-2; 31-02 Task 1; 31-03 Task 1 | COVERED |
| CONTEXT | D-16 | generator, verifier, test commands and retained legacy commands | 31-02 Task 2; 31-03 Task 2 | COVERED |

<verification>
Run npm run test:ai-faq-index, npm run test:ai-faq-slugs, npm run verify:ai-faq-index, node --test scripts/measure-build-pipeline.test.mjs, and npm run lint after Wave 3. Before phase verification, run the full suite, confirm a clean read-only parity diff, then record npm run build and npm run build:analyze on Node.js 20 CI.
</verification>

<success_criteria>
- The verifier detects semantic and byte drift without mutation and guides maintainers to the explicit generator.
- Standard and timed static-export paths fail before Next.js when source and index diverge.
- All four requirements and D-01 through D-16 are represented in executor-ready tasks with automated verification.
</success_criteria>

<output>
Create 31-03-SUMMARY.md with package command output, timed-stage order/failure proof, Node.js 20 CI URL, Vercel build-log evidence, commands, and deviations.
</output>
