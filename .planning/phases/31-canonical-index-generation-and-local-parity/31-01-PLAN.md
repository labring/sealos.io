---
phase: 31-canonical-index-generation-and-local-parity
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/ai-faq-index.mjs
  - scripts/ai-faq-fixture.mjs
  - scripts/ai-faq-index.test.mjs
autonomous: true
requirements: [SOURCE-01, SOURCE-02, PARITY-01]
requirements_addressed: [SOURCE-01, SOURCE-02, PARITY-01]
decisions: [D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-13, D-14, D-15]
must_haves:
  truths:
    - "D-01: every source filename resolves to a unique contiguous numeric ID and full slug in numeric order."
    - "D-02 and D-03: canonical records preserve category, question, description, and slug in fixed key order as compact bytes."
    - "D-05 through D-08: shared semantic comparison emits deterministic identity, ordering, field, and serialization findings."
    - "D-13 through D-15: Node built-in tests cover isolated fixtures and a read-only 2,000-record smoke."
  artifacts:
    - path: scripts/ai-faq-index.mjs
      provides: Shared source loading, projection, canonical serialization, comparison, and report formatting seams.
    - path: scripts/ai-faq-fixture.mjs
      provides: Compatibility adapter that retains loadFAQPages and groupByNormalizedSlug.
    - path: scripts/ai-faq-index.test.mjs
      provides: RED/GREEN fixture matrix and complete-corpus smoke coverage.
  key_links:
    - from: scripts/ai-faq-fixture.mjs
      to: scripts/ai-faq-index.mjs
      via: loadFAQPages delegates validated numeric source records into the legacy page shape.
      pattern: loadCanonicalFAQSource
    - from: scripts/ai-faq-index.test.mjs
      to: scripts/ai-faq-index.mjs
      via: direct ESM imports exercise domain functions before CLI wiring.
      pattern: node:test
---

<objective>
Create the shared canonical AI FAQ index domain through one RED/GREEN/REFACTOR
cycle.

Purpose: Give generation, parity verification, existing slug verification, and
focused tests one deterministic source, projection, comparison, and diagnostic
boundary.
Output: A reusable ESM module, a compatible fixture adapter, and behavior-first
tests for source and index semantics.
</objective>

<context>
@.planning/phases/31-canonical-index-generation-and-local-parity/31-CONTEXT.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-RESEARCH.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-VALIDATION.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-PATTERNS.md
@scripts/ai-faq-fixture.mjs
@scripts/verify-ai-faq-slugs.mjs
@scripts/generate-apps-api.test.mjs
@scripts/check-deployment-parity.test.mjs
@lib/utils/faq-slug.mjs
@AGENTS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Define the canonical source loader and four-field projection</name>
  <files>scripts/ai-faq-index.test.mjs, scripts/ai-faq-index.mjs, scripts/ai-faq-fixture.mjs</files>
  <read_first>
    - scripts/ai-faq-fixture.mjs for the existing loadFAQPages and groupByNormalizedSlug public seam.
    - scripts/verify-ai-faq-slugs.mjs for the exact 2,000-source assertion and ESM import pattern.
    - scripts/generate-apps-api.test.mjs and scripts/check-deployment-parity.test.mjs for node:test, temporary fixture, and injected-boundary conventions.
    - .planning/phases/31-canonical-index-generation-and-local-parity/31-CONTEXT.md for D-01 through D-04 and D-13 through D-16.
  </read_first>
  <behavior>
    - RED case: filenames 1-alpha.en.json through 3-gamma.en.json load as IDs 1, 2, and 3 regardless of directory enumeration order.
    - RED case: malformed names, duplicate numeric IDs, duplicate full slugs, gaps in the expected ID range, non-regular entries, invalid JSON, and blank projected fields each produce structured findings with source path and ID context.
    - RED case: a valid source record projects exactly { category, question, description, slug } in that key order; question uses source title and slug uses the filename without .en.json.
    - GREEN result: bounded batches of 32 reads return validated source records and preserve source strings after whitespace-aware validation.
  </behavior>
  <action>
    RED: add node:test cases in scripts/ai-faq-index.test.mjs using unique mkdtemp fixture roots and node:assert/strict. Establish failing imports for parseFAQSourceFilename, loadCanonicalFAQSource, projectFAQIndexRecord, and serializeCanonicalFAQIndex. Cover D-01 numeric identity, contiguous 1..expectedCount validation, exact full-slug uniqueness, D-02 exact four-field projection, and D-13/D-14 isolated filesystem cleanup.

    GREEN: add scripts/ai-faq-index.mjs with explicit node:fs/promises and node:path imports. Parse canonical filenames with ^([1-9]\\d*)-(.+)\\.en\\.json$, require regular entries, sort IDs numerically with deterministic filename tie-breaking, process reads through a named 32-item bounded batch constant, validate non-empty trimmed title/description/category values, and create fresh fixed-key projected objects. Implement D-03 compact JSON.stringify serialization without a trailing newline. Export the pure parsing, loading, validation, projection, and serialization seams used by later plans.

    REFACTOR: change scripts/ai-faq-fixture.mjs so loadFAQPages maps shared validated source records back to { slug, url, data } while retaining groupByNormalizedSlug and its normalized-group output. Preserve the current exact-slug verifier and Phase 32 route-verifier import contract.
  </action>
  <acceptance_criteria>
    - scripts/ai-faq-index.mjs exports the loader, projection, and serialization seams required by generator, verifier, adapter, and tests.
    - The source loader returns numeric ID order 1..2000 for the committed corpus and rejects malformed, duplicate, sparse, invalid, or blank source input with structured findings.
    - Every projected record has exactly category, question, description, slug in that order, and canonical bytes end with ] rather than a newline.
    - Existing npm run test:ai-faq-slugs continues to consume loadFAQPages and groupByNormalizedSlug.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/ai-faq-index.test.mjs &amp;&amp; npm run test:ai-faq-slugs</automated>
  </verify>
  <done>RED cases exist before production behavior, GREEN satisfies the loader/projection contract, and REFACTOR retains the legacy fixture API.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Define bidirectional parity findings and stable diagnostic formatting</name>
  <files>scripts/ai-faq-index.test.mjs, scripts/ai-faq-index.mjs</files>
  <read_first>
    - scripts/ai-faq-index.mjs after Task 1 for its validated source-record and canonical-byte seams.
    - scripts/verify-ai-faq-slugs.mjs for concise English CLI failure conventions.
    - scripts/check-deployment-parity.js for injectable stdout/stderr status handling.
    - .planning/phases/31-canonical-index-generation-and-local-parity/31-RESEARCH.md for the 16-category finding order and controlled-cascade rules.
  </read_first>
  <behavior>
    - RED case: valid unique IDs align source and index; exact full slug, question, description, and category compare as fields rather than identities.
    - RED case: source-only, index-only, duplicate ID, duplicate slug, malformed identifier, invalid schema, ordering, four field drifts, and non-canonical bytes yield separately counted categories.
    - RED case: a one-record membership error keeps ordering output bounded to valid unique relative-order records; a 21-finding category prints 20 deterministic detail lines and total 21.
    - GREEN result: expected and actual values render through JSON.stringify on one line, and failures end with npm run generate:ai-faq-index plus status 1 at the thin CLI boundary.
  </behavior>
  <action>
    RED: extend scripts/ai-faq-index.test.mjs with compact source/index arrays and fixture files that mutate one dimension per case. Assert D-05 numeric-ID map alignment, independent source/index duplicate groups, and relative order among valid unique IDs. Assert D-06 categories in this fixed sequence: malformed source identifiers, malformed index identifiers, invalid source projection schemas, invalid index projection schemas, duplicate source IDs, duplicate index IDs, duplicate source slugs, duplicate index slugs, source-only IDs, index-only IDs, ordering drift, slug drift, question drift, description drift, category drift, non-canonical serialization.

    GREEN: add compareFAQIndexRecords and formatFAQIndexReport to scripts/ai-faq-index.mjs. Build Map<number, Record[]> and Map<string, Record[]> for both sides, compare fields only for one valid record on each side, include one-based source/index positions where available, retain complete category totals, cap D-07 detail output at 20 ordered records per populated category, and JSON.stringify expected/actual values. Use D-08 shared serialization to report byte drift after parsed semantic findings.

    REFACTOR: keep comparison pure and formatting side-effect free so later generator and verifier inject streams and process status. Keep Phase 31 scope centered on source-to-index semantics; Phase 32 retains sitemap and detail-route set checks.
  </action>
  <acceptance_criteria>
    - Comparison distinguishes every D-06 finding category and reports numeric ID, positions, field name, expected value, and actual value where the category supports those fields.
    - The formatter has stable category and finding order, reports every category total, caps detail lines at 20, and includes npm run generate:ai-faq-index in failure text.
    - Canonical byte drift is detected even when parsed records remain semantically equal.
    - The real 2,000-record smoke reads the committed source collection through shared seams only.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/ai-faq-index.test.mjs</automated>
  </verify>
  <done>All parity cases have RED/GREEN evidence, stable structured diagnostics, and one shared domain implementation.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: local repository data ingestion and deterministic diagnostic output.

| Threat | Severity | Disposition | Planned control |
|---|---|---|---|
| T-31-01 malformed name, symlink, directory entry, invalid JSON, or blank source field reaches the public projection | medium | mitigate | D-01/D-02 loader requires canonical filename parsing, regular Dirent entries, exact scalar validation, and complete finding output before publication. |
| T-31-03 2,000 simultaneous reads consume descriptors or adversarial values alter terminal layout | medium | mitigate | Use sequential 32-item batches and JSON.stringify values in D-07 diagnostics. |
| Duplicate identity hides a later field mismatch | low | mitigate | D-05 builds grouped numeric-ID and full-slug maps, then limits field comparison to valid unique pairs. |
</threat_model>

## Artifacts this phase produces

- `scripts/ai-faq-index.mjs` — `parseFAQSourceFilename`, `loadCanonicalFAQSource`, `projectFAQIndexRecord`, `serializeCanonicalFAQIndex`, `compareFAQIndexRecords`, and `formatFAQIndexReport` exported seams.
- `scripts/ai-faq-index.test.mjs` — temporary-fixture, diagnostic-cap, and 2,000-record smoke tests.
- `scripts/ai-faq-fixture.mjs` — shared-loader compatibility adapter preserving `loadFAQPages` and `groupByNormalizedSlug`.

## Source Coverage Audit

| Source | ID | Covered by | Status |
|---|---|---|---|
| GOAL | canonical source-to-index semantic boundary | 31-01 Tasks 1-2 | COVERED |
| REQ | SOURCE-01 | 31-01 Task 1 | COVERED |
| REQ | SOURCE-02 | 31-01 Tasks 1-2 | COVERED |
| REQ | PARITY-01 | 31-01 Task 2 | COVERED |
| RESEARCH | numeric ordering, 32-read bound, structured controlled-cascade reports | 31-01 Tasks 1-2 | COVERED |
| CONTEXT | D-01, D-02, D-03, D-05, D-06, D-07, D-08, D-13, D-14, D-15 | 31-01 Tasks 1-2 | COVERED |

<verification>
Run node --test scripts/ai-faq-index.test.mjs after each task commit. Run node --test scripts/ai-faq-index.test.mjs && npm run test:ai-faq-slugs after the fixture-adapter change. Record RED, GREEN, and any behavior-neutral REFACTOR commit in 31-01-SUMMARY.md.
</verification>

<success_criteria>
- The shared domain produces numeric source order, exact projection, compact canonical bytes, semantic parity reports, and stable diagnostics.
- The existing FAQ slug verifier receives its legacy page shape through the adapter.
- Focused tests cover the whole D-01/D-02/D-03/D-05/D-06/D-07/D-08/D-13/D-14/D-15 contract before generator and build wiring.
</success_criteria>

<output>
Create 31-01-SUMMARY.md with RED/GREEN/REFACTOR commits, commands, fixture coverage, 2,000-record smoke result, and deviations.
</output>
