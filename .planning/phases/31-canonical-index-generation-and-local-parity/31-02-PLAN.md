---
phase: 31-canonical-index-generation-and-local-parity
plan: "02"
type: tdd
wave: 2
depends_on: ["31-01"]
files_modified:
  - scripts/generate-ai-faq-index.mjs
  - scripts/ai-faq-index.mjs
  - scripts/ai-faq-index.test.mjs
  - public/ai-faqs.en.json
autonomous: true
requirements: [SOURCE-01, SOURCE-02]
requirements_addressed: [SOURCE-01, SOURCE-02]
decisions: [D-03, D-04, D-08, D-13, D-14, D-15]
must_haves:
  truths:
    - "D-03 and D-04: generator emits compact canonical bytes only after complete validation, then atomically replaces the public asset."
    - "SOURCE-01 and SOURCE-02: public/ai-faqs.en.json contains source-derived 2,000 numeric-order records with the corrected 28 slugs and five descriptions."
    - "D-13 through D-15: focused Node tests prove deterministic generation and failure integrity."
  artifacts:
    - path: scripts/generate-ai-faq-index.mjs
      provides: Explicit source-to-index maintainer generator with injectable paths and streams.
    - path: public/ai-faqs.en.json
      provides: Canonical compact four-field client index derived from 2,000 English source files.
    - path: scripts/ai-faq-index.test.mjs
      provides: Atomic publication and double-generation integration cases.
  key_links:
    - from: scripts/generate-ai-faq-index.mjs
      to: scripts/ai-faq-index.mjs
      via: generator calls shared loader and serializer before one same-directory publication boundary.
      pattern: generateFAQIndex
    - from: scripts/generate-ai-faq-index.mjs
      to: public/ai-faqs.en.json
      via: exclusive sibling temporary file followed by rename.
      pattern: randomUUID
---

<objective>
Publish the canonical page-index asset through an explicit atomic generator.

Purpose: Make the source JSON collection the only hand-edited input while
preserving FAQSearch's compact static asset contract.
Output: A testable generator command, a regenerated 2,000-record asset, and
failure-integrity evidence.
</objective>

<context>
@.planning/phases/31-canonical-index-generation-and-local-parity/31-CONTEXT.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-RESEARCH.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-VALIDATION.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-PATTERNS.md
@.planning/phases/31-canonical-index-generation-and-local-parity/31-01-SUMMARY.md
@scripts/ai-faq-index.mjs
@scripts/generate-apps-api.js
@scripts/ai-faq-index.test.mjs
@app/[lang]/(home)/ai-quick-reference/components/FAQSearch.tsx
@public/ai-faqs.en.json
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Define atomic generator behavior before implementation</name>
  <files>scripts/ai-faq-index.test.mjs, scripts/generate-ai-faq-index.mjs</files>
  <read_first>
    - scripts/ai-faq-index.mjs from Plan 31-01 for shared load, projection, and serialization seams.
    - scripts/generate-apps-api.js for concise English generated-data orchestration and entrypoint shape.
    - scripts/ai-faq-index.test.mjs for existing temporary fixture and injected stream conventions.
    - app/[lang]/(home)/ai-quick-reference/components/FAQSearch.tsx for the exact four-field asset contract.
  </read_first>
  <behavior>
    - RED case: two consecutive generation calls over one valid fixture produce byte-identical compact output and no trailing newline.
    - RED case: validation, write, or rename failure retains the destination's original bytes and removes the exact owned temporary path.
    - RED case: success writes a sibling temporary file with exclusive creation, renames it over the destination, and emits English record count, output path, and byte count.
    - GREEN result: generation accepts injected sourceDirectory, outputPath, stdout, stderr, and filesystem seam inputs for test isolation.
  </behavior>
  <action>
    RED: extend scripts/ai-faq-index.test.mjs with unique source/output fixtures and injected write/rename failures. Capture status and streams for a forthcoming runGenerateFAQIndex boundary. Assert D-03 byte identity, D-04 same-directory temporary naming, exclusive wx publication, exact cleanup, retained old destination bytes, and English success summary fields.

    GREEN: create scripts/generate-ai-faq-index.mjs as an ESM CLI that calls loadCanonicalFAQSource and serializeCanonicalFAQIndex from scripts/ai-faq-index.mjs. Use node:crypto randomUUID, node:path dirname/basename/join, node:fs/promises writeFile with flag wx, and rename for one explicit sibling temporary path. Validate and serialize the entire collection before writeFile, clean only that generated exact path in finally, return status 0 or 1 through the entrypoint, and preserve the original failure when cleanup also reports an error.

    REFACTOR: keep the mutating path isolated in this CLI and retain shared D-08 logic in ai-faq-index.mjs. Keep the generator output concise English with record count, public/ai-faqs.en.json path, and byte count.
  </action>
  <acceptance_criteria>
    - Generator uses validated shared records and canonical bytes rather than an independent parser or serializer.
    - A successful run replaces exactly the requested output path through one UUID-named same-directory temporary file and leaves zero owned temporary files.
    - A controlled write or rename failure preserves destination bytes and returns non-zero status with an actionable English error.
    - Two valid generations produce byte-identical compact JSON with no trailing newline.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/ai-faq-index.test.mjs</automated>
  </verify>
  <done>Atomic publication has RED/GREEN evidence, a thin CLI boundary, and exact failure cleanup behavior.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: Generate and protect the committed 2,000-record client asset</name>
  <files>public/ai-faqs.en.json, scripts/ai-faq-index.test.mjs, scripts/generate-ai-faq-index.mjs</files>
  <read_first>
    - public/ai-faqs.en.json for the current compact asset shape and numeric order.
    - content/ai-quick-reference/ for the canonical 2,000 source files.
    - app/[lang]/(home)/ai-quick-reference/components/FAQSearch.tsx for category, question, description, slug consumption.
    - scripts/generate-ai-faq-index.mjs after Task 1 for the explicit maintainer command.
    - .planning/phases/31-canonical-index-generation-and-local-parity/31-RESEARCH.md for the measured 28 slug and five description drifts.
  </read_first>
  <behavior>
    - RED case: the pre-generation committed index reports the known 28 slug and five description drifts against canonical source bytes.
    - GREEN case: explicit generation emits 2,000 records with IDs 1..2000, exact category/question/description/slug mapping, fixed key order, compact JSON, and canonical byte equality.
    - GREEN case: a read-only full-corpus smoke observes zero source schema findings after the asset is regenerated.
  </behavior>
  <action>
    Execute node scripts/generate-ai-faq-index.mjs while staging this plan's changes, then regenerate public/ai-faqs.en.json once from content/ai-quick-reference/*.en.json. Plan 31-03 exposes the same entrypoint through npm run generate:ai-faq-index. Extend the focused test with a read-only corpus assertion for exactly 2,000 records, numeric order, four fixed fields, no trailing newline, and zero semantic/canonical-byte drift after generation. Record the source-derived correction of the 28 stale slug values and five description values while preserving FAQSearch, source.config.ts, lib/source.ts, and the Phase 32 route-verification boundary.
  </action>
  <acceptance_criteria>
    - public/ai-faqs.en.json parses as an array of 2,000 records and serializes byte-for-byte through the shared canonical serializer.
    - All records preserve the FAQSearch field contract in category, question, description, slug order.
    - The generated asset removes the 28 observed slug drifts and five observed description drifts through source projection.
    - The generated file remains a committed static asset with no generator temporary residue.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/ai-faq-index.test.mjs &amp;&amp; node scripts/generate-ai-faq-index.mjs</automated>
  </verify>
  <done>The committed client asset is canonical, source-derived, compact, and safe to publish through the explicit generator.</done>
</task>

</tasks>

<threat_model>
ASVS L1 scope: one local public-asset publication boundary.

| Threat | Severity | Disposition | Planned control |
|---|---|---|---|
| T-31-02 a temporary-path collision or partial output damages public/ai-faqs.en.json | medium | mitigate | D-04 validates before publication, creates a UUID sibling with wx, renames after complete write, and removes one exact path in finally. |
| Source validation failure publishes a partial collection | medium | mitigate | Shared D-08 loader completes all validation and canonical serialization before writeFile begins. |
| Generator output changes the browser contract | low | mitigate | D-03 and SOURCE-02 tests bind fixed key order and 2,000-record output to FAQSearch's public interface. |
</threat_model>

## Artifacts this phase produces

- `scripts/generate-ai-faq-index.mjs` — `runGenerateFAQIndex` and CLI entrypoint for atomic source-to-asset publication.
- `public/ai-faqs.en.json` — regenerated canonical 2,000-record four-field asset.
- `scripts/ai-faq-index.test.mjs` — double-generation, write/rename failure, cleanup, and corpus assertions.

## Source Coverage Audit

| Source | ID | Covered by | Status |
|---|---|---|---|
| GOAL | explicit maintainer regeneration | 31-02 Tasks 1-2 | COVERED |
| REQ | SOURCE-01 | 31-02 Tasks 1-2 | COVERED |
| REQ | SOURCE-02 | 31-02 Task 2 | COVERED |
| RESEARCH | same-directory rename, injected CLI, fixed FAQSearch contract | 31-02 Tasks 1-2 | COVERED |
| CONTEXT | D-03, D-04, D-08, D-13, D-14, D-15 | 31-02 Tasks 1-2 | COVERED |

<verification>
Run node --test scripts/ai-faq-index.test.mjs after each task. Run the generator twice against a temporary output and compare bytes before writing the tracked asset. Record RED/GREEN/REFACTOR commits and the tracked asset diff in 31-02-SUMMARY.md.
</verification>

<success_criteria>
- An explicit source-only generator publishes canonical JSON atomically.
- The committed static asset has 2,000 numeric-order records and exact FAQSearch data fields.
- Generator failure preserves the existing asset and removes its owned temporary file.
</success_criteria>

<output>
Create 31-02-SUMMARY.md with RED/GREEN/REFACTOR commits, generator output, asset counts/bytes, cleanup proof, and deviations.
</output>
