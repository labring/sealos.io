---
phase: 22-fastapi-postgresql-stage
plan: "03"
type: execute
wave: 3
depends_on:
  - 22-02
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_health.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
autonomous: true
requirements:
  - TDD-01
  - FAST-02
must_haves:
  truths:
    - "D-04: /health returns 200 only for reachable PostgreSQL with the migrated tasks schema and returns one stable 503 detail for every unready state."
    - "D-06: The tracked production migration Job runs alembic upgrade head from the future Stage 2 application image and reads DATABASE_URL from Secret key url."
    - "D-06: The source adapter reaches Complete twice against the same real database before readiness acceptance."
    - "D-08: Job evidence is redacted and both Job runs plus all supporting objects are removed by exact run identity."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_health.py"
      provides: "Public readiness states through HTTP"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml"
      provides: "Production application-image migration Job contract"
      contains: "alembic upgrade head"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml"
      provides: "Pre-image source-mounted migration completion adapter"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py"
      provides: "Static production migration Job manifest contract"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py"
      via: "health calls a real connection and tasks-schema probe"
      pattern: "Database is not ready"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml"
      to: "Kubernetes Secret key url"
      via: "DATABASE_URL valueFrom secretKeyRef"
      pattern: "secretKeyRef"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml"
      via: "run-token substitution, strict server validation, two create/wait/delete cycles, then readiness test"
      pattern: "condition=complete"
---

<objective>
Gate public readiness on migrated schema and prove the one-shot migration Job contract before the production image exists, implementing D-04, D-06, and D-08.

Purpose: Make migration completion the observable prerequisite for readiness and future scaling while retaining a production-image manifest that Phase 23 can consume unchanged.
Output: Schema-aware health, strict production Job manifest, source validation adapter, two real Job completions, and redacted lifecycle evidence.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-fastapi-postgresql-stage/22-CONTEXT.md
@.planning/phases/22-fastapi-postgresql-stage/22-RESEARCH.md
@.planning/phases/22-fastapi-postgresql-stage/22-PATTERNS.md
@.planning/phases/22-fastapi-postgresql-stage/22-VALIDATION.md
@.planning/phases/22-fastapi-postgresql-stage/22-02-SUMMARY.md
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh

<interfaces>
Health contract: ready returns `200 {"status":"ok"}`; absent configuration, unreachable database, and reachable unmigrated database each return `503 {"detail":"Database is not ready"}`. Internal causes may be logged without credentials. Production Job contract: `batch/v1`, one Uvicorn-stage image `ghcr.io/yangchuansheng/sealos-fastapi-tutorial:stage-2-postgresql`, working directory `/app`, command `alembic upgrade head`, `DATABASE_URL` from Secret key `url`, `restartPolicy: Never`, `backoffLimit: 1`, and `activeDeadlineSeconds: 300`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement schema-aware public readiness</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/conftest.py
    .planning/phases/22-fastapi-postgresql-stage/22-RESEARCH.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_health.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <behavior>
    - Missing database configuration returns 503 with the stable public detail.
    - A syntactically valid URL to a closed local port returns the same 503 detail within a bounded interval.
    - Reachable PostgreSQL before migration returns the same 503 detail.
    - Reachable PostgreSQL at Alembic head returns the established 200 health body.
  </behavior>
  <action>
Set `STATE_FILE=/tmp/sealos-fastapi-phase22-03-01.env`, run `./scripts/test-postgres.sh --session-start --state-file "$STATE_FILE"`, require `SESSION_READY`, then run `set -a; source "$STATE_FILE"; set +a`. Record rollout, in-pod `pg_isready`, port-forward TCP readiness, and `SELECT 1`; keep this exact handle/run through GREEN and cleanup once afterward. RED per D-04: add public TestClient cases for unconfigured, unreachable, reachable-unmigrated, and migrated states. Use `DATABASE_URL="$DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL"` for the latter two, downgrading to base and restoring head through the migration seam. The first reachable-unmigrated case must exit pytest with status 1 and exact signature `assert 200 == 503`, with observed body `{"status":"ok"}`; this proves the HTTP seam reached the accepted pre-readiness behavior after infrastructure success. Commit exactly `test(22-03): specify schema-aware database readiness`, changing only `tests/test_health.py`; stop on every different failure or scope.

GREEN: source `/tmp/sealos-fastapi-phase22-03-01.env` again and require the same `RUN_ID`; add one database readiness function that opens a real connection, uses SQLAlchemy inspection to confirm `tasks`, closes deterministically, and returns an internal readiness result. Make `/health` translate every configuration, connectivity, and missing-schema state to the exact 503 detail while logging a credential-free cause; preserve the ready 200 body. Add short connect timeout configuration for the unreachable case. Commit exactly `feat(22-03): gate health on migrated tasks schema` after the named file and accumulated API suite pass with the sourced URLs against the same PostgreSQL run. Require this GREEN to be the direct child of RED and change only `app/database.py` and `app/main.py`. Pass the same path to `--session-stop`, then `--assert-clean`; require zero exact-label objects, dead recorded processes, and state-file removal.
  </action>
  <acceptance_criteria>
    - RED fails for an exact readiness response mismatch after reaching public HTTP.
    - GREEN produces the same stable 503 response for all three unready states and exact 200 for migrated state.
    - Database URLs and credentials never appear in client responses or retained logs.
    - RED and GREEN are directly adjacent with test-only and production-only commit scopes.
    - One run ID serves both gates and cleanup executes once after GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; set -euo pipefail; STATE_FILE=/tmp/sealos-fastapi-phase22-03-01.env; test -r "$STATE_FILE"; set -a; source "$STATE_FILE"; set +a; DATABASE_URL="$DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" uv run pytest tests/test_health.py tests/test_api.py -q -x; RED=$(git log --format='%H' --grep='^test(22-03): specify schema-aware database readiness$' -1); GREEN=$(git log --format='%H' --grep='^feat(22-03): gate health on migrated tasks schema$' -1); test -n "$RED"; test -n "$GREEN"; test "$(git rev-parse "$GREEN^")" = "$RED"; test "$(git diff-tree --no-commit-id --name-only -r "$RED")" = tests/test_health.py; git diff-tree --no-commit-id --name-only -r "$GREEN" | sort | diff -u - &lt;(printf '%s\n' app/database.py app/main.py | sort); ./scripts/test-postgres.sh --session-stop --state-file "$STATE_FILE"; ./scripts/test-postgres.sh --assert-clean --state-file "$STATE_FILE"; test ! -e "$STATE_FILE"</automated>
  </verify>
  <done>Public readiness is RED then GREEN and cannot succeed before both database connectivity and the migrated tasks schema exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Specify and implement production and source migration Job contracts</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/requirements.txt
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/alembic.ini
    .planning/phases/22-fastapi-postgresql-stage/22-RESEARCH.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml, /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh</files>
  <behavior>
    - The production manifest passes strict Kubernetes server dry-run and carries the exact image, Secret, command, restart, retry, and deadline contract.
    - A run-scoped source Job installs the locked runtime export from mounted tracked source and completes `alembic upgrade head` against the real database.
    - Deleting and recreating the source Job completes a second time at the same Alembic head.
  </behavior>
  <action>
Set `STATE_FILE=/tmp/sealos-fastapi-phase22-03-02.env`, run `./scripts/test-postgres.sh --session-start --state-file "$STATE_FILE"`, require `SESSION_READY`, then run `set -a; source "$STATE_FILE"; set +a`. Record rollout, `pg_isready`, port-forward, `SELECT 1`, and revision `0001` success before RED; retain this exact handle/run through both source Job executions and clean once after GREEN. RED per D-06: add `tests/test_migration_job.py` as a static manifest contract. It reads `deploy/migration-job.yaml` directly and validates the exact API version, Job kind, future Stage 2 image, working directory, Alembic command, Secret key `url`, restart policy, backoff limit, and deadline. The test must never invoke the harness, kubectl, subprocesses, or source Jobs. RED must exit pytest with status 1 and exact signature `AssertionError: deploy/migration-job.yaml must exist` after the cluster/database preflight succeeds. Commit exactly `test(22-03): specify repeatable migration Job contract`, changing only `tests/test_migration_job.py`.

GREEN: source `/tmp/sealos-fastapi-phase22-03-02.env` again and require the same `RUN_ID`; add `deploy/migration-job.yaml` with the exact production contract from the interface block. Add `deploy/source-migration-job.yaml` as the Phase 22 adapter using `python:3.12.13-slim-bookworm`, run-token placeholders, the same `alembic upgrade head` command, a run-scoped Secret, and a run-scoped ConfigMap containing only the tracked application model, Alembic files, migration revision, and exact runtime export. Extend the harness to create the ConfigMap from those exact files, render placeholders into a temporary manifest, assert zero unresolved tokens, validate the production manifest with `kubectl apply --dry-run=server --validate=strict`, and directly create/wait/delete the source Jobs. The harness creates Job 1, waits for `condition=complete`, captures redacted status/current revision, deletes the exact Job, then creates Job 2 and repeats against the database addressed by the sourced URLs. Pytest performs static production-manifest validation only; harness code performs live server validation and both source Job executions in a one-way flow. After the second completion, run the migrated public health case before accepting the Job mode. Commit exactly `feat(22-03): add repeatable migration Job contracts` as the direct child of RED, changing only `deploy/migration-job.yaml`, `deploy/source-migration-job.yaml`, and `scripts/test-postgres.sh`. Run static pytest first, then attach the harness `--jobs-only --state-file "$STATE_FILE"` flow to the active session. Pass the same path to `--session-stop`, then `--assert-clean`; require zero exact-label objects, dead recorded processes, and state-file removal. Keep application image building, non-root runtime, replicas, logs, and rollback in Phase 23.
  </action>
  <acceptance_criteria>
    - RED fails for missing tracked Job artifacts after live environment preflight.
    - Production manifest server dry-run is strict and references the future Stage 2 application image plus Secret key `url`.
    - Two distinct run-labeled source Jobs reach Complete; the second reports revision `0001` and migrated readiness returns 200 afterward.
    - Both Jobs, source ConfigMap, Secret, database workload, Service, Pods, and port-forward are removed by exact run identity.
    - Static pytest contains no harness, kubectl, subprocess, or live Job invocation; the harness owns both source Job completions directly.
    - RED and GREEN are directly adjacent with exact test-only and production/deployment-only scopes.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; set -euo pipefail; STATE_FILE=/tmp/sealos-fastapi-phase22-03-02.env; test -r "$STATE_FILE"; set -a; source "$STATE_FILE"; set +a; uv run pytest tests/test_migration_job.py -q -x; ! rg 'test-postgres|kubectl|subprocess|jobs-only' tests/test_migration_job.py; ./scripts/test-postgres.sh --jobs-only --state-file "$STATE_FILE"; RED=$(git log --format='%H' --grep='^test(22-03): specify repeatable migration Job contract$' -1); GREEN=$(git log --format='%H' --grep='^feat(22-03): add repeatable migration Job contracts$' -1); test -n "$RED"; test -n "$GREEN"; test "$(git rev-parse "$GREEN^")" = "$RED"; test "$(git diff-tree --no-commit-id --name-only -r "$RED")" = tests/test_migration_job.py; git diff-tree --no-commit-id --name-only -r "$GREEN" | sort | diff -u - &lt;(printf '%s\n' deploy/migration-job.yaml deploy/source-migration-job.yaml scripts/test-postgres.sh | sort); ./scripts/test-postgres.sh --session-stop --state-file "$STATE_FILE"; ./scripts/test-postgres.sh --assert-clean --state-file "$STATE_FILE"; test ! -e "$STATE_FILE"</automated>
  </verify>
  <done>The production Job contract validates, the source adapter completes twice on real PostgreSQL before readiness, and exact RED/GREEN history plus cleanup proof exists.</done>
</task>

<task type="auto">
  <name>Task 3: Run the combined migration, readiness, HTTP, Job, and cleanup gate</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_health.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/source-migration-job.yaml
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh</files>
  <action>
Run the harness phase gate through one owned database lifecycle: pre-migration health 503, fresh and repeat local Alembic upgrade, full public HTTP suite, strict production manifest validation, two source Job completions, current revision `0001`, post-Job health 200, lock/export zero diff, redacted evidence output, and exact-label cleanup. Preserve every pre-existing namespace resource by recording the owned inventory before each create and querying only the generated label afterward. Any timeout, failed Job, evidence leak, remaining object, or live port-forward fails the gate. Store no Secret payload or unredacted URL.
  </action>
  <acceptance_criteria>
    - One terminating command covers migrations, HTTP, readiness, Jobs, dependency reproducibility, evidence redaction, and cleanup.
    - Job completion precedes the accepted readiness 200 observation.
    - Final exact-label inventory is empty and the recorded port-forward PID is dead.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; ./scripts/test-postgres.sh --phase-gate</automated>
  </verify>
  <done>The complete pre-publication Stage 2 behavior passes in one bounded real-cluster run and leaves no owned runtime footprint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public health request to database probe | Anonymous HTTP triggers a bounded database/schema check. |
| Kubernetes Job to Secret | Migration process receives the database URL from cluster configuration. |
| Source ConfigMap to migration interpreter | Tracked code and requirements execute inside a temporary external Job. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-10 | Information Disclosure | Readiness errors | high | mitigate | Return one stable public detail, redact internal logs, and gate evidence against URL/password patterns. |
| T-22-11 | Tampering | Job manifest substitution | high | mitigate | Permit only named run tokens, fail on unresolved tokens, validate server-side strictly, and retain rendered manifest hashes without Secret values. |
| T-22-12 | Elevation of Privilege | Migration Job | high | mitigate | Run one bounded command against the run Secret, use exact source files, and isolate the Job under the run label. |
| T-22-13 | Denial of Service | Migration Job retry | medium | mitigate | Set backoffLimit 1, activeDeadlineSeconds 300, wait timeout, and deterministic delete/recreate. |
| T-22-14 | Tampering | Mutable future image reference | medium | accept | Phase 22 validates the declared Stage 2 source-tag contract; Phase 23 publishes and replaces it with immutable image evidence. |
</threat_model>

<verification>
- Run the health RED/GREEN file and accumulated API file through the real database harness.
- Run static pytest for the production manifest, then let the harness run strict server dry-run and execute both source Jobs directly.
- Observe two distinct source Job Complete conditions and revision `0001` before health 200.
- Run the combined phase gate and prove exact-label cleanup plus stopped port-forward.
</verification>

<success_criteria>
- TDD-01 includes schema-aware health, docs, and complete CRUD through public HTTP on real PostgreSQL.
- FAST-02 includes a reusable production Job contract and two actual source Job completions.
- D-04, D-06, and D-08 are fully executable with ordered RED/GREEN commits and bounded cleanup.
</success_criteria>

## Artifacts This Phase Produces

- Public readiness behaviors for missing, unreachable, unmigrated, and migrated database states.
- A production application-image migration Job for Phase 23.
- A source-mounted Phase 22 adapter with two real Job completion proofs.
- A combined bounded integration gate with redacted status and cleanup evidence.

## Source Coverage Audit

| Source | ID | Feature or Requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | - | Migration completion precedes readiness | 22-03 | COVERED | Job-to-health ordering is explicit. |
| REQ | TDD-01 | Public health/docs/CRUD on real PostgreSQL | 22-02, 22-03 | COVERED | Health portion is owned here. |
| REQ | FAST-02 | Repeatable migration Job | 22-03 | COVERED | Production contract and two source runs. |
| CONTEXT | D-01 | Real Sealos PostgreSQL | 22-01, 22-03 | COVERED | Harness reused here. |
| CONTEXT | D-02 | SQLAlchemy/psycopg runtime | 22-02 | COVERED | Readiness consumes its runtime. |
| CONTEXT | D-03 | Alembic ownership | 22-01, 22-03 | COVERED | Job invokes the same owner. |
| CONTEXT | D-04 | Stable schema-aware readiness | 22-03 | COVERED | Four public states. |
| CONTEXT | D-05 | Stable HTTP behavior | 22-02 | COVERED | Accumulated gate runs here. |
| CONTEXT | D-06 | Production/source Jobs | 22-03 | COVERED | Strict manifest plus two completions. |
| CONTEXT | D-07 | Source identity | 22-04 | COVERED | Publication remains next plan. |
| CONTEXT | D-08 | Evidence and cleanup | 22-03, 22-04 | COVERED | Runtime proof here, retained package next. |
| RESEARCH | Health probe and Job lifecycle pitfalls | 22-03 | COVERED | Stable response, delete/recreate, bounded waits. |
| RESEARCH | Image hardening, replicas, rollback | Phase 23 | EXCLUDED | Explicit Phase 23 scope. |

<output>
Create `.planning/phases/22-fastapi-postgresql-stage/22-03-SUMMARY.md` with health and Job RED/GREEN commits, server dry-run, two Job identities/statuses, ordering evidence, redaction checks, and empty cleanup inventory.
</output>
