---
phase: 22-fastapi-postgresql-stage
plan: "04"
type: execute
wave: 4
depends_on:
  - 22-03
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/README.md
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/commands.txt
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/migrations.txt
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/http.jsonl
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/jobs.txt
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/cleanup.txt
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/checksums.txt
  - .planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md
autonomous: true
requirements:
  - TDD-01
  - FAST-02
must_haves:
  truths:
    - "D-07: Public main equals the locally accepted PostgreSQL stage, protected stage-1-deploy remains byte-for-byte unchanged, and protected annotated stage-2-postgresql peels to public main."
    - "D-07: A clean public Stage 2 clone regenerates its lock export and passes the entire real-PostgreSQL phase gate."
    - "D-08: Readers receive a complete migration-before-readiness workflow and maintainers retain redacted migration, HTTP, Job, and cleanup evidence."
    - "D-08: Final namespace evidence contains zero owned Deployment, Pod, Service, Job, Secret, or ConfigMap objects and no live owned port-forward."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md"
      provides: "Stage 2 clone, locked install, migration, readiness, CRUD, Job, rerun, and cleanup workflow"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/README.md"
      provides: "Redacted evidence ledger and reproduction mapping"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/cleanup.txt"
      provides: "Zero-resource and stopped-process proof"
    - path: "refs/tags/stage-2-postgresql"
      provides: "Protected annotated public Stage 2 identity"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh"
      via: "documented real-PostgreSQL acceptance command"
      pattern: "test-postgres\\.sh"
    - from: "refs/tags/stage-2-postgresql^{}"
      to: "refs/heads/main"
      via: "local and remote peeled commit equality"
      pattern: "stage-2-postgresql"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/checksums.txt"
      to: "retained Phase 22 evidence"
      via: "SHA-256 manifest generated after redaction"
      pattern: "sha256"
---

<objective>
Publish the accepted PostgreSQL stage with reproducible reader instructions, retained redacted evidence, immutable protected identity, fresh-clone replay, and complete cleanup, closing D-07 and D-08.

Purpose: Make Stage 2 independently consumable while recovering safely from matching partial publication state and halting on any conflicting GitHub state.
Output: Stage 2 README/evidence, accepted public main, annotated protected `stage-2-postgresql`, unchanged Stage 1, clean-clone replay, and zero-footprint proof.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-fastapi-postgresql-stage/22-CONTEXT.md
@.planning/phases/22-fastapi-postgresql-stage/22-RESEARCH.md
@.planning/phases/22-fastapi-postgresql-stage/22-VALIDATION.md
@.planning/phases/22-fastapi-postgresql-stage/22-03-SUMMARY.md
@.planning/phases/21-fastapi-deploy-stage/21-VERIFICATION.md
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh

<interfaces>
Accepted Stage 1 commit is `276aa00e4d5bb7a0d5e375fee530cde3240b2ce8`; its verified annotated tag object is `77e57a281ecc087041b54273c1bfc63b66f13d1a`. Read both from Phase 21 verification and verify live before publication. Stage 2 recovery accepts only absent external state or state whose owner, visibility, default branch, direct tag type, tag message, peeled commit, and main commit exactly match the local accepted values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document Stage 2 and retain a redacted full-gate evidence package</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/pyproject.toml
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/deploy/migration-job.yaml
    .planning/phases/22-fastapi-postgresql-stage/22-CONTEXT.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md, /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/README.md, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/commands.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/migrations.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/http.jsonl, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/jobs.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/cleanup.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/checksums.txt</files>
  <action>
Per D-07 and D-08, update README for the immutable `stage-2-postgresql` clone, Python 3.12 and uv prerequisites, locked install/export, `DATABASE_URL`, local `alembic upgrade head`, repeat migration, migration-before-readiness ordering, full public HTTP verification, production Job contract, source-adapter validation role, schema-aware 503/200 states, process-restart persistence, and the owned integration harness. Keep Phase 23 image hardening, replicas, logs, and rollback described only as the next source stage.

Make the harness accept an explicit `PHASE22_EVIDENCE_DIR` and write deterministic redacted records for commands, migration revision/fresh-repeat status, public HTTP status/bodies, two Job names/conditions, final exact-label inventory, and port-forward termination. Replace database URLs, passwords, Secret data, tokens, namespace credentials, and generated credential-bearing commands before disk write. Add an evidence README mapping each retained file to its reproduction command and scope. Run the full phase gate into `evidence/phase-22`, scan the package for credential/URL-secret patterns, verify cleanup, then generate `checksums.txt` last over the other retained evidence files. Commit exactly `docs(22-04): document PostgreSQL reader workflow` only after lock/export, tests, evidence redaction, checksums, and cleanup pass.
  </action>
  <acceptance_criteria>
    - README puts successful migration before readiness and explains every reader-visible Stage 2 behavior.
    - Evidence proves fresh/repeat migration, CRUD persistence, health ordering, two Job completions, and empty cleanup without sensitive values.
    - The documentation commit is the accepted local Stage 2 candidate and the worktree is clean.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; rm -rf evidence/phase-22 &amp;&amp; PHASE22_EVIDENCE_DIR=evidence/phase-22 ./scripts/test-postgres.sh --phase-gate &amp;&amp; sha256sum -c evidence/phase-22/checksums.txt &amp;&amp; git diff --check &amp;&amp; git log -1 --format=%s | grep -Fx 'docs(22-04): document PostgreSQL reader workflow' &amp;&amp; test -z "$(git status --porcelain)"</automated>
  </verify>
  <done>The accepted local candidate contains the complete reader workflow and a checksum-protected, redacted, real-run evidence package with proven cleanup.</done>
</task>

<task type="auto">
  <name>Task 2: Replay fresh local clones and freeze all tracked evidence before publication</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/cleanup.txt
    .planning/phases/22-fastapi-postgresql-stage/22-VALIDATION.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/commands.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/migrations.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/http.jsonl, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/jobs.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/cleanup.txt, /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/checksums.txt</files>
  <action>
Before any push or Stage 2 tag creation, clone the Task 1 candidate through a local transport with `git clone --no-local`, run `uv sync --locked`, regenerate `requirements.txt` with zero diff, and run the complete phase gate against a fresh owned PostgreSQL service. Copy only redacted migration, HTTP, Job, and cleanup results from this independent run into the tracked Phase 22 evidence package; regenerate checksums last; scan for Secret values and credential-bearing URLs; and commit exactly `test(22-04): record fresh-clone PostgreSQL replay`. This commit becomes the accepted Stage 2 SHA.

Clone the accepted commit again through `git clone --no-local`, send phase-gate output to an external temporary evidence directory, and rerun locked sync, export comparison, migrations, HTTP, health, static production manifest test, two direct source Jobs, and cleanup. The second clone validates the exact final tree while leaving the source worktree and tracked evidence unchanged. Require both clone runs to show zero exact-label objects and dead owned port-forwards. End with a clean accepted source tree. Public GitHub mutation starts only in Task 3.
  </action>
  <acceptance_criteria>
    - Both pre-public fresh local clones reproduce the exact locked source and complete phase gate.
    - Every tracked evidence update and checksum is committed before the accepted SHA is published.
    - The second clone tests the final accepted commit and writes output only to a temporary external directory.
    - Final local source is clean and every clone run has explicit zero-resource and dead-port-forward proof.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; test "$(git log -1 --format=%s)" = 'test(22-04): record fresh-clone PostgreSQL replay' &amp;&amp; test -z "$(git status --porcelain)" &amp;&amp; sha256sum -c evidence/phase-22/checksums.txt &amp;&amp; tmp=$(mktemp -d) &amp;&amp; trap 'rm -rf "$tmp"' EXIT &amp;&amp; git clone --no-local . "$tmp/repo" &amp;&amp; cd "$tmp/repo" &amp;&amp; uv sync --locked &amp;&amp; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt &amp;&amp; git diff --exit-code -- requirements.txt &amp;&amp; PHASE22_EVIDENCE_DIR="$tmp/post-final" ./scripts/test-postgres.sh --phase-gate &amp;&amp; git diff --exit-code</automated>
  </verify>
  <done>The exact final Stage 2 candidate passes a fresh-clone replay, all tracked evidence is frozen, and publication has not started.</done>
</task>

<task type="auto">
  <name>Task 3: Publish immutable Stage 2, read back every public contract, and replay HTTPS without tracked writes</name>
  <read_first>
    .planning/phases/21-fastapi-deploy-stage/21-VERIFICATION.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/README.md
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/evidence/phase-22/checksums.txt
    .planning/phases/22-fastapi-postgresql-stage/22-VALIDATION.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/.git/refs, refs/heads/main, refs/tags/stage-1-deploy, refs/tags/stage-2-postgresql, .planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md</files>
  <action>
Capture the clean accepted SHA and verify all tracked evidence is committed. Before mutation, read GitHub owner `yangchuansheng`, visibility `PUBLIC`, default branch `main`, remote main, Stage 1 remote direct tag object, Stage 1 remote peeled commit, Stage 1 annotated message, and the detailed active tag ruleset. Require Stage 1 direct object `77e57a281ecc087041b54273c1bfc63b66f13d1a`, peeled commit `276aa00e4d5bb7a0d5e375fee530cde3240b2ce8`, exact message `FastAPI deploy stage`, exactly one active ruleset targeting tags with include `refs/tags/stage-*`, an empty exclude list, both `update` and `deletion` rules, and an empty bypass list. Abort before mutation on every mismatch.

For remote main, accept the Phase 21 commit as the fast-forward starting state or the exact accepted Stage 2 SHA as matching recovery state; abort every other SHA. Push main with a normal fast-forward command when required. Compute local Stage 2 presence with `git show-ref --verify refs/tags/stage-2-postgresql` and remote presence from both direct and peeled `git ls-remote` rows, then handle exactly four states. (1) Local absent / remote absent: create annotated `stage-2-postgresql` at accepted main with exact message `FastAPI PostgreSQL stage`, validate locally, then push. (2) Local present / remote absent: require local object type `tag`, exact message, and peeled accepted SHA, then push that validated tag. (3) Local absent / remote present: fetch `refs/tags/stage-2-postgresql` into the same local tag ref, then require the fetched direct object to equal the remote direct object, its exact message, and local/remote peeled accepted SHA. (4) Local present / remote present: require identical local/remote direct objects, identical local/remote peeled accepted SHA, local object type `tag`, and exact message. Treat a lone remote direct row without a peeled row, a peeled row without a direct row, every SHA/message/type disagreement, and every tag outside these four coherent states as a conflict and abort. Force push, tag replacement, and ruleset bypass remain outside the workflow.

After publication, repeat the complete public readback: owner, visibility, default branch, remote main; Stage 1 and Stage 2 remote direct objects and peeled commits; both local object types `tag`; both annotated messages; and the exact active `refs/tags/stage-*` ruleset target, include/exclude, update/deletion rules, enforcement, and empty bypass. Clone public HTTPS at `stage-2-postgresql`, run locked sync, export zero diff, and the full phase gate with evidence directed to a temporary path outside the clone. Write only the redacted public replay SHA, run ID, pass statuses, and zero-cleanup result to `.planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md`. The public Reference Application worktree and immutable tag receive no tracked update after publication.
  </action>
  <acceptance_criteria>
    - Public owner, visibility, default branch, main, both remote direct/peeled tag identities, both messages, and the complete active ruleset read back exactly.
    - Stage 1 identities remain unchanged and protected Stage 2 peels to accepted public main.
    - Existing matching publication state resumes safely; every mismatch aborts before forceful mutation.
    - Public HTTPS clone passes the full gate, and its result exists only in temporary output plus the planning summary.
    - Reference Application source remains clean and byte-identical to the accepted tag after public replay.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; set -euo pipefail; ACCEPTED=$(git rev-parse HEAD); gh repo view yangchuansheng/sealos-fastapi-tutorial --json owner,visibility,defaultBranchRef | jq -e '.owner.login == "yangchuansheng" and .visibility == "PUBLIC" and .defaultBranchRef.name == "main"'; test "$(git ls-remote origin refs/heads/main | cut -f1)" = "$ACCEPTED"; test "$(git rev-parse stage-1-deploy)" = 77e57a281ecc087041b54273c1bfc63b66f13d1a; test "$(git rev-parse 'stage-1-deploy^{}')" = 276aa00e4d5bb7a0d5e375fee530cde3240b2ce8; test "$(git ls-remote origin refs/tags/stage-1-deploy | cut -f1)" = 77e57a281ecc087041b54273c1bfc63b66f13d1a; test "$(git ls-remote origin 'refs/tags/stage-1-deploy^{}' | cut -f1)" = 276aa00e4d5bb7a0d5e375fee530cde3240b2ce8; test "$(git cat-file -t stage-1-deploy)" = tag; test "$(git for-each-ref --format='%(contents:subject)' refs/tags/stage-1-deploy)" = 'FastAPI deploy stage'; test "$(git cat-file -t stage-2-postgresql)" = tag; test "$(git for-each-ref --format='%(contents:subject)' refs/tags/stage-2-postgresql)" = 'FastAPI PostgreSQL stage'; test "$(git ls-remote origin refs/tags/stage-2-postgresql | cut -f1)" = "$(git rev-parse stage-2-postgresql)"; test "$(git ls-remote origin 'refs/tags/stage-2-postgresql^{}' | cut -f1)" = "$ACCEPTED"; RULESET_ID=$(gh api repos/yangchuansheng/sealos-fastapi-tutorial/rulesets | jq -er '[.[] | select(.target == "tag" and .enforcement == "active")] | if length == 1 then .[0].id else error("expected one active tag ruleset") end'); gh api "repos/yangchuansheng/sealos-fastapi-tutorial/rulesets/$RULESET_ID" | jq -e '.target == "tag" and .enforcement == "active" and .conditions.ref_name.include == ["refs/tags/stage-*"] and .conditions.ref_name.exclude == [] and ([.rules[].type] | sort) == ["deletion","update"] and .bypass_actors == []'; tmp=$(mktemp -d); trap 'rm -rf "$tmp"' EXIT; git clone --branch stage-2-postgresql https://github.com/yangchuansheng/sealos-fastapi-tutorial.git "$tmp/repo"; cd "$tmp/repo"; uv sync --locked; uv export --locked --no-dev --no-emit-project --no-hashes --format requirements.txt --output-file requirements.txt; git diff --exit-code -- requirements.txt; PHASE22_EVIDENCE_DIR="$tmp/public-replay" ./scripts/test-postgres.sh --phase-gate; git diff --exit-code</automated>
  </verify>
  <done>The immutable public Stage 2 source passes complete GitHub readback and HTTPS replay while all post-public evidence remains outside its tracked tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime evidence to tracked Git | External command output may contain credentials or unstable identifiers. |
| Local accepted Git to public GitHub | Push and tag operations mutate durable public state. |
| Public clone to Sealos Kubernetes | Independent source replays external resource creation and cleanup. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-15 | Information Disclosure | Tracked evidence | critical | mitigate | Redact before disk, scan for Secret/URL/token patterns, retain only public bodies/status, and checksum the reviewed package. |
| T-22-16 | Tampering | Public main and tags | high | mitigate | Require exact owner/state/SHA/type/message/ruleset matches, use fast-forward push only, and abort conflicts. |
| T-22-17 | Repudiation | Stage identity | medium | mitigate | Record accepted commit, direct annotated objects, peeled commits, public readback, and ruleset ID in the phase summary. |
| T-22-18 | Denial of Service | Fresh-clone replay | medium | mitigate | Reuse bounded harness timeouts and exact-label cleanup; remove the temporary clone. |
| T-22-19 | Elevation of Privilege | GitHub tag mutation | high | mitigate | Verify active update/deletion protection with no bypass and never replace an existing tag. |
</threat_model>

<verification>
- Run the local full phase gate into the retained evidence directory and verify redaction/checksums.
- Run two fresh local-transport clone replays before publication; the second targets the final accepted commit and writes outside the tracked tree.
- Verify clean accepted Git state before any external mutation.
- Verify public owner, visibility, default branch, main, both direct/peeled tag objects, both tag messages, and the exact active protection ruleset after publication.
- Run locked sync, export regeneration, and full real-PostgreSQL gate from a fresh public Stage 2 clone with output retained only outside the Reference Application tree and in the planning summary.
- Verify every recorded run has zero exact-label resources and no live owned port-forward.
</verification>

<success_criteria>
- TDD-01 and FAST-02 reproduce locally and from the protected public Stage 2 tag.
- D-07 publication preserves Stage 1 and accepts only exact matching recovery state.
- D-08 leaves a redacted checksum-protected evidence package and a provably empty runtime footprint.
</success_criteria>

## Artifacts This Phase Produces

- A complete Stage 2 reader workflow and retained redacted evidence ledger.
- Public main aligned with the accepted PostgreSQL stage.
- Protected annotated `stage-2-postgresql` identity with unchanged `stage-1-deploy`.
- Independent fresh-clone acceptance and full cleanup proof.

## Source Coverage Audit

| Source | ID | Feature or Requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | - | Reader can run Tasks API after single-owner migration | 22-01 through 22-04 | COVERED | Public replay closes the outcome. |
| REQ | TDD-01 | Public health/docs/CRUD on real PostgreSQL | 22-02, 22-03, 22-04 | COVERED | Fresh clone reruns the public seam. |
| REQ | FAST-02 | Complete PostgreSQL source stage | 22-01 through 22-04 | COVERED | Lock, persistence, Jobs, publication. |
| CONTEXT | D-01 | Dedicated owned PostgreSQL | 22-01, 22-04 | COVERED | Fresh clone reuses the harness. |
| CONTEXT | D-02 | Synchronous SQLAlchemy and psycopg | 22-02 | COVERED | Published source includes it. |
| CONTEXT | D-03 | Alembic-only schema | 22-01 | COVERED | Published/replayed migration. |
| CONTEXT | D-04 | Schema-aware readiness | 22-03 | COVERED | Published/replayed health gate. |
| CONTEXT | D-05 | Stable public CRUD | 22-02 | COVERED | Published/replayed HTTP suite. |
| CONTEXT | D-06 | Migration Job contracts | 22-03 | COVERED | Published/replayed Job gate. |
| CONTEXT | D-07 | Lock, preserved Stage 1, protected Stage 2 | 22-01, 22-04 | COVERED | Exact recovery and identity checks. |
| CONTEXT | D-08 | Evidence and complete cleanup | 22-01, 22-03, 22-04 | COVERED | Retained redaction and final zero inventory. |
| RESEARCH | README migration-before-readiness order | 22-04 | COVERED | Reader workflow. |
| RESEARCH | Publication recovery and ruleset | 22-04 | COVERED | Matching-state acceptance and mismatch abort. |
| RESEARCH | Fresh public clone replay | 22-04 | COVERED | Full phase gate from tag. |
| RESEARCH | Production image/runtime hardening | Phase 23 | EXCLUDED | Explicit next-phase responsibility. |

<output>
Create `.planning/phases/22-fastapi-postgresql-stage/22-04-SUMMARY.md` with accepted/public SHAs, both direct/peeled tag identities, ruleset readback, evidence checksum result, fresh-clone gate, and final empty resource/process inventory.
</output>
