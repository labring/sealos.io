---
phase: 22-fastapi-postgresql-stage
plan: "02"
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/conftest.py
  - /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
autonomous: true
requirements:
  - TDD-01
  - FAST-02
must_haves:
  truths:
    - "D-02: Every request uses one closing synchronous SQLAlchemy 2 Session created from the explicit PostgreSQL URL."
    - "D-05: A task created by one create_app() instance is observable through a later instance using the same real database."
    - "D-05: Docs, create, list, item read, complete PUT, delete, 422 validation, and stable 404 responses preserve the Stage 1 public contract."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py"
      provides: "Explicit database runtime, engine, session factory, request dependency, and disposal"
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      provides: "Database-backed public Tasks API without changing HTTP shapes"
      exports: ["Task", "TaskCreate", "TaskUpdate", "app", "create_app"]
    - path: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py"
      provides: "Public HTTP persistence and inherited behavior specifications against real PostgreSQL"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py"
      via: "create_app(database_url) builds one runtime and injects one Session per request"
      pattern: "create_app\\(database_url"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py"
      via: "TestClient(create_app(TEST_DATABASE_URL)) and later HTTP reads"
      pattern: "TestClient\\(create_app"
    - from: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py"
      to: "/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/models.py"
      via: "SQLAlchemy selects and mutations against TaskRecord"
      pattern: "TaskRecord"
---

<objective>
Replace process-local CRUD with durable PostgreSQL behavior through vertical public HTTP tracer bullets, implementing D-02 and D-05.

Purpose: Preserve the accepted Tasks API while making persistence observable only through later HTTP requests and later application instances.
Output: Synchronous SQLAlchemy runtime, database-backed CRUD, real-PostgreSQL fixtures, and ordered RED/GREEN history for every persistence behavior.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
@/Users/longnv/.agents/skills/tdd/tests.md
@/Users/longnv/.agents/skills/tdd/mocking.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-fastapi-postgresql-stage/22-CONTEXT.md
@.planning/phases/22-fastapi-postgresql-stage/22-RESEARCH.md
@.planning/phases/22-fastapi-postgresql-stage/22-PATTERNS.md
@.planning/phases/22-fastapi-postgresql-stage/22-VALIDATION.md
@.planning/phases/22-fastapi-postgresql-stage/22-01-SUMMARY.md
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/app/models.py
@/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py

<interfaces>
Preserve exact Stage 1 HTTP payloads and codes: POST `/tasks` returns 201 `Task`; GET collection and item return 200; complete PUT retains the integer ID; DELETE returns empty 204; invalid titles return framework-native 422; unknown GET/PUT/DELETE return `{"detail":"Task not found"}`. Extend `create_app` to `create_app(database_url: str | None = None) -> FastAPI`; tests always pass `TEST_DATABASE_URL`, and module `app` resolves `DATABASE_URL`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist create and item read across application instances</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/models.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/conftest.py
    .planning/phases/22-fastapi-postgresql-stage/22-PATTERNS.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/conftest.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <behavior>
    - POST through application A returns the established 201 body.
    - GET of that ID through independently constructed application B returns the same body.
    - Tests use the run-scoped real PostgreSQL URL and assert persistence exclusively through HTTP.
  </behavior>
  <action>
Set `STATE_FILE=/tmp/sealos-fastapi-phase22-02-01.env`, run `./scripts/test-postgres.sh --session-start --state-file "$STATE_FILE"`, require `SESSION_READY`, then run `set -a; source "$STATE_FILE"; set +a`. Migrate that session to head and record rollout, in-pod `pg_isready`, port-forward, `SELECT 1`, and revision `0001` success before RED. Retain this exact handle and run ID through GREEN. RED per D-05: add the cross-instance POST then GET behavior and run it with the sourced `DATABASE_URL` and `TEST_DATABASE_URL`. The named pytest case must exit 1 with the exact comparison signature `assert 404 == 200` and the observed body `{"detail":"Task not found"}` from application B. Commit exactly `test(22-02): specify task persistence across application instances`, changing only `tests/test_api.py`; stop on any different signature or scope.

GREEN per D-02: source `/tmp/sealos-fastapi-phase22-02-01.env` again and require the same `RUN_ID`; create `app/database.py` with `create_engine(..., pool_pre_ping=True)`, synchronous `sessionmaker(..., expire_on_commit=False)`, one closing Session dependency per request, and deterministic engine disposal. Extend `create_app(database_url: str | None = None)` so explicit input wins and module construction reads `DATABASE_URL`. Replace only create and item read with parameter-bound SQLAlchemy operations against `TaskRecord`; commit writes and refresh created records. Preserve Pydantic request/response models in `app/main.py`. Run the named behavior and accumulated create/get behaviors with the sourced URLs against the same task-session database, then commit exactly `feat(22-02): persist task creation and retrieval`. Require the GREEN commit parent to equal the RED commit and the GREEN scope to contain only `app/database.py` and `app/main.py`. Run `--session-stop --state-file "$STATE_FILE"` once, then `--assert-clean --state-file "$STATE_FILE"` to prove zero run-labeled objects and dead recorded processes before removing the handle.
  </action>
  <acceptance_criteria>
    - RED fails with an HTTP 404 from application B for the retained task ID.
    - GREEN returns the identical task through application B and closes both clients and engines.
    - No test reads the database to prove application behavior and no internal collaborator is mocked.
    - RED and GREEN are direct parent/child commits with exact test-only and production-only scopes.
    - One database run serves both gates and cleans once after GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; set -euo pipefail; STATE_FILE=/tmp/sealos-fastapi-phase22-02-01.env; test -r "$STATE_FILE"; set -a; source "$STATE_FILE"; set +a; DATABASE_URL="$DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" uv run pytest tests/test_api.py -q -k 'survives or create_task or get_task' -x; RED=$(git log --format='%H' --grep='^test(22-02): specify task persistence across application instances$' -1); GREEN=$(git log --format='%H' --grep='^feat(22-02): persist task creation and retrieval$' -1); test -n "$RED"; test -n "$GREEN"; test "$(git rev-parse "$GREEN^")" = "$RED"; test "$(git diff-tree --no-commit-id --name-only -r "$RED")" = tests/test_api.py; git diff-tree --no-commit-id --name-only -r "$GREEN" | sort | diff -u - &lt;(printf '%s\n' app/database.py app/main.py | sort); ./scripts/test-postgres.sh --session-stop --state-file "$STATE_FILE"; ./scripts/test-postgres.sh --assert-clean --state-file "$STATE_FILE"; test ! -e "$STATE_FILE"</automated>
  </verify>
  <done>The first persistence tracer is RED then GREEN, and a later application instance retrieves the committed record through public HTTP.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist list, complete PUT, and delete through serialized tracer bullets</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/database.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
    /Users/longnv/.agents/skills/tdd/SKILL.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <behavior>
    - A list from application B contains the record created by application A ordered by integer ID.
    - A complete PUT from application B is visible through a later GET from application C.
    - A DELETE from application B returns empty 204 and a later GET from application C returns the stable 404 body.
  </behavior>
  <action>
Set `STATE_FILE=/tmp/sealos-fastapi-phase22-02-02.env`, run `./scripts/test-postgres.sh --session-start --state-file "$STATE_FILE"`, require `SESSION_READY`, then run `set -a; source "$STATE_FILE"; set +a`. Migrate to revision `0001`, prove `pg_isready`, port-forward TCP readiness, and `SELECT 1`, and retain that exact handle/run across all three strict one-test/one-implementation cycles. Every RED and GREEN command receives `DATABASE_URL="$DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL"`. Listing RED must exit 1 with `assert [] == [{...}]` because application B still lists its Stage 1 local store; commit `test(22-02): specify persisted task listing`, changing only `tests/test_api.py`. Implement ordered SQLAlchemy selection only, then commit `feat(22-02): list persisted tasks`, changing only `app/main.py`, as the direct child of its RED. Update RED must exit 1 with `assert 404 == 200` and `{"detail":"Task not found"}` because application B still resolves PUT from local state; commit `test(22-02): specify persisted task updates`, changing only `tests/test_api.py`. Implement mapped-row complete replacement and commit only, then commit `feat(22-02): update persisted tasks`, changing only `app/main.py`, directly after its RED. Delete RED must exit 1 with the exact `assert 404 == 204` signature and `{"detail":"Task not found"}` body because application B still resolves DELETE from local state. Commit `test(22-02): specify persisted task deletion`, changing only `tests/test_api.py`; implement mapped-row deletion and commit only, then commit `feat(22-02): delete persisted tasks`, changing only `app/main.py`, directly after its RED. Per D-05, each mutation is verified through a new later application instance. Run the accumulated API file after every GREEN. After the final GREEN, pass the same path to `--session-stop`, then `--assert-clean`; require zero exact-label objects, dead recorded processes, and state-file removal.
  </action>
  <acceptance_criteria>
    - Each named RED fails for its exact remaining process-local route behavior.
    - Each GREEN changes only the route needed by its preceding behavior.
    - All six exact commit subjects exist as three direct RED-to-GREEN parent/child pairs with test-only RED and `app/main.py`-only GREEN scopes.
    - Later HTTP reads prove list, update, and delete durability.
    - One migrated database run serves all three cycles and cleanup runs once after the final GREEN.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; set -euo pipefail; STATE_FILE=/tmp/sealos-fastapi-phase22-02-02.env; test -r "$STATE_FILE"; set -a; source "$STATE_FILE"; set +a; test -n "$RUN_ID"; test -n "$DATABASE_URL"; test -n "$TEST_DATABASE_URL"; DATABASE_URL="$DATABASE_URL" TEST_DATABASE_URL="$TEST_DATABASE_URL" uv run pytest tests/test_api.py -q -x; for stem in 'persisted task listing|list persisted tasks' 'persisted task updates|update persisted tasks' 'persisted task deletion|delete persisted tasks'; do red_subject="test(22-02): specify ${stem%%|*}"; green_subject="feat(22-02): ${stem##*|}"; RED=$(git log --format='%H' --grep="^${red_subject}$" -1); GREEN=$(git log --format='%H' --grep="^${green_subject}$" -1); test -n "$RED"; test -n "$GREEN"; test "$(git rev-parse "$GREEN^")" = "$RED"; test "$(git diff-tree --no-commit-id --name-only -r "$RED")" = tests/test_api.py; test "$(git diff-tree --no-commit-id --name-only -r "$GREEN")" = app/main.py; done; ./scripts/test-postgres.sh --session-stop --state-file "$STATE_FILE"; ./scripts/test-postgres.sh --assert-clean --state-file "$STATE_FILE"; test ! -e "$STATE_FILE"</automated>
  </verify>
  <done>Collection, complete update, and deletion are independently specified and implemented as durable PostgreSQL behaviors through public HTTP.</done>
</task>

<task type="auto">
  <name>Task 3: Re-run the complete Stage 1 HTTP contract on PostgreSQL</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py
    /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py
    .planning/phases/21-fastapi-deploy-stage/21-VERIFICATION.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-fastapi-tutorial/tests/test_api.py, /Users/longnv/bin/repo/sealos-fastapi-tutorial/app/main.py</files>
  <action>
Per D-05, retain generated Swagger UI, exact response models/status codes, integer IDs, title bounds for POST and PUT, empty 204 responses, and the shared stable 404 body. Add an explicit complete-PUT invalid-title regression if the inherited suite lacks it. Run the full file against real PostgreSQL. Assertions stay on public HTTP responses; setup and truncation may use the fixture boundary. Keep the accepted tracer implementation unchanged after the final GREEN so its direct adjacency remains auditable; record cleanup opportunities in the Phase 22 summary for code review.
  </action>
  <acceptance_criteria>
    - `/docs` serves Swagger UI and every inherited CRUD/validation/error behavior passes against real PostgreSQL.
    - POST and PUT share the 1-to-200 title boundary and unknown operations share the exact 404 response.
    - The run ends with an empty owned Kubernetes inventory and stopped port-forward.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-fastapi-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_api.py -q -x &amp;&amp; ! rg 'unittest\.mock|pytest-mock|monkeypatch' tests/test_api.py tests/conftest.py</automated>
  </verify>
  <done>The complete public Tasks API contract passes with real framework collaborators and real PostgreSQL, including cross-instance durability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP request to FastAPI | Untrusted path and JSON data enter validation and persistence. |
| FastAPI request to PostgreSQL | Application credentials and parameterized statements cross the database boundary. |
| Test process to shared run database | Multiple application instances observe committed data while fixtures control isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-22-06 | Tampering | Task inputs | medium | mitigate | Preserve Pydantic length/type validation and use SQLAlchemy parameter binding. |
| T-22-07 | Information Disclosure | Database errors | medium | mitigate | Keep persistence failures out of public task responses; Plan 03 defines stable readiness detail. |
| T-22-08 | Denial of Service | Stale pooled connections | medium | mitigate | Enable pool pre-ping, use one short-lived Session per request, and dispose test engines. |
| T-22-09 | Repudiation | Mutation persistence | low | accept | The educational public API has no identity contract; later HTTP reads provide deterministic behavior evidence. |
</threat_model>

<verification>
- Replay every exact RED failure before its matching minimum GREEN implementation.
- Run the accumulated API file after each GREEN against run-scoped PostgreSQL.
- Inspect commit order for all create/read, list, update, and delete subjects.
- Run the full HTTP suite and verify harness cleanup after the final slice.
</verification>

<success_criteria>
- TDD-01 covers docs and all task behaviors through public HTTP with real PostgreSQL.
- FAST-02 database-backed CRUD survives application reconstruction without changing Stage 1 response contracts.
- D-02 and D-05 are fully represented in executable RED/GREEN tasks.
</success_criteria>

## Artifacts This Phase Produces

- An explicit synchronous SQLAlchemy database runtime and one-Session-per-request dependency.
- Database-backed Tasks API CRUD preserving all Stage 1 HTTP contracts.
- Durable public behavior specifications for cross-instance create/read, list, update, and delete.

## Source Coverage Audit

| Source | ID | Feature or Requirement | Plan | Status | Notes |
|--------|----|------------------------|------|--------|-------|
| GOAL | - | Tasks API runs against migrated PostgreSQL | 22-01 through 22-03 | COVERED | This plan supplies reader-visible persistence. |
| REQ | TDD-01 | Public HTTP with real test PostgreSQL | 22-02, 22-03 | COVERED | CRUD here; health in Plan 03. |
| REQ | FAST-02 | Database-backed CRUD | 22-02 | COVERED | SQLAlchemy 2 and psycopg runtime. |
| CONTEXT | D-01 | Real Sealos PostgreSQL | 22-01 | COVERED | Consumed through its harness. |
| CONTEXT | D-02 | Sync sessions and explicit URLs | 22-02 | COVERED | Runtime/factory implementation. |
| CONTEXT | D-03 | Alembic-only schema | 22-01 | COVERED | Consumed migration. |
| CONTEXT | D-04 | Readiness | 22-03 | COVERED | Explicit later plan. |
| CONTEXT | D-05 | Stable HTTP and persistence | 22-02 | COVERED | Four vertical tracer groups. |
| CONTEXT | D-06 | Migration Jobs | 22-03 | COVERED | Explicit later plan. |
| CONTEXT | D-07 | Reproducible stage | 22-01, 22-04 | COVERED | Lock and publication plans. |
| CONTEXT | D-08 | Evidence and cleanup | 22-01, 22-04 | COVERED | Harness plus phase gate. |
| RESEARCH | Session-per-request and committed cross-instance reads | 22-02 | COVERED | Public HTTP only. |
| RESEARCH | Health, Jobs, publication | 22-03, 22-04 | COVERED | Explicit later plans. |

<output>
Create `.planning/phases/22-fastapi-postgresql-stage/22-02-SUMMARY.md` with each RED failure, GREEN result, exact commit SHA, real database run ID, full HTTP result, and cleanup proof.
</output>
