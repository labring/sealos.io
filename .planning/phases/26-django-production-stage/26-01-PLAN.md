---
phase: 26-django-production-stage
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/.dockerignore
  - /Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml
  - /Users/longnv/bin/repo/sealos-django-tutorial/Dockerfile
  - /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
  - /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock
  - .planning/phases/26-django-production-stage/26-01-SUMMARY.md
autonomous: true
requirements: [DJAN-03]
must_haves:
  truths:
    - "D-01/D-02: The accepted Django 5.2.16 and psycopg 3.3.4 graph gains exact Gunicorn 26.0.0 and WhiteNoise 6.12.0 pins, reproduces its runtime export, and builds from the approved Python 3.12.13 and uv 0.10.9 indexes."
    - "D-03/D-04: Production requires an explicit secret and localhost host policy, serves through one Gunicorn sync worker on port 8000, and runs as UID/GID 10001 with build-time collected static assets."
    - "D-05: Every serving worker emits one credential-free source-release and immutable-image-reference record."
    - "D-08: One normalized 40-character TARGET_SHA controls checkout, tests, OCI revision, the sole sha-<TARGET_SHA> tag, and the public linux/amd64 digest."
    - "D-11: The image contract is introduced by the exact test-only RED and its direct implementation-only GREEN; the GREEN source is Image A."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py"
      provides: "Public image, settings, WhiteNoise, Gunicorn, and publisher contract"
      contains: "Dockerfile must exist"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/Dockerfile"
      provides: "Pinned multi-stage, collected-static, non-root production image"
      contains: "USER 10001:10001"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml"
      provides: "Exact-source public GHCR publisher"
      contains: "TARGET_SHA"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py"
      provides: "Explicit production secret, hosts, WhiteNoise, and manifest static storage"
      contains: "CompressedManifestStaticFilesStorage"
    - path: ".planning/phases/26-django-production-stage/26-01-SUMMARY.md"
      provides: "Machine-readable Image A source, workflow, digest, and OCI identity"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/uv.lock"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/Dockerfile"
      via: "uv sync --locked --no-dev in the builder"
      pattern: "uv sync.*--locked.*--no-dev"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/tasks/static/tasks/styles.css"
      via: "collectstatic plus WhiteNoise compressed manifest storage"
      pattern: "STATIC_ROOT|CompressedManifestStaticFilesStorage"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml"
      to: "ghcr.io/yangchuansheng/sealos-django-tutorial"
      via: "full-source-SHA lookup tag and anonymous digest/config/manifest readback"
      pattern: "sha-.*TARGET_SHA"
---

<objective>
Specify, publish, and independently accept the first production-capable Django image per D-01 through D-05, D-08, and D-11.

Purpose: Establish the immutable Gunicorn/WhiteNoise rollback target directly above protected Stage 2.
Output: One exact RED/GREEN pair, public Image A source, successful exact-source workflow, and anonymous public Image A digest.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-django-production-stage/26-CONTEXT.md
@.planning/phases/26-django-production-stage/26-RESEARCH.md
@.planning/phases/26-django-production-stage/26-PATTERNS.md
@.planning/phases/26-django-production-stage/26-VALIDATION.md
@.planning/phases/25-django-postgresql-stage/25-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-resolve the supply chain and prove the protected Stage 2 precondition</name>
  <read_first>
    .planning/phases/26-django-production-stage/26-RESEARCH.md
    .planning/phases/25-django-postgresql-stage/25-VERIFICATION.md
    /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
    /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock
    /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt
  </read_first>
  <files>.planning/phases/26-django-production-stage/26-01-SUMMARY.md</files>
  <action>
    Before any target-repository write, require a clean main at protected Stage 2 `16279958ca774f7a34c25b0102a483df53160d6f`, 24 commits, 32 tracked files, migration SHA-256 `745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3`, exact Stage 1/2 refs, and ruleset `19014157`. Run `uv lock --check`, reproduce requirements outside the tree with a BSD-safe `mktemp -d "${TMPDIR:-/tmp}/django26-lock.XXXXXX"`, run the full inherited real-PostgreSQL phase gate, and finish with its read-only `--assert-clean-all`.

    Re-resolve Gunicorn `26.0.0`, WhiteNoise `6.12.0`, Python index `sha256:d50fb7611f86d04a3b0471b46d7557818d88983fc3136726336b2a4c657aa30b` and linux/amd64 child `sha256:72d3d75f2639ab82b34b29390ad3d6e0827c775befee94edda8e9976818f488d`, uv index `sha256:10902f58a1606787602f303954cea099626a4adb02acbac4c69920fe9d278f82` and child `sha256:f8f31ad675ac1a6e17ccabe0e5bbb7fe941b2a27f38cb509c799870570b2f1a7`, PostgreSQL digest `sha256:4f736ae292687621d4dbe0d499ffd024a36bd2ee7d8ca6f2ccd4c800f047b394`, and all six Action refs from research. The user has already approved the official identities; any drift halts before mutation and requires a research correction. Record only public identities and pass/fail fields in the summary scratch notes.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test -z "$(git status --short)" &amp;&amp; test "$(git rev-parse HEAD)" = 16279958ca774f7a34c25b0102a483df53160d6f &amp;&amp; test "$(git rev-list --count HEAD)" = 24 &amp;&amp; test "$(git ls-tree -r --name-only HEAD | wc -l | tr -d ' ')" = 32 &amp;&amp; test "$(shasum -a 256 tasks/migrations/0001_initial.py | awk '{print $1}')" = 745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3 &amp;&amp; uv lock --check &amp;&amp; ./scripts/test-postgres.sh --assert-clean-all</automated>
  </verify>
  <acceptance_criteria>
    - Protected Stage 2, both earlier tag objects/messages, ruleset 19014157, lock/export, migration hash, 24/32 inventory, inherited PostgreSQL gate, and global cleanup all pass.
    - Package, image, and Action identities equal the exact values in 26-RESEARCH.md immediately before the RED.
    - The target repository remains byte-clean and receives no pre-RED commit.
  </acceptance_criteria>
  <done>The accepted Stage 2 source and current immutable supply-chain identities are a fail-closed starting point.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED - specify the Django production image contract</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
    /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgresql.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_public_http.py
    .planning/phases/26-django-production-stage/26-PATTERNS.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py</files>
  <behavior>
    - Exact Gunicorn/WhiteNoise pins and runtime-only export are public source contracts.
    - Production settings require `DJANGO_SECRET_KEY`, require `DJANGO_ALLOWED_HOSTS=localhost`, set DEBUG false, place WhiteNoise directly after SecurityMiddleware, and configure `/static/`, `/app/staticfiles`, and compressed manifest storage.
    - The pinned Dockerfile performs locked build-time collection, copies allowlisted runtime assets, runs as 10001:10001, and starts one sync Gunicorn worker without preload.
    - A real Gunicorn subprocess emits exactly one worker release record and serves health/board/hashed CSS with production settings.
    - The publisher uses the approved six Action SHAs and one normalized full source SHA for test, build, tag, OCI, and digest identity.
  </behavior>
  <action>
    Create only `tests/test_production.py`. Use pathlib, subprocess, Django public settings/HTTP behavior, and exact public-file assertions; retain real Django, PostgreSQL, Gunicorn, WhiteNoise, static storage, and process collaborators. Make the first absent-artifact failure exactly `AssertionError: Dockerfile must exist`. Cover D-01 through D-05, D-08, the exact package/base/Action pins, allowlisted Docker copies, build-time `collectstatic`, manifest-hashed Task CSS, immutable cache response, fixed numeric identity, one worker, `/tmp`, SOURCE_RELEASE/IMAGE_REFERENCE record, exact-source workflow, and mutable-tag exclusion. Run through `./scripts/test-postgres.sh --pytest-only tests/test_production.py -q -x`; retain status 1 at the intended assertion. Commit exactly this file as `test(26-01): specify Django production image contract` with Stage 2 as its parent.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test "$(git log -1 --format=%s)" = "test(26-01): specify Django production image contract" &amp;&amp; test "$(git rev-parse HEAD^)" = 16279958ca774f7a34c25b0102a483df53160d6f &amp;&amp; test "$(git show --format= --name-only HEAD | sed '/^$/d')" = tests/test_production.py</automated>
  </verify>
  <acceptance_criteria>
    - The owned real-PostgreSQL wrapper reaches pytest collection and exits 1 at `Dockerfile must exist`.
    - RED changes exactly `tests/test_production.py`, has the exact subject, and directly follows protected Stage 2.
    - Tests define the complete public image/settings/static/process/publisher behavior before production implementation exists.
  </acceptance_criteria>
  <done>The committed RED proves the production seam is reachable and absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN - add, publish, and accept the hardened Image A source</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml
    /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py
    /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py
    .planning/phases/23-fastapi-production-stage/23-01-SUMMARY.md
    .planning/phases/26-django-production-stage/26-RESEARCH.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/.dockerignore, /Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml, /Users/longnv/bin/repo/sealos-django-tutorial/Dockerfile, /Users/longnv/bin/repo/sealos-django-tutorial/pyproject.toml, /Users/longnv/bin/repo/sealos-django-tutorial/requirements.txt, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/settings.py, /Users/longnv/bin/repo/sealos-django-tutorial/taskboard/wsgi.py, /Users/longnv/bin/repo/sealos-django-tutorial/uv.lock, .planning/phases/26-django-production-stage/26-01-SUMMARY.md</files>
  <behavior>
    - The focused RED turns green with zero test-file change.
    - The complete inherited Django/PostgreSQL suite and lock/export stay green.
    - Public main fast-forwards to the GREEN and Image A resolves anonymously by digest with matching source identity.
  </behavior>
  <action>
    Add exactly the eight implementation files in this task's target-repository scope. Add Gunicorn `26.0.0` and WhiteNoise `6.12.0` to runtime dependencies and regenerate `uv.lock` plus runtime-only `requirements.txt`. Extend settings with `DJANGO_PRODUCTION=1`, mandatory `DJANGO_SECRET_KEY`, mandatory comma-free `DJANGO_ALLOWED_HOSTS=localhost`, DEBUG false, WhiteNoise directly after SecurityMiddleware, `/static/`, `/app/staticfiles`, compressed manifest storage, and stable stdout/stderr logging while preserving the accepted PostgreSQL parser and exact health bodies. Extend WSGI so one non-preloaded worker import emits `event=service_start source_release=<40-hex> image_reference=<name@sha256:digest>` without credentials.

    Use the exact Python/uv index digests from research in a multi-stage Dockerfile. Resolve the committed lock with `uv sync --locked --no-dev`; copy only `manage.py`, `taskboard/`, `tasks/`, the virtual environment, and collected `staticfiles`; run `collectstatic --noinput` in the builder with a build-only secret; create UID/GID 10001; expose 8000; exec Gunicorn `taskboard.wsgi:application` with one sync worker, bind `0.0.0.0:8000`, worker tmp `/tmp`, timeout 30, graceful timeout 25, access/error logs to standard streams, and no preload. `.dockerignore` excludes VCS, workflows, tests, deploy, scripts, evidence, caches, local state, README, and common secret files.

    Add the publisher with PostgreSQL `17.10-bookworm` at the research digest and exactly these Action commits: checkout `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0`, setup-uv `11f9893b081a58869d3b5fccaea48c9e9e46f990`, login `af1e73f918a031802d376d3c8bbc3fe56130a9b0`, buildx `bb05f3f5519dd87d3ba754cc423b652a5edd6d2c`, metadata `dc802804100637a589fabce1cb79ff13a1411302`, and build-push `53b7df96c91f9c12dcc8a07bcb9ccacbed38856a`. Require exact checkout, lock/export, repeat migration, `[X] 0001_initial`, zero drift, all tests, linux/amd64 build, build-time static inspection, one `sha-<40-character-TARGET_SHA>` tag, OCI source/revision, and anonymous digest/config/manifest readback. Commit only the eight implementation files as `feat(26-01): add hardened Django production image`, directly after RED.

    Run focused and full gates, push public main only when remote is Stage 2 or this exact GREEN, and accept an exact successful push/dispatch workflow for the GREEN. Workflow recovery states are: exact success accepts; queued/running waits boundedly; absent dispatches the exact source; one infrastructure failure may rerun that same run; source/test failure halts. GHCR tag state is absent-before-publish or exact-after-publish; any different digest/OCI identity halts. Make the linked package public only after owner/source linkage agrees. Use a fresh empty mode-0700 DOCKER_CONFIG and clear all registry/token variables for each anonymous gate. Record `baseline_source`, `baseline_image`, workflow ID/URL/event/head/target, linux/amd64 child, OCI labels, collected manifest/hashed CSS, package visibility, and zero scratch residue in 26-01-SUMMARY.md.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_production.py -q -x &amp;&amp; uv lock --check &amp;&amp; ./scripts/test-postgres.sh --phase-gate &amp;&amp; ./scripts/test-postgres.sh --assert-clean-all &amp;&amp; test "$(git log -1 --format=%s)" = "feat(26-01): add hardened Django production image" &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "test(26-01): specify Django production image contract" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' ')" = ".dockerignore .github/workflows/publish-image.yml Dockerfile pyproject.toml requirements.txt taskboard/settings.py taskboard/wsgi.py uv.lock " &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-django-tutorial/commits/main --jq .sha)" = "$(git rev-parse HEAD)"</automated>
  </verify>
  <acceptance_criteria>
    - GREEN directly follows RED, changes exactly eight implementation files, and changes zero tests.
    - Lock/export, focused production tests, all inherited real-PostgreSQL behaviors, Gunicorn smoke, collected manifest/hashed CSS, WhiteNoise response/cache, and global cleanup pass.
    - Public main equals the GREEN through a normal fast-forward; the exact workflow succeeds and its sole full-SHA tag resolves anonymously to a public linux/amd64 Image A digest with matching OCI source/revision.
    - Stage 1/2 tag objects/messages, migration hash, and ruleset 19014157 remain exact; all scratch registry configs and workflow logs are credential-free and removed.
  </acceptance_criteria>
  <done>Image A is the first public production-capable child of Stage 2 and a reproducible immutable rollback target.</done>
</task>

</tasks>

## Artifacts this phase produces

- `tests/test_production.py`
- Production setting contracts `DJANGO_PRODUCTION`, `DJANGO_SECRET_KEY`, and `DJANGO_ALLOWED_HOSTS`
- Static settings `STATIC_ROOT`, `STATIC_URL`, and `STORAGES`
- Worker release record fields `event`, `source_release`, and `image_reference`
- `Dockerfile`, `.dockerignore`, `.github/workflows/publish-image.yml`
- Workflow input `source_sha`, normalized `TARGET_SHA`, and digest output
- Public Image A tag `sha-<40-character-source>` and immutable GHCR digest
- `.planning/phases/26-django-production-stage/26-01-SUMMARY.md`

<external_mutation_state_machine>
GitHub and GHCR mutations start only after local tests, exact scope, and clean ancestry pass. Remote main accepts Stage 2 before a normal fast-forward or the exact GREEN after recovery. Workflow/tag/package states accept absent, active bounded-wait, exact-success, or one infrastructure-only rerun; every source, target, digest, owner, OCI, or mutable-tag mismatch halts before further mutation. Every temporary registry config uses a fresh BSD-safe directory, mode 0700, a cleanup trap, and a cleared credential environment.
</external_mutation_state_machine>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI and container registries to lock/image | External package and base identities become executable runtime bytes. |
| GitHub source to Actions/GHCR | One source commit becomes a public image tag and digest. |
| Runtime environment to Django/Gunicorn | Secrets, host policy, database URL, and release identity enter the process. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-01-S | Spoofing | Source/image identity | high | mitigate | Correlate TARGET_SHA, checkout HEAD, full-SHA tag, workflow digest, OCI revision/source, and anonymous readback. |
| T-26-01-T | Tampering | Package/base/Action inputs | high | mitigate | Re-resolve approved exact versions, index/child digests, and six full Action SHAs before mutation. |
| T-26-01-I | Information Disclosure | Settings/workflow logs | high | mitigate | Secret-only runtime input, credential-free release records, redacted logs, and isolated anonymous registry configs. |
| T-26-01-E | Elevation of Privilege | Runtime image | high | mitigate | UID/GID 10001, allowlisted copies, one worker, and image tests for runtime contents. |
| T-26-01-SC | Tampering | PyPI installs | high | mitigate | User-approved official Gunicorn/WhiteNoise identities plus fail-closed pre-install drift check. |
</threat_model>

<verification>
1. Prove protected Stage 2 and current immutable supply-chain inputs before mutation.
2. Prove the exact RED failure, one-file scope, subject, and parent.
3. Prove direct GREEN adjacency, exact eight-file scope, static/real PostgreSQL behavior, lock/export, process/static asset behavior, and clean state.
4. Prove normal public main fast-forward plus exact successful workflow/package/tag/digest/OCI/anonymous Image A identity.
</verification>

<success_criteria>
- The first TDD pair has exact subjects, direct ancestry, and disjoint test/implementation scopes.
- Image A serves Django through one non-root Gunicorn worker with collected immutable WhiteNoise assets and stable release logs.
- Public Image A is anonymously reproducible from its exact full source SHA and leaves zero temporary footprint.
</success_criteria>

<output>
Create `.planning/phases/26-django-production-stage/26-01-SUMMARY.md` with package/image/Action preflight, TDD commits/scopes, Image A source/workflow/tag/digest/OCI/static identities, public-main result, and cleanup.
</output>
