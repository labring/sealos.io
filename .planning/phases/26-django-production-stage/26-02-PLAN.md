---
phase: 26-django-production-stage
plan: "02"
type: tdd
wave: 2
depends_on: ["26-01"]
files_modified:
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
  - /Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml
  - /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml
  - /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
  - /Users/longnv/bin/repo/sealos-django-tutorial/README.md
  - .planning/phases/26-django-production-stage/26-02-SUMMARY.md
autonomous: true
requirements: [DJAN-03]
must_haves:
  truths:
    - "D-06: The same immutable candidate digest and run-owned Secret feed a completed Django migration Job before a two-replica Deployment becomes ready."
    - "D-03/D-04: The workload uses localhost host validation, one Gunicorn sync worker per Pod, UID/GID 10001, restricted security, read-only root, and bounded memory-backed /tmp."
    - "D-08: The final reader source publishes as a second distinct linux/amd64 image from the README-only Image B commit."
    - "D-11: The workload contract uses the exact three-file test-only RED, its direct three-file implementation-only GREEN, and one README-only source freeze commit."
    - "The frozen Image B tree has exactly 29 commits, 38 tracked files, the five exact Phase 26 subjects, and immutable migration 0001."
  artifacts:
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml"
      provides: "Parameterized two-replica Deployment and Service"
      contains: "__IMAGE_REFERENCE__"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml"
      provides: "Same-image production migration Job"
      contains: "django-secret-key"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh"
      provides: "Registry, renderer, four-state runtime, evidence, publication, and read-only cleanup state machine"
      contains: "--assert-clean-all"
    - path: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      provides: "Immutable production reader workflow"
      contains: "stage-3-production"
    - path: ".planning/phases/26-django-production-stage/26-02-SUMMARY.md"
      provides: "Machine-readable Image A/Image B source and digest inputs"
  key_links:
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml"
      via: "one validated digest and one Secret name rendered into both resources"
      pattern: "__IMAGE_REFERENCE__|__SECRET_NAME__"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh"
      via: "owned PostgreSQL session lifecycle and final read-only global audit"
      pattern: "--session-start|--assert-clean-all"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/README.md"
      to: "ghcr.io/yangchuansheng/sealos-django-tutorial@sha256:<digest>"
      via: "full-source-SHA lookup followed by digest-only deployment"
      pattern: "sha-"
---

<objective>
Specify and freeze the complete production workload and final Image B source per D-03, D-04, D-06, D-08, and D-11.

Purpose: Connect immutable Django images to migration-first Sealos rollout, hardened two-Pod runtime, evidence, cleanup, and a reproducible reader workflow.
Output: The second exact RED/GREEN pair, README-only Image B source, public final workflow, and two distinct accepted image digests.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
@/Users/longnv/.agents/skills/tdd/SKILL.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-django-production-stage/26-CONTEXT.md
@.planning/phases/26-django-production-stage/26-RESEARCH.md
@.planning/phases/26-django-production-stage/26-PATTERNS.md
@.planning/phases/26-django-production-stage/26-VALIDATION.md
@.planning/phases/26-django-production-stage/26-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED - specify the Django production workload contract</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
    /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
    .planning/phases/26-django-production-stage/26-PATTERNS.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py, /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py</files>
  <behavior>
    - The application template contains exact run/app/image/source/Secret tokens and renders a two-replica restricted Deployment plus ClusterIP Service.
    - The production Job and Deployment consume the same digest and Secret; the Job runs migrate before readiness and settings receive the runtime secret and localhost host policy.
    - The production harness validates exact token sets, mode-0600 renders, server-side strict dry-runs, four ordered rollout states, nine live evidence files, ten publication files, atomic checksums, credential scanning, and cleanup.
    - `--assert-clean-all` reads production/PostgreSQL ownership ledgers, seven Kubernetes kinds, processes, browsers, registry configs, renders, state, clones, and evidence scratch paths without mutation.
  </behavior>
  <action>
    Modify exactly the three named test files. Extend the exact migration-Job text for `DJANGO_SECRET_KEY`, `DJANGO_PRODUCTION=1`, `DJANGO_ALLOWED_HOSTS=localhost`, and the same digest/Secret contract. Extend the harness source tests for a production runner that owns `--run`, `--preflight-evidence publication`, `--verify-evidence live`, `--verify-evidence publication`, `--assert-clean-all`, and `--help`; validate exact renderer token sets, BSD-safe temporary paths, mode 0600, strict server dry-run, baseline/final Job ordering, four runtime states, evidence schemas/counts, atomic checksum order, credential patterns, cleanup traps, and read-only audit source. Extend production tests for the exact Deployment/Service resource, readiness Host `localhost`, two replicas, RollingUpdate 0/1, revision history 3, progress deadline 180, port 8000, one-worker command, resources, security, and bounded `/tmp`. The first intended failure must be `AssertionError: deploy/application.yaml must exist`. Run the three focused files through the real PostgreSQL wrapper and retain status 1. Commit exactly these three tests as `test(26-02): specify Django production workload contract`, directly after Image A GREEN.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test "$(git log -1 --format=%s)" = "test(26-02): specify Django production workload contract" &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "feat(26-01): add hardened Django production image" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' ')" = "tests/test_migration_job.py tests/test_postgres_harness.py tests/test_production.py "</automated>
  </verify>
  <acceptance_criteria>
    - Focused real-PostgreSQL pytest collects and exits 1 at `deploy/application.yaml must exist`.
    - RED directly follows Image A GREEN, changes exactly the three named tests, and has the exact subject.
    - Public/static tests fully specify migration-first workload, four-state runner, evidence, redaction, and read-only cleanup before implementation exists.
  </acceptance_criteria>
  <done>The second committed RED reaches the public workload/harness seam and fails for the absent application template.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN - add the migration-first workload and production harness</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_migration_job.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_postgres_harness.py
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/deploy/source-migration-job.yaml
    .planning/phases/23-fastapi-production-stage/23-02-SUMMARY.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml, /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml, /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh</files>
  <behavior>
    - All three RED files turn green with zero test change.
    - The committed resources can be strictly rendered for either immutable candidate with one run identity and Secret.
    - The production runner exposes complete registry/runtime/evidence/publication/cleanup modes while the Stage 2 source Job remains byte-exact.
  </behavior>
  <action>
    Add exactly `deploy/application.yaml` and `scripts/test-production.sh`, and evolve exactly `deploy/migration-job.yaml`. Application tokens are `__RUN_ID__`, `__APP_NAME__`, `__IMAGE_REFERENCE__`, `__SOURCE_RELEASE__`, and `__SECRET_NAME__`. Render a two-replica Deployment and Service with exact run labels, revision history 3, progress deadline 180, RollingUpdate maxUnavailable 0/maxSurge 1, digest-only image, atomic SOURCE_RELEASE/IMAGE_REFERENCE, database and Django secret keyRefs, `DJANGO_PRODUCTION=1`, `DJANGO_ALLOWED_HOSTS=localhost`, one explicit Gunicorn sync worker, readiness GET `/health` with Host localhost, 100m/128Mi requests, 500m/512Mi limits, UID/GID 10001, RuntimeDefault, disabled service token/escalation, dropped ALL capabilities, read-only root, termination grace 35, and 64Mi memory-backed `/tmp`. The Service maps port 8000 to named `http`.

    Preserve the production Job's bounded one-shot security/resource contract, add runtime secret/host/production/release environment, and ensure it uses the identical candidate digest and Secret. Preserve `deploy/source-migration-job.yaml` byte-for-byte. Implement `scripts/test-production.sh` as Bash 3.2-compatible composition over the existing PostgreSQL session. Validate exact newline-free token values and digest/source/name formats before strict mode-0600 rendering and server-side dry-run. Install cleanup before first mutation. Own application Deployment, ReplicaSets, Pods, Service, two candidate Jobs, Secret augmentation, app port-forwards, named browser, state/renders/clones, evidence scratch, and every isolated registry config. Implement the fixed baseline migration/deploy/task-create, final migration/deploy, `kubectl rollout undo`, and explicit final apply sequence; per-state assertions belong to `--run`. Evidence modes require the exact nine live and ten publication data files described in 26-PATTERNS.md. `--assert-clean-all` performs only read operations while combining the existing Stage 2 global audit with production ownership checks.

    Run the three focused tests, `bash -n`, `--help`, fixture rendering and semantic evidence tests, the complete inherited PostgreSQL phase gate, credential scans, migration hash, and global cleanup. Commit exactly these three implementation files as `feat(26-02): add Django production workload contracts`, directly after RED and with zero test change.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-postgres.sh --pytest-only tests/test_migration_job.py tests/test_postgres_harness.py tests/test_production.py -q -x &amp;&amp; bash -n scripts/test-production.sh &amp;&amp; ./scripts/test-production.sh --help &gt;/dev/null &amp;&amp; ./scripts/test-postgres.sh --phase-gate &amp;&amp; ./scripts/test-production.sh --assert-clean-all &amp;&amp; test "$(git log -1 --format=%s)" = "feat(26-02): add Django production workload contracts" &amp;&amp; test "$(git log -1 --format=%s HEAD^)" = "test(26-02): specify Django production workload contract" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort | tr '\n' ' ')" = "deploy/application.yaml deploy/migration-job.yaml scripts/test-production.sh "</automated>
  </verify>
  <acceptance_criteria>
    - GREEN directly follows RED, changes exactly three implementation files, and changes zero tests.
    - Focused tests, strict render fixture gates, shell syntax/help, inherited full PostgreSQL gate, immutable source Job comparison, migration hash, and read-only global audit pass.
    - Application and migration templates share the exact candidate digest and Secret, and the runtime satisfies every D-03/D-04/D-06 security/readiness/process constraint.
    - Runner evidence and cleanup modes implement exact schemas, credential scanning, atomic checksums, and fail-closed external recovery states.
  </acceptance_criteria>
  <done>The frozen public workload source is statically green and its real-collaborator runner is ready without further implementation changes.</done>
</task>

<task type="auto">
  <name>Task 3: Freeze reader documentation and publish the final Image B</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/README.md
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml
    .planning/phases/26-django-production-stage/26-01-SUMMARY.md
  </read_first>
  <files>/Users/longnv/bin/repo/sealos-django-tutorial/README.md, .planning/phases/26-django-production-stage/26-02-SUMMARY.md</files>
  <action>
    Update only README.md with Python 3.12 lock/export, exact full-SHA image lookup, digest-only same-image migration Job and Deployment rendering, migration-before-readiness, localhost Host policy, Gunicorn one-worker/two-Pod topology, WhiteNoise hashed static verification, `/health`, board/admin continuity, hardening, four-state rollback/recovery, three source stages, evidence verification, and read-only cleanup. Keep dynamic digests in Phase 26 evidence so the README commit stays the final OCI source. Commit exactly README.md as `docs(26-02): document immutable Django production release`.

    Freeze this commit as FINAL_SHA/Image B and prohibit every later target-repository source write in Phase 26. Mechanically require the five exact Phase 26 subjects in order, 29 total commits, 38 tracked files, zero duplicate subjects, clean migration hash, and six added files. Public-main recovery accepts Image A before a normal fast-forward or this exact FINAL_SHA after recovery; every other main halts. Push FINAL_SHA normally. Accept or dispatch the exact-source publisher using the same bounded workflow/package/tag recovery grammar as Plan 26-01. Require a distinct public digest, linux/amd64, matching OCI source/revision, build-time collected manifest and hashed CSS, and isolated anonymous digest/config/manifest readback for Image B and then Image A with separate fresh mode-0700 configs.

    Write exactly four machine-readable lines in 26-02-SUMMARY.md: `baseline_source=<40 hex>`, `baseline_image=ghcr.io/yangchuansheng/sealos-django-tutorial@sha256:<digest>`, `final_source=<40 hex>`, and `final_image=ghcr.io/yangchuansheng/sealos-django-tutorial@sha256:<digest>`. Also record both workflow IDs/head/target/conclusion, package visibility, Image A/B architecture/OCI/static identities, exact five-commit/29-commit/38-file proof, source tree checksum, Stage 1/2/ruleset preservation, credential scan, and zero scratch residue.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test "$(git log -1 --format=%s)" = "docs(26-02): document immutable Django production release" &amp;&amp; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sed '/^$/d')" = README.md &amp;&amp; test "$(git rev-list --count HEAD)" = 29 &amp;&amp; test "$(git ls-tree -r --name-only HEAD | wc -l | tr -d ' ')" = 38 &amp;&amp; test "$(git log --format=%s | sort | uniq -d | wc -l | tr -d ' ')" = 0 &amp;&amp; test "$(shasum -a 256 tasks/migrations/0001_initial.py | awk '{print $1}')" = 745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3 &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-django-tutorial/commits/main --jq .sha)" = "$(git rev-parse HEAD)" &amp;&amp; test -z "$(git status --short)"</automated>
  </verify>
  <acceptance_criteria>
    - The final source sequence is exactly Pair 1 RED/GREEN, Pair 2 RED/GREEN, and README-only docs; total inventory is 29 commits and 38 files with no duplicate subject.
    - README contains the complete immutable production reader workflow and is the only file in the final source commit.
    - Public main equals FINAL_SHA; Image B is distinct from Image A and both resolve anonymously to linux/amd64 digests with matching OCI/static identities.
    - The migration hash, protected Stage 1/2 refs/messages, and ruleset 19014157 remain exact; the target tree is clean and source-frozen.
  </acceptance_criteria>
  <done>The complete reader source is frozen on public main and maps to two distinct production-capable immutable image digests.</done>
</task>

</tasks>

## Artifacts this phase produces

- Extended `tests/test_migration_job.py`, `tests/test_postgres_harness.py`, and `tests/test_production.py`
- `deploy/application.yaml`
- Production-secret-aware `deploy/migration-job.yaml`
- `scripts/test-production.sh` modes `--run`, `--preflight-evidence publication`, `--verify-evidence live`, `--verify-evidence publication`, `--assert-clean-all`, and `--help`
- Render tokens `__RUN_ID__`, `__APP_NAME__`, `__IMAGE_REFERENCE__`, `__SOURCE_RELEASE__`, `__SECRET_NAME__`, and `__JOB_NAME__`
- README-only Image B source and public full-SHA tag/digest
- `.planning/phases/26-django-production-stage/26-02-SUMMARY.md`

<external_mutation_state_machine>
Kubernetes fixture validation uses dry-run only in this plan. GitHub main accepts Image A before the normal FINAL_SHA fast-forward or exact FINAL_SHA after recovery. Workflow, GHCR tag, and package states follow absent, bounded-active, exact-success, or one infrastructure-only rerun branches. Source/test failure, unexpected main, digest collision, OCI mismatch, mutable tag, or migration-hash drift halts. After Image B acceptance, all dynamic facts are written only in Sealos.io planning artifacts.
</external_mutation_state_machine>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer input to Kubernetes API | Run names, source SHAs, image digests, and Secret names become workload objects. |
| GHCR image to PostgreSQL Secret | Migration and serving containers execute with run-owned credentials. |
| Source freeze to public image | README-only FINAL_SHA becomes Image B OCI revision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-02-T | Tampering | Manifest renderer | high | mitigate | Exact token equality, strict formats, mode-0600 output, unresolved-token rejection, and server-side validation. |
| T-26-02-I | Information Disclosure | Secret/evidence boundary | high | mitigate | KeyRefs only, generated ephemeral values, curated semantic evidence, credential scans, and cleanup traps. |
| T-26-02-E | Elevation of Privilege | Job and Deployment | high | mitigate | Fixed 10001 identity, RuntimeDefault, dropped ALL, disabled escalation/token, read-only root, and bounded tmpfs. |
| T-26-02-R | Repudiation | Final source/image | high | mitigate | Exact commit sequence/scope, 29/38 inventory, workflow head/target, digest, OCI identity, and no-later-write freeze. |
| T-26-02-D | Denial of Service | Rollout/migration | medium | mitigate | Bounded Job/rollout deadlines, resources, readiness, two replicas, and 35-second termination grace. |
</threat_model>

<verification>
1. Prove Pair 2 RED exact three-test scope, intended failure, subject, and Image A parent.
2. Prove direct GREEN exact three-implementation scope, static/fixture behavior, inherited real PostgreSQL gate, and read-only cleanup.
3. Prove README-only final commit, exact five-subject sequence, 29/38 inventory, immutable migration, and clean source freeze.
4. Prove normal public main fast-forward and distinct exact-source Image B workflow/tag/digest/OCI/static identity while preserving Image A.
</verification>

<success_criteria>
- The second TDD pair has exact subjects, direct ancestry, and disjoint three-file test/implementation scopes.
- The final source implements same-digest migration-first deployment, two hardened Gunicorn Pods, strict readiness, evidence, and read-only cleanup.
- Public main equals the 29-commit/38-file Image B source, and Image A plus Image B are distinct anonymous immutable release inputs.
</success_criteria>

<output>
Create `.planning/phases/26-django-production-stage/26-02-SUMMARY.md` with Pair 2 proof, workload/harness contracts, exact source inventory, Image A/B release block, workflow/package/image readback, source freeze, and cleanup.
</output>
