---
phase: 26-django-production-stage
plan: "04"
type: execute
wave: 4
depends_on: ["26-03"]
files_modified:
  - .planning/phases/26-django-production-stage/evidence/publication.txt
  - .planning/phases/26-django-production-stage/evidence/cleanup.txt
  - .planning/phases/26-django-production-stage/evidence/checksums.txt
  - .planning/phases/26-django-production-stage/26-04-SUMMARY.md
autonomous: true
requirements: [DJAN-03, DJAN-04]
must_haves:
  truths:
    - "D-10: Public main equals the frozen 29-commit/38-file Image B source and protected annotated stage-3-production has exact message `Django production stage`."
    - "D-10: Stage 1 direct/peeled/message, Stage 2 direct/peeled/message, and active ruleset 19014157 remain exact before and after Stage 3 publication."
    - "D-08: Fresh anonymous configurations reproduce distinct Image A and Image B digests, linux/amd64 manifests, and OCI source/revision from their full-SHA tags."
    - "Fresh public HTTPS clones of Stages 1, 2, and 3 reproduce their exact source boundaries; Stage 3 proves the exact five subjects, 29/38 inventory, static contracts, and a fresh real-PostgreSQL gate."
    - "D-12: Ten curated data files pass checksum-independent publication preflight, a ten-entry atomic checksum manifest, one full verifier, read-only sealing, and final zero-residue audit."
    - "D-12: Phase-owned temporary GHCR versions are deleted only by verified immutable ID, or an externally blocked exact ID is recorded for Phase 28 while Image A and Image B remain durable."
  artifacts:
    - path: ".planning/phases/26-django-production-stage/evidence/publication.txt"
      provides: "Public main, three tag identities, ruleset, clones, workflows, package, and two-image replay"
      contains: "stage_3_message=Django production stage"
    - path: ".planning/phases/26-django-production-stage/evidence/checksums.txt"
      provides: "Ten-entry final evidence integrity manifest"
    - path: ".planning/phases/26-django-production-stage/evidence/cleanup.txt"
      provides: "Final read-only zero inventory across runtime and replay ownership"
      contains: "inventory=0"
    - path: ".planning/phases/26-django-production-stage/26-04-SUMMARY.md"
      provides: "Independent verifier handoff for DJAN-03 and DJAN-04"
  key_links:
    - from: "refs/tags/stage-3-production^{}"
      to: "refs/heads/main"
      via: "exact frozen FINAL_SHA equality"
      pattern: "stage-3-production"
    - from: "fresh public Stage 3 clone"
      to: "ghcr.io/yangchuansheng/sealos-django-tutorial:sha-<FINAL_SHA>"
      via: "clone-derived full source SHA and isolated anonymous OCI replay"
      pattern: "org.opencontainers.image.revision"
    - from: ".planning/phases/26-django-production-stage/evidence/publication.txt"
      to: ".planning/phases/26-django-production-stage/evidence/checksums.txt"
      via: "checksum-independent semantic preflight followed by atomic final manifest"
      pattern: "publication.txt"
---

<objective>
Publish protected Django Stage 3 and independently reproduce the complete source/image release per D-08, D-10, and D-12.

Purpose: Give readers three immutable public source boundaries, two anonymous production image inputs, and sealed evidence with an empty practice footprint.
Output: Protected annotated Stage 3, three fresh public clone replays, two anonymous image replays, ten checksums, read-only evidence, and final cleanup.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-django-production-stage/26-CONTEXT.md
@.planning/phases/26-django-production-stage/26-VALIDATION.md
@.planning/phases/26-django-production-stage/26-02-SUMMARY.md
@.planning/phases/26-django-production-stage/26-03-SUMMARY.md
@.planning/phases/26-django-production-stage/evidence/checksums.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish protected Stage 3 through one coherent recovery state</name>
  <read_first>
    .planning/phases/26-django-production-stage/26-02-SUMMARY.md
    .planning/phases/26-django-production-stage/26-03-SUMMARY.md
    .planning/phases/26-django-production-stage/evidence/images.txt
    .planning/phases/26-django-production-stage/evidence/rollback.txt
    /Users/longnv/bin/repo/sealos-django-tutorial/README.md
    /Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/publication.txt</files>
  <action>
    Before tag mutation, require local target clean at FINAL_SHA, public main equal FINAL_SHA, exact five Phase 26 subjects, 29 commits, 38 files, immutable migration, Image B digest equal the live recovered digest, both exact successful workflows with coherent API head/normalized target, public linked package, and isolated anonymous Image A/B digest/config/manifest/OCI identity. Re-run Image workflow recovery only through exact success, bounded active wait, absent exact source dispatch, or one infrastructure-only rerun; source/test failure or identity drift halts. Record workflow API head and normalized target separately.

    Freeze Stage 1 direct `0d9254d37914976898039ff3c55f94399aa1d7c0`, peeled `ca115bf21b599c14e667b336bd78e3c587c24208`, message `Django deploy stage`; Stage 2 direct `16f60a44885216fa35d67b0334914d8b8d4e8577`, peeled `16279958ca774f7a34c25b0102a483df53160d6f`, message `Django PostgreSQL stage`; and active ruleset `19014157` for `refs/tags/stage-*` with update/deletion rules and empty exclude/bypass. Recover `stage-3-production` through exactly: absent/absent creates one annotated tag at FINAL_SHA with message `Django production stage` then pushes only that ref; exact-local/absent-remote pushes only the validated ref; absent-local/exact-remote fetches and validates it; exact/exact requires identical direct object. Lightweight, different object/message/peeled source, or any fifth state halts without update/delete/force. Re-read all constants after publication and write curated values only.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; FINAL_SHA=$(git rev-parse HEAD) &amp;&amp; test "$(gh api repos/yangchuansheng/sealos-django-tutorial/commits/main --jq .sha)" = "$FINAL_SHA" &amp;&amp; test "$(git cat-file -t stage-3-production)" = tag &amp;&amp; test "$(git rev-parse 'stage-3-production^{}')" = "$FINAL_SHA" &amp;&amp; test "$(git for-each-ref refs/tags/stage-3-production --format='%(contents:subject)')" = "Django production stage" &amp;&amp; test "$(git ls-remote origin 'refs/tags/stage-3-production^{}' | awk '{print $1}')" = "$FINAL_SHA"</automated>
  </verify>
  <acceptance_criteria>
    - Public main/Image B/live recovery/workflow/package/anonymous OCI identity agree before tag mutation.
    - Stage 1/2 direct objects, peeled commits, messages, and ruleset 19014157 are exact before and after publication.
    - Stage 3 is one protected annotated object at FINAL_SHA with exact message and matching local/remote direct object.
    - Publication uses only normal fast-forward branch and single-ref tag creation/push; target source receives zero commit.
  </acceptance_criteria>
  <done>Public main, three protected source stages, and both immutable images form one coherent publication state.</done>
</task>

<task type="auto">
  <name>Task 2: Replay all three source stages and both images from public inputs</name>
  <read_first>
    .planning/phases/26-django-production-stage/evidence/publication.txt
    .planning/phases/26-django-production-stage/evidence/images.txt
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/tests/test_production.py
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/publication.txt, .planning/phases/26-django-production-stage/evidence/cleanup.txt</files>
  <action>
    Create three separate BSD-safe mode-0700 temporary directories outside both repositories and install cleanup before cloning. Clone each tag by public HTTPS. Stage 1 must peel to `ca115bf21b599c14e667b336bd78e3c587c24208`, reproduce lock/export, and pass its public suite. Stage 2 must peel to `16279958ca774f7a34c25b0102a483df53160d6f`, reproduce lock/export and migration hash, and pass its real PostgreSQL contract. Stage 3 must peel to FINAL_SHA, have exactly 29 commits and 38 files, contain the exact ordered five Phase 26 subjects with unique history, reproduce lock/export and migration hash, pass all production/migration/harness tests, shell syntax/help, strict render fixtures, source/static/README checks, and a fresh real-PostgreSQL phase gate with evidence outside the clone. Every clone must end clean.

    Derive `sha-${FINAL_SHA}` from the Stage 3 clone and `sha-${BASELINE_SHA}` from the recorded first GREEN. For each image create a different fresh empty mode-0700 DOCKER_CONFIG, clear all registry/token variables, anonymously read digest/config/manifest, and require recorded digest, linux/amd64, exact OCI source/revision, collected manifest/hashed CSS, non-root identity, and Gunicorn command. Append only source/tag/test/image semantic results. Stop each owned database session, run the final production `--assert-clean-all`, remove all clones, external replay evidence, and registry configs, and append exact zero fields to cleanup.txt.

    Enumerate GHCR package versions through the authenticated package API and map every version to immutable digest, tags, and source revision before mutation. Preserve the accepted Image A and Image B versions. Delete only phase-owned temporary versions by exact verified version ID. If GitHub rejects an exact deletion for an external policy or permission reason, retain the version and append its exact ID, digest/tags, source revision, attempted operation, stable error class, and `phase_28_disposition=pending` to publication.txt and cleanup.txt. Any unattributed version, identity mismatch, or broad delete request fails closed.
  </action>
  <verify>
    <automated>cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; rg -q '^public_stage_1_clone=passed ' .planning/phases/26-django-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_stage_2_clone=passed ' .planning/phases/26-django-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_stage_3_clone=passed ' .planning/phases/26-django-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_image_a_replay=passed ' .planning/phases/26-django-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_image_b_replay=passed ' .planning/phases/26-django-production-stage/evidence/publication.txt &amp;&amp; rg -q '^public_clone_cleanup=passed ' .planning/phases/26-django-production-stage/evidence/cleanup.txt</automated>
  </verify>
  <acceptance_criteria>
    - Three independent public clones peel to exact Stage 1/2/3 commits and reproduce each stage's lock/export and applicable tests.
    - Stage 3 proves the exact five subjects, 29/38 inventory, production static contracts, fresh real PostgreSQL behavior, and clean tree.
    - Separate anonymous configs reproduce Image A and Image B digests, linux/amd64, OCI identities, collected assets, non-root identity, and Gunicorn command.
    - Every clone, external replay directory, exact-run object/process/browser/path, and registry config is removed.
    - Phase-owned temporary GHCR versions are absent, or each externally blocked version is recorded by exact immutable ID for Phase 28; Image A and Image B remain untouched.
  </acceptance_criteria>
  <done>A reader starting from public HTTPS tags and anonymous GHCR can reproduce all three source stages and both release images.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize, verify, seal, and read back the publication evidence</name>
  <read_first>
    .planning/phases/26-django-production-stage/evidence/workflow.txt
    .planning/phases/26-django-production-stage/evidence/images.txt
    .planning/phases/26-django-production-stage/evidence/collectstatic.txt
    .planning/phases/26-django-production-stage/evidence/migrations.txt
    .planning/phases/26-django-production-stage/evidence/runtime.txt
    .planning/phases/26-django-production-stage/evidence/logs.txt
    .planning/phases/26-django-production-stage/evidence/http.jsonl
    .planning/phases/26-django-production-stage/evidence/rollback.txt
    .planning/phases/26-django-production-stage/evidence/publication.txt
    .planning/phases/26-django-production-stage/evidence/cleanup.txt
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/checksums.txt, .planning/phases/26-django-production-stage/26-04-SUMMARY.md</files>
  <action>
    Complete publication.txt and cleanup.txt before checksum work. Run checksum-independent `--preflight-evidence publication`; require exactly ten data files, all live semantics, three direct/peeled/message identities, ruleset 19014157, public main equality, 29/38, both workflow head/target pairs, package, both isolated image replays, three clone outcomes, source freeze, final cleanup, the accepted Image A/B version IDs, and either zero temporary GHCR versions or exact blocked-ID Phase 28 records. Require the credential scan. The preflight may treat only checksums.txt as stale or absent.

    Generate checksums.txt atomically through `mktemp "$EVIDENCE/.checksums.XXXXXX"` with exactly ten sorted repository-relative `shasum -a 256` entries: workflow, images, collectstatic, migrations, runtime, logs, http.jsonl, rollback, publication, cleanup. Rename only the complete manifest, run full `--verify-evidence publication` once, verify every checksum, then set all eleven evidence files read-only. From that point allow only parsing, counts, hashes, and public/live readback.

    Re-read public main, all three direct/peeled refs/messages, ruleset, both workflows, package visibility/linkage, both anonymous images/OCI identities, migration hash, target clean status, and the exact five/29/38 inventory. Run final production `--assert-clean-all` and exact recorded-label/ownership-ledger audits after sealing; require seven-kind inventory, processes, browsers, state, renders, clones, configs, scratch, and ledgers all zero. Create 26-04-SUMMARY.md outside evidence with publication recovery branch, Stage 3 direct object, all constants, public replays, final manifest, modes, source freeze, and cleanup. Create no target commit and perform no post-seal mutation.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; EVIDENCE=/Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/26-django-production-stage/evidence &amp;&amp; ./scripts/test-production.sh --verify-evidence publication --evidence-dir "$EVIDENCE" &amp;&amp; ./scripts/test-production.sh --assert-clean-all &amp;&amp; cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; test "$(wc -l &lt; .planning/phases/26-django-production-stage/evidence/checksums.txt | tr -d ' ')" = 10 &amp;&amp; shasum -a 256 -c .planning/phases/26-django-production-stage/evidence/checksums.txt &amp;&amp; test "$(find .planning/phases/26-django-production-stage/evidence -type f -perm -u=w -print | wc -l | tr -d ' ')" = 0</automated>
  </verify>
  <acceptance_criteria>
    - Publication preflight accepts exactly ten data files and every semantic/redaction/cleanup link while checksums are pending.
    - Atomic final manifest contains exactly ten sorted entries; one full verifier and every checksum pass before all eleven files become read-only.
    - Final read-only public/source/image/runtime audit agrees with Stage 1/2/3, ruleset, Image A/B, 29/38, migration, and target-source freeze.
    - Final read-only cleanup reports every run-owned cluster/local category zero.
    - GHCR cleanup preserves accepted Image A/B and proves zero phase-owned temporary versions or exact blocked-ID handoff to Phase 28.
  </acceptance_criteria>
  <done>DJAN-03/DJAN-04 verification inputs are sealed, reproducible from public inputs, and closed with an empty temporary footprint.</done>
</task>

</tasks>

## Artifacts this phase produces

- Protected annotated `stage-3-production`
- Exact tag message `Django production stage`
- `evidence/publication.txt`
- Ten-entry final `evidence/checksums.txt`
- Read-only eleven-file evidence package
- `.planning/phases/26-django-production-stage/26-04-SUMMARY.md`

<external_mutation_state_machine>
Tag mutation begins only after public main, Image B, live recovery, Stage 1/2 constants, ruleset, workflows, package, and anonymous images agree. Stage 3 permits exactly absent/absent, exact-local/absent-remote, absent-local/exact-remote, or exact/exact. Public replay owns exact clone/database/registry identities under one cleanup trap. GHCR cleanup targets only verified temporary version IDs and preserves accepted Image A/B; blocked exact IDs become explicit Phase 28 handoff records. After final checksum verification, external operations and evidence access are read-only.
</external_mutation_state_machine>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local Git to protected public refs | One annotated object becomes durable reader source. |
| Public source/GHCR to replay environment | Unauthenticated bytes enter dependency, test, database, and OCI checks. |
| Final evidence to tutorial authoring | Sealed identities become Phase 27 claims and screenshot inputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-04-S | Spoofing | Stage/image mapping | high | mitigate | Verify direct/peeled/message/main, clone-derived full-SHA tags, digests, OCI source/revision, and live-recovered equality. |
| T-26-04-T | Tampering | Protected stages/evidence | high | mitigate | Four coherent Stage 3 states, immutable Stage 1/2/ruleset, atomic checksums, read-only seal, and no force/update/delete. |
| T-26-04-R | Repudiation | Publication recovery | high | mitigate | Record chosen state branch, Git/GitHub/workflow/package/OCI reads, clone results, and final checksums. |
| T-26-04-I | Information Disclosure | Clone/runtime evidence | high | mitigate | Public-only semantic fields, isolated configs, explicit credential scan, exact cleanup, and sealed files. |
| T-26-04-D | Denial of Service | Fresh database replay | medium | mitigate | Bounded run-scoped lifecycle, exact ownership, and read-only final audit. |
</threat_model>

<verification>
1. Verify frozen public main/Image B, live recovery, Stage 1/2/ruleset, workflows, package, and both images before Stage 3 mutation.
2. Publish/recover Stage 3 through one coherent annotated-tag state and re-read every identity.
3. Replay all three source stages and both images from fresh public/anonymous inputs.
4. Run ten-file preflight, atomic ten-checksum generation, one full verifier, read-only sealing, and final zero-residue audit.
</verification>

<success_criteria>
- Public main equals protected annotated Stage 3 while Stage 1/2 and ruleset 19014157 remain exact.
- Three public clones and two anonymous images reproduce the complete release, including Stage 3 29/38 and fresh PostgreSQL behavior.
- Ten checksums verify, eleven evidence files are read-only, target source remains frozen, and all temporary ownership is zero or an externally blocked GHCR version is recorded by exact ID for Phase 28.
</success_criteria>

<output>
Create `.planning/phases/26-django-production-stage/26-04-SUMMARY.md` with Stage 3/public-main/ruleset/image/workflow/package identities, three clone and two image replays, final checksums/modes, target freeze, cleanup, and DJAN-03/DJAN-04 verifier handoff.
</output>
