---
phase: 26-django-production-stage
plan: "03"
type: execute
wave: 3
depends_on: ["26-02"]
files_modified:
  - .planning/phases/26-django-production-stage/evidence/workflow.txt
  - .planning/phases/26-django-production-stage/evidence/images.txt
  - .planning/phases/26-django-production-stage/evidence/collectstatic.txt
  - .planning/phases/26-django-production-stage/evidence/migrations.txt
  - .planning/phases/26-django-production-stage/evidence/runtime.txt
  - .planning/phases/26-django-production-stage/evidence/logs.txt
  - .planning/phases/26-django-production-stage/evidence/http.jsonl
  - .planning/phases/26-django-production-stage/evidence/rollback.txt
  - .planning/phases/26-django-production-stage/evidence/cleanup.txt
  - .planning/phases/26-django-production-stage/evidence/checksums.txt
  - .planning/phases/26-django-production-stage/26-03-SUMMARY.md
autonomous: true
requirements: [DJAN-03]
must_haves:
  truths:
    - "D-06/D-07: Image A migration and baseline rollout, Image B migration and final rollout, rollout undo to Image A, and explicit Image B recovery execute in that order on one owned PostgreSQL 17 database."
    - "D-04/D-05: Every state has two Ready Pods, one Gunicorn master plus one sync worker per Pod, exact imageID/source tuple, one startup record per worker, UID/GID 10001, read-only root, writable bounded /tmp, and restricted security."
    - "D-02/D-03: Health, Task Board, hashed WhiteNoise CSS, admin login/readback, strict localhost Host validation, and credential-free logs pass in every accepted state."
    - "D-09: One Task created through the baseline CSRF form survives final rollout, rollout undo, and explicit final recovery in both board and authenticated Django admin."
    - "D-12: Nine curated live data files pass semantic, credential, nine-entry checksum, source-freeze, exact-label cleanup, ownership-ledger, and final read-only audits."
  artifacts:
    - path: ".planning/phases/26-django-production-stage/evidence/runtime.txt"
      provides: "Four-state two-Pod process/image/security evidence"
      contains: "state=final-recovered"
    - path: ".planning/phases/26-django-production-stage/evidence/collectstatic.txt"
      provides: "Image manifest, hashed stylesheet, rendered URL, content type, and immutable cache evidence"
      contains: "hashed_css=passed"
    - path: ".planning/phases/26-django-production-stage/evidence/http.jsonl"
      provides: "Health, board, CSS, CSRF write, and admin continuity observations"
      contains: "final-recovered"
    - path: ".planning/phases/26-django-production-stage/evidence/rollback.txt"
      provides: "Controller revisions and Image A/B/undo/recovery tuples"
      contains: "rollout_undo=passed"
    - path: ".planning/phases/26-django-production-stage/evidence/checksums.txt"
      provides: "Nine-entry live evidence integrity manifest"
  key_links:
    - from: ".planning/phases/26-django-production-stage/26-02-SUMMARY.md"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh"
      via: "exact four-line Image A/Image B release block"
      pattern: "baseline_image|final_image"
    - from: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml"
      to: "/Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml"
      via: "same candidate digest and Secret before each rollout"
      pattern: "__IMAGE_REFERENCE__|__SECRET_NAME__"
    - from: ".planning/phases/26-django-production-stage/evidence/rollback.txt"
      to: ".planning/phases/26-django-production-stage/evidence/http.jsonl"
      via: "one Task identity/title across the four controller states"
      pattern: "task_id|task_title"
---

<objective>
Execute the complete real Django production state machine and retain credential-free proof per D-02 through D-07, D-09, and D-12.

Purpose: Convert the two frozen public images into direct migration, Kubernetes, Gunicorn, WhiteNoise, HTTP/admin, rollback, persistence, cleanup, and integrity evidence.
Output: One four-state Sealos run, nine reviewed live data files, a nine-entry checksum manifest, exact cleanup, and zero target-source commits.
</objective>

<execution_context>
@/Users/longnv/.codex/gsd-core/workflows/execute-plan.md
@/Users/longnv/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-django-production-stage/26-CONTEXT.md
@.planning/phases/26-django-production-stage/26-VALIDATION.md
@.planning/phases/26-django-production-stage/26-01-SUMMARY.md
@.planning/phases/26-django-production-stage/26-02-SUMMARY.md
@.planning/phases/25-django-postgresql-stage/25-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-accept the frozen public source and both anonymous image inputs</name>
  <read_first>
    .planning/phases/26-django-production-stage/26-01-SUMMARY.md
    .planning/phases/26-django-production-stage/26-02-SUMMARY.md
    /Users/longnv/bin/repo/sealos-django-tutorial/.github/workflows/publish-image.yml
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/workflow.txt, .planning/phases/26-django-production-stage/evidence/images.txt, .planning/phases/26-django-production-stage/evidence/collectstatic.txt</files>
  <action>
    Parse exactly one baseline source/image and one final source/image from 26-02-SUMMARY.md. Require distinct 40-hex sources and distinct digest refs; local target HEAD and public main must equal FINAL_SHA, history must be 29/38 with the exact five subjects, the tree must be clean, and migration SHA-256 must remain `745bc45f655bf0b164c1464c0c22786d22910e2aea235772e5dcea38495e4bf3`. Source is frozen: this and later plans create zero target-repository commit.

    Re-read both exact workflows, normalized targets, conclusions, package visibility/linkage, full-SHA tags, linux/amd64 manifests, OCI sources/revisions, and digests. For each image use a separate fresh empty BSD-safe mode-0700 DOCKER_CONFIG, clear GH_TOKEN, GITHUB_TOKEN, GHCR_TOKEN, REGISTRY_TOKEN, DOCKER_AUTH_CONFIG, REGISTRY_AUTH_FILE, and CRANE_AUTH, and perform anonymous digest/config/manifest plus image-filesystem readback. Require `staticfiles.json`, one hashed `tasks/styles.<hash>.css`, non-root config, Gunicorn command, and expected source identity. External recovery accepts exact success, bounded active run, absent exact run/tag through source-SHA dispatch, or one infrastructure-only rerun; every source/test/identity mismatch halts before Sealos mutation. Create an empty mode-0700 evidence directory and write only curated public fields to workflow/images/collectstatic.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; test "$(git rev-parse HEAD)" = "$(gh api repos/yangchuansheng/sealos-django-tutorial/commits/main --jq .sha)" &amp;&amp; test "$(git rev-list --count HEAD)" = 29 &amp;&amp; test "$(git ls-tree -r --name-only HEAD | wc -l | tr -d ' ')" = 38 &amp;&amp; test -z "$(git status --short)" &amp;&amp; ./scripts/test-production.sh --assert-clean-all</automated>
  </verify>
  <acceptance_criteria>
    - Local/public FINAL_SHA, exact 29/38 inventory, five subjects, clean tree, and immutable migration agree before cluster mutation.
    - Image A and B pass distinct workflow head/target, package, anonymous digest/config/manifest, linux/amd64, OCI, static manifest/hashed CSS, non-root, and Gunicorn command checks.
    - Evidence starts empty, contains curated public fields only, and all anonymous registry directories are removed.
  </acceptance_criteria>
  <done>The frozen source and both public immutable images are coherent, anonymous, and ready for one exact owned runtime run.</done>
</task>

<task type="auto">
  <name>Task 2: Execute Image A, Image B, rollout undo, and explicit recovery on one database</name>
  <read_first>
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-postgres.sh
    /Users/longnv/bin/repo/sealos-django-tutorial/deploy/application.yaml
    /Users/longnv/bin/repo/sealos-django-tutorial/deploy/migration-job.yaml
    .planning/phases/26-django-production-stage/26-02-SUMMARY.md
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/migrations.txt, .planning/phases/26-django-production-stage/evidence/runtime.txt, .planning/phases/26-django-production-stage/evidence/logs.txt, .planning/phases/26-django-production-stage/evidence/http.jsonl, .planning/phases/26-django-production-stage/evidence/rollback.txt, .planning/phases/26-django-production-stage/evidence/cleanup.txt</files>
  <action>
    Preflight authenticated context `dn9ue3wz@sealos`, namespace `ns-let51wad`, required create/get/list/watch/log/exec/apply/patch/delete permissions, server-side validation, quota, and zero global production/PostgreSQL ownership. Generate one 12-hex run ID; every object, process, browser session, mode-0600 render/state file, and path must have its exact label, prefix, PID command, session name, or ownership-ledger entry. Generate the database and Django secret values in memory or mode-0600 state, pass them only through the run-owned Secret, and retain no values or raw outputs. Install the complete EXIT cleanup trap before first mutation.

    Invoke the frozen production runner with Image A/B source/digest arguments and the Phase 26 evidence directory. Execute exactly: (1) owned PostgreSQL 17 and Secret; Image A migration Job Complete=True, succeeded=1, failed=0, `[X] 0001_initial`; Image A Deployment 2/2 Ready; create one Task through the public CSRF form; (2) Image B migration Job with the same checks; atomically apply Image B digest/source/reference; wait 2/2; (3) `kubectl rollout undo` to the retained Image A Pod template; wait 2/2; (4) explicitly apply the Image B digest/source/reference tuple; wait 2/2. Refresh the owned Service port-forward and browser endpoint after every rollout.

    At every state require Deployment Available, one active ReplicaSet at the expected revision, two Ready Pods with exact imageIDs, UID/GID 10001, one Gunicorn master and one sync worker per Pod, listener on 0.0.0.0:8000, one matching worker-start record per Pod, read-only root, writable/removable bounded `/tmp`, effective service-account/seccomp/escalation/capability policy, `/health` exact 200 body, board 200 with retained Task, hashed CSS 200/content type/immutable cache, and admin login surface 200. Authenticate with ephemeral native Django admin credentials and read the same Task after final, rollback, and recovery. Curate state tuples, controller revisions, Job facts, process/security facts, HTTP semantics, and Task identity only. Finish through the trap with zero exact-run seven-kind inventory, processes, browsers, state, renders, clones, registry configs, evidence scratch, and ledgers.
  </action>
  <verify>
    <automated>cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; RELEASES="$PWD/.planning/phases/26-django-production-stage/26-02-SUMMARY.md" &amp;&amp; EVIDENCE="$PWD/.planning/phases/26-django-production-stage/evidence" &amp;&amp; BASELINE_SOURCE=$(sed -n 's/^baseline_source=//p' "$RELEASES") &amp;&amp; BASELINE_IMAGE=$(sed -n 's/^baseline_image=//p' "$RELEASES") &amp;&amp; FINAL_SOURCE=$(sed -n 's/^final_source=//p' "$RELEASES") &amp;&amp; FINAL_IMAGE=$(sed -n 's/^final_image=//p' "$RELEASES") &amp;&amp; cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; ./scripts/test-production.sh --run --baseline-image "$BASELINE_IMAGE" --baseline-source "$BASELINE_SOURCE" --final-image "$FINAL_IMAGE" --final-source "$FINAL_SOURCE" --evidence-dir "$EVIDENCE"</automated>
  </verify>
  <acceptance_criteria>
    - Both same-digest migration Jobs complete and report applied 0001 before their respective rollouts.
    - Baseline, final, baseline-rollback, and final-recovered each pass two-Pod image/process/log/security/filesystem/readiness/board/CSS/admin checks.
    - The same Task identity/title survives all four states and is read through both board and authenticated native admin.
    - `kubectl rollout undo` performs the baseline transition; one explicit immutable tuple apply performs final recovery.
    - EXIT cleanup succeeds on every branch and leaves exact run inventory and ownership ledgers empty.
  </acceptance_criteria>
  <done>One real database and four controller states prove production behavior, rollback, recovery, and Django data continuity.</done>
</task>

<task type="auto">
  <name>Task 3: Verify live evidence, checksums, source freeze, and final read-only cleanup</name>
  <read_first>
    .planning/phases/26-django-production-stage/evidence/workflow.txt
    .planning/phases/26-django-production-stage/evidence/images.txt
    .planning/phases/26-django-production-stage/evidence/collectstatic.txt
    .planning/phases/26-django-production-stage/evidence/migrations.txt
    .planning/phases/26-django-production-stage/evidence/runtime.txt
    .planning/phases/26-django-production-stage/evidence/logs.txt
    .planning/phases/26-django-production-stage/evidence/http.jsonl
    .planning/phases/26-django-production-stage/evidence/rollback.txt
    .planning/phases/26-django-production-stage/evidence/cleanup.txt
    /Users/longnv/bin/repo/sealos-django-tutorial/scripts/test-production.sh
  </read_first>
  <files>.planning/phases/26-django-production-stage/evidence/checksums.txt, .planning/phases/26-django-production-stage/26-03-SUMMARY.md</files>
  <action>
    Run `--preflight-evidence live` before checksums; require exactly the nine nonempty data files in the documented schema, ordered four states, two Jobs, two Pods/state, worker topology/log tuples, static manifest/HTTP evidence, one Task continuity witness, rollback/recovery revisions, and zero cleanup. Scan explicit files for PostgreSQL userinfo/URL assignments, password or Secret values, cookies, CSRF values, GitHub/bearer/service-account tokens, registry auth, kubeconfig/private keys, unresolved tokens, traceback, and exception dumps; report only filename/rule. Generate `checksums.txt` atomically through `mktemp "$EVIDENCE/.checksums.XXXXXX"` with nine sorted repository-relative `shasum -a 256` entries, then run `--verify-evidence live` and verify all entries.

    Re-read target local/public FINAL_SHA, 29/38, migration hash, tree cleanliness, both exact image identities, and both protected prior tags. Run production `--assert-clean-all`, which must be read-only and cover seven Kubernetes kinds plus every owned local category. Create 26-03-SUMMARY.md outside evidence with run ID, Jobs, four-state tuples, process/static/HTTP/admin results, Task witness, evidence manifest, source freeze, and zero residue. Create no target-repository commit. Keep evidence writable only for the specified Plan 26-04 publication append/rechecksum; every later access in this plan is read-only.
  </action>
  <verify>
    <automated>cd /Users/longnv/bin/repo/sealos-django-tutorial &amp;&amp; EVIDENCE=/Users/longnv/.codex/worktrees/19b8/sealos.io/.planning/phases/26-django-production-stage/evidence &amp;&amp; ./scripts/test-production.sh --verify-evidence live --evidence-dir "$EVIDENCE" &amp;&amp; ./scripts/test-production.sh --assert-clean-all &amp;&amp; cd /Users/longnv/.codex/worktrees/19b8/sealos.io &amp;&amp; test "$(wc -l &lt; .planning/phases/26-django-production-stage/evidence/checksums.txt | tr -d ' ')" = 9 &amp;&amp; shasum -a 256 -c .planning/phases/26-django-production-stage/evidence/checksums.txt</automated>
  </verify>
  <acceptance_criteria>
    - Live preflight and verifier require exactly nine data files and a sorted nine-entry checksum manifest with all semantic/source/image/runtime/cleanup links coherent.
    - Credential scans pass before checksum creation; every checksum verifies.
    - Target source remains clean at public FINAL_SHA with 29/38, the immutable migration, and zero post-Image-B commit.
    - Independent final read-only audit reports zero cluster objects, processes, browsers, paths, configs, renders, clones, scratch, and ledgers.
  </acceptance_criteria>
  <done>The live DJAN-03 proof is coherent, credential-free, checksum-valid, source-frozen, and closed with zero temporary footprint.</done>
</task>

</tasks>

## Artifacts this phase produces

- Live evidence files `workflow.txt`, `images.txt`, `collectstatic.txt`, `migrations.txt`, `runtime.txt`, `logs.txt`, `http.jsonl`, `rollback.txt`, and `cleanup.txt`
- Nine-entry `evidence/checksums.txt`
- Runtime states `baseline`, `final`, `baseline-rollback`, and `final-recovered`
- Persistent `task_id`/`task_title` continuity witness
- `.planning/phases/26-django-production-stage/26-03-SUMMARY.md`

<external_mutation_state_machine>
Cluster mutation begins only after frozen source, both workflows, anonymous images, permissions, quota, exact-label absence, and empty evidence preconditions pass. One EXIT trap owns every subsequent branch. A complete coherent interrupted state may be cleaned by its exact ledger before a fresh run; partial, mismatched, unlabeled, or foreign state halts for forensics. Runtime transitions accept only the fixed four-state order, and every candidate migration must complete before its rollout.
</external_mutation_state_machine>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public GHCR to shared Sealos namespace | Immutable external bytes execute with a run-owned PostgreSQL Secret. |
| Deployment history to rollback | Controller revisions select the active source/image tuple. |
| HTTP/log/browser output to evidence | Runtime observations become durable repository artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-26-03-S | Spoofing | Pod/source identity | high | mitigate | Correlate Deployment digest, ReplicaSet revision, both Pod imageIDs, source env, OCI revision, and worker log at every state. |
| T-26-03-T | Tampering | Migration/rollback/task | high | mitigate | Same-digest completed Jobs, fixed state order, controller revisions, and one public/admin Task witness. |
| T-26-03-I | Information Disclosure | Secret/browser/evidence | high | mitigate | KeyRefs, ephemeral credentials, curated fields, explicit scans, atomic checksums, and cleanup. |
| T-26-03-D | Denial of Service | Shared namespace | high | mitigate | Bounded resources/waits, one exact label/prefix, readiness, and preinstalled EXIT cleanup. |
| T-26-03-E | Elevation of Privilege | Serving Pods | high | mitigate | Live UID/GID, topology, read-only root, tmpfs, seccomp, capability, escalation, and token checks. |
</threat_model>

<verification>
1. Re-accept frozen FINAL_SHA and both anonymous immutable images before namespace mutation.
2. Execute both migration-first rollouts, controller undo, and explicit recovery on one database.
3. Verify two-Pod Gunicorn/static/security/HTTP/admin/data behavior at every state.
4. Verify exact live evidence schema, credential scan, nine checksums, source freeze, and read-only zero-residue audit.
</verification>

<success_criteria>
- The four exact states pass complete two-Pod Django production acceptance and preserve one Task.
- Image A is the executable rollback state and Image B is the explicit recovered state on unchanged migration 0001.
- Nine checksum-verified data files remain as the only run artifact; all temporary cluster and local ownership is empty.
</success_criteria>

<output>
Create `.planning/phases/26-django-production-stage/26-03-SUMMARY.md` with the four-state runtime proof, evidence schema/checksums, frozen source/image identities, deviations, and exact cleanup audit.
</output>
