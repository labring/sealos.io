---
quick_id: 260813-gu4
status: in_progress
---

# Plan: Recenter the Sealos Skills Hero

## Task 1: Add the Agent Logo Rotator

- Create a small client component that derives the nine Agent Guide logos from
  existing data and renders a fixed-size vertical slide window.
- Pause on hover, focus, document visibility changes, and reduced motion.
- Expose an accessible hidden label and stable `data-*` hooks for tests.

## Task 2: Rebuild the Hub Hero

- Replace the desktop two-column Hero with a centered Insforge-style stack.
- Add a macOS terminal header with window dots, Codex label, copy action, code,
  and invocation.
- Add same-page Agent Logo links with matching card IDs below the terminal.
- Keep the existing copy and analytics IDs; remove the duplicate standalone Hero
  CTA buttons.

## Task 3: Validate and document

- Extend the page contract test for centered Hero, terminal chrome, nine logos,
  rotator pause/accessibility behavior, card targets, and content hash.
- Run page/Rybbit tests, TypeScript, build, static checks, and browser QA at
  desktop/mobile viewports with reduced motion.
- Record Summary and Verification artifacts and commit code/docs atomically.
