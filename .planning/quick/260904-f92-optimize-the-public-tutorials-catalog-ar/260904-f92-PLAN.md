# Quick Task 260904-f92: Optimize the Public Tutorials Catalog

**Status:** Ready

## Goal

Align `/tutorials/` with the accepted intent-qualified content strategy: publish
one canonical Core Deployment Tutorial per qualified technology, expose only
published pages in the public catalog, and keep future opportunities in the
operational Tutorial Matrix.

## Tasks

### 1. Replace the public planning matrix with a published tutorial catalog

- Render catalog cards directly from the published tutorial collection.
- Reuse the Blog index header, card typography, gradient treatment, and the
  existing Django result image.
- Keep one generic tutorial-request action and its GTM event.
- Update page metadata and copy to describe the published catalog accurately.
- Delete the fixed Deploy/PostgreSQL/Production matrix, status, journey, and
  generated inventory modules.

### 2. Align domain language and verify the public surface

- Define the operational Tutorial Matrix separately from the public Tutorial
  Catalog in `CONTEXT.md`.
- Update the legacy redirect test description to use Tutorial Catalog language.
- Format touched files, run tutorial and redirect tests, run TypeScript
  validation, and verify the local `/tutorials/` route.

## Verification

- `node --test scripts/validate-tutorials.test.mjs`
- `node --test scripts/check-static-output.test.mjs`
- `pnpm lint`
- Local HTTP and browser inspection of `/tutorials/`
