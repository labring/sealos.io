---
quick_id: 260731-n14
status: planned
created: 2026-07-31
---

# Quick Task 260731-n14: Align the contact footer with the homepage

## Goal

Make `/contact` use the homepage footer and bottom wordmark treatment while
removing the contact hero grid background.

## Tasks

1. Match the homepage page-shell and footer composition.
   - Files: `app/[lang]/contact/page.tsx`
   - Action: Use `FooterV2` explicitly and align the top-level stacking
     structure with `app/[lang]/(home)/layout.tsx`.
   - Verify: Confirm the shared footer and fixed bottom `Sealos` wordmark are
     visible at the bottom of the contact page.

2. Remove the contact hero grid.
   - Files: `app/[lang]/contact/page.tsx`
   - Action: Remove `LegacyHeroBackground` while preserving the homepage ray
     lighting and all contact content.
   - Verify: Confirm no hero-grid asset or grid component appears on `/contact`.

3. Validate responsive presentation.
   - Files: `app/[lang]/contact/page.tsx`
   - Action: Format and type-check the change, then inspect desktop and mobile
     contact renders.
   - Verify: Run Prettier, TypeScript validation, `git diff --check`, and
     browser screenshot checks.
