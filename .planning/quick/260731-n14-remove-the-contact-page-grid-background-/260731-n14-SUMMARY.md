---
status: complete
quick_id: 260731-n14
completed: 2026-07-31
---

# Quick Task 260731-n14 Summary

## Completed

- Reused `FooterV2` explicitly so `/contact` and the homepage share the same
  footer implementation and fixed bottom `Sealos` wordmark.
- Aligned the contact page shell and header stacking structure with the
  homepage layout so the fixed wordmark renders at the intended layer.
- Removed `LegacyHeroBackground` and the contact-only fixed background layer
  while retaining the homepage top-ray lighting.

## Verification

- Browser checks passed at 1440x1000 and 390x844.
- The footer and fixed `Sealos` wordmark were visible at both sizes, with the
  homepage responsive wordmark sizes of 360px and 120px respectively.
- No hero-grid asset was present, and the mobile page had no horizontal
  overflow.
- Prettier and `git diff --check` passed.
- `npm run lint` reached one existing error outside the changed file:
  `tests/rybbit-cta.test.ts` imports a `.ts` path without
  `allowImportingTsExtensions`.

## Commit

`fc4bbc2` (`fix(contact): match homepage footer treatment`)
