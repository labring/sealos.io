---
quick_id: 260812-k69
status: complete
created: 2026-08-12
---

# Quick Task 260812-k69: Consolidate Sealos Skills Installation

## Goal

Replace the repetitive agent directory, support matrix, and install tabs with
one installation decision section that preserves every documented path while
making Codex and Claude Code the primary choices.

## Tasks

1. Consolidate installation data and copy.
   - Use `AGENT_TARGETS` as the single source for all ten paths.
   - Replace the three repeated headings with one action-led install heading.
   - Rename the proof item to `documented install paths`.

2. Build one progressive-disclosure install surface.
   - Render Codex and Claude Code as keyboard-operable tabs.
   - Render the remaining eight paths as a single-open accordion.
   - Preserve commands, invocation, integration type, copy feedback, guide
     links, and the `compatibility`, `support`, and `install` anchors.

3. Remove repeated sections and verify the conversion contract.
   - Remove the agent card wall and support matrix from page composition.
   - Normalize install copy and guide analytics to one event per action.
   - Update source-contract tests and validate desktop/mobile layout and
     interactions.

## Acceptance Criteria

- The page contains one complete agent installation section after Capabilities.
- Codex is the default primary tab and Claude Code is the second primary tab.
- `More install paths` contains Qoder, Gemini CLI, Qwen Code, OpenClaw,
  CodeBuddy, Amp, Kimi, and skills.sh with one item open at a time.
- All ten install commands, invocation details, guide links, and deep-link
  anchors remain available.
- Each copy or guide action emits one unique install event.
- Focused tests, TypeScript, static source checks, and responsive browser QA
  pass with zero horizontal overflow.
