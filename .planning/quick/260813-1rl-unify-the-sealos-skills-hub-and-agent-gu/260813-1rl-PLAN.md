---
quick_id: 260813-1rl
status: in_progress
created: 2026-08-13
must_haves:
  truths:
    - The Hub and every Agent guide use the Homepage neutral background, Geist headings, Homepage blue emphasis, and shared neutral surfaces.
    - The Hub prioritizes the install command, renders complete mobile proof, and uses continuous Agent and capability panels.
    - Agent guides expose an accessible active sticky navigation and preserve all install, prompt, FAQ, resource, and analytics behavior.
    - Page copy, typed content, routes, schemas, anchors, analytics IDs, and static locale output remain unchanged.
  artifacts:
    - app/[lang]/(home)/sealos-skills/shared.tsx
    - app/[lang]/(home)/sealos-skills/top-sections.tsx
    - app/[lang]/(home)/sealos-skills/agent-directory.tsx
    - app/[lang]/(home)/sealos-skills/[agent]/agent-guide-page.tsx
    - app/[lang]/(home)/sealos-skills/[agent]/agent-guide-nav.tsx
    - tests/sealos-skills-page.test.ts
  key_links:
    - SkillsPageShell owns the shared visual root for the Hub and Agent guides.
    - AGENT_GUIDES continues to feed the Hub directory and all generated detail routes.
    - AgentGuideNav observes the existing detail-page anchor IDs and exposes aria-current state.
---

# Quick Task 260813-1rl: Sealos Skills visual system

## Task 1: Build the shared visual system and redesign the Hub

**Action:**
- Add `SkillsPageShell` and normalize shared typography, color, focus, button,
  icon, code, and surface styles.
- Rebuild the Hero around the install command, switch proof to a responsive
  grid, and remove the rendered Codex screenshot.
- Convert the Agent directory and capability section into continuous divided
  panels while preserving content, ordering, links, spans, and analytics.
- Restyle Workflow, Setup/FAQ, distribution, and final CTA with the same
  neutral Homepage system.

**Verify:**
- Hub source contains `bg-background`, Geist-compatible heading classes,
  Homepage blue utilities, continuous panel contracts, and no rendered product
  screenshot, Georgia font, or purple theme token.
- The Hub retains all section ordering, anchors, copy, and analytics IDs.

## Task 2: Redesign Agent guides and add active anchor navigation

**Action:**
- Apply `SkillsPageShell` to all Agent pages and normalize Hero, command,
  Quick Start, evidence, prompt, FAQ, resource, related-guide, and final CTA
  surfaces.
- Add `AgentGuideNav` with IntersectionObserver, active styling,
  `aria-current="location"`, stable anchor targets, and reduced-motion-safe
  interaction.
- Keep command and prompt overflow inside their local code surfaces.

**Verify:**
- The detail route still renders every existing section and action.
- Scrollspy updates the active anchor, keyboard focus is visible, and both page
  sizes have zero page-level horizontal overflow.

## Task 3: Lock contracts and validate static output

**Action:**
- Update source-contract tests for the new visual system, continuous layouts,
  mobile proof, removed screenshot rendering, and Agent scrollspy.
- Run page tests, Rybbit tests, TypeScript, production build, locale/static
  checks, exact Agent route audits, content hash verification, and diff checks.
- Run browser QA for Hub and Codex at 1440x1000 and 390x844, including copy,
  tabs, FAQ, active anchors, keyboard focus, reduced motion, and overflow.

**Verify:**
- Every named check passes and `content.ts` retains SHA-256
  `4e7d08050b640903ba7602ffb456734f3ae623e8dda6d6efb126ea54d86dc085`.
- Browser evidence is recorded in the quick-task directory.
